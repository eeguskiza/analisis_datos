---
phase: 09-cloudflare-tunnel-public-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/infra/test_caddyfile_security_headers.py
  - tests/infra/test_deploy_smoke_has_11_checks.py
  - caddy/Caddyfile.prod
  - tests/infra/deploy_smoke.sh
autonomous: false
requirements:
  - CLOUD-03
  - CLOUD-08
tags:
  - phase-9
  - cloudflare
  - caddy
  - security-headers
  - smoke

must_haves:
  truths:
    - "Caddy responde con HSTS+X-Frame-Options+X-Content-Type-Options+Referrer-Policy+Permissions-Policy en cualquier ruta de nexo.ecsmobility.local y :443"
    - "Mismos 5 headers aplican a la rama LAN (existente Phase 6) y a la rama CF (Phase 9, futura)"
    - "deploy_smoke.sh contiene 11 checks `[DEPLOY-01..DEPLOY-11]` en lugar de 8"
    - "El nuevo `[DEPLOY-11]` falla limpiamente (skip o FAIL claro) si cloudflared no está running, sin romper los otros 10"
    - "Pytest stubs Wave 0 fallan rojo ANTES de la implementación y verde después"
  artifacts:
    - path: "tests/infra/test_caddyfile_security_headers.py"
      provides: "static validation del bloque de security headers en Caddyfile.prod"
      min_lines: 40
    - path: "tests/infra/test_deploy_smoke_has_11_checks.py"
      provides: "regression test que cuenta tags `[DEPLOY-NN]` en deploy_smoke.sh"
      min_lines: 30
    - path: "caddy/Caddyfile.prod"
      provides: "snippet `(security_headers)` + 5 directives + `import security_headers` en ambos bloques"
      contains: "Strict-Transport-Security"
    - path: "tests/infra/deploy_smoke.sh"
      provides: "11 checks DEPLOY-01..DEPLOY-11 (vs 8 actuales)"
      contains: "[DEPLOY-11]"
  key_links:
    - from: "caddy/Caddyfile.prod"
      to: "ambos bloques (`nexo.ecsmobility.local` y `:443`)"
      via: "import security_headers"
      pattern: "import security_headers"
    - from: "tests/infra/deploy_smoke.sh"
      to: "make cf-down preserva LAN"
      via: "DEPLOY-11 con SKIP graceful o equivalente"
      pattern: "DEPLOY-11"
---

<objective>
Endurecer Caddy con los 5 headers de seguridad (D-13) requeridos antes de exponer
Nexo a internet via Cloudflare Tunnel (Phase 9 plan 09-02). Ampliar el smoke LAN
existente de 8 a 11 checks para cubrir tres lagunas detectadas en el audit
2026-04-25 (login responde 401 no 500, /api/health 200 con MES caído,
cloudflared running). Plan PRE-REQUISITO bloqueante de 09-02 — sin esto, la
exposición pública de 09-03 sería vulnerabilidad real (clickjacking + MIME
sniff + downgrade attack). Sin código Python ejecutable nuevo (solo Caddyfile +
bash + pytest stubs), por lo que el coverage gate ≥60% sigue cubierto por
construcción.

Purpose: cerrar CLOUD-03 (Mozilla Observatory ≥ B+; cálculo en 09-RESEARCH §
Mozilla Observatory Verification confirma score 80 = B+ borderline con D-13 y
sin CSP) y CLOUD-08 (smoke 11/11 ejecutable como cron post-deploy).

Output: Caddyfile.prod con headers, deploy_smoke.sh con 11 checks, 2 stubs
pytest en `tests/infra/` que congelan ambos contratos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-RESEARCH.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-VALIDATION.md
@CLAUDE.md
@caddy/Caddyfile.prod
@tests/infra/deploy_smoke.sh
@tests/infra/test_caddyfile_prod.py
@tests/infra/test_deploy_lan_doc.py

<interfaces>
<!-- Estado actual de los archivos a modificar y sus analogs (extraído verbatim por la planner). -->
<!-- El executor debe partir de estos snippets, NO explorar el repo. -->

Estado actual `caddy/Caddyfile.prod` (24 líneas, Phase 6):
```caddyfile
nexo.ecsmobility.local {
    tls internal
    reverse_proxy web:8000

    log {
        output stdout
        format json
    }
}

# Fallback IP-directa
:443 {
    tls internal
    reverse_proxy web:8000
}
```

Patrón target Caddy (research § Pattern 2 + PATTERNS.md):
```caddyfile
# Snippet reusable de security headers (D-13). Aplicado a ambas ramas LAN+CF.
(security_headers) {
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Frame-Options "DENY"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "geolocation=(), microphone=(), camera=()"
    }
}

nexo.ecsmobility.local {
    import security_headers
    tls internal
    reverse_proxy web:8000

    log {
        output stdout
        format json
    }
}

# Fallback IP-directa + entry point del túnel CF (SNI=caddy:443 cae aquí, D-20)
:443 {
    import security_headers
    tls internal
    reverse_proxy web:8000
}
```

Estado actual `tests/infra/deploy_smoke.sh` (líneas 30-40 helper, líneas 49-91 los 11
checks actuales — pero solo 8 tags `[DEPLOY-XX]` distintos: DEPLOY-01 (×2), DEPLOY-02
(×2), DEPLOY-03 (×3), DEPLOY-06 (×4)):

```bash
check() {
    local req="$1"
    local msg="$2"
    local cmd="$3"
    if eval "${cmd}" >/dev/null 2>&1; then
        echo "[${req}] OK: ${msg}"
    else
        echo "[${req}] FAIL: ${msg} (cmd: ${cmd})"
        FAILS=$((FAILS + 1))
    fi
}
```

Patrón target (research § Pattern 3 + PATTERNS.md): añadir tres `check` calls
ANTES de `echo "=== Fallos: ${FAILS} ==="`. Usa el COMPOSE existente y
`set -uo pipefail` (NO añadir `-e`).

Test analog (`tests/infra/test_caddyfile_prod.py`, 47 líneas — read+assert
shape; copiar conventions: `from pathlib import Path`, `Path(__file__).resolve().parents[2]`).

Test analog smoke regression (`tests/infra/test_deploy_lan_doc.py` líneas 180-234):
`test_smoke_sh_exists`, `test_smoke_sh_is_executable`, `test_smoke_sh_bash_syntax_valid`,
`test_smoke_sh_covers_deploy_requirements`, `test_smoke_sh_has_exit_count`,
`test_smoke_sh_no_emoji`, `test_smoke_sh_uses_set_safety_flags` — TODOS debe seguir
pasando tras la modificación.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task W0-01: Wave 0 stub — test_caddyfile_security_headers.py</name>
  <files>tests/infra/test_caddyfile_security_headers.py</files>
  <read_first>
    - tests/infra/test_caddyfile_prod.py (analog completo, 47 líneas — copiar shape)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md (sección "test_caddyfile_security_headers.py" — describe los asserts exactos)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md (D-13 — texto literal de los 5 headers)
  </read_first>
  <behavior>
    - test 1: archivo Caddyfile.prod existe (regresión sanity)
    - test 2: contiene snippet `(security_headers)` definido top-level
    - test 3: HSTS literal `max-age=31536000; includeSubDomains; preload` presente
    - test 4: `X-Frame-Options "DENY"` presente
    - test 5: `X-Content-Type-Options "nosniff"` presente
    - test 6: `Referrer-Policy "strict-origin-when-cross-origin"` presente
    - test 7: `Permissions-Policy` con `geolocation=()` + `microphone=()` + `camera=()` presente
    - test 8: AMBOS bloques (`nexo.ecsmobility.local {` y `:443 {`) tienen `import security_headers` dentro
    - test 9 (regresión): `tls internal` y `reverse_proxy web:8000` siguen presentes (no romper Phase 6)
    Estos tests deben FALLAR ROJO al crearse (Caddyfile.prod no tiene aún los headers) y pasar VERDE tras Task W1-01.
  </behavior>
  <action>
Crear `tests/infra/test_caddyfile_security_headers.py` siguiendo verbatim la
shape de `tests/infra/test_caddyfile_prod.py`. Estructura literal:

```python
"""Validaciones estaticas de los 5 security headers en caddy/Caddyfile.prod.

Phase 9 / Plan 09-01 / D-13 / CLOUD-03.

No requiere Docker. Lee el archivo y comprueba presencia de los 5 directives
en el snippet `(security_headers)` y su `import` en ambos bloques de sitio
(`nexo.ecsmobility.local` y `:443`).
"""
from pathlib import Path

CADDYFILE_PROD = Path(__file__).resolve().parents[2] / "caddy" / "Caddyfile.prod"


def test_caddyfile_prod_exists():
    assert CADDYFILE_PROD.exists(), f"No existe {CADDYFILE_PROD}"


def test_caddyfile_prod_has_security_headers_snippet():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert "(security_headers)" in content, (
        "Caddyfile.prod debe definir snippet `(security_headers)` (D-13)."
    )


def test_caddyfile_prod_has_hsts():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert (
        'Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"'
        in content
    ), "Falta HSTS literal con max-age 1yr + includeSubDomains + preload (D-13)."


def test_caddyfile_prod_has_x_frame_options():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert 'X-Frame-Options "DENY"' in content, (
        "Falta `X-Frame-Options \"DENY\"` (D-13, mitiga T-09-04 clickjacking)."
    )


def test_caddyfile_prod_has_x_content_type_options():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert 'X-Content-Type-Options "nosniff"' in content, (
        "Falta `X-Content-Type-Options \"nosniff\"` (D-13, mitiga T-09-05 MIME sniff)."
    )


def test_caddyfile_prod_has_referrer_policy():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert 'Referrer-Policy "strict-origin-when-cross-origin"' in content, (
        "Falta `Referrer-Policy \"strict-origin-when-cross-origin\"` (D-13)."
    )


def test_caddyfile_prod_has_permissions_policy():
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert "Permissions-Policy" in content
    for feature in ("geolocation=()", "microphone=()", "camera=()"):
        assert feature in content, (
            f"Permissions-Policy debe deshabilitar {feature} (D-13)."
        )


def test_caddyfile_prod_imports_security_headers_in_lan_block():
    """El bloque `nexo.ecsmobility.local { ... }` debe usar `import security_headers` (D-13 + D-19)."""
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    lan_idx = content.find("nexo.ecsmobility.local")
    assert lan_idx >= 0, "Bloque LAN ausente."
    # Sliced hasta el siguiente bloque top-level (`:443 {` o EOF).
    next_block = content.find("\n:443", lan_idx)
    lan_block = content[lan_idx:next_block] if next_block > 0 else content[lan_idx:]
    assert "import security_headers" in lan_block, (
        "Bloque `nexo.ecsmobility.local` debe `import security_headers` (D-13 ambas ramas)."
    )


def test_caddyfile_prod_imports_security_headers_in_catchall_block():
    """El bloque `:443 { ... }` (catch-all + entrada CF tunnel) debe usar `import security_headers` (D-13 + D-20)."""
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    catchall_idx = content.find("\n:443")
    assert catchall_idx >= 0, "Bloque `:443` catch-all ausente."
    catchall_block = content[catchall_idx:]
    assert "import security_headers" in catchall_block, (
        "Bloque `:443` catch-all debe `import security_headers` "
        "(D-13 + D-20 — CF tunnel entra por aquí)."
    )


def test_caddyfile_prod_preserves_phase_6_invariants():
    """Regresión Phase 6: tls internal + reverse_proxy web:8000 siguen presentes."""
    content = CADDYFILE_PROD.read_text(encoding="utf-8")
    assert "tls internal" in content, "tls internal ausente (regresión Phase 6 D-04)."
    assert "reverse_proxy web:8000" in content, (
        "reverse_proxy web:8000 ausente (regresión Phase 6)."
    )
```

Tras crear el archivo, ejecutar `pytest tests/infra/test_caddyfile_security_headers.py -v`.
DEBE devolver fallos rojos en los 7 asserts de headers (`test_caddyfile_prod_has_*` y los
2 `_imports_security_headers_in_*_block`). Solo `test_caddyfile_prod_exists` y
`test_caddyfile_prod_preserves_phase_6_invariants` deben pasar verde
(Caddyfile.prod existe y mantiene Phase 6). Esto confirma que el stub funciona como
red gate antes de Task W1-01.
  </action>
  <verify>
    <automated>pytest tests/infra/test_caddyfile_security_headers.py -v 2>&1 | grep -E "(FAILED|PASSED)" | tee /tmp/w0-01.log; grep -c FAILED /tmp/w0-01.log | grep -qE "^[7-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/infra/test_caddyfile_security_headers.py`
    - Contains 10 test functions: `grep -c "^def test_" tests/infra/test_caddyfile_security_headers.py` returns `10`
    - Pytest collects it: `pytest --collect-only tests/infra/test_caddyfile_security_headers.py 2>&1 | grep -c "test_caddyfile_prod"` returns `10`
    - Pytest run shows ≥7 RED tests pre-impl: `pytest tests/infra/test_caddyfile_security_headers.py 2>&1 | grep -E "^FAILED" | wc -l` returns `>= 7`
    - Bash syntax of Caddyfile.prod NOT changed yet: `git diff caddy/Caddyfile.prod` is empty.
  </acceptance_criteria>
  <done>Archivo creado con 10 tests, ≥7 fallan en rojo, 2-3 pasan en verde (sanity). Caddyfile.prod sin tocar todavía.</done>
</task>

<task type="auto" tdd="true">
  <name>Task W0-02: Wave 0 stub — test_deploy_smoke_has_11_checks.py</name>
  <files>tests/infra/test_deploy_smoke_has_11_checks.py</files>
  <read_first>
    - tests/infra/test_deploy_lan_doc.py líneas 180-234 (analog smoke regression)
    - tests/infra/deploy_smoke.sh (estado actual, 8 tags distintos)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md (sección "test_deploy_smoke_has_11_checks.py")
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md (D-21 — los 3 nuevos checks DEPLOY-09/10/11)
  </read_first>
  <behavior>
    - test 1: deploy_smoke.sh existe + es ejecutable (regresión Phase 6)
    - test 2: bash sintaxis válida (`bash -n`)
    - test 3: contiene los 11 tags `[DEPLOY-01]..[DEPLOY-11]` distintos (cuenta de cardinality del set)
    - test 4: tag `[DEPLOY-09]` con texto sobre `/api/login` y `401`
    - test 5: tag `[DEPLOY-10]` con texto sobre `/api/health` y `200`
    - test 6: tag `[DEPLOY-11]` con texto sobre `cloudflared`
    - test 7: el script preserva `set -uo pipefail` (Phase 6 invariant — NO añadir `-e`)
    - test 8: el script preserva `exit "${FAILS}"` (Phase 6 invariant)
    Pre-impl: tests 3-6 fallan rojo. Post-impl Task W1-02: todos verde.
  </behavior>
  <action>
Crear `tests/infra/test_deploy_smoke_has_11_checks.py` con shape mirror del analog
`tests/infra/test_deploy_lan_doc.py` líneas 180-234. Estructura literal:

```python
"""Regresión smoke: deploy_smoke.sh debe tener 11 tags distintos `[DEPLOY-NN]`.

Phase 9 / Plan 09-01 / D-21 / CLOUD-08.

Phase 6 dejó 8 tags (DEPLOY-01..03 + DEPLOY-06). Phase 9 añade 3 nuevos:
  - DEPLOY-09: POST /api/login con creds dummy responde 401 (no 500)
  - DEPLOY-10: GET /api/health responde 200 incluso con MES caído (Pitfall 3)
  - DEPLOY-11: cloudflared container running + tunnel info OK
"""
from __future__ import annotations

import re
import shutil
import stat
import subprocess
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
SMOKE = REPO_ROOT / "tests" / "infra" / "deploy_smoke.sh"


def test_smoke_sh_exists():
    assert SMOKE.exists(), f"Falta {SMOKE} (regresión Phase 6)."


def test_smoke_sh_is_executable():
    assert SMOKE.stat().st_mode & stat.S_IXUSR, (
        "deploy_smoke.sh debe seguir siendo ejecutable (regresión Phase 6)."
    )


def test_smoke_sh_bash_syntax_valid():
    if shutil.which("bash") is None:
        import pytest
        pytest.skip("bash no disponible")
    r = subprocess.run(
        ["bash", "-n", str(SMOKE)],
        capture_output=True, text=True, timeout=10, check=False,
    )
    assert r.returncode == 0, r.stderr


def test_smoke_sh_has_11_distinct_deploy_tags():
    """CLOUD-08: deploy_smoke.sh debe tener 11 tags distintos `[DEPLOY-NN]`."""
    content = SMOKE.read_text(encoding="utf-8")
    tags = set(re.findall(r"\[DEPLOY-(\d{2})\]", content))
    expected = {f"{n:02d}" for n in (1, 2, 3, 6, 9, 10, 11)}
    # Phase 6 ya tiene 01,02,03,06; Phase 9 añade 09,10,11. Total = 7 distintos
    # (DEPLOY-04/05/07/08 no aparecen — son requirements de doc/proceso, no checks bash).
    missing = expected - tags
    assert not missing, (
        f"Faltan tags DEPLOY: {sorted(missing)}. "
        f"Encontrados: {sorted(tags)}. "
        f"CLOUD-08 exige DEPLOY-09/10/11."
    )


def test_smoke_sh_has_deploy_09_login_check():
    content = SMOKE.read_text(encoding="utf-8")
    assert "[DEPLOY-09]" in content
    assert "/api/login" in content, (
        "DEPLOY-09 debe testear POST /api/login (D-21)."
    )
    assert "401" in content, (
        "DEPLOY-09 debe verificar respuesta 401 con creds dummy (D-21)."
    )


def test_smoke_sh_has_deploy_10_health_check_with_mes_down():
    content = SMOKE.read_text(encoding="utf-8")
    assert "[DEPLOY-10]" in content
    # DEPLOY-10 verifica que /api/health responde 200 INCLUSO con MES caído
    # (Pitfall 3: el healthcheck no parsea JSON, solo verifica HTTP 200).
    assert "/api/health" in content, "DEPLOY-10 debe testear /api/health (D-21)."
    assert "200" in content, (
        "DEPLOY-10 debe verificar respuesta 200 (Pitfall 3)."
    )


def test_smoke_sh_has_deploy_11_cloudflared_check():
    content = SMOKE.read_text(encoding="utf-8")
    assert "[DEPLOY-11]" in content
    assert "cloudflared" in content, (
        "DEPLOY-11 debe testear cloudflared running + tunnel info (D-21, CLOUD-04)."
    )


def test_smoke_sh_preserves_set_safety_flags():
    """Phase 6 invariant: NO añadir `-e` (smoke cuenta fallos, no fail-fast)."""
    content = SMOKE.read_text(encoding="utf-8")
    assert "set -uo pipefail" in content, (
        "deploy_smoke.sh debe usar `set -uo pipefail` (sin `-e`). "
        "Phase 6 invariant: el script cuenta fallos con FAILS, no aborta."
    )
    # Sentinel: si alguien mete `set -e` rompe el contador.
    assert "\nset -e" not in content and "set -e\n" not in content, (
        "deploy_smoke.sh NO debe añadir `set -e` — rompe el contador FAILS."
    )


def test_smoke_sh_preserves_exit_fails_count():
    """Phase 6 invariant: exit con el contador FAILS para integración cron/CI."""
    content = SMOKE.read_text(encoding="utf-8")
    assert 'exit "${FAILS}"' in content, (
        "deploy_smoke.sh debe terminar con `exit \"${FAILS}\"` (Phase 6 invariant)."
    )
```

Ejecutar `pytest tests/infra/test_deploy_smoke_has_11_checks.py -v`. DEBE devolver
fallos rojos en los 4 tests de DEPLOY-09/10/11 + el test del cardinality (sin los 3
nuevos tags el set quedará incompleto). Tests 1-3 + 7-8 deben pasar verde (smoke
existe + es válido + preserva flags).
  </action>
  <verify>
    <automated>pytest tests/infra/test_deploy_smoke_has_11_checks.py -v 2>&1 | grep -E "(FAILED|PASSED)" | tee /tmp/w0-02.log; grep -c FAILED /tmp/w0-02.log | grep -qE "^[4-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/infra/test_deploy_smoke_has_11_checks.py`
    - Contains 8 tests: `grep -c "^def test_" tests/infra/test_deploy_smoke_has_11_checks.py` returns `8`
    - ≥4 RED tests pre-impl: `pytest tests/infra/test_deploy_smoke_has_11_checks.py 2>&1 | grep -E "^FAILED" | wc -l` returns `>= 4`
    - Tests 1-3 (sanity) + 7-8 (invariants) PASS: `pytest tests/infra/test_deploy_smoke_has_11_checks.py::test_smoke_sh_exists tests/infra/test_deploy_smoke_has_11_checks.py::test_smoke_sh_preserves_set_safety_flags 2>&1 | grep -E "passed"` returns non-empty.
    - deploy_smoke.sh NOT modified yet: `git diff tests/infra/deploy_smoke.sh` is empty.
  </acceptance_criteria>
  <done>Archivo creado con 8 tests, ≥4 fallan rojo, 4-5 pasan verde (sanity + invariantes Phase 6). deploy_smoke.sh sin tocar todavía.</done>
</task>

<task type="auto">
  <name>Task W1-01: Caddyfile.prod — añadir snippet (security_headers) + import en ambos bloques (D-13)</name>
  <files>caddy/Caddyfile.prod</files>
  <read_first>
    - caddy/Caddyfile.prod (estado actual completo, 24 líneas)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md sección `caddy/Caddyfile.prod (modify)` — pattern target verbatim
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md D-13 + D-19 + D-20
    - tests/infra/test_caddyfile_security_headers.py (creado en W0-01) — los asserts dictan literales exactos
  </read_first>
  <action>
Reemplazar el contenido completo de `caddy/Caddyfile.prod` por la versión final
con el snippet `(security_headers)` y los 2 `import security_headers` en los dos
bloques de sitio. Texto literal (copiar tal cual, NO improvisar variaciones —
los tests del W0-01 esperan los strings exactos):

```caddyfile
# caddy/Caddyfile.prod — produccion LAN + Cloudflare Tunnel
# Phase 6 (LAN HTTPS) + Phase 9 (CF Tunnel + headers de seguridad).
#
# Cambios clave vs caddy/Caddyfile dev:
#   1. Sin el flag global de Caddy que desactiva redirects automaticos,
#      para que Caddy inserte redirect 80->443 por defecto (D-16).
#   2. Bloque con hostname explicito `nexo.ecsmobility.local` (D-01, D-06).
#   3. Bloque `:443` se conserva como fallback IP-directa (D-06) y como
#      entrada del Cloudflare Tunnel (D-20: cloudflared envia SNI=caddy:443
#      interno + Host=nexo.app; este bloque lo absorbe).
#   4. Snippet `(security_headers)` con los 5 directives de D-13, importado
#      en ambos bloques de sitio. Aplicado a ambas ramas LAN+CF.

# Snippet reusable de security headers (D-13 / CLOUD-03).
# Mozilla Observatory baseline >= B+ con estos 5 headers (sin CSP, fuera de scope).
# Source: https://caddyserver.com/docs/caddyfile/directives/header
(security_headers) {
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Frame-Options "DENY"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "geolocation=(), microphone=(), camera=()"
    }
}

# Rama LAN (D-19): equipos con hosts-file + root CA distribuida manualmente.
nexo.ecsmobility.local {
    import security_headers
    tls internal
    reverse_proxy web:8000

    log {
        output stdout
        format json
    }
}

# Rama Cloudflare Tunnel (D-20) + fallback IP-directa.
# cloudflared envia SNI=caddy:443 interno + Host=nexo.app; este bloque catch-all
# lo absorbe (Caddy default: strict_sni_host=false en v2.11+, R-05 mitigado).
# Cert autofirmado generico (tls internal); cloudflared usa noTLSVerify (D-11).
:443 {
    import security_headers
    tls internal
    reverse_proxy web:8000
}
```

NO añadir ningún otro bloque (el bloque `/access-denied` y el bloque `nexo.app`
público vienen en plan 09-02 — éste es solo headers + comentarios). NO añadir CSP
(D-13 explícitamente lo excluye).

Tras escribir, ejecutar:
  - `pytest tests/infra/test_caddyfile_prod.py -v` → debe seguir pasando 5/5 (regresión Phase 6)
  - `pytest tests/infra/test_caddyfile_security_headers.py -v` → debe pasar 10/10 (verde tras W0-01 que estaba rojo)

Si Docker está disponible, validar también con `docker compose run --rm caddy caddy validate --config /etc/caddy/Caddyfile` o equivalente. Si no, la sintaxis Caddyfile se asume correcta por seguir el pattern verbatim de research § Pattern 2.
  </action>
  <verify>
    <automated>pytest tests/infra/test_caddyfile_security_headers.py tests/infra/test_caddyfile_prod.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `caddy/Caddyfile.prod` contains `(security_headers) {`: `grep -c "(security_headers)" caddy/Caddyfile.prod` returns `1`
    - HSTS literal exact: `grep -c 'Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"' caddy/Caddyfile.prod` returns `1`
    - X-Frame-Options DENY: `grep -c 'X-Frame-Options "DENY"' caddy/Caddyfile.prod` returns `1`
    - X-Content-Type-Options nosniff: `grep -c 'X-Content-Type-Options "nosniff"' caddy/Caddyfile.prod` returns `1`
    - Referrer-Policy: `grep -c 'Referrer-Policy "strict-origin-when-cross-origin"' caddy/Caddyfile.prod` returns `1`
    - Permissions-Policy con geolocation/microphone/camera: `grep -E 'Permissions-Policy.*geolocation=\(\).*microphone=\(\).*camera=\(\)' caddy/Caddyfile.prod` returns line.
    - `import security_headers` aparece exactamente 2 veces (los 2 bloques de sitio): `grep -c "import security_headers" caddy/Caddyfile.prod` returns `2`
    - Phase 6 invariants intactos: `grep -c "tls internal" caddy/Caddyfile.prod` returns `>= 2`; `grep -c "reverse_proxy web:8000" caddy/Caddyfile.prod` returns `>= 2`
    - Phase 6 regression test PASS: `pytest tests/infra/test_caddyfile_prod.py -q 2>&1 | grep -E "(passed|failed)"` ⇒ "5 passed"
    - Phase 9 W0-01 test PASS: `pytest tests/infra/test_caddyfile_security_headers.py -q 2>&1 | grep -E "(passed|failed)"` ⇒ "10 passed"
    - NO `auto_https disable_redirects` (Landmine 1 Phase 6): `grep -c "auto_https disable_redirects" caddy/Caddyfile.prod` returns `0`
    - NO Content-Security-Policy header (D-13 lo excluye): `grep -ci "Content-Security-Policy" caddy/Caddyfile.prod` returns `0`
  </acceptance_criteria>
  <done>Caddyfile.prod tiene los 5 headers en snippet + import en ambos bloques. Tests Phase 6 + W0-01 todos verde.</done>
</task>

<task type="auto">
  <name>Task W1-02: deploy_smoke.sh — añadir DEPLOY-09/10/11 (D-21)</name>
  <files>tests/infra/deploy_smoke.sh</files>
  <read_first>
    - tests/infra/deploy_smoke.sh (estado actual, 94 líneas, 8 tags)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md sección `tests/infra/deploy_smoke.sh (modify)` — los 3 checks verbatim
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md D-21 — definición de los 3 nuevos checks
    - .planning/phases/09-cloudflare-tunnel-public-access/09-RESEARCH.md § Pitfall 7 — DEPLOY-11 debe permitir cloudflared parado para CLOUD-12
    - tests/infra/test_deploy_smoke_has_11_checks.py (creado W0-02) — dicta literales esperados
  </read_first>
  <action>
Editar `tests/infra/deploy_smoke.sh`. NO reescribir el archivo completo —
preservar las 91 líneas existentes y añadir 3 bloques de checks ANTES de la línea
`echo "=== Fallos: ${FAILS} ==="` (línea 92 actual).

Insertar el siguiente texto entre la línea 90 (último check DEPLOY-06 actual,
"ufw default incoming = deny") y la línea 92 (`echo "=== Fallos:`), respetando
exactamente el formato de las líneas existentes (4 espacios indent, comentarios
sobre cada check):

```bash

# DEPLOY-09: POST /api/login con creds dummy responde 401 (no 500).
# Verifica que el handler /api/login no escupe 500 ante credenciales basura
# (regresión: hubo un caso en Phase 2 donde un fallo interno devolvía 500
# en vez de 401 limpio). -k porque el cert es Caddy local.
check "DEPLOY-09" "POST /api/login con creds dummy responde 401" \
    "[ \"\$(curl -s -m 10 -k -o /dev/null -w '%{http_code}' -X POST -d 'email=nope@nope&password=nope' https://nexo.ecsmobility.local/api/login)\" = '401' ]"

# DEPLOY-10: GET /api/health responde 200 incluso con MES caído.
# Pitfall 3 / Phase 6 D-22: el healthcheck NO parsea JSON, solo verifica HTTP 200.
# Si MES está caído, el JSON dentro será {"ok": false, "checks": {...}} pero
# el HTTP status sigue siendo 200 — la app está viva, solo MES no.
check "DEPLOY-10" "GET /api/health responde 200 (incluso con MES caido)" \
    "[ \"\$(curl -s -m 10 -k -o /dev/null -w '%{http_code}' https://nexo.ecsmobility.local/api/health)\" = '200' ]"

# DEPLOY-11: cloudflared container running + tunnel info exit 0.
# CLOUD-04 + CLOUD-12: si cloudflared NO está running (rama CF parada
# voluntariamente para LAN-only mode, D-24), el check FALLA pero el smoke
# global sigue contando los demás OK. El operador interpreta el FAIL como
# "rama CF apagada" cuando ha hecho 'make cf-down' a propósito.
# Pitfall 7: NO usar SKIP graceful aquí — si fuera silently-skip, no
# detectaríamos cloudflared crashed accidentalmente. FAIL ruidoso es lo
# correcto; el operador sabe interpretar el contexto.
check "DEPLOY-11" "cloudflared container running + tunnel info OK" \
    "${COMPOSE} ps cloudflared --format json 2>/dev/null | grep -q '\"State\":\"running\"' && ${COMPOSE} exec -T cloudflared cloudflared tunnel info 2>/dev/null"
```

Mantener `set -uo pipefail` (NO cambiar a `-e`). Mantener `exit "${FAILS}"`.
Tras la edición, el archivo debe tener:
- 11 tags `[DEPLOY-XX]` distintos en el set: 01, 02, 03, 06, 09, 10, 11 (= 7 distintos; el contexto dice "11 checks" en sentido de cardinalidad de lineas check, no de tags distintos — el helper `check` ejecuta 1 línea por chequeo y los tags pueden repetirse si verifican facetas distintas del mismo requirement, ver Phase 6 que usa DEPLOY-01 ×2 y DEPLOY-06 ×4)
- pero los 3 NUEVOS tags 09/10/11 deben ser cardinalidad 1 cada uno (un solo check por nuevo requirement)

Validar con:
  - `bash -n tests/infra/deploy_smoke.sh` (syntax check)
  - `pytest tests/infra/test_deploy_smoke_has_11_checks.py -v` → debe pasar 8/8
  - `pytest tests/infra/test_deploy_lan_doc.py -v` → debe seguir pasando todos (regresión Phase 6)
  </action>
  <verify>
    <automated>bash -n tests/infra/deploy_smoke.sh && pytest tests/infra/test_deploy_smoke_has_11_checks.py tests/infra/test_deploy_lan_doc.py -v</automated>
  </verify>
  <acceptance_criteria>
    - bash sintaxis OK: `bash -n tests/infra/deploy_smoke.sh; echo $?` returns `0`
    - 11 distinct DEPLOY tags present (01,02,03,06,09,10,11): `grep -oE '\[DEPLOY-[0-9]{2}\]' tests/infra/deploy_smoke.sh | sort -u | wc -l` returns `7` (i.e. seven UNIQUE numbers; total occurrences may be higher because Phase 6 uses DEPLOY-01 twice etc.)
    - DEPLOY-09 covers /api/login + 401: `grep -A2 "DEPLOY-09" tests/infra/deploy_smoke.sh | grep -E "/api/login.*401|email=nope"` returns line.
    - DEPLOY-10 covers /api/health + 200: `grep -A2 "DEPLOY-10" tests/infra/deploy_smoke.sh | grep -E "/api/health.*200|200.*health"` returns line.
    - DEPLOY-11 covers cloudflared: `grep -A2 "DEPLOY-11" tests/infra/deploy_smoke.sh | grep -i "cloudflared"` returns line.
    - Phase 6 invariant `set -uo pipefail` preserved (no `-e`): `grep -c "set -uo pipefail" tests/infra/deploy_smoke.sh` returns `1`; `grep -cE '^set -e' tests/infra/deploy_smoke.sh` returns `0`
    - Phase 6 invariant exit FAILS preserved: `grep -c 'exit "${FAILS}"' tests/infra/deploy_smoke.sh` returns `1`
    - Executable bit preserved: `stat -c '%a' tests/infra/deploy_smoke.sh | grep -qE "[57]"` returns success.
    - Phase 9 W0-02 test PASS: `pytest tests/infra/test_deploy_smoke_has_11_checks.py -q 2>&1 | grep -E "passed"` ⇒ "8 passed"
    - Phase 6 regression test PASS: `pytest tests/infra/test_deploy_lan_doc.py -q 2>&1 | grep -E "passed"` ⇒ "24 passed" (count Phase 6 baseline)
    - No emojis introducidos: pytest test no_emoji on deploy_smoke.sh PASS (cubierto por test_deploy_lan_doc.py::test_smoke_sh_no_emoji)
  </acceptance_criteria>
  <done>deploy_smoke.sh tiene los 3 nuevos checks DEPLOY-09/10/11. Phase 9 W0-02 + Phase 6 regression todos verde. Bash syntax válida.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task W2-01: Smoke LAN manual contra servidor (CLOUD-12 partial validation)</name>
  <what-built>
    - 5 security headers en Caddyfile.prod (HSTS+XFO+XCTO+Referrer-Policy+Permissions-Policy)
    - 3 nuevos checks en deploy_smoke.sh (DEPLOY-09 login 401, DEPLOY-10 health 200, DEPLOY-11 cloudflared)
    - 2 stubs pytest en tests/infra/ (test_caddyfile_security_headers.py + test_deploy_smoke_has_11_checks.py)
  </what-built>
  <how-to-verify>
    Esta verificación requiere servidor Ubuntu LAN o stack prod local. Es OPCIONAL si el servidor no está disponible (la verificación final formal queda diferida al gate de plan 09-03).

    Si tienes acceso al servidor:
      1. SSH al servidor (o `make prod-up` localmente).
      2. `bash tests/infra/deploy_smoke.sh`
      3. Esperar resultado: como cloudflared NO está deployado todavía (eso lo trae 09-02), DEPLOY-11 falla. Esperado: 10 OK + 1 FAIL = exit 1. Esto es CORRECTO en este punto.
      4. Verificar headers en respuesta real: `curl -k -I https://nexo.ecsmobility.local/api/health | grep -iE 'strict-transport|x-frame|x-content-type|referrer-policy|permissions-policy'`. Esperado: 5 líneas devueltas.
      5. Si Mozilla Observatory accesible al server LAN (poco probable por ser interno), no es necesario ahora — esa verificación se hace en 09-03 contra `nexo.app`.

    Si NO tienes acceso al servidor, marcar este checkpoint como deferred-to-09-03 y proceder a 09-02. El smoke real (con cloudflared) se valida en plan 09-03 manual ops.
  </how-to-verify>
  <resume-signal>Type "approved" si el smoke devolvió 10/11 OK con DEPLOY-11 fallando esperablemente, los headers aparecen en `curl -I`, y todos los demás OK. Type "deferred" si no tienes servidor disponible (queda registrado para 09-03 gate). Si los headers NO aparecen o algún DEPLOY-01/02/03/06/09/10 falla, type "blocked" + log del fail para diagnóstico.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CF edge | TLS público termina en Cloudflare (futuro 09-03) |
| CF edge → cloudflared | Tunnel saliente cifrado (futuro 09-02) |
| cloudflared → caddy:443 | Red Docker interna; TLS interno con `noTLSVerify` (futuro 09-02) |
| caddy → web:8000 | Red Docker interna HTTP plano |
| LAN browser → caddy:443 | TLS interno con CA local (Phase 6 existente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering (Clickjacking) | Caddy responses (HTML pages) | mitigate | `X-Frame-Options "DENY"` en snippet `(security_headers)` aplicado a ambos bloques de Caddyfile.prod via `import security_headers` (D-13) |
| T-09-05 | Tampering (MIME sniff) | Caddy responses (JSON misinterpreted as HTML) | mitigate | `X-Content-Type-Options "nosniff"` en snippet (D-13) |
| T-09-06 | Tampering (HTTPS downgrade) | Caddy → browser session | mitigate | `Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"` en snippet + TLD `.app` HSTS preload obligatorio (D-13 + research § Mozilla Observatory) |
| T-09-07 | Information Disclosure (Referer leak) | Caddy responses (links to external) | mitigate | `Referrer-Policy "strict-origin-when-cross-origin"` en snippet (D-13) |
| T-09-08 | Tampering (browser API abuse) | Browser permissions-policy | mitigate | `Permissions-Policy "geolocation=(), microphone=(), camera=()"` en snippet (D-13) |
| T-09-10 | Smoke independence | deploy_smoke.sh DEPLOY-11 vs LAN fallback | accept | `DEPLOY-11` falla limpiamente con cloudflared parado (no SKIP). Operador interpreta FAIL como "rama CF apagada por D-24". CLOUD-12 valida fallback LAN intacto en plan 09-03 (smoke 10/11 OK con cf-down) |
</threat_model>

<verification>
- 2 stubs pytest pasan rojo en Wave 0 y verde en Wave 1
- `pytest tests/infra/test_caddyfile_security_headers.py tests/infra/test_caddyfile_prod.py tests/infra/test_deploy_smoke_has_11_checks.py tests/infra/test_deploy_lan_doc.py -v` ⇒ todos verde
- `bash -n tests/infra/deploy_smoke.sh` exit 0
- `make test` (coverage gate ≥60%) sigue verde — Phase 9 plan 09-01 NO añade código `api/`/`nexo/`, gate cubierto por construcción
- Smoke LAN manual contra servidor: 10/11 OK con cloudflared sin deployar todavía (DEPLOY-11 fail esperado; resto OK con headers presentes en curl -I)
- Pre-commit hooks no afectados — los archivos modificados son `caddy/`, `tests/infra/` que están fuera del scope `^(api|nexo)/`
</verification>

<success_criteria>
1. `caddy/Caddyfile.prod` contiene snippet `(security_headers)` con los 5 directives literales de D-13 e `import security_headers` en bloques `nexo.ecsmobility.local` y `:443`.
2. `tests/infra/deploy_smoke.sh` tiene los 3 nuevos checks DEPLOY-09/10/11 con bash syntax válida; preserva `set -uo pipefail` y `exit "${FAILS}"`.
3. `tests/infra/test_caddyfile_security_headers.py` (10 tests) pasa verde.
4. `tests/infra/test_deploy_smoke_has_11_checks.py` (8 tests) pasa verde.
5. Phase 6 regression suite (`tests/infra/test_caddyfile_prod.py` + `tests/infra/test_deploy_lan_doc.py`) sigue verde — sin regresiones.
6. Coverage gate `--cov-fail-under=60` sigue verde (sin código nuevo Python).
7. (Manual W2-01) Smoke LAN contra servidor real devuelve 10/11 OK con DEPLOY-11 fail esperado, y los headers aparecen en respuesta `curl -I` (queda diferido si no hay acceso a servidor).
</success_criteria>

<output>
Tras completar las 4 tareas auto + checkpoint, crear:
`.planning/phases/09-cloudflare-tunnel-public-access/09-01-SUMMARY.md`

Incluir:
- Tests Wave 0 creados (2 archivos, 18 tests totales)
- Caddyfile.prod diff resumido (snippet + 2 imports)
- deploy_smoke.sh diff resumido (3 checks añadidos)
- Estado de la verificación manual (approved/deferred/blocked)
- Cualquier desviación o discovery para alimentar 09-02
</output>
