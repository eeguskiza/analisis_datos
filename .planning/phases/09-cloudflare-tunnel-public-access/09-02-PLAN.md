---
phase: 09-cloudflare-tunnel-public-access
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - tests/infra/test_cloudflared_compose_service.py
  - tests/infra/test_env_example_has_cf_token.py
  - tests/infra/test_cloudflare_smoke_has_6_checks.py
  - tests/infra/test_makefile_cf_targets.py
  - tests/infra/test_cloudflare_docs_present.py
  - docker-compose.prod.yml
  - .env.example
  - .env.prod.example
  - caddy/Caddyfile.prod
  - static/cloudflare-denial.html
  - tests/infra/cloudflare_smoke.sh
  - scripts/cloudflare-bootstrap.sh
  - Makefile
  - docs/CLOUDFLARE_DEPLOY.md
  - CLAUDE.md
  - CHANGELOG.md
autonomous: true
requirements:
  - CLOUD-04
  - CLOUD-05
  - CLOUD-07
  - CLOUD-09
  - CLOUD-10
  - CLOUD-11
tags:
  - phase-9
  - cloudflare
  - cloudflared
  - docker-compose
  - docs
  - access-denied

must_haves:
  truths:
    - "El servicio `cloudflared` aparece en `docker-compose.prod.yml` con image, env vars TUNNEL_TOKEN, healthcheck, depends_on caddy healthy, restart unless-stopped"
    - "`make cf-up` arranca SOLO cloudflared; `make cf-down` lo para sin tocar el resto del stack"
    - "El binario `make cf-up` falla con mensaje claro si CLOUDFLARE_TUNNEL_TOKEN no está en .env"
    - "`/access-denied` se sirve PÚBLICAMENTE por Caddy desde `static/cloudflare-denial.html` (sin Access gating, sin auth Nexo, branding Nexo)"
    - "`tests/infra/cloudflare_smoke.sh` tiene 6 tags `[CF-01..CF-06]`, ejecutable, syntax bash válida, ejecutable desde fuera de la LAN"
    - "`docs/CLOUDFLARE_DEPLOY.md` cubre paso a paso desde 'crear cuenta CF' hasta 'smoke 4G' y permite a admin de respaldo replicar en <30 min"
    - "ADR-001 status sigue 'Proposed' hasta plan 09-03 (no se cambia aquí)"
    - "CLAUDE.md actualizado con override D-28 (sección Despliegue + sección Qué NO hacer)"
    - "CHANGELOG.md tiene entry [Phase 9] bajo [Unreleased]/Added"
    - "Coverage gate ≥60% sigue verde (Phase 9 NO añade código api/ ni nexo/)"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "servicio cloudflared remote-managed + bind mount cloudflare-denial.html"
      contains: "cloudflared:"
    - path: ".env.example"
      provides: "CLOUDFLARE_TUNNEL_TOKEN documentada para dev"
      contains: "CLOUDFLARE_TUNNEL_TOKEN"
    - path: ".env.prod.example"
      provides: "CLOUDFLARE_TUNNEL_TOKEN con placeholder <CHANGEME-> + procedencia"
      contains: "CLOUDFLARE_TUNNEL_TOKEN=<CHANGEME"
    - path: "caddy/Caddyfile.prod"
      provides: "ruta pública /access-denied servida desde /srv/static/cloudflare-denial.html sin Access ni auth Nexo"
      contains: "/access-denied"
    - path: "static/cloudflare-denial.html"
      provides: "HTML estático con branding Nexo + email Erik"
      contains: "e.eguskiza@ecsmobility.com"
    - path: "tests/infra/cloudflare_smoke.sh"
      provides: "6 checks externos CF-01..CF-06"
      contains: "[CF-06]"
    - path: "scripts/cloudflare-bootstrap.sh"
      provides: "validación pre-deploy + arranque + smoke local del túnel"
      contains: "CLOUDFLARE_TUNNEL_TOKEN"
    - path: "Makefile"
      provides: "5 cf-* targets"
      contains: "cf-up:"
    - path: "docs/CLOUDFLARE_DEPLOY.md"
      provides: "runbook 14 secciones + 2 apéndices, mirror DEPLOY_LAN.md"
      min_lines: 300
    - path: "CLAUDE.md"
      provides: "override D-28 aplicado"
      contains: "Cloudflare Tunnel"
    - path: "CHANGELOG.md"
      provides: "entry [Phase 9] bajo [Unreleased]/Added"
      contains: "Phase 9"
  key_links:
    - from: "docker-compose.prod.yml services.cloudflared.environment"
      to: "TUNNEL_TOKEN canonical"
      via: "TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN} mapping (CORRECTION-01)"
      pattern: "TUNNEL_TOKEN=\\$\\{CLOUDFLARE_TUNNEL_TOKEN\\}"
    - from: "caddy/Caddyfile.prod /access-denied block"
      to: "static/cloudflare-denial.html via /srv/static bind mount"
      via: "docker-compose.prod.yml caddy.volumes"
      pattern: "static/cloudflare-denial.html:/srv/static/cloudflare-denial.html:ro"
    - from: "Makefile cf-up"
      to: "docker compose ... up -d cloudflared"
      via: "PROD_COMPOSE var"
      pattern: "PROD_COMPOSE.*up -d cloudflared"
    - from: "docs/CLOUDFLARE_DEPLOY.md"
      to: "make cf-up + cloudflare_smoke.sh + ADR-001"
      via: "doc cross-references"
      pattern: "make cf-up|cloudflare_smoke.sh|ADR-001"
---

<objective>
Aterrizar el grueso de Phase 9 en repo: servicio Docker `cloudflared` (token-based
remote-managed, D-07/D-08/D-09 con CORRECTION-01 aplicada), denial page pública
servida por Caddy en `/access-denied` (D-17 revisado / BLOCKER-01 Opción B),
smoke externo `cloudflare_smoke.sh` con 6 checks CF-01..CF-06, Makefile con 5
targets `cf-*`, runbook completo `docs/CLOUDFLARE_DEPLOY.md`, override D-28 en
CLAUDE.md y entry en CHANGELOG. Plan ADITIVO PURO — nada existente se rompe;
sin cuenta CF/dominio comprado todavía, los archivos quedan listos para que
plan 09-03 active.

Purpose: cerrar CLOUD-04 (servicio cloudflared), CLOUD-05 (env var documentada),
CLOUD-07 (denial page Free-tier compatible vía Caddy público), CLOUD-09
(smoke externo 6 checks), CLOUD-10 (Makefile cf-*), CLOUD-11 (docs + CLAUDE.md +
CHANGELOG). ADR-001 status flip y CLAUDE.md "Qué NO hacer" override quedan en
plan 09-03 al cerrar la activación real (decisión de no flipearlo a "Accepted"
hasta tener 24h healthy).

Output: 5 stubs pytest Wave 0 + 7 archivos nuevos (denial.html, cloudflare_smoke.sh,
cloudflare-bootstrap.sh, CLOUDFLARE_DEPLOY.md, 3 docs hooks via test) + 5 archivos
modificados (compose, 2 env templates, Caddyfile, Makefile, CLAUDE.md, CHANGELOG).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-RESEARCH.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-VALIDATION.md
@.planning/phases/09-cloudflare-tunnel-public-access/09-01-SUMMARY.md
@CLAUDE.md
@docker-compose.prod.yml
@caddy/Caddyfile.prod
@.env.example
@.env.prod.example
@Makefile
@CHANGELOG.md
@docs/DEPLOY_LAN.md
@docs/decisions/ADR-001-cloudflare-tunnel.md
@scripts/deploy.sh
@tests/infra/deploy_smoke.sh
@tests/infra/test_compose_override.py
@tests/infra/test_env_prod_example.py
@tests/infra/test_makefile_devex.py
@tests/infra/test_deploy_lan_doc.py

<interfaces>
<!-- Snippets verbatim de los analogs y los pattern targets de PATTERNS.md. -->
<!-- El executor copia tal cual y rellena placeholders concretos. -->

### docker-compose.prod.yml — caddy block actual (líneas 43-58, ANALOG)

```yaml
caddy:
  volumes: !override
    - ./caddy/Caddyfile.prod:/etc/caddy/Caddyfile:ro
    - caddy_data:/data
    - caddy_config:/config
  healthcheck:
    test: ["CMD", "wget", "--spider", "-q", "http://localhost/"]
    interval: 30s
    timeout: 5s
    retries: 3
    start_period: 10s
  deploy:
    resources:
      limits:
        cpus: "0.5"
        memory: 256m
```

### docker-compose.prod.yml — bloque target a añadir (research § Code Examples + PATTERNS.md)

(a) Añadir `- ./static/cloudflare-denial.html:/srv/static/cloudflare-denial.html:ro` a `caddy.volumes`.

(b) Añadir top-level service `cloudflared`:

```yaml
cloudflared:
  image: cloudflare/cloudflared:latest
  command: tunnel --no-autoupdate run
  environment:
    # CORRECTION-01: cloudflared lee TUNNEL_TOKEN, no CLOUDFLARE_TUNNEL_TOKEN.
    # Mapeamos la variable de proyecto al nombre canónico aquí.
    - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    # TUNNEL_METRICS necesario para `cloudflared tunnel ready` healthcheck (Pitfall 4)
    - TUNNEL_METRICS=0.0.0.0:20241
  depends_on:
    caddy:
      condition: service_healthy
  restart: unless-stopped
  healthcheck:
    test: ["CMD", "cloudflared", "tunnel", "--metrics", "127.0.0.1:20241", "ready"]
    interval: 30s
    timeout: 5s
    retries: 3
    start_period: 30s
  deploy:
    resources:
      limits:
        cpus: "0.25"
        memory: 128m
```

### Caddyfile.prod — bloque /access-denied a añadir (BLOCKER-01 Opción B)

Añadir DENTRO del bloque `:443 { ... }` existente (creado en 09-01) un `handle_path`
que sirva `/access-denied` desde un fileserver, ANTES del reverse_proxy:

```caddyfile
:443 {
    import security_headers
    tls internal

    # BLOCKER-01 Opción B (D-17 revisado): denial page pública para usuarios
    # rechazados por Cloudflare Access. Sin gating, sin auth Nexo. AuditMiddleware
    # debe excluir esta ruta (no es path autenticado).
    handle_path /access-denied {
        root * /srv/static
        rewrite * /cloudflare-denial.html
        file_server
    }

    reverse_proxy web:8000
}
```

E idéntica adición al bloque `nexo.ecsmobility.local { ... }` (LAN fallback consistente).

### .env.prod.example — bloque a añadir al final (research § Code Examples)

```bash
# ── Cloudflare Tunnel (Phase 9) ───────────────────────────────────────────────
# Token del tunnel "nexo-prod" en Cloudflare Zero Trust.
# Obtener desde: dash.cloudflare.com -> Zero Trust -> Networks -> Tunnels ->
#                nexo-prod -> Install connector -> token (string largo base64-like)
# Rotacion: dashboard CF -> Configure -> Token -> Refresh; pegar nuevo valor aqui
#           y ejecutar `make cf-down && make cf-up` (sin downtime LAN).
# NUNCA commitear este archivo con valor real (placeholder <CHANGEME-...> obligatorio).
CLOUDFLARE_TUNNEL_TOKEN=<CHANGEME-cf-tunnel-token>
```

### Makefile — 5 targets cf-* a añadir (PATTERNS.md verbatim)

```makefile
# ── Cloudflare Tunnel (Phase 9) ──────────────────────────────────────────────
# Targets cf-* operan SOLO sobre el container cloudflared.
# El stack prod sigue arrancando con make prod-up (incluye cloudflared automaticamente
# por estar en docker-compose.prod.yml). Estos targets son para operar el tunel
# de forma independiente (rollback rapido D-24, debug, etc.).

cf-up: ## Arranca solo cloudflared (requiere CLOUDFLARE_TUNNEL_TOKEN en .env)
	$(PROD_COMPOSE) up -d cloudflared
	@echo "  cloudflared arrancado. Verifica con 'make cf-status'."

cf-down: ## Para SOLO cloudflared (LAN fallback intacto, D-24)
	$(PROD_COMPOSE) stop cloudflared
	@echo "  cloudflared parado. LAN fallback sigue accesible en https://nexo.ecsmobility.local"

cf-status: ## Estado del cloudflared + tunnel info
	$(PROD_COMPOSE) ps cloudflared
	@$(PROD_COMPOSE) exec -T cloudflared cloudflared tunnel --metrics 127.0.0.1:20241 ready && \
		echo "  tunnel HEALTHY" || echo "  tunnel UNHEALTHY"

cf-logs: ## Logs cloudflared en tiempo real
	$(PROD_COMPOSE) logs -f cloudflared

cf-smoke-external: ## Ejecuta cloudflare_smoke.sh (smoke desde fuera de la LAN, requiere ejecutar en maquina externa)
	bash tests/infra/cloudflare_smoke.sh
```

Y añadir a `.PHONY:` (línea 1): `cf-up cf-down cf-status cf-logs cf-smoke-external`.

### CHANGELOG.md — entry a añadir bajo [Unreleased] / ### Added (PATTERNS.md verbatim)

```markdown
- **Phase 9 (Sprint 7) — Cloudflare Tunnel + Public Access:** Acceso público a
  Nexo desde cualquier dispositivo via `https://nexo.app` con allowlist email
  Cloudflare Access (OTP, cookie 24h, Free tier). Servicio `cloudflared`
  remote-managed en `docker-compose.prod.yml` (target `https://caddy:443`,
  TLS verify off, noTLSVerify documentado). 5 headers de seguridad en Caddy
  (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy,
  Permissions-Policy) aplicados a ambas ramas LAN+CF — Mozilla Observatory >= B+.
  Denial page branded en `static/cloudflare-denial.html` servida públicamente
  via Caddy (Free tier respetado, BLOCKER-01 resuelto). Smoke LAN ampliado de
  8 a 11 checks; smoke externo `tests/infra/cloudflare_smoke.sh` con 6 checks
  CF-*. Makefile `cf-up/down/status/logs/smoke-external`. Runbook
  `docs/CLOUDFLARE_DEPLOY.md` + ADR-001 en `docs/decisions/`. **Fallback LAN
  preservado** (`docker compose stop cloudflared` revierte sin pérdida).
```

### CLAUDE.md — D-28 override (PATTERNS.md sección CLAUDE.md)

Sección "Qué es Nexo" línea ~26:

  ANTES: `Despliegue: LAN interna ECS (Ubuntu Server 24.04). Sin exposición a internet.`

  DESPUÉS: `Despliegue: LAN interna ECS (Ubuntu Server 24.04) + público vía Cloudflare Tunnel saliente con allowlist Cloudflare Access (ver docs/decisions/ADR-001-cloudflare-tunnel.md).`

Sección "Qué NO hacer":

  QUITAR la línea: `- No comprar dominio público ni exponer Nexo a internet.`

  AÑADIR la línea: `- No exponer puertos del servidor a internet directamente — todo tráfico público pasa obligatoriamente por Cloudflare Tunnel saliente, autenticado por Cloudflare Access antes de tocar Nexo. UFW debe seguir cerrando 80/443 al WAN.`

</interfaces>

</context>

<tasks>

<!-- ════════════════ WAVE 0 — pytest stubs (5 archivos) ════════════════ -->

<task type="auto" tdd="true">
  <name>Task W0-01: Stub test_cloudflared_compose_service.py</name>
  <files>tests/infra/test_cloudflared_compose_service.py</files>
  <read_first>
    - tests/infra/test_compose_override.py (analog, especialmente líneas 1-110 — pattern parsing YAML/raw text)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md sección "test_cloudflared_compose_service.py"
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md D-07/D-08/D-09 + CORRECTION-01 + Pitfall 4 + Pitfall 5
    - .planning/phases/09-cloudflare-tunnel-public-access/09-RESEARCH.md § Code Examples (bloque cloudflared completo)
  </read_first>
  <behavior>
    - test 1: docker-compose.prod.yml existe (regresión Phase 6)
    - test 2: contiene servicio `cloudflared:`
    - test 3: image == `cloudflare/cloudflared:latest`
    - test 4: command empieza con `tunnel ` (prefijo, no requiere args exactos)
    - test 5: environment mapea TUNNEL_TOKEN explicit (CORRECTION-01)
    - test 6: environment NO usa CLI arg `--token` (Pitfall 5)
    - test 7: environment incluye `TUNNEL_METRICS=0.0.0.0:20241` (Pitfall 4)
    - test 8: depends_on caddy con condition: service_healthy
    - test 9: restart: unless-stopped
    - test 10: healthcheck con `cloudflared tunnel` y `ready`
    - test 11: NO publica ports al host (outbound-only)
    - test 12: caddy.volumes incluye bind mount `./static/cloudflare-denial.html:/srv/static/cloudflare-denial.html:ro`
    Pre-impl Wave 1: todos rojos. Post W1-01 + W1-02: todos verde.
  </behavior>
  <action>
Crear `tests/infra/test_cloudflared_compose_service.py`. Como `docker-compose.prod.yml`
usa tags YAML custom `!reset` y `!override`, hay que parsearlo con un
constructor permisivo o leer el archivo raw para los matches simples. Mirar
`test_compose_override.py` líneas 57-110 que combina raw read con `_compose_config_json`
opcional. Aquí usaremos predominantemente raw read para evitar la complejidad de
yaml.add_constructor:

```python
"""Validaciones estaticas del servicio cloudflared en docker-compose.prod.yml.

Phase 9 / Plan 09-02 / D-07/D-08/D-09 (CORRECTION-01) / CLOUD-04.

No requiere Docker. Lee el archivo y comprueba presencia de literales criticos.
Usa parseo raw (regex/substring) en lugar de yaml.safe_load porque el override
contiene tags custom `!reset` y `!override` (Compose v2.24+). El test
test_compose_override.py existente ya valida la estructura merged via docker
compose config; aqui nos limitamos a contractos del bloque cloudflared.
"""
from __future__ import annotations

import re
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
COMPOSE_PROD = REPO_ROOT / "docker-compose.prod.yml"


def _cloudflared_block() -> str:
    """Devuelve el sub-bloque YAML del servicio cloudflared (raw text)."""
    content = COMPOSE_PROD.read_text(encoding="utf-8")
    # Localizar `  cloudflared:` (2 espacios indent, top-level service).
    idx = content.find("\n  cloudflared:")
    assert idx >= 0, "Servicio cloudflared ausente en docker-compose.prod.yml (CLOUD-04)."
    block = content[idx:]
    # Acotar al siguiente service top-level (2 espacios) o EOF.
    m = re.search(r"\n  [a-z_-]+:", block[1:])
    if m:
        block = block[: m.start() + 1]
    return block


def test_compose_prod_exists():
    assert COMPOSE_PROD.exists(), f"Falta {COMPOSE_PROD} (regresion Phase 6)."


def test_cloudflared_service_declared():
    content = COMPOSE_PROD.read_text(encoding="utf-8")
    assert "\n  cloudflared:" in content, (
        "Servicio cloudflared debe declararse top-level en docker-compose.prod.yml (D-07, CLOUD-04)."
    )


def test_cloudflared_image_official_latest():
    block = _cloudflared_block()
    assert "image: cloudflare/cloudflared:latest" in block, (
        "image debe ser exactamente `cloudflare/cloudflared:latest` (D-07)."
    )


def test_cloudflared_command_uses_tunnel_run():
    block = _cloudflared_block()
    assert re.search(r"command:\s*tunnel\b", block), (
        "command debe empezar con `tunnel` (D-08 token-based remote-managed)."
    )
    # Pitfall 5: no `--token` en CLI args (token leak via docker inspect).
    assert "--token" not in block, (
        "command NO debe pasar `--token` por CLI (Pitfall 5: token leak via "
        "docker inspect). Usa env var TUNNEL_TOKEN."
    )


def test_cloudflared_env_maps_tunnel_token_correction_01():
    """CORRECTION-01: cloudflared lee TUNNEL_TOKEN (no CLOUDFLARE_TUNNEL_TOKEN)."""
    block = _cloudflared_block()
    assert "TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}" in block, (
        "environment debe mapear `TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}` "
        "(CORRECTION-01: container reads TUNNEL_TOKEN; project naming preserves "
        "CLOUDFLARE_TUNNEL_TOKEN in .env)."
    )


def test_cloudflared_env_includes_tunnel_metrics():
    """Pitfall 4: TUNNEL_METRICS=0.0.0.0:20241 necesario para healthcheck `tunnel ready`."""
    block = _cloudflared_block()
    assert "TUNNEL_METRICS=0.0.0.0:20241" in block, (
        "environment debe incluir `TUNNEL_METRICS=0.0.0.0:20241` (Pitfall 4: "
        "sin esto, `cloudflared tunnel ready` falla con connection refused)."
    )


def test_cloudflared_depends_on_caddy_healthy():
    block = _cloudflared_block()
    # Permite varios formatos de YAML pero el contrato es claro.
    assert "depends_on:" in block, "Falta depends_on (D-07)."
    assert "caddy:" in block, "depends_on debe incluir caddy."
    assert "service_healthy" in block, (
        "depends_on.caddy.condition debe ser `service_healthy` (D-10: cloudflared "
        "no debe arrancar antes que caddy esté operativo)."
    )


def test_cloudflared_restart_unless_stopped():
    block = _cloudflared_block()
    assert "restart: unless-stopped" in block, (
        "restart debe ser `unless-stopped` (D-07; consistencia con resto del stack)."
    )


def test_cloudflared_healthcheck_uses_tunnel_ready():
    block = _cloudflared_block()
    assert "healthcheck:" in block, "Falta healthcheck (Pitfall 4)."
    # `cloudflared tunnel ready` es comando oficial PR #1135.
    assert "cloudflared" in block and "tunnel" in block and "ready" in block, (
        "healthcheck debe usar `cloudflared tunnel ... ready` (Pitfall 4)."
    )


def test_cloudflared_no_published_ports():
    """Outbound-only: cloudflared inicia conexion saliente, NO publica puertos."""
    block = _cloudflared_block()
    # `ports:` clave directa en cloudflared NO debe aparecer.
    assert not re.search(r"^\s+ports:\s*\n\s+- ", block, re.MULTILINE), (
        "cloudflared NO debe publicar ports al host (outbound-only por diseño)."
    )


def test_caddy_volumes_includes_denial_html_bind_mount():
    """BLOCKER-01 Opción B: denial.html bind-monteado para servir /access-denied desde Caddy."""
    content = COMPOSE_PROD.read_text(encoding="utf-8")
    assert (
        "./static/cloudflare-denial.html:/srv/static/cloudflare-denial.html:ro"
        in content
    ), (
        "caddy.volumes debe bind-mount `./static/cloudflare-denial.html:/srv/static/cloudflare-denial.html:ro` "
        "para que el bloque `/access-denied` del Caddyfile.prod pueda servir el HTML estático."
    )


def test_cloudflared_resource_limits_present():
    """Higiene: limites de CPU/memoria definidos para no comerse el host."""
    block = _cloudflared_block()
    assert "deploy:" in block, "Falta deploy block."
    assert "resources:" in block, "Falta deploy.resources."
    assert "limits:" in block, "Falta deploy.resources.limits."
```

Ejecutar `pytest tests/infra/test_cloudflared_compose_service.py -v`. Pre-impl
deben fallar TODOS o casi todos los 12 tests (test_compose_prod_exists pasa
porque el archivo existe de Phase 6).
  </action>
  <verify>
    <automated>pytest tests/infra/test_cloudflared_compose_service.py -v 2>&1 | grep -E "(FAILED|PASSED)" | tee /tmp/w0-cloudflared.log; grep -c FAILED /tmp/w0-cloudflared.log | grep -qE "^(1[0-2]|9)$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/infra/test_cloudflared_compose_service.py`
    - 12 tests defined: `grep -c "^def test_" tests/infra/test_cloudflared_compose_service.py` returns `12`
    - ≥10 RED pre-impl: `pytest tests/infra/test_cloudflared_compose_service.py 2>&1 | grep -E "^FAILED" | wc -l` returns `>= 10`
    - test_compose_prod_exists PASS (sanity): pytest filter shows passed.
    - docker-compose.prod.yml NOT modified yet: `git diff docker-compose.prod.yml` is empty.
  </acceptance_criteria>
  <done>12 tests, ≥10 fallan rojo, archivo Phase 6 sin tocar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task W0-02: Stub test_env_example_has_cf_token.py</name>
  <files>tests/infra/test_env_example_has_cf_token.py</files>
  <read_first>
    - tests/infra/test_env_prod_example.py (analog, especialmente líneas 69-79 + 96-115 patrón <CHANGEME->)
    - .env.example (estado actual — verificar que NO tiene CLOUDFLARE_*)
    - .env.prod.example (estado actual)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md sección "test_env_example_has_cf_token.py"
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md D-09 + Pitfall 8 (dual sources)
  </read_first>
  <behavior>
    - test 1: .env.example existe y contiene `CLOUDFLARE_TUNNEL_TOKEN=` (sin valor real, comentario procedencia presente)
    - test 2: .env.prod.example existe y contiene `CLOUDFLARE_TUNNEL_TOKEN=<CHANGEME-...>` (placeholder pattern, mirror test_env_prod_example_no_real_secrets)
    - test 3: ambos archivos mencionan "Cloudflare" en comentario contiguo a la línea (procedencia documentada)
    - test 4: ambos archivos mencionan al menos uno de los strings de procedencia: "Zero Trust", "Tunnels", o "Install connector" (D-09)
    - test 5: regression Phase 6: las NEXO_* del test_env_prod_example existente siguen presentes en .env.prod.example
  </behavior>
  <action>
Crear `tests/infra/test_env_example_has_cf_token.py`:

```python
"""Validaciones de CLOUDFLARE_TUNNEL_TOKEN en .env.example y .env.prod.example.

Phase 9 / Plan 09-02 / D-09 (revisado CORRECTION-01) / Pitfall 8 / CLOUD-05.

Pitfall 8: el repo tiene 2 archivos (.env.example dev + .env.prod.example prod
canónico). El operador en producción copia .env.prod.example -> /opt/nexo/.env,
así que la variable DEBE estar en ambos. .env.example puede tener valor vacío
(dev no arranca cloudflared), pero .env.prod.example DEBE usar placeholder
<CHANGEME-...> consistente con el resto de secretos (regla _no_real_secrets
del test analog).
"""
from __future__ import annotations

import re
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
ENV_EXAMPLE = REPO_ROOT / ".env.example"
ENV_PROD_EXAMPLE = REPO_ROOT / ".env.prod.example"


def _line_with_comment_context(path: Path, var: str, ctx_lines: int = 5) -> str:
    """Devuelve la línea var=... + N líneas previas (contexto comentario)."""
    content = path.read_text(encoding="utf-8")
    lines = content.splitlines()
    for i, line in enumerate(lines):
        if line.startswith(f"{var}="):
            start = max(0, i - ctx_lines)
            return "\n".join(lines[start : i + 1])
    return ""


def test_env_example_exists():
    assert ENV_EXAMPLE.exists(), f"Falta {ENV_EXAMPLE} (regresion Phase 1)."


def test_env_prod_example_exists():
    assert ENV_PROD_EXAMPLE.exists(), f"Falta {ENV_PROD_EXAMPLE} (regresion Phase 6)."


def test_env_example_has_cloudflare_tunnel_token():
    """CLOUD-05: .env.example debe declarar CLOUDFLARE_TUNNEL_TOKEN."""
    content = ENV_EXAMPLE.read_text(encoding="utf-8")
    assert re.search(r"^CLOUDFLARE_TUNNEL_TOKEN=", content, re.MULTILINE), (
        ".env.example debe contener línea `CLOUDFLARE_TUNNEL_TOKEN=` (CLOUD-05)."
    )


def test_env_prod_example_has_cloudflare_tunnel_token():
    """CLOUD-05: .env.prod.example debe declarar CLOUDFLARE_TUNNEL_TOKEN (Pitfall 8)."""
    content = ENV_PROD_EXAMPLE.read_text(encoding="utf-8")
    assert re.search(r"^CLOUDFLARE_TUNNEL_TOKEN=", content, re.MULTILINE), (
        ".env.prod.example debe contener línea `CLOUDFLARE_TUNNEL_TOKEN=` (Pitfall 8)."
    )


def test_env_prod_example_cloudflare_token_uses_placeholder():
    """No-real-secrets: .env.prod.example debe usar <CHANGEME-...> placeholder."""
    content = ENV_PROD_EXAMPLE.read_text(encoding="utf-8")
    m = re.search(r"^CLOUDFLARE_TUNNEL_TOKEN=(.*)$", content, re.MULTILINE)
    assert m is not None, "Línea CLOUDFLARE_TUNNEL_TOKEN ausente."
    value = m.group(1).strip()
    assert value.startswith("<CHANGEME") or value == "", (
        f"Valor de CLOUDFLARE_TUNNEL_TOKEN debe ser <CHANGEME-...> o vacío, "
        f"no `{value!r}` (regresión _no_real_secrets pattern Phase 6 D-08)."
    )


def test_env_example_cloudflare_token_has_cloudflare_comment():
    """Procedencia: comentario contiguo debe mencionar Cloudflare."""
    ctx = _line_with_comment_context(ENV_EXAMPLE, "CLOUDFLARE_TUNNEL_TOKEN")
    assert "Cloudflare" in ctx, (
        f"El bloque comentario sobre CLOUDFLARE_TUNNEL_TOKEN en .env.example debe "
        f"mencionar Cloudflare (procedencia D-09). Contexto encontrado: {ctx!r}"
    )


def test_env_prod_example_cloudflare_token_documents_origin():
    """D-09: comentario debe explicar dónde obtener el token."""
    ctx = _line_with_comment_context(ENV_PROD_EXAMPLE, "CLOUDFLARE_TUNNEL_TOKEN")
    assert "Cloudflare" in ctx, "Comentario no menciona Cloudflare."
    # Debe haber al menos uno de los siguientes keywords del path en el dashboard.
    keywords = ("Zero Trust", "Tunnels", "Install connector", "tunnel")
    assert any(kw in ctx for kw in keywords), (
        f"El comentario sobre CLOUDFLARE_TUNNEL_TOKEN debe mencionar al menos uno "
        f"de: {keywords} (procedencia D-09). Contexto: {ctx!r}"
    )
```

Ejecutar `pytest tests/infra/test_env_example_has_cf_token.py -v`. Pre-impl: tests
1-2 verde (archivos existen), tests 3-7 rojos (variable no añadida).
  </action>
  <verify>
    <automated>pytest tests/infra/test_env_example_has_cf_token.py -v 2>&1 | grep -E "(FAILED|PASSED)" | tee /tmp/w0-env.log; grep -c FAILED /tmp/w0-env.log | grep -qE "^[4-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/infra/test_env_example_has_cf_token.py`
    - 7 tests defined: `grep -c "^def test_" tests/infra/test_env_example_has_cf_token.py` returns `7`
    - ≥4 RED pre-impl: `pytest tests/infra/test_env_example_has_cf_token.py 2>&1 | grep -E "^FAILED" | wc -l` returns `>= 4`
    - .env.example NOT modified yet: `git diff .env.example` is empty.
    - .env.prod.example NOT modified yet: `git diff .env.prod.example` is empty.
  </acceptance_criteria>
  <done>7 tests, ≥4 fallan rojo, env templates sin tocar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task W0-03: Stub test_cloudflare_smoke_has_6_checks.py</name>
  <files>tests/infra/test_cloudflare_smoke_has_6_checks.py</files>
  <read_first>
    - tests/infra/test_deploy_lan_doc.py líneas 180-234 (analog smoke regression)
    - .planning/phases/09-cloudflare-tunnel-public-access/09-PATTERNS.md sección "test_cloudflare_smoke_has_6_checks.py" + sección sobre cloudflare_smoke.sh
    - .planning/phases/09-cloudflare-tunnel-public-access/09-CONTEXT.md D-22
    - .planning/phases/09-cloudflare-tunnel-public-access/09-RESEARCH.md § Pattern 3 (smoke skeleton)
  </read_first>
  <behavior>
    - test 1: tests/infra/cloudflare_smoke.sh existe
    - test 2: ejecutable (bit `x` user)
    - test 3: bash sintaxis válida (`bash -n`)
    - test 4: contiene 6 tags `[CF-01]..[CF-06]` distintos
    - test 5: usa `set -uo pipefail` (consistencia Phase 6 deploy_smoke.sh)
    - test 6: no contiene emojis (regresión Phase 6 no_emoji policy)
    - test 7: termina con `exit "${FAILS}"` (cron-ready)
    - test 8: helper `check()` igual al de deploy_smoke.sh (regresión consistencia)
    - test 9: CF-06 menciona `cloudflared` (CF-06 valida tunnel HEALTHY desde el server)
  </behavior>
  <action>
Crear `tests/infra/test_cloudflare_smoke_has_6_checks.py`:

```python
"""Validacion estatica de tests/infra/cloudflare_smoke.sh.

Phase 9 / Plan 09-02 / D-22 / CLOUD-09.

cloudflare_smoke.sh es el smoke externo (4G tethering) que valida la rama CF
end-to-end. Contractualmente debe tener 6 tags `[CF-0X]` distintos, ser
ejecutable, y respetar las convenciones bash de deploy_smoke.sh (set -uo
pipefail, exit count, no emojis).
"""
from __future__ import annotations

import re
import shutil
import stat
import subprocess
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
SMOKE = REPO_ROOT / "tests" / "infra" / "cloudflare_smoke.sh"


def test_cloudflare_smoke_exists():
    assert SMOKE.exists(), f"Falta {SMOKE} (CLOUD-09)."


def test_cloudflare_smoke_is_executable():
    assert SMOKE.stat().st_mode & stat.S_IXUSR, (
        "cloudflare_smoke.sh debe ser ejecutable (chmod +x)."
    )


def test_cloudflare_smoke_bash_syntax_valid():
    if shutil.which("bash") is None:
        import pytest
        pytest.skip("bash no disponible")
    r = subprocess.run(
        ["bash", "-n", str(SMOKE)],
        capture_output=True, text=True, timeout=10, check=False,
    )
    assert r.returncode == 0, f"bash -n falló: {r.stderr}"


def test_cloudflare_smoke_has_6_distinct_cf_tags():
    """CLOUD-09: cloudflare_smoke.sh debe tener 6 tags distintos `[CF-01..CF-06]`."""
    content = SMOKE.read_text(encoding="utf-8")
    tags = set(re.findall(r"\[CF-(\d{2})\]", content))
    expected = {f"{n:02d}" for n in range(1, 7)}
    missing = expected - tags
    assert not missing, (
        f"Faltan tags CF: {sorted(missing)}. Encontrados: {sorted(tags)}. "
        f"D-22 exige CF-01..CF-06."
    )


def test_cloudflare_smoke_uses_set_safety_flags():
    content = SMOKE.read_text(encoding="utf-8")
    # Phase 6 invariante: -uo pipefail SIN -e (smoke cuenta fallos).
    assert "set -uo pipefail" in content or "set -uoe pipefail" in content, (
        "cloudflare_smoke.sh debe usar `set -uo pipefail` (consistencia Phase 6)."
    )


def test_cloudflare_smoke_has_exit_fails_count():
    content = SMOKE.read_text(encoding="utf-8")
    assert "FAILS" in content or "FAIL_COUNT" in content, (
        "smoke debe tener contador de fallos."
    )
    assert 'exit "${FAILS}"' in content or "exit ${FAILS}" in content, (
        "cloudflare_smoke.sh debe terminar con `exit ${FAILS}`."
    )


def test_cloudflare_smoke_no_emoji():
    content = SMOKE.read_text(encoding="utf-8")
    emoji_re = re.compile(
        r"[\U0001F300-\U0001FAFF\U00002600-\U000027BF\U0001F600-\U0001F64F]"
    )
    m = emoji_re.search(content)
    assert m is None, (
        f"Emoji prohibido en cloudflare_smoke.sh (CLAUDE.md): {m.group(0)!r}"
    )


def test_cloudflare_smoke_uses_check_helper():
    """Consistencia: misma funcion `check()` que deploy_smoke.sh."""
    content = SMOKE.read_text(encoding="utf-8")
    # El helper define una function `check() { ... }`.
    assert "check() {" in content or "check () {" in content, (
        "cloudflare_smoke.sh debe definir helper `check() { ... }` "
        "(consistencia con deploy_smoke.sh líneas 30-40)."
    )


def test_cloudflare_smoke_cf_06_mentions_cloudflared():
    """CF-06 valida tunnel info HEALTHY desde el servidor."""
    content = SMOKE.read_text(encoding="utf-8")
    cf06_idx = content.find("[CF-06]")
    assert cf06_idx >= 0, "Tag [CF-06] ausente."
    cf06_block = content[cf06_idx : cf06_idx + 500]
    assert "cloudflared" in cf06_block, (
        "CF-06 debe verificar `cloudflared tunnel ...` (D-22 / Pitfall 4)."
    )


def test_cloudflare_smoke_cf_05_checks_security_headers():
    """CF-05 valida que los 5 headers de D-13 llegan al cliente externo."""
    content = SMOKE.read_text(encoding="utf-8")
    cf05_idx = content.find("[CF-05]")
    assert cf05_idx >= 0, "Tag [CF-05] ausente."
    cf05_block = content[cf05_idx : cf05_idx + 800]
    # Al menos verifica HSTS y X-Frame-Options llegan
    headers_found = sum(
        1 for h in ("strict-transport", "x-frame", "x-content-type")
        if re.search(h, cf05_block, re.IGNORECASE)
    )
    assert headers_found >= 2, (
        "CF-05 debe verificar al menos 2 headers de seguridad llegando al "
        f"cliente externo. Solo encontrado {headers_found}."
    )
```

Ejecutar pytest. Pre-impl: TODOS rojos (archivo no existe).
  </action>
  <verify>
    <automated>pytest tests/infra/test_cloudflare_smoke_has_6_checks.py -v 2>&1 | grep -E "(FAILED|PASSED)" | tee /tmp/w0-cfsmoke.log; grep -c FAILED /tmp/w0-cfsmoke.log | grep -qE "^([89]|10)$"