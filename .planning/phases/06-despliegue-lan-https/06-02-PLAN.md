---
phase: 06-despliegue-lan-https
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/deploy.sh
  - scripts/backup_nightly.sh
  - Makefile
  - tests/infra/test_deploy_script.py
  - tests/infra/test_backup_script.py
autonomous: true
requirements: [DEPLOY-04]
tags: [infra, deploy, backup, cron, scripts]

must_haves:
  truths:
    - "`scripts/deploy.sh` ejecuta idempotentemente git pull --ff-only, pg_dump pre-deploy (atomic rename), build --pull, up -d, smoke curl https://nexo.ecsmobility.local/api/health."
    - "`scripts/deploy.sh` falla con exit 1 sin mutar estado si git pull detecta divergencia (no force)."
    - "`scripts/backup_nightly.sh` genera `/var/backups/nexo/nexo-YYYYMMDD-HHMM.sql.gz` con atomic rename (.tmp → final), chmod 600 y rota archivos mtime > 7 días."
    - "Ambos scripts son `bash -n` válidos y tienen bit ejecutable committeado."
    - "El Makefile ofrece targets `make prod-up`, `make prod-down`, `make prod-logs`, `make prod-status`, `make prod-health`, `make deploy`, `make backup`."
    - "`make up` / `make dev` / `make clean` dev siguen funcionando exactamente como antes (sin regresión)."
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "Deploy idempotente: git pull --ff-only, pre-deploy backup predeploy-<hash>, build --pull, up -d, smoke HTTPS"
      contains: "set -euo pipefail"
      min_lines: 60
    - path: "scripts/backup_nightly.sh"
      provides: "pg_dump | gzip al dir /var/backups/nexo/ con atomic rename, chmod 600 y retención 7d"
      contains: "mv"
      min_lines: 20
    - path: "Makefile"
      provides: "Targets prod-* + deploy + backup; dev targets intactos"
      contains: "PROD_COMPOSE"
    - path: "tests/infra/test_deploy_script.py"
      provides: "Pytest que valida deploy.sh sintaxis + pasos clave + landmines prohibidas"
      contains: "def test_"
    - path: "tests/infra/test_backup_script.py"
      provides: "Pytest que valida backup_nightly.sh sintaxis + atomic rename + retención + chmod 600"
      contains: "def test_"
  key_links:
    - from: "scripts/deploy.sh"
      to: "docker-compose.prod.yml"
      via: "`docker compose -f docker-compose.yml -f docker-compose.prod.yml ...`"
      pattern: "docker-compose.prod.yml"
    - from: "scripts/deploy.sh"
      to: "caddy/Caddyfile.prod (via compose override)"
      via: "smoke curl -k https://nexo.ecsmobility.local/api/health"
      pattern: "https://nexo.ecsmobility.local/api/health"
    - from: "scripts/backup_nightly.sh"
      to: "docker compose exec -T db pg_dump"
      via: "pg_dump via compose exec prod"
      pattern: "pg_dump"
    - from: "Makefile"
      to: "scripts/deploy.sh + scripts/backup_nightly.sh"
      via: "targets make deploy / make backup invocan directamente"
      pattern: "scripts/deploy.sh"
---

<objective>
Crear la capa de automatización: `scripts/deploy.sh` (idempotente, smoke-gated, pre-deploy backup), `scripts/backup_nightly.sh` (invocado por cron, atomic write, retención 7d), y targets Makefile `prod-*` + `deploy` + `backup`. Añadir tests de infra que validan sintaxis y presencia de pasos críticos.

Purpose: el runbook DEPLOY_LAN.md (Plan 06-03) referencia estos scripts. Sin ellos, el deploy sería una receta manual larga que el operador de respaldo (D-12, bus factor 2) tendría que ejecutar comando por comando cada vez.

Output: 2 scripts shell + edición del Makefile + 2 archivos de test. Todo committeable y verificable en WSL sin servidor Ubuntu real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-despliegue-lan-https/06-CONTEXT.md
@.planning/phases/06-despliegue-lan-https/06-RESEARCH.md
@.planning/phases/06-despliegue-lan-https/06-VALIDATION.md
@.planning/phases/06-despliegue-lan-https/06-01-PLAN.md
@CLAUDE.md
@Makefile
@docker-compose.yml
@docker-compose.prod.yml
@caddy/Caddyfile.prod

<interfaces>
### deploy.sh — contrato

- Input: CWD cualquier (script hace `cd "${PROJECT_DIR}"`), args: ninguno, ENV: `.env` leído por compose.
- Output: stdout log `[deploy ISO8601] <msg>`; containers arrancados con base+override; backup `/var/backups/nexo/predeploy/predeploy-<hash>-<stamp>.sql.gz`; exit 0 si smoke OK.

### backup_nightly.sh — contrato

- Input: CWD cualquier, args: ninguno, asume PROJECT_DIR=/opt/nexo.
- Output: `/var/backups/nexo/nexo-<stamp>.sql.gz` (chmod 600), rotación -mtime +7 en el mismo dir, stdout log capturado por cron.

### Makefile — targets nuevos

`prod-up` / `prod-down` / `prod-logs` / `prod-status` / `prod-health` / `deploy` / `backup`. Dev targets sin cambios.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear scripts/deploy.sh + tests/infra/test_deploy_script.py</name>
  <files>scripts/deploy.sh, tests/infra/test_deploy_script.py</files>

  <read_first>
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Topic 7" (líneas 794–879) — script completo verbatim
    - .planning/phases/06-despliegue-lan-https/06-CONTEXT.md §"D-25, D-26" — pasos canónicos
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Pitfall 4, 5, 6" — atomic rename, git divergencia, up -d
    - scripts/ (no hay precedente bash en repo; scripts actuales son Python)
    - CLAUDE.md §"Política de commits" y §"Qué NO hacer" — no force, no --no-verify, no emojis
  </read_first>

  <action>
Crear `scripts/deploy.sh` con el contenido literal siguiente (copiado de RESEARCH.md Topic 7 con el añadido de atomic rename `.tmp`→final al backup pre-deploy, que RESEARCH.md recomienda pero el snippet de Topic 7 omitió). Sin emojis. Sin caracteres Unicode decorativos.

```bash
#!/usr/bin/env bash
# scripts/deploy.sh — Deploy idempotente Nexo (D-25, D-26)
# Uso: bash scripts/deploy.sh
#
# Pasos: git pull --ff-only -> pre-deploy backup -> build --pull -> up -d -> smoke.
#
# Exit codes:
#   0  = deploy OK (smoke pasa)
#   1  = cualquier step falla
#
# NO hay rollback automatico — el log incluye hint; el operador decide.

set -euo pipefail
IFS=$'\n\t'

PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${PROJECT_DIR}"

COMPOSE="docker compose -f docker-compose.yml -f docker-compose.prod.yml"
PREDEPLOY_DIR="/var/backups/nexo/predeploy"
LOG_PREFIX="[deploy $(date -u +%FT%TZ)]"

log() { echo "${LOG_PREFIX} $*"; }
fail() { echo "${LOG_PREFIX} ERROR: $*" >&2; exit 1; }

log "start"
log "project=${PROJECT_DIR} branch=$(git rev-parse --abbrev-ref HEAD) commit=$(git rev-parse --short HEAD)"

# 1. git pull --ff-only (sin force)
log "step 1/5: git pull --ff-only"
git pull --ff-only || fail "git pull fallo (posible divergencia local). Resolver manualmente con 'git status'. No commitear en el servidor."

# 2. Pre-deploy backup (skip si db no corre aun)
log "step 2/5: pre-deploy backup"
if $COMPOSE ps db --format json 2>/dev/null | grep -q '"State":"running"'; then
    mkdir -p "${PREDEPLOY_DIR}"
    HASH="$(git rev-parse --short HEAD)"
    STAMP="$(date -u +%Y%m%d-%H%M)"
    OUT="${PREDEPLOY_DIR}/predeploy-${HASH}-${STAMP}.sql.gz"
    $COMPOSE exec -T db bash -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
        | gzip > "${OUT}.tmp" || fail "pg_dump pre-deploy fallo"
    mv "${OUT}.tmp" "${OUT}"
    chmod 600 "${OUT}"
    log "  backup: ${OUT} ($(du -h "${OUT}" | cut -f1))"
    find "${PREDEPLOY_DIR}" -name "predeploy-*.sql.gz" -mtime +30 -delete 2>/dev/null || true
else
    log "  db no esta corriendo — primer deploy, skip backup"
fi

# 3. Build imagenes
log "step 3/5: build --pull"
$COMPOSE build --pull || fail "build fallo"

# 4. Up en background
log "step 4/5: up -d"
$COMPOSE up -d || fail "up -d fallo"

# 5. Smoke test — /api/health via Caddy HTTPS
log "step 5/5: smoke test"
# sleep 15: dar tiempo a start_period=20s de web y 10s de caddy.
# -k: TLS internal; el host de deploy no siempre tiene la root CA instalada.
# -m 10: timeout total 10s.
sleep 15
if curl -fs -m 10 -k https://nexo.ecsmobility.local/api/health >/dev/null; then
    log "smoke OK"
else
    fail "smoke fallo — rollback manual: git reset --hard HEAD~1 && bash scripts/deploy.sh"
fi

log "DONE"
```

Tras crearlo, `chmod +x scripts/deploy.sh` (asegurar bit ejecutable en el commit).

Crear `tests/infra/test_deploy_script.py`:

```python
"""Validaciones estáticas de scripts/deploy.sh (Phase 6 / Plan 06-02).

No ejecuta el script. Valida sintaxis bash, presencia de pasos D-25, y
ausencia de landmines (--force, --no-verify, emojis, rm -rf).
"""
import re
import shutil
import stat
import subprocess
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
DEPLOY_SH = REPO_ROOT / "scripts" / "deploy.sh"


def test_deploy_sh_exists():
    assert DEPLOY_SH.exists(), f"Falta {DEPLOY_SH} (DEPLOY-04)."


def test_deploy_sh_is_executable():
    mode = DEPLOY_SH.stat().st_mode
    assert mode & stat.S_IXUSR, "scripts/deploy.sh debe ser ejecutable (chmod +x)."


def test_deploy_sh_bash_syntax_valid():
    if shutil.which("bash") is None:
        import pytest
        pytest.skip("bash no disponible")
    r = subprocess.run(["bash", "-n", str(DEPLOY_SH)],
                       capture_output=True, text=True, timeout=10, check=False)
    assert r.returncode == 0, f"bash -n fallo: {r.stderr}"


def test_deploy_sh_has_set_euo_pipefail():
    assert "set -euo pipefail" in DEPLOY_SH.read_text(encoding="utf-8")


def test_deploy_sh_git_pull_ff_only():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    assert "git pull --ff-only" in content, "D-25: git pull --ff-only obligatorio."
    assert "--force" not in content, "CLAUDE.md prohibe --force en git."


def test_deploy_sh_pg_dump_predeploy():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    assert "pg_dump" in content, "D-25: pg_dump pre-deploy obligatorio."
    assert "predeploy" in content, "Backup debe taggearse con 'predeploy-'."


def test_deploy_sh_atomic_rename():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    # Pitfall 4: .tmp + mv
    assert ".tmp" in content, "Pitfall 4: backup debe escribir a .tmp."
    assert "mv " in content, "Pitfall 4: mv .tmp final obligatorio."


def test_deploy_sh_build_and_up():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    assert "build --pull" in content
    assert "up -d" in content


def test_deploy_sh_smoke_curl_https():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    assert "curl" in content and "/api/health" in content
    assert "https://nexo.ecsmobility.local" in content


def test_deploy_sh_uses_compose_override():
    assert "docker-compose.prod.yml" in DEPLOY_SH.read_text(encoding="utf-8")


def test_deploy_sh_no_no_verify():
    assert "--no-verify" not in DEPLOY_SH.read_text(encoding="utf-8")


def test_deploy_sh_no_emoji():
    content = DEPLOY_SH.read_text(encoding="utf-8")
    emoji_re = re.compile(
        r"[\U0001F300-\U0001FAFF\U00002600-\U000027BF\U0001F600-\U0001F64F]"
    )
    m = emoji_re.search(content)
    assert m is None, f"Emoji prohibido: {m.group(0)!r}"


def test_deploy_sh_rollback_hint():
    assert "rollback" in DEPLOY_SH.read_text(encoding="utf-8").lower()
```

Landmines surfaceadas como tests: Pitfall 4 (atomic rename), Pitfall 5 (--force prohibido), CLAUDE.md (no --no-verify, no emojis, hint de rollback manual).
  </action>

  <verify>
    <automated>pytest -x -q tests/infra/test_deploy_script.py</automated>
  </verify>

  <acceptance_criteria>
    - `test -x scripts/deploy.sh` exit 0
    - `bash -n scripts/deploy.sh` exit 0
    - `grep -q 'set -euo pipefail' scripts/deploy.sh` exit 0
    - `grep -q 'git pull --ff-only' scripts/deploy.sh` exit 0
    - `grep -q 'pg_dump' scripts/deploy.sh` exit 0
    - `grep -q 'predeploy' scripts/deploy.sh` exit 0
    - `grep -q '\.tmp' scripts/deploy.sh` exit 0
    - `grep -q 'mv ' scripts/deploy.sh` exit 0
    - `grep -q 'build --pull' scripts/deploy.sh` exit 0
    - `grep -q 'up -d' scripts/deploy.sh` exit 0
    - `grep -q 'https://nexo.ecsmobility.local/api/health' scripts/deploy.sh` exit 0
    - `grep -q 'docker-compose.prod.yml' scripts/deploy.sh` exit 0
    - `grep -q '\-\-force' scripts/deploy.sh` exit 1
    - `grep -q '\-\-no-verify' scripts/deploy.sh` exit 1
    - `pytest -x -q tests/infra/test_deploy_script.py` exit 0 con 13 tests pasados
  </acceptance_criteria>

  <done>
    `scripts/deploy.sh` existe, ejecutable, pasa `bash -n`, implementa los 5 pasos D-25 con atomic rename del pre-deploy backup, sin --force/--no-verify/emojis. Los 13 tests pasan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear scripts/backup_nightly.sh + tests/infra/test_backup_script.py</name>
  <files>scripts/backup_nightly.sh, tests/infra/test_backup_script.py</files>

  <read_first>
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Topic 4" (líneas 533–650) — script completo
    - .planning/phases/06-despliegue-lan-https/06-CONTEXT.md §"D-07, D-08, D-09, D-10" — cron 03:00 UTC, /var/backups/nexo/, 7d, RPO 24h
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Pitfall 4" + §"Landmine 6, 7"
    - scripts/deploy.sh (patrón `$COMPOSE exec -T db bash -c 'pg_dump ...'` idéntico)
  </read_first>

  <action>
Crear `scripts/backup_nightly.sh` con contenido literal siguiente:

```bash
#!/usr/bin/env bash
# scripts/backup_nightly.sh — Backup nightly de Postgres Nexo.
# Invocado por cron (D-07: 03:00 UTC daily).
#
# Comportamiento: pg_dump plain SQL | gzip -> /var/backups/nexo/nexo-<stamp>.sql.gz
# con atomic rename (Pitfall 4) y rotacion -mtime +7 (D-09).
#
# Exit codes:
#   0 = backup OK
#   1 = pg_dump o gzip fallaron (archivo .tmp queda para debug)

set -euo pipefail
IFS=$'\n\t'

# /opt/nexo = convencion Linux ops (documentada en DEPLOY_LAN.md).
PROJECT_DIR="/opt/nexo"
BACKUP_DIR="/var/backups/nexo"
STAMP="$(date -u +%Y%m%d-%H%M)"
OUT="${BACKUP_DIR}/nexo-${STAMP}.sql.gz"

mkdir -p "${BACKUP_DIR}"
cd "${PROJECT_DIR}"

COMPOSE="docker compose -f docker-compose.yml -f docker-compose.prod.yml"

# docker compose exec -T (sin tty): necesario bajo cron.
# POSTGRES_USER / POSTGRES_DB inyectados por compose en el container.
# Password nunca por argv — pg_dump hereda env.
$COMPOSE exec -T db bash -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
    | gzip > "${OUT}.tmp"

# Atomic rename (Pitfall 4): si peto a mitad, .tmp queda pero rotacion NO lo valida.
mv "${OUT}.tmp" "${OUT}"

# chmod 600: backup contiene passwords argon2 + audit_log sensibles.
chmod 600 "${OUT}"

# Rotacion (D-09): nexo-*.sql.gz mtime > 7 dias.
find "${BACKUP_DIR}" -maxdepth 1 -name "nexo-*.sql.gz" -type f -mtime +7 -delete

echo "[$(date -u +%FT%TZ)] backup ok: ${OUT} ($(du -h "${OUT}" | cut -f1))"
```

Tras crearlo, `chmod +x scripts/backup_nightly.sh`.

Crear `tests/infra/test_backup_script.py`:

```python
"""Validaciones estáticas de scripts/backup_nightly.sh (Phase 6 / Plan 06-02).

D-07, D-08, D-09, D-10 + Pitfall 4 + Landmine 6/7.
"""
import shutil
import stat
import subprocess
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
BACKUP_SH = REPO_ROOT / "scripts" / "backup_nightly.sh"


def test_backup_sh_exists():
    assert BACKUP_SH.exists()


def test_backup_sh_is_executable():
    assert BACKUP_SH.stat().st_mode & stat.S_IXUSR


def test_backup_sh_bash_syntax_valid():
    if shutil.which("bash") is None:
        import pytest
        pytest.skip("bash no disponible")
    r = subprocess.run(["bash", "-n", str(BACKUP_SH)],
                       capture_output=True, text=True, timeout=10, check=False)
    assert r.returncode == 0, r.stderr


def test_backup_sh_has_set_euo_pipefail():
    assert "set -euo pipefail" in BACKUP_SH.read_text(encoding="utf-8")


def test_backup_sh_target_dir():
    assert "/var/backups/nexo" in BACKUP_SH.read_text(encoding="utf-8")


def test_backup_sh_pg_dump_via_compose():
    content = BACKUP_SH.read_text(encoding="utf-8")
    assert "pg_dump" in content
    assert "docker compose" in content or "compose exec" in content


def test_backup_sh_atomic_rename():
    content = BACKUP_SH.read_text(encoding="utf-8")
    assert ".tmp" in content and "mv " in content, "Pitfall 4: .tmp + mv"


def test_backup_sh_retention_7d():
    content = BACKUP_SH.read_text(encoding="utf-8")
    assert "-mtime +7" in content and "-delete" in content, "D-09"


def test_backup_sh_chmod_600():
    content = BACKUP_SH.read_text(encoding="utf-8")
    assert "chmod 600" in content or "umask" in content


def test_backup_sh_uses_prod_override():
    assert "docker-compose.prod.yml" in BACKUP_SH.read_text(encoding="utf-8")


def test_backup_sh_no_rm_rf():
    assert "rm -rf" not in BACKUP_SH.read_text(encoding="utf-8"), "Landmine 6"
```

Landmines surfaced: Pitfall 4 (atomic rename), Landmine 6 (no `rm -rf`), seguridad (chmod 600 en dump con hashes argon2).
  </action>

  <verify>
    <automated>pytest -x -q tests/infra/test_backup_script.py</automated>
  </verify>

  <acceptance_criteria>
    - `test -x scripts/backup_nightly.sh` exit 0
    - `bash -n scripts/backup_nightly.sh` exit 0
    - `grep -q 'set -euo pipefail' scripts/backup_nightly.sh` exit 0
    - `grep -q '/var/backups/nexo' scripts/backup_nightly.sh` exit 0
    - `grep -q 'pg_dump' scripts/backup_nightly.sh` exit 0
    - `grep -q '\.tmp' scripts/backup_nightly.sh` exit 0
    - `grep -q 'mv ' scripts/backup_nightly.sh` exit 0
    - `grep -q 'mtime +7' scripts/backup_nightly.sh` exit 0
    - `grep -q '\-delete' scripts/backup_nightly.sh` exit 0
    - `grep -q 'chmod 600' scripts/backup_nightly.sh` exit 0
    - `grep -q 'docker-compose.prod.yml' scripts/backup_nightly.sh` exit 0
    - `grep -q 'rm -rf' scripts/backup_nightly.sh` exit 1
    - `pytest -x -q tests/infra/test_backup_script.py` exit 0 con 11 tests pasados
  </acceptance_criteria>

  <done>
    `scripts/backup_nightly.sh` existe, ejecutable, `bash -n` válido, hace pg_dump via compose exec prod con atomic rename, chmod 600, rotación 7d. Sin rm -rf. 11 tests pasan.
  </done>
</task>

<task type="auto">
  <name>Task 3: Añadir targets Makefile prod-* + deploy + backup (sin romper targets dev)</name>
  <files>Makefile</files>

  <read_first>
    - Makefile completo — ver targets existentes up/down/dev/health/db-shell/nexo-*/clean
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Project Constraints" (línea 1390+) — make up / make dev NO arrancan mcp (debe mantenerse)
    - .planning/phases/06-despliegue-lan-https/06-RESEARCH.md §"Landmine 6" — NUNCA `down -v` en prod
    - scripts/deploy.sh y scripts/backup_nightly.sh (Tasks 1 y 2)
  </read_first>

  <action>
Editar `Makefile` con 2 cambios precisos. NO tocar targets existentes (up, down, build, rebuild, restart, logs, logs-web, logs-db, logs-mcp, status, db-shell, nexo-*, dev, install, clean, health, help, ip).

**Cambio 1**: Actualizar la línea `.PHONY` inicial (línea 1) para incluir los nuevos targets prod:

De:
```makefile
.PHONY: up down build rebuild restart logs status dev db-shell clean help ip nexo-init nexo-owner nexo-verify nexo-smoke nexo-setup test-data
```

A:
```makefile
.PHONY: up down build rebuild restart logs status dev db-shell clean help ip nexo-init nexo-owner nexo-verify nexo-smoke nexo-setup test-data prod-up prod-down prod-logs prod-status prod-health deploy backup
```

**Cambio 2**: Añadir un bloque nuevo `# ── Producción (Phase 6) ──` JUSTO ANTES de la línea `# ── Ayuda ─` (que es donde está el target `help`). El bloque contiene:

```makefile
# ── Producción (Phase 6) ─────────────────────────────────────────────────────
# Todos los targets prod-* usan el override docker-compose.prod.yml.
# NUNCA usar `docker compose ... down -v` en prod: borra pgdata (Landmine 6).
# El target dev `make clean` SI usa -v — no ejecutarlo en prod jamas.
PROD_COMPOSE = docker compose -f docker-compose.yml -f docker-compose.prod.yml

prod-up: ## Arranca stack prod (base + override). Requiere .env con secretos reales.
	$(PROD_COMPOSE) up -d --build
	@echo ""
	@echo "  Nexo prod arrancado:"
	@echo "    URL:     https://nexo.ecsmobility.local  (requiere hosts-file + root CA)"
	@echo "    Smoke:   make prod-health"
	@echo "    Logs:    make prod-logs"
	@echo ""

prod-down: ## Para stack prod (SIN -v — pgdata persiste)
	$(PROD_COMPOSE) down

prod-logs: ## Logs stack prod en tiempo real
	$(PROD_COMPOSE) logs -f

prod-status: ## Estado de containers prod
	$(PROD_COMPOSE) ps

prod-health: ## Health check via /api/health del stack prod (local, sin HTTPS)
	@$(PROD_COMPOSE) exec -T web curl -fs http://localhost:8000/api/health | python3 -m json.tool

deploy: ## Ejecuta scripts/deploy.sh (git pull + build + up + smoke)
	bash scripts/deploy.sh

backup: ## Ejecuta scripts/backup_nightly.sh manualmente (pg_dump + rotacion 7d)
	bash scripts/backup_nightly.sh

```

Las recipes usan TAB (no 4-spaces) — Makefile lo exige.

**No regression check**: tras el cambio, `make -n up` debe seguir produciendo `docker compose up -d` exactamente como antes. `make help` debe listar los nuevos targets prod.
  </action>

  <verify>
    <automated>grep -qE '^prod-up:' Makefile && grep -qE '^prod-down:' Makefile && grep -qE '^prod-logs:' Makefile && grep -qE '^prod-status:' Makefile && grep -qE '^prod-health:' Makefile && grep -qE '^deploy:' Makefile && grep -qE '^backup:' Makefile && grep -q 'PROD_COMPOSE' Makefile && make -n prod-up >/dev/null && make -n deploy >/dev/null && make -n backup >/dev/null && make -n up >/dev/null && make -n clean >/dev/null</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qE '^prod-up:' Makefile` exit 0
    - `grep -qE '^prod-down:' Makefile` exit 0
    - `grep -qE '^prod-logs:' Makefile` exit 0
    - `grep -qE '^prod-status:' Makefile` exit 0
    - `grep -qE '^prod-health:' Makefile` exit 0
    - `grep -qE '^deploy:' Makefile` exit 0
    - `grep -qE '^backup:' Makefile` exit 0
    - `grep -q 'PROD_COMPOSE = docker compose -f docker-compose.yml -f docker-compose.prod.yml' Makefile` exit 0
    - `grep -q 'prod-up prod-down prod-logs prod-status prod-health deploy backup' Makefile` exit 0 (PHONY actualizado)
    - `make -n up` output contiene `docker compose up -d` exacto (sin -f override — dev intacto)
    - `make -n prod-up` output contiene `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build`
    - `make -n deploy` output contiene `bash scripts/deploy.sh`
    - `make -n backup` output contiene `bash scripts/backup_nightly.sh`
    - `make -n clean` output contiene `docker compose down -v` exacto (target dev NO se modificó)
    - `make help` lista al menos: up, down, dev, prod-up, prod-down, prod-health, deploy, backup
  </acceptance_criteria>

  <done>
    Makefile contiene 7 nuevos targets (prod-up/prod-down/prod-logs/prod-status/prod-health/deploy/backup) + variable `PROD_COMPOSE` + .PHONY actualizado. Targets dev (up/down/dev/clean/health/nexo-*) sin cambios — verificado via `make -n <target>`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador ops → scripts/deploy.sh | Script corre como root del servidor (cron + manual). Confiado. |
| cron (root) → scripts/backup_nightly.sh | Scheduled job sin interacción. Confiado. |
| Script → docker compose exec -T db pg_dump | Credenciales Postgres nunca pasan por argv — heredadas del container. |
| Backup resultante → filesystem /var/backups/nexo | `chmod 600` restringe a root del host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | Backup `.sql.gz` con permisos laxos filtrable por otros usuarios del host | mitigate | `chmod 600 "${OUT}"` en backup_nightly.sh y deploy.sh (pre-deploy). Test `test_backup_sh_chmod_600` bloquea regresión. |
| T-06-08 | Tampering | Backup parcial (proceso muere mid-dump) queda en disco con nombre final y pasa rotación como válido | mitigate | Atomic rename: escribir a `.tmp`, `mv .tmp final` sólo tras éxito. Tests Pitfall 4 en ambos scripts. |
| T-06-09 | Elevation of Privilege | deploy.sh invoca `git pull --force` por error → trae código no revisado y lo despliega | mitigate | `git pull --ff-only` + test `--force not in content` (Pitfall 5, CLAUDE.md). |
| T-06-10 | Repudiation | Backup fallido silencioso — operador asume que hay backup cuando no lo hay | mitigate | `set -euo pipefail` + `|| fail` en cada paso + log cierre con path+size sólo tras `mv` exitoso. Cron captura stdout a syslog (documentado en runbook 06-03). |
| T-06-11 | Denial of Service | `rm -rf` accidental sobre path computado destruye pgdata | mitigate | Test `test_backup_sh_no_rm_rf` + uso exclusivo de `find ... -delete` para rotación. Landmine 6 (no `down -v` en prod) documentada en comentario del Makefile. |
| T-06-12 | Information Disclosure | `.env` leído por compose contiene secretos; deploy.sh no debe loguearlos | accept | deploy.sh nunca lee `.env` directamente; log incluye sólo commit hash + branch, no env vars. Riesgo residual aceptado (deploy corre como root en servidor trusted). |

Severity: T-06-07, T-06-08, T-06-09, T-06-10, T-06-11 = HIGH (tests bloquean regresión). T-06-12 = LOW (aceptado).
</threat_model>

<verification>
Validación global tras las 3 tareas:

```bash
# 1. Archivos creados
test -x scripts/deploy.sh
test -x scripts/backup_nightly.sh
test -f tests/infra/test_deploy_script.py
test -f tests/infra/test_backup_script.py

# 2. Tests pasan
pytest -x -q tests/infra/test_deploy_script.py tests/infra/test_backup_script.py
# Esperado: 24 tests pasados (13 + 11)

# 3. Makefile no regresiona dev
make -n up    # debe mostrar `docker compose up -d` (sin -f override)
make -n dev   # debe mostrar el script dev original
make -n clean # debe mostrar `docker compose down -v`

# 4. Targets prod funcionan (dry-run sin docker)
make -n prod-up     # `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build`
make -n deploy      # `bash scripts/deploy.sh`
make -n backup      # `bash scripts/backup_nightly.sh`

# 5. Suite completa de infra Wave 0 (06-01 + 06-02)
pytest -x -q tests/infra/
# Esperado: ~42 tests pasados (18 del plan 06-01 + 24 de 06-02)
```
</verification>

<success_criteria>
1. `scripts/deploy.sh` existe con bit ejecutable, pasa `bash -n`, implementa 5 pasos D-25 (git pull --ff-only + pg_dump pre-deploy con atomic rename + build --pull + up -d + smoke HTTPS).
2. `scripts/backup_nightly.sh` existe con bit ejecutable, pasa `bash -n`, genera `/var/backups/nexo/nexo-<stamp>.sql.gz` con atomic rename, chmod 600 y retención -mtime +7.
3. Makefile tiene 7 nuevos targets prod (prod-up/prod-down/prod-logs/prod-status/prod-health/deploy/backup) con variable `PROD_COMPOSE` y .PHONY actualizado.
4. Targets dev (up, down, dev, clean, health, nexo-*) NO modificados — `make -n <target>` produce output idéntico al pre-plan.
5. 24 tests de infra Wave 0 (13 deploy + 11 backup) pasan.
6. Ni `deploy.sh` ni `backup_nightly.sh` contienen `--force`, `--no-verify`, `rm -rf` ni emojis.
7. Requirement cubierto: DEPLOY-04 (deploy.sh idempotente con build+up).
</success_criteria>

<output>
After completion, create `.planning/phases/06-despliegue-lan-https/06-02-SUMMARY.md` incluyendo:
- Archivos creados (5) y editado (Makefile)
- Landmines surfaceadas como tests (Pitfall 4, 5, Landmine 6)
- DEPLOY-04 cerrado (smoke pendiente hasta servidor físico disponible)
- Comandos de verificación ejecutados
- Next: Plan 06-03 consume estos scripts en el runbook DEPLOY_LAN.md y añade el smoke manual end-to-end.
</output>
