---
phase: 05-ui-por-roles
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-03"]
files_modified:
  - templates/ajustes.html
  - templates/ajustes_conexion.html
  - api/routers/pages.py
  - tests/routers/test_ajustes_split.py
autonomous: true
requirements: [UIROL-03]
tags: [templates, routers, ajustes, rbac, phase-5]
must_haves:
  truths:
    - "`/ajustes` hub muestra 6 cards (conexión, usuarios, auditoría, solicitudes, límites, rendimiento) y NO muestra card SMTP (D-04 — out of scope Mark-III)."
    - "Cada card del hub está gateada con `can(current_user, \"<perm>\")` — NO con `role == 'propietario'` (consistencia con sidebar)."
    - "Nueva página `/ajustes/conexion` renderiza `ajustes_conexion.html` con el bloque 'Conexion SQL Server' + componente Alpine `ajustesConexionPage()` (renombrado desde `ajustesPage()`)."
    - "`/ajustes/conexion` está gateado con `require_permission(\"conexion:config\")` (propietario-only por [] en PERMISSION_MAP — Pitfall 6)."
    - "El hub `ajustes.html` YA NO contiene el bloque inline 'Conexion SQL Server' (líneas ~74-168) ni el componente Alpine `ajustesPage()` (líneas ~180-207)."
    - "Los endpoints backend (`/api/conexion/config`, `/api/conexion/status`) NO se modifican — el split es 100% template (Open Q4 research)."
  artifacts:
    - path: templates/ajustes_conexion.html
      provides: "Nueva sub-página de ajustes con Conexion SQL Server + info sistema + componente Alpine renombrado."
      min_lines: 90
      contains: "ajustesConexionPage"
    - path: templates/ajustes.html
      provides: "Hub refactorizado: 6 cards (conexión + 5 existentes) gateadas por `can()`, sin bloque inline de conexión."
      contains: "can(current_user"
    - path: api/routers/pages.py
      provides: "Nueva ruta `GET /ajustes/conexion` con `require_permission(\"conexion:config\")`."
      contains: "ajustes_conexion_page"
    - path: tests/routers/test_ajustes_split.py
      provides: "Integration tests: propietario ve 6 cards, /ajustes/conexion carga, non-propietario no accede."
      min_lines: 60
  key_links:
    - from: "templates/ajustes.html conexion card"
      to: "/ajustes/conexion"
      via: "<a href=\"/ajustes/conexion\">"
      pattern: "href=\"/ajustes/conexion\""
    - from: "api/routers/pages.py::ajustes_conexion_page"
      to: "nexo.services.auth.require_permission('conexion:config')"
      via: "dependencies=[Depends(require_permission('conexion:config'))]"
      pattern: 'require_permission\\("conexion:config"\\)'
---

<objective>
Wave 4 — Split de `/ajustes` hub + página `/ajustes/conexion`. Extraer el
bloque "Conexión SQL Server" (hoy embebido en `ajustes.html` líneas 74-168 +
componente Alpine en líneas 180-207) a una sub-página dedicada siguiendo el
patrón de `ajustes_limites.html` (Plan 04-04). Refactorizar el hub para que
las 6 cards (conexión + 5 ya existentes) se gateen con `can()` en lugar de
`role == 'propietario'`.

Purpose: D-06 (página propia para conexión) + D-05 (hub propietario-only,
mantenido) + D-04 (sin SMTP) + consistencia `can()` (sidebar + hub).

Output: 2 templates (1 nuevo + 1 refactorizado), 1 nueva ruta en
`pages.py`, tests de integración.

Reuse register consumed (from 05-PATTERNS.md):
- #15 Hub sub-page template shell (`ajustes_limites.html:1-10`).
- #16 Hub card markup (`ajustes.html:10-21`).
- #17 Alpine `ajustesPage()` component — mueve verbatim renombrado a
  `ajustesConexionPage()`.
- #18 `ajustes_page` route pattern — clonar para `ajustes_conexion_page`.

Pitfalls prevenidos:
- Pitfall 6: la nueva ruta DEBE tener `require_permission("conexion:config")`
  — si se olvida, queda públicamente accesible para cualquier autenticado.
- D-04: NO crear `ajustes_smtp.html` ni card SMTP.
- Open Q4: NO duplicar endpoints `/api/conexion/*` — el backend no cambia.
- No breakage de `ajustes_page` existente (D-05 — sigue propietario-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-por-roles/05-CONTEXT.md
@.planning/phases/05-ui-por-roles/05-RESEARCH.md
@.planning/phases/05-ui-por-roles/05-PATTERNS.md
@.planning/phases/05-ui-por-roles/05-01-PLAN.md
@.planning/phases/05-ui-por-roles/05-03-PLAN.md
@templates/ajustes.html
@templates/ajustes_limites.html
@api/routers/pages.py

<interfaces>
<!-- Card → permission mapping para el hub refactorizado. -->

Cards esperadas en `/ajustes` hub (6 total — sin SMTP per D-04):
- Conexión (NEW)  → `conexion:config`
- Usuarios        → `usuarios:manage`
- Auditoría       → `auditoria:read`
- Solicitudes     → `aprobaciones:manage`
- Límites         → `limites:manage`
- Rendimiento     → `rendimiento:read`

Hoy TODAS tienen [] en PERMISSION_MAP → propietario-only (equivalente al
`role == 'propietario'` actual); refactorizar a `can()` por consistencia
(05-RESEARCH.md §Open Q5).

Ajustes hub route (existente, no se toca):
```python
@router.get("/ajustes",
            dependencies=[Depends(require_permission("ajustes:manage"))])
def ajustes_page(request: Request): ...
```

Ajustes conexion route (NEW):
```python
@router.get("/ajustes/conexion",
            dependencies=[Depends(require_permission("conexion:config"))])
def ajustes_conexion_page(request: Request): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create templates/ajustes_conexion.html with extracted Conexion block</name>
  <files>templates/ajustes_conexion.html</files>
  <behavior>
    - El template extiende `base.html`.
    - `{% block title %}Conexión SQL Server — {{ app_name }}{% endblock %}`.
    - `{% block page_title %}Ajustes · Conexión{% endblock %}`.
    - El bloque `content` contiene el markup extraído verbatim de
      `ajustes.html` líneas ~74-168 (card "Conexion SQL Server") +
      líneas ~170-177 si aplica (info sistema, belongs naturally aquí).
    - El componente Alpine al final del template se llama
      `ajustesConexionPage()` (renombrado desde `ajustesPage()`) — mismo
      cuerpo, pero con el nombre nuevo para evitar colisión si ambos
      templates coexistieran durante desarrollo.
    - Los endpoints consumidos (`/api/conexion/config`, `/api/conexion/status`,
      etc.) NO cambian — el componente llama a los mismos.
  </behavior>
  <action>
    1. LEER PRIMERO `templates/ajustes.html` completo (hasta ~208 líneas)
       para identificar exactamente:
       - El rango del bloque "Conexion SQL Server" (empieza con comentario
         `<!-- ── Conexion SQL Server ──` línea 74, termina antes del
         siguiente bloque distinto).
       - El rango del bloque "Info sistema" si existe (líneas ~170-177).
       - El componente Alpine `ajustesPage()` (líneas ~180-207).

    2. LEER `templates/ajustes_limites.html:1-10` para usar exactamente el
       mismo scaffolding de template (pattern #15).

    3. Crear `templates/ajustes_conexion.html` con estructura:

       ```jinja
       {% extends "base.html" %}

       {% block title %}Conexión SQL Server — {{ app_name }}{% endblock %}
       {% block page_title %}Ajustes · Conexión{% endblock %}

       {% block content %}
       <div x-data="ajustesConexionPage()" x-init="loadConfig()"
            class="max-w-2xl mx-auto space-y-6">

         {# ── Conexion SQL Server (extraído de ajustes.html:74-168) ── #}
         ... (pegar verbatim el markup de Conexion SQL Server) ...

         {# ── Info sistema (si aplica, extraído de ajustes.html:~170-177) #}
         ... (si existía bloque de info sistema con links a docs) ...
       </div>

       <script>
         /* Extraído verbatim de ajustes.html:180-207 ajustesPage().
            Renombrado a ajustesConexionPage() para evitar colisión de
            scope Alpine global. */
         function ajustesConexionPage() {
           // ... cuerpo idéntico al de ajustesPage(), solo cambia nombre ...
         }
       </script>
       {% endblock %}
       ```

    4. Pegar el markup EXACTO (no reescribir ni "mejorar") del bloque
       Conexion. Preservar todas las `@click`, `x-model`, `x-show`,
       `class` de Tailwind, IDs, etc. Solo cambiar:
       - El nombre de la función Alpine (`ajustesPage` →
         `ajustesConexionPage`) en el `x-data` Y en la `function`
         declaration Y en el `x-init`.

    5. Un breadcrumb mínimo arriba de la card (opcional pero recomendado):
       link `← Ajustes` que vuelva a `/ajustes`. Tomar el shape del
       breadcrumb de `ajustes_limites.html` si existe; si no, un simple
       `<a href="/ajustes" class="text-sm text-brand-600 hover:underline">
       ← Volver a Ajustes</a>` antes del `<div x-data=...>`.

    Anti-patrones (per 05-PATTERNS.md):
    - NO extraer el Alpine component a un archivo JS separado (no es
      necesario y rompe el patrón de los otros sub-pages).
    - NO crear un `ajustes_smtp.html` — D-04 lo prohíbe.
    - NO tocar los endpoints backend — Open Q4.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from jinja2 import Environment, FileSystemLoader; e = Environment(loader=FileSystemLoader('templates')); e.parse(open('templates/ajustes_conexion.html').read()); print('OK: ajustes_conexion.html parses')" && grep -n "ajustesConexionPage" templates/ajustes_conexion.html && grep -c "Conexion SQL Server" templates/ajustes_conexion.html</automated>
  </verify>
  <done>
    - Archivo existe (≥ 90 líneas).
    - `grep -c "ajustesConexionPage" templates/ajustes_conexion.html` ≥ 2
      (`x-data` + `function` declaration; posible 3ª en `x-init`).
    - Template parsea sin error Jinja.
    - El componente Alpine no referencia `ajustesPage` (solo
      `ajustesConexionPage`).
  </done>
  <commit>feat(templates): add ajustes_conexion.html sub-page (D-06, UIROL-03)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register /ajustes/conexion route in pages.py</name>
  <files>api/routers/pages.py</files>
  <behavior>
    - Nueva ruta `GET /ajustes/conexion` registrada al final del archivo
      (después de `ajustes_page`).
    - Gateada con `Depends(require_permission("conexion:config"))` (Pitfall 6).
    - Usa `render("ajustes_conexion.html", request, _common_extra(
      "ajustes_conexion"))`.
    - `_common_extra` no necesita cambios; solo aceptar el string nuevo.
  </behavior>
  <action>
    Editar `api/routers/pages.py` añadiendo al final del archivo (tras la
    definición de `ajustes_page` línea 111):

    ```python
    @router.get(
        "/ajustes/conexion",
        dependencies=[Depends(require_permission("conexion:config"))],
    )
    def ajustes_conexion_page(request: Request):
        return render(
            "ajustes_conexion.html", request, _common_extra("ajustes_conexion")
        )
    ```

    Anti-patrones (per 05-PATTERNS.md):
    - NO usar `conexion:read` (Pitfall 6): el PUT subyacente
      (`/api/conexion/config`) requiere `conexion:config`; la página HTML
      debe alinearse.
    - NO registrar sin `dependencies=[...]` — queda accesible a cualquier
      autenticado (Pitfall 6 explícito).
    - NO mover el import de `require_permission` — ya está en línea 12.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from api.main import app; routes = {r.path: r for r in app.routes if hasattr(r, 'path')}; assert '/ajustes/conexion' in routes, 'route missing'; print('OK: /ajustes/conexion registered')"</automated>
  </verify>
  <done>
    - Ruta `/ajustes/conexion` registrada en la app.
    - `grep -n "ajustes_conexion_page" api/routers/pages.py` match.
    - `grep -n 'require_permission("conexion:config")' api/routers/pages.py` match.
    - `ruff check api/routers/pages.py` OK.
  </done>
  <commit>feat(routers): add GET /ajustes/conexion guarded by conexion:config (D-06)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor templates/ajustes.html hub (remove inline conexion, refactor gates to can())</name>
  <files>templates/ajustes.html</files>
  <behavior>
    - Hub sigue con `<div x-data="ajustesPage()" x-init="loadConfig()">` —
      OJO: el `ajustesPage` actual se mueve a conexion; si el hub YA NO
      necesita su estado, eliminar `x-data` y `x-init` del `<div>`
      contenedor (simplificando a `<div class="max-w-2xl mx-auto space-y-6">`).
      **Decisión explícita**: si el hub NO llama a nada de Alpine
      (revisar el archivo antes de decidir), eliminar el wrapper Alpine
      completo. Si lo sigue usando para algo, dejarlo pero cambiar el
      nombre de la función (p.ej. `ajustesHubPage()`) y definir un stub
      vacío. Ver acción paso 1 para confirmar.
    - YA NO contiene el bloque inline "Conexion SQL Server" (antiguas
      líneas 74-168).
    - YA NO contiene el `<script>function ajustesPage() {...}</script>`
      al final (antiguas líneas 180-207).
    - Contiene una nueva card "Conexión SQL Server" dentro de la grid, con
      `href="/ajustes/conexion"`.
    - Las 5 cards existentes (usuarios, auditoría, solicitudes, límites,
      rendimiento) siguen visibles pero gateadas individualmente por
      `{% if can(current_user, "<perm>") %}` en vez del wrapper
      `{% if current_user and current_user.role == 'propietario' %}`
      (líneas 34 + 71).
    - NO hay card SMTP.
  </behavior>
  <action>
    1. LEER PRIMERO `templates/ajustes.html` completo. Detectar:
       - Si el `<div x-data="ajustesPage()">` en línea 6 necesita estado
         para el hub en sí, o solo servía al bloque de conexión. Si es
         solo para conexión → eliminar el wrapper Alpine del hub.
       - Localizar las líneas donde aparece
         `{% if current_user and current_user.role == 'propietario' %}` y
         el `{% endif %}` correspondiente (líneas ~34 y ~71).

    2. EDITAR con 4 cambios:

       **Cambio A — `<div>` contenedor (línea ~6):** Si Alpine ya no es
       necesario, cambiar:
       ```jinja
       <div x-data="ajustesPage()" x-init="loadConfig()" class="max-w-2xl mx-auto space-y-6">
       ```
       a:
       ```jinja
       <div class="max-w-2xl mx-auto space-y-6">
       ```

       **Cambio B — Gates de las 5 cards existentes:** Reemplazar el
       wrapper `{% if current_user and current_user.role == 'propietario' %}
       ... {% endif %}` por gates individuales per-card:
       ```jinja
       {% if can(current_user, "usuarios:manage") %}
       <a href="/ajustes/usuarios" class="group ...">...</a>
       {% endif %}

       {% if can(current_user, "auditoria:read") %}
       <a href="/ajustes/auditoria" class="group ...">...</a>
       {% endif %}

       {% if can(current_user, "aprobaciones:manage") %}
       <a href="/ajustes/solicitudes" class="group ...">...</a>
       {% endif %}

       {% if can(current_user, "limites:manage") %}
       <a href="/ajustes/limites" class="group ...">...</a>
       {% endif %}

       {% if can(current_user, "rendimiento:read") %}
       <a href="/ajustes/rendimiento" class="group ...">...</a>
       {% endif %}
       ```
       (Mantener el markup interno de cada card exactamente como está —
       solo cambiar el gate.)

       **Cambio C — Añadir nueva card Conexión:** Al inicio de la grid
       (antes de Usuarios, o en un punto lógico), insertar:
       ```jinja
       {% if can(current_user, "conexion:config") %}
       <a href="/ajustes/conexion"
          class="group bg-white rounded-2xl border border-surface-200 shadow-sm p-5 hover:border-brand-400 hover:shadow-md transition-all">
         <div class="flex items-start justify-between">
           <div>
             <h3 class="font-bold text-gray-800 group-hover:text-brand-700 transition-colors">Conexión SQL Server</h3>
             <p class="text-xs text-gray-500 mt-1">Credenciales, puerto, driver ODBC. Probar conexión en vivo.</p>
           </div>
           <svg class="w-5 h-5 text-gray-300 group-hover:text-brand-500 transition-colors shrink-0 ml-2" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
             <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3"/>
           </svg>
         </div>
       </a>
       {% endif %}
       ```

       **Cambio D — Eliminar bloque inline de conexión + componente
       Alpine:** BORRAR las líneas 74-168 (sección "Conexion SQL Server"
       + "Info sistema" si estaba ahí) y las líneas ~180-207 (`<script>
       function ajustesPage() {...}</script>`).

    3. NO añadir card SMTP (D-04).

    4. Verificar que el `{% endblock %}` final sigue cerrando el `content`
       block correctamente.

    Anti-patrones (per 05-PATTERNS.md):
    - NO dejar una card SMTP comentada o stubbeada.
    - NO mixear gates — todas las cards usan `can()`.
    - NO duplicar el componente Alpine — solo vive en `ajustes_conexion.html`.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from jinja2 import Environment, FileSystemLoader; e = Environment(loader=FileSystemLoader('templates')); e.parse(open('templates/ajustes.html').read()); print('OK: ajustes.html parses')" && grep -c "Conexion SQL Server" templates/ajustes.html | head -c 10 && echo && grep -c "function ajustesPage" templates/ajustes.html && grep -c 'current_user.role == .propietario.' templates/ajustes.html</automated>
  </verify>
  <done>
    - `grep -c "function ajustesPage" templates/ajustes.html` devuelve 0.
    - `grep -c "current_user.role == 'propietario'" templates/ajustes.html` devuelve 0.
    - `grep -c 'href="/ajustes/conexion"' templates/ajustes.html` == 1.
    - `grep -c 'can(current_user' templates/ajustes.html` ≥ 6 (5 existentes + 1 conexión).
    - Template parsea sin error Jinja.
    - No quedan referencias a SMTP ni a "ajustes_smtp".
  </done>
  <commit>refactor(ajustes): extract conexion sub-page, per-card can() gates, drop SMTP (D-04, D-05, D-06)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration tests for ajustes split</name>
  <files>tests/routers/test_ajustes_split.py</files>
  <behavior>
    Tests E2E con TestClient:

    - `test_ajustes_hub_propietario_sees_six_cards`: propietario GET `/ajustes`
      → 200, HTML contiene 6 hrefs distintos: `/ajustes/conexion`,
      `/ajustes/usuarios`, `/ajustes/auditoria`, `/ajustes/solicitudes`,
      `/ajustes/limites`, `/ajustes/rendimiento`. NO contiene
      `/ajustes/smtp` (D-04).
    - `test_ajustes_conexion_propietario_renders`: propietario GET
      `/ajustes/conexion` → 200, HTML contiene `ajustesConexionPage`.
    - `test_ajustes_conexion_non_propietario_blocked_html`: usuario
      ingenieria-usuario GET `/ajustes/conexion` con `Accept: text/html` →
      302 a `/` (flujo D-07 de Plan 05-03).
    - `test_ajustes_conexion_non_propietario_blocked_api`: usuario GET
      `/ajustes/conexion` con `Accept: application/json` → 403 JSON.
    - `test_ajustes_hub_non_propietario_blocked`: ya gateado por el
      existing `require_permission("ajustes:manage")`; confirmar que el
      refactor no rompió ese comportamiento.
    - `test_ajustes_hub_no_smtp_card`: confirmar explícitamente que el
      HTML NO contiene strings "SMTP" ni "ajustes/smtp".
  </behavior>
  <action>
    Crear `tests/routers/test_ajustes_split.py`.

    1. Reusar fixtures de login de otros tests de `tests/routers/`
       (mismo patrón que Plan 05-02 Task 3 y Plan 05-03 Task 5).

    2. Tests (seleccionar los definidos en <behavior>).

    3. Para el test "no SMTP card":
       ```python
       def test_ajustes_hub_no_smtp_card(client, propietario_session):
           r = client.get("/ajustes", cookies=propietario_session)
           assert r.status_code == 200
           body = r.text
           assert 'href="/ajustes/smtp"' not in body
           assert 'SMTP' not in body  # si el caller decide, relajar a case-insensitive
       ```

    4. Para el 302 test, usar `follow_redirects=False` como en 05-03.

    5. Marcar `pytestmark = pytest.mark.integration` si aplica.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/routers/test_ajustes_split.py -x -q</automated>
  </verify>
  <done>
    - ≥ 6 tests, todos verdes.
    - Suite completa sin regresión.
    - `ruff check tests/routers/test_ajustes_split.py` OK.
  </done>
  <commit>test(routers): integration tests for ajustes hub split + conexion sub-page</commit>
</task>

</tasks>

<success_criteria>
- `templates/ajustes_conexion.html` existe y renderiza.
- Ruta `/ajustes/conexion` gateada por `conexion:config`.
- Hub `/ajustes` muestra 6 cards, ninguna SMTP, todas con `can()`.
- No hay duplicación del componente Alpine entre hub y sub-página.
- Suite completa verde.
- 4 commits atómicos (nuevo template, nueva ruta, refactor hub, tests).
</success_criteria>

<output>
`.planning/phases/05-ui-por-roles/05-04-SUMMARY.md` con:
- Confirmación de que `ajustes.html` bajó > 60 LOC (cut del bloque inline).
- Confirmación de que el endpoint backend (`/api/conexion/*`) NO cambió.
- Snapshot del HTML de `/ajustes` (lista de hrefs observados) como pre-smoke
  manual.
- Flag "ready for 05-05 / Wave 5 (button gating + HTML GET hardening)".
</output>
