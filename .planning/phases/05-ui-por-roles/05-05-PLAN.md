---
phase: 05-ui-por-roles
plan: 05
type: execute
wave: 5
depends_on: ["05-01", "05-03"]
files_modified:
  - templates/pipeline.html
  - templates/historial.html
  - templates/recursos.html
  - templates/ciclos_calc.html
  - api/routers/pages.py
  - tests/routers/test_button_gating.py
  - tests/routers/test_html_get_guarded.py
autonomous: false
requirements: [UIROL-04, UIROL-05]
tags: [templates, rbac, buttons, hardening, phase-5]
must_haves:
  truths:
    - "Los 11 botones sensibles enumerados en 05-RESEARCH.md §Catalogue están envueltos en `{% if can(current_user, \"<perm>\") %}`."
    - "8 rutas HTML GET (`/pipeline`, `/recursos`, `/historial`, `/ciclos-calc`, `/operarios`, `/datos`, `/bbdd`, `/capacidad`) tienen `dependencies=[Depends(require_permission(\"<perm>:read\"))]` (Pitfall 4 hardening)."
    - "Un usuario de rrhh que teclee `/pipeline` recibe 302 a `/` + flash (flujo D-07 ya implementado en Plan 05-03)."
    - "Un usuario de produccion que abra `/pipeline` VE el botón 'Ejecutar' (tiene `pipeline:run`)."
    - "Un usuario de ingeniería abre `/recursos` y VE los botones 'Detectar', 'Guardar todo', 'Eliminar', 'Añadir' (tiene `recursos:edit`)."
    - "Los botones de los modales de confirmación (pipeline.html:349, pipeline.html:371, bbdd.html:325, bbdd.html:347) NO se envuelven — están gateados por backend (approval flow Phase 4)."
    - "UIROL-05 checklist de smoke manual documentado en el SUMMARY para ejecución en checkpoint."
  artifacts:
    - path: templates/pipeline.html
      provides: "Botón 'Ejecutar' (line ~185) gateado con `pipeline:run`."
      contains: 'can(current_user, "pipeline:run")'
    - path: templates/historial.html
      provides: "Botones 'Generar PDFs' (line ~107) y 'Borrar' (line ~112) gateados con `informes:delete`."
      contains: 'can(current_user, "informes:delete")'
    - path: templates/recursos.html
      provides: "6 botones edit (detectar, añadir x2, guardar, eliminar x2) gateados con `recursos:edit`."
      contains: 'can(current_user, "recursos:edit")'
    - path: templates/ciclos_calc.html
      provides: "Botón 'Guardar' (line ~208) gateado con `ciclos:edit`."
      contains: 'can(current_user, "ciclos:edit")'
    - path: api/routers/pages.py
      provides: "8 rutas HTML GET con `require_permission` (Pitfall 4 hardening)."
      contains: "require_permission(\"pipeline:read\")"
    - path: tests/routers/test_button_gating.py
      provides: "Tests: propietario ve todos los botones; usuario con perm ve; usuario sin perm no ve."
      min_lines: 80
    - path: tests/routers/test_html_get_guarded.py
      provides: "Tests: rrhh → /pipeline → 302 a / con flash; ingenieria → /pipeline → 200."
      min_lines: 60
  key_links:
    - from: "templates/*.html sensitive buttons"
      to: "nexo.services.auth.can (via Jinja global)"
      via: "{% if can(current_user, '<perm>') %}"
      pattern: "can\\(current_user, \""
    - from: "api/routers/pages.py HTML GETs"
      to: "nexo.services.auth.require_permission"
      via: "dependencies=[Depends(require_permission('<perm>:read'))]"
      pattern: "require_permission\\(\"\\w+:read\"\\)"
---

<objective>
Wave 5 — Button gating + HTML GET hardening. Dos bloques complementarios:

1. **Button gating (UIROL-04):** Envolver los 11 botones sensibles
   enumerados en 05-RESEARCH.md §Catalogue con `{% if can(current_user,
   "<perm>") %}` para que un usuario sin permiso NO vea el botón en el
   DOM (zero-trust DOM, D-02).

2. **HTML GET hardening (Pitfall 4 de 05-RESEARCH.md):** Añadir
   `dependencies=[Depends(require_permission("<perm>:read"))]` a 8 rutas
   HTML GET de `pages.py` que hoy NO tienen gate backend. Sin esto, un
   usuario de rrhh puede teclear `/pipeline` y ver la página vacía
   (inconsistente con D-01 + D-07). Con esto, el flujo D-07 se dispara
   correctamente en URL-typing.

Purpose: UIROL-04 literal + cerrar el gap coherente con D-01/D-07 que
CONTEXT.md no menciona explícitamente pero que 05-RESEARCH.md documenta
como necesario.

Output: 4 templates con wraps, 1 router con 8 dependencies añadidas,
tests de gating + hardening, y un checkpoint de smoke manual UIROL-05 al
final.

Reuse register consumed (from 05-PATTERNS.md):
- #1 `can(user, permission)` — Jinja global de 05-01.
- #3 `require_permission(perm)` — factory existente.
- #18 `ajustes_page` route pattern — clonar para las 8 rutas HTML GET.

Pitfalls prevenidos:
- Pitfall 4: sin este hardening, D-07 no se dispara en URL-typing; UX
  incoherente con el sidebar.
- NO envolver botones del modal de confirmación (pipeline.html:349,
  bbdd.html:325, etc.) — ya gateados por backend + approval flow Phase 4.
- NO envolver filtros de UI, toggles, botones de navegación (`step = X`,
  `sortBy`, `toggleExpand`).
- NO wrappear `/` (dashboard) — `None` en nav_items, siempre visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-por-roles/05-CONTEXT.md
@.planning/phases/05-ui-por-roles/05-RESEARCH.md
@.planning/phases/05-ui-por-roles/05-PATTERNS.md
@.planning/phases/05-ui-por-roles/05-01-PLAN.md
@.planning/phases/05-ui-por-roles/05-03-PLAN.md
@templates/pipeline.html
@templates/historial.html
@templates/recursos.html
@templates/ciclos_calc.html
@api/routers/pages.py
@nexo/services/auth.py

<interfaces>
<!-- Catálogo completo de botones sensibles (de 05-RESEARCH.md §Catalogue + 05-PATTERNS.md). -->

| # | Template         | Line  | Action                        | Permission        |
|---|------------------|-------|-------------------------------|-------------------|
| 1 | pipeline.html    | ~185  | "Ejecutar" (play)             | pipeline:run      |
| 2 | historial.html   | ~107  | "Generar PDFs" (regenerar)    | informes:delete   |
| 3 | historial.html   | ~112  | "Borrar ejecución"            | informes:delete   |
| 4 | recursos.html    | ~18   | "Detectar"                    | recursos:edit     |
| 5 | recursos.html    | ~41   | "Añadir máquina"              | recursos:edit     |
| 6 | recursos.html    | ~45   | "Añadir sección"              | recursos:edit     |
| 7 | recursos.html    | ~52   | "Guardar todo"                | recursos:edit     |
| 8 | recursos.html    | ~76   | "Eliminar sección"            | recursos:edit     |
| 9 | recursos.html    | ~157  | "Eliminar máquina"            | recursos:edit     |
| 10| ciclos_calc.html | ~208  | "Openar guardar"              | ciclos:edit       |
| 11| (ajustes.html)   | N/A   | ya resuelto en 05-04          | per-card can()    |

<!-- HTML GET hardening target (Pitfall 4): 8 rutas en api/routers/pages.py -->

| Line | Handler              | Permission       |
|------|----------------------|------------------|
| 29   | pipeline_page        | pipeline:read    |
| 46   | recursos_page        | recursos:read    |
| 58   | historial_page       | historial:read   |
| 69   | ciclos_calc_page     | ciclos:read      |
| 80   | operarios_page       | operarios:read   |
| 85   | datos_page           | datos:read       |
| 96   | bbdd_page            | bbdd:read        |
| 101  | capacidad_page       | capacidad:read   |

(route `/` at line 24 = dashboard, stays public post-login. route
`/ajustes` at line 106 already has `ajustes:manage`. route
`/informes` at line 41 is a redirect to /historial, no gate needed.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap 11 sensitive buttons across 4 templates</name>
  <files>templates/pipeline.html, templates/historial.html, templates/recursos.html, templates/ciclos_calc.html</files>
  <behavior>
    Cada botón sensible (catálogo anterior) queda dentro de un
    `{% if can(current_user, "<perm>") %}` + `{% endif %}`. En `recursos.html`
    varios botones comparten el mismo permiso `recursos:edit` — pueden
    agruparse bajo un solo wrap si están contiguos, pero NO fusionar
    secciones no relacionadas.

    Lo que NO cambia:
    - El markup interno de cada botón (classes, `@click`, `:disabled`, SVGs).
    - Los botones de navegación/UI (`step = X`, `sortBy`, `setRange`,
      `toggleExpand`, `sidebarOpen`, `showPass`, `cancel`, `closeModal`).
    - Los botones del flujo approval ya gateados por backend
      (`pipeline.html:349 confirmRun`, `pipeline.html:371 requestApproval`,
      `bbdd.html:325`, `bbdd.html:347`).
  </behavior>
  <action>
    Aplicar cambios template por template. En cada archivo, LEER primero
    la región relevante con números de línea cercanos a los del catálogo
    (pueden haber drifted por cambios previos; usar grep de texto para
    localizar el botón exacto).

    **1A. `templates/pipeline.html` línea ~185 (botón "Ejecutar"):**
    Localizar el `<button @click="ejecutar()">` con el SVG de play
    (path `M8 5v14l11-7z`). Envolver ese `<button>` completo en:
    ```jinja
    {% if can(current_user, "pipeline:run") %}
    <button @click="ejecutar()" class="btn-success !px-5 !py-3" :disabled="selectedModules.length === 0">
      <svg ...><path d="M8 5v14l11-7z"/></svg>
    </button>
    {% endif %}
    ```
    El botón "Atras" (línea ~184, `step = source === 'db' ? 3 : 2`)
    queda FUERA del wrap (navegación, no destructivo).

    **1B. `templates/historial.html` líneas ~107 + ~112:**
    Localizar los botones "Generar PDFs" (con `@click="regenerar()"`) y
    "Borrar" (trash icon, `@click="borrar(...)"`). Envolver AMBOS dentro
    de un solo `{% if can(current_user, "informes:delete") %}` si están
    contiguos en el DOM:
    ```jinja
    {% if can(current_user, "informes:delete") %}
    <button @click="regenerar()" ...>Generar PDFs</button>
    <button @click="borrar(...)" ...><svg>...trash...</svg></button>
    {% endif %}
    ```
    Si NO están contiguos (hay otros elementos en medio), usar dos
    bloques separados idénticos.

    **1C. `templates/recursos.html` líneas 18, 35/41/45, 52, 76, 157:**
    - Línea ~18: botón "Detectar" (`@click="detectar()"`). Wrap simple.
    - Líneas ~35-50 (dropdown "Añadir"): envolver el `<div class="relative"
      x-data="{ addOpen: false }">` completo — así ocultas el dropdown
      trigger Y los items internos (Añadir máquina / Añadir sección).
    - Línea ~52: botón "Guardar todo". Wrap simple (puede agruparse con
      los dos anteriores si están en la misma toolbar y son contiguos).
    - Línea ~76: botón "Eliminar sección" (X icon en header de sección).
      Wrap individual dentro del `{% for %}` de secciones.
    - Línea ~157: botón "Eliminar máquina" (trash en drawer). Wrap
      individual dentro del `{% for %}` de máquinas.

    Toda envoltura usa `{% if can(current_user, "recursos:edit") %}`.

    **1D. `templates/ciclos_calc.html` línea ~208:**
    Localizar `<div class="shrink-0" @click.stop>` con el `<button
    @click="openSaveDialog(r)">`. Envolver el `<div>` completo:
    ```jinja
    {% if can(current_user, "ciclos:edit") %}
      <div class="shrink-0" @click.stop>
        <button @click="openSaveDialog(r)" ...>...</button>
      </div>
    {% endif %}
    ```

    Anti-patrones (per 05-PATTERNS.md):
    - NO wrappear botones de navegación/filtros.
    - NO wrappear los botones de los modales de confirmación del approval
      flow — ya gateados por backend.
    - NO crear un wrap gigante que cubra secciones no relacionadas.
    - NO cambiar el markup interno de los botones.
    - NO usar `informes:read` para "Generar PDFs" — regeneración
      destruye archivos previos (§Catalogue #3).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && for f in templates/pipeline.html templates/historial.html templates/recursos.html templates/ciclos_calc.html; do python -c "from jinja2 import Environment, FileSystemLoader; e = Environment(loader=FileSystemLoader('templates')); e.parse(open('$f').read()); print('OK: $f parses')" || exit 1; done && echo "--- counts ---" && grep -c 'can(current_user, "pipeline:run")' templates/pipeline.html && grep -c 'can(current_user, "informes:delete")' templates/historial.html && grep -c 'can(current_user, "recursos:edit")' templates/recursos.html && grep -c 'can(current_user, "ciclos:edit")' templates/ciclos_calc.html</automated>
  </verify>
  <done>
    - Los 4 templates parsean sin errores Jinja.
    - `grep -c 'can(current_user, "pipeline:run")' templates/pipeline.html` ≥ 1.
    - `grep -c 'can(current_user, "informes:delete")' templates/historial.html` ≥ 1.
    - `grep -c 'can(current_user, "recursos:edit")' templates/recursos.html` ≥ 1.
    - `grep -c 'can(current_user, "ciclos:edit")' templates/ciclos_calc.html` ≥ 1.
    - `grep -c 'can(current_user,' templates/pipeline.html templates/historial.html templates/recursos.html templates/ciclos_calc.html` suma total ≥ 7.
  </done>
  <commit>feat(templates): gate sensitive buttons with can() (UIROL-04, 11 actions)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add require_permission to 8 HTML GET routes in pages.py</name>
  <files>api/routers/pages.py</files>
  <behavior>
    Las 8 rutas HTML GET enumeradas ganan `dependencies=[Depends(
    require_permission("<perm>:read"))]`. La firma de los handlers NO
    cambia (siguen recibiendo `request: Request` y opcional `db`). El
    comportamiento 403 lo dispara `require_permission` + el handler HTML
    de 05-03 lo convierte en 302 + flash.
  </behavior>
  <action>
    Editar `api/routers/pages.py` para cada una de las 8 rutas:

    Patrón a aplicar (clon de `ajustes_page` existente en línea 106-111):
    ```python
    @router.get(
        "/pipeline",
        dependencies=[Depends(require_permission("pipeline:read"))],
    )
    def pipeline_page(request: Request, db: Session = Depends(get_db)):
        ...
    ```

    Aplicar idéntico patrón a:
    - `/pipeline` → `pipeline:read` (línea ~29).
    - `/recursos` → `recursos:read` (línea ~46).
    - `/historial` → `historial:read` (línea ~58).
    - `/ciclos-calc` → `ciclos:read` (línea ~69).
    - `/operarios` → `operarios:read` (línea ~80).
    - `/datos` → `datos:read` (línea ~85).
    - `/bbdd` → `bbdd:read` (línea ~96).
    - `/capacidad` → `capacidad:read` (línea ~101).

    NO tocar:
    - `/` (línea ~24) — dashboard siempre accesible post-login.
    - `/informes` (línea ~40) — redirect a `/historial`, el gate lo hace
      la ruta destino.
    - `/ajustes` (línea ~106) — ya tiene `ajustes:manage`.
    - `/ajustes/conexion` (añadida en Plan 05-04) — ya tiene
      `conexion:config`.

    `require_permission` y `Depends` ya están importados (líneas 6, 12).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && grep -c 'require_permission("\w\+:read")' api/routers/pages.py | grep -q '^8$' && echo "OK: 8 :read gates added" && python -c "from api.main import app; from starlette.routing import Route; guarded = []; [guarded.append(r.path) for r in app.routes if isinstance(r, Route) and any('require_permission' in str(d.call) for d in (r.dependant.dependencies if hasattr(r, 'dependant') else []))]; print('guarded HTML pages:', guarded)" 2>&1 | head -5 && ruff check api/routers/pages.py</automated>
  </verify>
  <done>
    - `grep -c 'require_permission("\w\+:read")' api/routers/pages.py` == 8.
    - `ruff check api/routers/pages.py` OK.
    - No se ha tocado `/` ni `/informes` ni `/ajustes` ni `/ajustes/conexion`.
  </done>
  <commit>feat(routers): guard HTML GET pages with require_permission (Pitfall 4 hardening)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests for button gating</name>
  <files>tests/routers/test_button_gating.py</files>
  <behavior>
    Tests E2E que hacen login como distintos usuarios, cargan la página
    relevante (tras hardening del Task 2 es accesible solo si tienen
    `:read`), y verifican presencia/ausencia del markup del botón en el
    HTML.

    Casos mínimos:
    - `test_pipeline_propietario_sees_ejecutar`: propietario GET
      `/pipeline` → HTML contiene `@click="ejecutar()"`.
    - `test_pipeline_produccion_user_sees_ejecutar`: produccion-usuario
      tiene `pipeline:run`, GET `/pipeline` → ve el botón.
    - `test_pipeline_gerencia_user_does_not_see_ejecutar`: gerencia tiene
      `pipeline:read` (puede entrar) pero no `pipeline:run` → HTML NO
      contiene `@click="ejecutar()"`.
    - `test_historial_propietario_sees_delete_and_regen`: propietario GET
      `/historial` → HTML contiene `@click="borrar(` y `@click="regenerar(`.
    - `test_historial_produccion_sees_no_destructive_buttons`: produccion
      tiene `historial:read` + `informes:read` pero no `informes:delete`
      (solo ingeniería) → NO ve botones destructivos.
    - `test_recursos_ingenieria_sees_edit_buttons`: ingeniería-usuario
      tiene `recursos:edit` → ve "Detectar", "Guardar todo".
    - `test_recursos_produccion_sees_no_edit_buttons`: produccion tiene
      `recursos:read` pero no `:edit` → NO ve los botones.
    - `test_ciclos_calc_non_ingenieria_blocked_at_get`: rrhh GET
      `/ciclos-calc` → 302 (gate backend del Task 2, no llega al template).
  </behavior>
  <action>
    Crear `tests/routers/test_button_gating.py`.

    1. Reusar fixtures de login (mismo patrón que 05-02/05-03/05-04).

    2. Para cada test, buscar el marker exacto del botón (no solo el
       texto del label — el HTML puede tener espacios/newlines):
       - `"@click=\"ejecutar()\""` para pipeline ejecutar.
       - `"@click=\"borrar("` para historial borrar (substring OK).
       - `"@click=\"regenerar()\""` para historial regenerar.
       - `"@click=\"detectar()\""` para recursos detectar.
       - `"@click=\"guardarTodo()\""` para recursos guardar.
       - `"@click=\"openSaveDialog("` para ciclos_calc guardar.

    3. Para el test de 302 (Task 3 del Plan 05-03 ya cubre el flujo;
       aquí solo confirmamos la interacción wave 5 → wave 3):
       ```python
       r = client.get("/ciclos-calc", cookies=rrhh_session,
                      follow_redirects=False)
       assert r.status_code == 302
       ```

    4. Marcar `pytestmark = pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/routers/test_button_gating.py -x -q</automated>
  </verify>
  <done>
    - ≥ 8 tests, todos verdes.
    - Sin duplicación de helpers de login (reusa fixtures).
    - `ruff check tests/routers/test_button_gating.py` OK.
  </done>
  <commit>test(routers): integration tests for per-role button visibility (UIROL-04)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration tests for HTML GET hardening (Pitfall 4)</name>
  <files>tests/routers/test_html_get_guarded.py</files>
  <behavior>
    Tests específicos del Task 2: confirman que las 8 rutas HTML GET
    añadidas a la hardening list rechazan correctamente a usuarios sin
    permiso y que los usuarios con permiso pasan.

    Casos mínimos (una tabla parametrizada con los pares rol/ruta):
    - rrhh-usuario GET /pipeline → 302 + flash.
    - produccion-usuario GET /pipeline → 200.
    - rrhh-usuario GET /bbdd → 302.
    - ingenieria-usuario GET /bbdd → 200.
    - comercial-usuario GET /operarios → 302.
    - rrhh-usuario GET /operarios → 200.
    - gerencia-usuario GET /datos → 302 (datos:read = [ingenieria, produccion]).
    - produccion-usuario GET /datos → 200.
    - comercial-usuario GET /capacidad → 200 (capacidad:read incluye comercial).
    - rrhh-usuario GET /capacidad → 302.

    Además: API path (JSON) para UNA ruta, confirmando que el 403 JSON
    también sale correctamente (contract estable):
    - rrhh-usuario GET /bbdd con `Accept: application/json` → 403 JSON
      con `{"detail": "Permiso requerido: bbdd:read"}`.
  </behavior>
  <action>
    Crear `tests/routers/test_html_get_guarded.py`. Considerar
    `pytest.mark.parametrize` para reducir boilerplate:

    ```python
    @pytest.mark.parametrize("role_dept,route,expected", [
        ("rrhh_user",        "/pipeline",    302),
        ("produccion_user",  "/pipeline",    200),
        ("rrhh_user",        "/bbdd",        302),
        ("ingenieria_user",  "/bbdd",        200),
        ("comercial_user",   "/operarios",   302),
        ("rrhh_user",        "/operarios",   200),
        ("gerencia_user",    "/datos",       302),
        ("produccion_user",  "/datos",       200),
        ("comercial_user",   "/capacidad",   200),
        ("rrhh_user",        "/capacidad",   302),
    ])
    def test_html_get_guarded(client, sessions, role_dept, route, expected):
        r = client.get(route, cookies=sessions[role_dept],
                       follow_redirects=False)
        assert r.status_code == expected
    ```

    Plus test adicional:
    ```python
    def test_html_get_api_path_returns_403_json(client, sessions):
        r = client.get("/bbdd", cookies=sessions["rrhh_user"],
                       headers={"Accept": "application/json"},
                       follow_redirects=False)
        assert r.status_code == 403
        assert r.json()["detail"] == "Permiso requerido: bbdd:read"
    ```

    Si las sesiones por rol/dept no están todas en las fixtures
    existentes, extender la fixture con los que falten (reusando el
    helper de creación de usuarios que ya exista en tests/conftest.py o
    tests/routers/conftest.py — NO duplicar builders).

    Marcar `pytestmark = pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/routers/test_html_get_guarded.py -x -q</automated>
  </verify>
  <done>
    - ≥ 11 tests (10 parametrizados + 1 JSON), todos verdes.
    - `ruff check tests/routers/test_html_get_guarded.py` OK.
    - Suite completa sin regresión.
  </done>
  <commit>test(routers): verify HTML GET hardening (Pitfall 4) per role/dept</commit>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UIROL-05 smoke manual — 3 roles, capturar sidebar y accesos</name>
  <what-built>
    Phase 5 completa tras Waves 1-5: helper `can()` + sidebar refactor +
    forbidden redirect + ajustes split + button gating + HTML GET hardening.
  </what-built>
  <how-to-verify>
    Checklist de smoke manual UIROL-05. Ejecutar SIN seed-data modificado
    (usuarios reales del entorno de desarrollo). Si no hay 3 usuarios con
    las combinaciones necesarias, crear via `/ajustes/usuarios` ANTES del
    test.

    **Setup:** `make dev` o equivalente; browser limpio (sin cookies
    previas); devtools abierto para inspeccionar Set-Cookie y Network.

    **Test A — Propietario:**
    1. Login como propietario. Esperado: sidebar muestra los 11 items
       (Centro Mando, Datos, Análisis, Historial, Capacidad, separador,
       Recursos, Calcular Ciclos, Operarios, BBDD, Ajustes) + Solicitudes
       (con badge HTMX 30s).
    2. GET `/ajustes` → 6 cards visibles (Conexión, Usuarios, Auditoría,
       Solicitudes, Límites, Rendimiento). NO hay card SMTP.
    3. GET `/ajustes/conexion` → página nueva carga; formulario de
       credenciales renderiza; botón "Probar conexión" presente.
    4. GET `/pipeline` → botón "Ejecutar" visible.
    5. GET `/historial` → botones "Generar PDFs" y "Borrar" visibles.
    6. GET `/recursos` → botones "Detectar", "Añadir", "Guardar todo",
       trash icons visibles.

    **Test B — Directivo de ingeniería:**
    1. Sidebar: dashboard, datos, pipeline, historial, capacidad, sep,
       recursos, ciclos_calc, bbdd. **NO** ajustes. **NO** Solicitudes.
       **NO** operarios.
    2. Teclea `/ajustes` en la URL → **302 a `/`** con toast "No tienes
       permiso para acceder a la configuración" (flujo D-07).
    3. GET `/pipeline` → 200, botón "Ejecutar" visible (tiene pipeline:run
       via dept ingeniería).
    4. GET `/recursos` → 200, botones edit visibles (tiene recursos:edit).
    5. GET `/operarios` → **302 a `/`** con toast (no tiene operarios:read).
    6. GET `/bbdd` → 200, botón "Enviar consulta" visible o accesible.

    **Test C — Usuario de rrhh:**
    1. Sidebar: dashboard, historial, sep, operarios. Nada más.
    2. Teclea `/pipeline` → **302 a `/`** con toast "No tienes permiso
       para acceder a análisis".
    3. Teclea `/bbdd` → **302 a `/`** con toast.
    4. GET `/operarios` → 200, datos cargan.
    5. GET `/historial` → 200. Botones "Generar PDFs" y "Borrar" NO
       visibles (rrhh tiene `historial:read` pero no `informes:delete`).
    6. Teclea `/ajustes/conexion` → **302 a `/`** con toast.

    **Test D — JSON contract estable:**
    Con `curl` o Postman, como usuario rrhh autenticado:
    ```
    curl -X GET http://localhost:8000/pipeline \
         -H "Accept: application/json" \
         --cookie "session=..."
    ```
    Esperado: **403** con body `{"detail":"Permiso requerido: pipeline:read"}`.
    NO redirect.

    **Test E — Solicitudes badge:**
    Logueado como propietario, badge de Solicitudes en sidebar se pinta
    cada 30s via HTMX. Verificar Network tab: cada 30s hay GET
    `/api/approvals/count`.

    **Capturar:** screenshots de sidebar por rol (3 pantallazos), captura
    del toast post-redirect, captura del 403 JSON. Subirlas al SUMMARY.
  </how-to-verify>
  <resume-signal>
    Escribir "UIROL-05 OK" y adjuntar los 5 checks al SUMMARY; o
    describir issues concretos (rol → ruta → expected vs actual) para
    planificar 05-06 de gap closure si aparecen.
  </resume-signal>
</task>

</tasks>

<success_criteria>
- Los 11 botones del §Catalogue están gateados con `can()`.
- Las 8 rutas HTML GET tienen `require_permission("<perm>:read")`.
- Tests de button gating + HTML GET hardening verdes.
- Smoke manual UIROL-05 completado y documentado (5 tests A-E).
- Suite completa verde sin regresión.
- 4 commits atómicos (button wraps, GET hardening, button tests, GET tests)
  + 1 checkpoint manual (no commit, documentación en SUMMARY).
</success_criteria>

<output>
`.planning/phases/05-ui-por-roles/05-05-SUMMARY.md` con:
- Lista concreta de (archivo, línea, permiso) para cada botón wrappeado.
- Lista de (ruta, permiso) para cada HTML GET hardening aplicado.
- Resultado del smoke manual UIROL-05 (Tests A-E con OK/KO y screenshots
  o logs si hay issues).
- Flag "Phase 5 complete — ready for /gsd-verify-work 5".
</output>
