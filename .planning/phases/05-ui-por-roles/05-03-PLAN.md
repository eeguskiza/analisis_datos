---
phase: 05-ui-por-roles
plan: 03
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - nexo/middleware/flash.py
  - api/main.py
  - api/deps.py
  - templates/base.html
  - tests/middleware/test_flash.py
  - tests/routers/test_forbidden_redirect.py
autonomous: true
requirements: [UIROL-02]
tags: [middleware, exception-handler, flash, ux, phase-5]
must_haves:
  truths:
    - "Un non-propietario que teclea `/ajustes` (o cualquier ruta HTML con `require_permission` denegado) recibe 302 a `/` + cookie `nexo_flash` con mensaje user-friendly."
    - "Un cliente `/api/*` o con `Accept: application/json` o `HX-Request: true` sigue recibiendo 403 JSON con `{\"detail\": \"Permiso requerido: ...\"}` — contract estable."
    - "En el siguiente request post-redirect, el toast se dispara vía `window.showToast` y la cookie se borra en la misma response (read-and-clear)."
    - "El handler de 500 existente (`global_exception_handler`, NAMING-07) NO regresiona — siguen siendo handlers separados por tipo de excepción."
    - "La cookie es `HttpOnly=True`, `SameSite=Lax`, `Secure=not settings.debug` (Pitfall 2)."
  artifacts:
    - path: nexo/middleware/flash.py
      provides: "FlashMiddleware lee cookie `nexo_flash`, expone en `request.state.flash`, borra en response post `call_next`."
      contains: "class FlashMiddleware(BaseHTTPMiddleware)"
    - path: api/main.py
      provides: "Handler `@app.exception_handler(StarletteHTTPException)` + registro `app.add_middleware(FlashMiddleware)` entre Audit y Auth + imports."
      contains: "StarletteHTTPException"
    - path: api/deps.py
      provides: "`render()` pasa `flash_message=request.state.flash` al contexto."
      contains: "flash_message"
    - path: templates/base.html
      provides: "Bloque de script que dispara `showToast('info', 'Aviso', ...)` cuando `flash_message` está presente."
      contains: "flash_message|tojson"
    - path: tests/middleware/test_flash.py
      provides: "Unit: middleware lee cookie, pobla state, borra en response."
      min_lines: 50
    - path: tests/routers/test_forbidden_redirect.py
      provides: "Integration: HTML 403 → 302+Set-Cookie; API/JSON 403 → JSON intacto."
      min_lines: 80
  key_links:
    - from: "api/main.py::http_exception_handler_403"
      to: "api/main.py::_wants_json"
      via: "reuse del heurístico Accept existente (línea 126-136), NO triplicar"
      pattern: "_wants_json\\(request\\)"
    - from: "nexo/middleware/flash.py::FlashMiddleware"
      to: "request.state.flash"
      via: "set pre-call_next, response.delete_cookie post-call_next"
      pattern: "request\\.state\\.flash"
    - from: "templates/base.html flash block"
      to: "window.showToast"
      via: "DOMContentLoaded listener + {{ flash_message|tojson }}"
      pattern: "showToast\\('info', 'Aviso',"
---

<objective>
Wave 3 — Forbidden UX + flash pipeline. Implementar el flujo D-07 / D-08
completo: cuando una ruta HTML gateada por `require_permission` lanza 403,
el handler redirige a `/` y setea una cookie flash que el siguiente request
lee-y-borra, disparando un toast con el mensaje de denegación. El contract
JSON de `/api/*` queda intacto para no romper HTMX ni el badge de sidebar.

Purpose: UX limpia para typing-URL; consistencia con D-01 (sidebar ya
filtra, pero URL-typing sigue siendo posible).

Output: nuevo middleware `FlashMiddleware`, handler
`@app.exception_handler(StarletteHTTPException)` con Accept negotiation,
extensión de `render()`, render del toast en `base.html`, tests unit +
integration.

Reuse register consumed (from 05-PATTERNS.md):
- #4 `_wants_json(request)` — reusado desde `api/main.py:126-136` (NO copiar
  una tercera vez; la duplicación con `api/middleware/auth.py:70-80` ya
  existe y NO se triplica).
- #7 `render()` — firma inmutable (Pitfall 7); solo extiende contexto.
- #8 `render()` context dict — append `flash_message`.
- #9 `BaseHTTPMiddleware` + `dispatch` — analog `api/middleware/audit.py` + `nexo/middleware/query_timing.py`.
- #10 `window.showToast` — REUSAR, NO reinventar.
- #12 `RedirectResponse("/", 302)` + `.set_cookie(...)` — patrón Starlette.
- #13 `@app.exception_handler(...)` registro — sibling del de NAMING-07.
- #14 `fastapi.exception_handlers.http_exception_handler` — delegation target.
- #19 `settings.debug` — `secure=not settings.debug` (Pitfall 2).
- #20 Jinja `|tojson` — XSS-safe interpolation del flash.

Pitfalls prevenidos:
- Pitfall 1: registrar sobre `StarletteHTTPException` (padre), NO sobre
  `fastapi.HTTPException`.
- Pitfall 2: `secure=not settings.debug` para que la cookie sobreviva en
  dev HTTP local.
- Pitfall 3: `current_user` sigue viniendo por contexto, NO por globals.
- Pitfall 5: registro de `can` en Task anterior, ya hecho — aquí no aplica.
- Pitfall 7: `render()` no se renombra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-por-roles/05-CONTEXT.md
@.planning/phases/05-ui-por-roles/05-RESEARCH.md
@.planning/phases/05-ui-por-roles/05-PATTERNS.md
@api/main.py
@api/deps.py
@api/middleware/audit.py
@nexo/middleware/query_timing.py
@templates/base.html

<interfaces>
<!-- `_wants_json` ya existe en api/main.py:126-136 y api/middleware/auth.py:70-80. -->
<!-- NO crear una tercera copia — consumir la de main.py desde el handler nuevo. -->

FlashMiddleware contract (NEW):
```python
class FlashMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        # pre: request.state.flash = request.cookies.get("nexo_flash")  # str | None
        # call_next
        # post: if flash is not None: response.delete_cookie("nexo_flash", path="/")
```

HTTPException handler contract (NEW):
```python
@app.exception_handler(StarletteHTTPException)
async def http_exception_handler(request, exc):
    # status != 403 → delegate to fastapi.exception_handlers.http_exception_handler
    # status == 403 AND _wants_json(request) → delegate
    # status == 403 AND HTML → RedirectResponse("/", 302) + set_cookie("nexo_flash", ...)
```

render() context extension (api/deps.py:42-45):
```python
ctx: dict[str, Any] = {
    "request": request,
    "current_user": getattr(request.state, "user", None),
    "flash_message": getattr(request.state, "flash", None),   # NEW
}
```

Middleware order (api/main.py:200-202, post-modification):
```python
app.add_middleware(QueryTimingMiddleware)  # innermost
app.add_middleware(AuditMiddleware)
app.add_middleware(FlashMiddleware)        # NEW
app.add_middleware(AuthMiddleware)         # outermost
```
Justificación: LIFO → AuthMiddleware se ejecuta primero (401 corta si no
sesión); Flash después (pobla `state.flash` sobre request ya autenticado
pero accesible también en requests anónimos); Audit después (loguea con
flash disponible como metadato futuro si se quiere); QueryTiming innermost.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FlashMiddleware</name>
  <files>nexo/middleware/flash.py</files>
  <behavior>
    - Middleware BaseHTTPMiddleware con `dispatch(request, call_next)`.
    - Pre-call_next: lee `request.cookies.get("nexo_flash")` → asigna a
      `request.state.flash` (puede ser `None`).
    - Post-call_next: si `flash is not None`, llama
      `response.delete_cookie("nexo_flash", path="/")`.
    - NO depende de DB, NO logea, NO captura excepciones.
    - Constante privada `_FLASH_COOKIE = "nexo_flash"` al top del módulo.
  </behavior>
  <action>
    Crear `nexo/middleware/flash.py` siguiendo el shape de
    `api/middleware/audit.py:88-151` + `nexo/middleware/query_timing.py:83-197`:

    ```python
    """FlashMiddleware — lee cookie `nexo_flash`, la expone y la expira.

    Fuente del mensaje: ``http_exception_handler_403`` (api/main.py) la
    escribe tras 403 HTML. Posibles futuras fuentes: acciones con redirect
    (p.ej. "Usuario creado OK") — ver 05-CONTEXT.md §Deferred ideas.

    Cookie policy (D-08): HttpOnly=True, Secure=not settings.debug,
    SameSite=Lax, max_age=60s. Read-and-clear: se lee en el request actual
    y se borra en la response (Set-Cookie con Max-Age=0).

    NO depende de sesión (anon requests también pueden cargar flash).
    NO escribe en DB ni log — failure mode silencioso y seguro.
    """
    from __future__ import annotations

    from starlette.middleware.base import BaseHTTPMiddleware
    from starlette.requests import Request


    _FLASH_COOKIE = "nexo_flash"


    class FlashMiddleware(BaseHTTPMiddleware):
        async def dispatch(self, request: Request, call_next):
            flash = request.cookies.get(_FLASH_COOKIE)
            request.state.flash = flash

            response = await call_next(request)

            if flash is not None:
                response.delete_cookie(_FLASH_COOKIE, path="/")
            return response
    ```

    Anti-patrón (per 05-PATTERNS.md):
    - NO integrar en `AuthMiddleware` — single-responsibility.
    - NO logear — se considera "silencioso por diseño".
    - NO añadir métricas — flash es transporte UI, no señal ops.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.middleware.flash import FlashMiddleware, _FLASH_COOKIE; assert _FLASH_COOKIE == 'nexo_flash'; import inspect; assert inspect.iscoroutinefunction(FlashMiddleware.dispatch); print('OK: FlashMiddleware imports + async dispatch')"</automated>
  </verify>
  <done>
    - Archivo existe, ≤ 40 líneas efectivas (excluyendo docstring).
    - Pasa `ruff check nexo/middleware/flash.py`.
    - El one-liner de verify imprime "OK".
  </done>
  <commit>feat(middleware): add FlashMiddleware for nexo_flash cookie (D-08)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register FlashMiddleware + add StarletteHTTPException handler in api/main.py</name>
  <files>api/main.py</files>
  <behavior>
    - Imports nuevos al top: `from starlette.exceptions import HTTPException
      as StarletteHTTPException`, `from fastapi.exception_handlers import
      http_exception_handler as _default_http_handler`, `from
      fastapi.responses import RedirectResponse` (si no está ya), `from
      nexo.middleware.flash import FlashMiddleware`.
    - Nuevo handler `@app.exception_handler(StarletteHTTPException)` que:
      - status != 403 → delegar a `_default_http_handler`.
      - status == 403 + `_wants_json(request)` → delegar (JSON 403 intacto).
      - status == 403 + HTML → `RedirectResponse("/", 302)` +
        `set_cookie("nexo_flash", f"No tienes permiso para acceder a
        {friendly}", max_age=60, httponly=True, secure=not settings.debug,
        samesite="lax", path="/")`.
    - Helper `_friendly_permission_label(perm)` que lookuppea en un dict
      `_PERMISSION_LABELS` local (per 05-RESEARCH.md §Open Q1 opción (a) —
      los labels viven en main.py junto al handler; si cambian en el
      futuro, mover a `nexo/services/auth.py` en Mark-IV).
    - `app.add_middleware(FlashMiddleware)` registrado ENTRE
      `AuditMiddleware` (línea 201) y `AuthMiddleware` (línea 202). La
      posición respeta el orden LIFO documentado en líneas 180-199.
  </behavior>
  <action>
    Editar `api/main.py` en 3 regiones distintas:

    **Región 1 — Imports (cerca de línea 11-27, bloque de imports):**
    Añadir (si no están ya):
    ```python
    from starlette.exceptions import HTTPException as StarletteHTTPException
    from fastapi.exception_handlers import http_exception_handler as _default_http_handler
    from fastapi.responses import RedirectResponse
    from nexo.middleware.flash import FlashMiddleware
    ```
    `RedirectResponse` puede que ya esté importado — si lo está, NO duplicar.

    **Región 2 — Handler (insertar después de la línea 167, cierre del
    `global_exception_handler` existente, antes del bloque rate_limit en
    línea 170):**

    ```python
    # ── Handler 403 HTML (D-07) — Accept-aware redirect + flash ──────────
    # Sibling del ``global_exception_handler`` de NAMING-07. El handler
    # ``@app.exception_handler(Exception)`` de arriba NO captura
    # ``HTTPException`` (FastAPI tiene handler default separado). Registrar
    # sobre ``StarletteHTTPException`` (padre de ``fastapi.HTTPException``)
    # para capturar ambas ramas.

    _PERMISSION_LABELS: dict[str, str] = {
        "bbdd:read":           "el explorador de BBDD",
        "pipeline:read":       "análisis",
        "pipeline:run":        "ejecutar el pipeline",
        "historial:read":      "el historial",
        "capacidad:read":      "capacidad",
        "recursos:read":       "recursos",
        "recursos:edit":       "editar recursos",
        "ciclos:read":         "cálculo de ciclos",
        "ciclos:edit":         "editar ciclos",
        "operarios:read":      "operarios",
        "datos:read":          "datos",
        "ajustes:manage":      "la configuración",
        "auditoria:read":      "el log de auditoría",
        "usuarios:manage":     "la gestión de usuarios",
        "aprobaciones:manage": "solicitudes de aprobación",
        "limites:manage":      "límites de queries",
        "rendimiento:read":    "métricas de rendimiento",
        "conexion:config":     "la configuración de conexión",
        "conexion:read":       "la conexión",
        "informes:read":       "informes",
        "informes:delete":     "borrar informes",
    }


    def _friendly_permission_label(perm: str) -> str:
        return _PERMISSION_LABELS.get(perm, perm)


    @app.exception_handler(StarletteHTTPException)
    async def http_exception_handler_403(
        request: Request, exc: StarletteHTTPException
    ):
        """Negocia Accept para 403: HTML → redirect + flash; resto → default.

        Para cualquier otro status code (401, 404, 422, 500 vía default
        fastapi), delega al handler default de FastAPI — preserva el
        contract JSON existente sin regresión.
        """
        if exc.status_code != 403:
            return await _default_http_handler(request, exc)

        if _wants_json(request):
            return await _default_http_handler(request, exc)

        # HTML path: redirect + flash toast.
        raw = str(exc.detail or "")
        perm = raw.replace("Permiso requerido: ", "") if raw.startswith(
            "Permiso requerido: "
        ) else ""
        friendly = _friendly_permission_label(perm) if perm else "esta sección"
        response = RedirectResponse("/", status_code=302)
        response.set_cookie(
            "nexo_flash",
            f"No tienes permiso para acceder a {friendly}",
            max_age=60,
            httponly=True,
            secure=not settings.debug,   # Pitfall 2: HTTP dev
            samesite="lax",
            path="/",
        )
        return response
    ```

    **Región 3 — Middleware stack (líneas 200-202):**
    Añadir `FlashMiddleware` entre Audit y Auth. Respetar el orden LIFO
    documentado arriba:
    ```python
    app.add_middleware(QueryTimingMiddleware)  # innermost
    app.add_middleware(AuditMiddleware)
    app.add_middleware(FlashMiddleware)        # NEW (05-03) — entre Audit y Auth
    app.add_middleware(AuthMiddleware)         # outermost
    ```

    Anti-patrones (per 05-PATTERNS.md):
    - NO registrar sobre `fastapi.HTTPException` — debe ser
      `StarletteHTTPException` (Pitfall 1).
    - NO re-implementar `_wants_json` — consumir el de línea 126 (ya está
      en el mismo archivo).
    - NO usar `delete_cookie` en el handler — la cookie LA BORRA
      `FlashMiddleware` en el siguiente request (read-and-clear).
    - NO fold el `_PERMISSION_LABELS` en `nexo/services/auth.py` todavía —
      05-RESEARCH.md recomienda opción (a) para Phase 5 (local a
      main.py), migración opcional a opción (b) en Mark-IV.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from api.main import app, _friendly_permission_label, _PERMISSION_LABELS; assert _friendly_permission_label('bbdd:read') == 'el explorador de BBDD'; assert _friendly_permission_label('permiso:desconocido') == 'permiso:desconocido'; handlers = app.exception_handlers; from starlette.exceptions import HTTPException as SHE; assert SHE in handlers; print('OK: handler registered + labels work')"</automated>
  </verify>
  <done>
    - El one-liner de verify imprime "OK".
    - `grep -n "app.add_middleware(FlashMiddleware)" api/main.py` matches 1.
    - `grep -n "@app.exception_handler(StarletteHTTPException)" api/main.py` matches 1.
    - `grep -c "_wants_json" api/main.py` == 2 (definición + una llamada en el nuevo handler).
    - `ruff check api/main.py` OK.
  </done>
  <commit>feat(errors): handle 403 HTML with redirect+flash, JSON contract preserved (D-07)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend render() context + base.html toast render</name>
  <files>api/deps.py, templates/base.html</files>
  <behavior>
    - `render()` inyecta `flash_message=getattr(request.state, "flash", None)`
      en el context dict (línea 42-45 de deps.py).
    - La firma de `render()` NO cambia.
    - Al final del body de `base.html` (antes de `</body>`), un bloque
      `{% if flash_message %}<script>...</script>{% endif %}` dispara
      `showToast('info', 'Aviso', {{ flash_message|tojson }})` bajo
      `DOMContentLoaded` — per 05-RESEARCH.md Example F.
    - Si el user entra a `/login` (anon) sin cookie, no se emite el script
      (no falla, silencioso).
  </behavior>
  <action>
    **Parte A — api/deps.py (líneas 42-45):**
    Extender el ctx dict de `render()`:
    ```python
    ctx: dict[str, Any] = {
        "request": request,
        "current_user": getattr(request.state, "user", None),
        "flash_message": getattr(request.state, "flash", None),   # NEW (05-03)
    }
    ```
    NO tocar el resto de la función — la firma, el `if extra:`, el
    `TemplateResponse(...)`.

    **Parte B — templates/base.html (antes de `</body>`):**

    Primero, identificar la línea exacta de cierre del `toastSystem`
    script (línea ≈ 233 según 05-PATTERNS.md) y `</body>` (línea ≈ 236).
    Insertar JUSTO DESPUÉS del `</script>` del toastSystem y ANTES de
    `</body>`:

    ```jinja
    {% if flash_message %}
    <script>
      // Flash toast (D-07/D-08) — dispara el sistema existente. Debe
      // esperar a DOMContentLoaded porque showToast() se define arriba
      // pero el contenedor Alpine puede no estar listo aún.
      document.addEventListener('DOMContentLoaded', function () {
        if (typeof showToast === 'function') {
          showToast('info', 'Aviso', {{ flash_message|tojson }});
        }
      });
    </script>
    {% endif %}
    ```

    Anti-patrones (per 05-PATTERNS.md):
    - NO introducir un nuevo sistema de toast — reusar el de
      `base.html:188-223` via `showToast(...)`.
    - NO interpolar sin `|tojson` — evita XSS en el mensaje.
    - NO llamar `showToast` sin esperar `DOMContentLoaded` — la función
      se define más arriba pero el contenedor Alpine que la acepta puede
      no estar listo.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from api.deps import render; import inspect; src = inspect.getsource(render); assert 'flash_message' in src; print('OK: render() has flash_message')" && grep -n "flash_message|tojson" templates/base.html && grep -c 'showToast..info.., .Aviso.,' templates/base.html</automated>
  </verify>
  <done>
    - `grep -n "flash_message" api/deps.py` match.
    - `grep -n "flash_message|tojson" templates/base.html` match 1 línea.
    - `grep -n "showToast('info', 'Aviso'" templates/base.html` match 1 línea.
    - Template parsea sin error Jinja.
    - `ruff check api/deps.py` OK.
  </done>
  <commit>feat(templates): render flash toast in base.html via showToast (D-08)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit tests for FlashMiddleware</name>
  <files>tests/middleware/test_flash.py</files>
  <behavior>
    Tests unit con una mini-app Starlette que monte SOLO FlashMiddleware y
    un endpoint dummy. Sin auth, sin DB. Verifican:
    - Request sin cookie `nexo_flash` → `request.state.flash is None` en el
      endpoint; response no contiene `Set-Cookie: nexo_flash`.
    - Request con cookie `nexo_flash=hola` → `request.state.flash == 'hola'`;
      response incluye header `Set-Cookie` con `nexo_flash=""` / `Max-Age=0`
      (el delete_cookie de Starlette).
    - Request con cookie vacía (string `""`) — edge case: debe tratarse
      como presente (el `if flash is not None` es True aunque `flash == ""`).
      Decisión documentada: lo tratamos como flash vacío pero presente,
      así se borra.
  </behavior>
  <action>
    Crear `tests/middleware/test_flash.py`.

    1. Construir una mini-app con `starlette.applications.Starlette`:
       ```python
       from starlette.applications import Starlette
       from starlette.responses import PlainTextResponse
       from starlette.routing import Route
       from starlette.testclient import TestClient
       from nexo.middleware.flash import FlashMiddleware, _FLASH_COOKIE

       async def _peek(request):
           return PlainTextResponse(
               request.state.flash if request.state.flash is not None else "NONE"
           )

       app = Starlette(
           routes=[Route("/peek", _peek)],
           middleware=[Middleware(FlashMiddleware)],
       )
       client = TestClient(app)
       ```

    2. Tests:
       - `test_flash_absent_returns_none_and_no_set_cookie`.
       - `test_flash_present_populates_state_and_deletes_cookie`.
       - `test_flash_empty_string_is_treated_as_present`.
       - `test_flash_multiple_requests_read_and_clear_sequence` — request
         1 con cookie → response borra; request 2 sin cookie → state is None.

    3. No depender de `api.main.app` — mantener tests hermeticos.

    4. Marcar `pytestmark = pytest.mark.unit` si aplica convención.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/middleware/test_flash.py -x -q</automated>
  </verify>
  <done>
    - ≥ 4 tests, todos verdes.
    - `ruff check tests/middleware/test_flash.py` OK.
    - Tests NO dependen de `api.main.app` ni de la DB.
  </done>
  <commit>test(middleware): unit tests for FlashMiddleware read-and-clear</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Integration tests for 403 redirect + API JSON contract</name>
  <files>tests/routers/test_forbidden_redirect.py</files>
  <behavior>
    Tests E2E contra `api.main.app` con TestClient. Login real con usuarios
    no-propietario. Casos:

    - **HTML path:** usuario rrhh (sin `ajustes:manage`) hace
      `GET /ajustes` con `Accept: text/html` (default) → 302, `Location: /`,
      header `Set-Cookie: nexo_flash=...` presente, valor del mensaje
      contiene "No tienes permiso para acceder a".
    - **Siguiente request:** GET `/` con la cookie flash → 200, body HTML
      contiene el bloque `{% if flash_message %}...showToast...{% endif %}`
      renderizado (buscar "showToast('info', 'Aviso'," en el HTML).
    - **Read-and-clear:** un TERCER GET `/` (sin cookie esta vez) → 200,
      body NO contiene el bloque showToast.
    - **API JSON path:** usuario rrhh hace `GET /ajustes` con
      `Accept: application/json` → 403 JSON con
      `{"detail": "Permiso requerido: ajustes:manage"}`, NO redirect, NO
      `Set-Cookie: nexo_flash`.
    - **HX-Request path:** `GET /ajustes` con header `HX-Request: true` →
      403 JSON (HTMX también esperaba contract), NO redirect.
    - **No regression:** 404 (ruta inexistente) sigue respondiendo como el
      handler default de FastAPI (JSON `{"detail":"Not Found"}` en API,
      HTML default en navegador). Sin regresión del handler NAMING-07 de
      500.

    Para 500, puede usarse un endpoint test-only que lance `raise Exception`
    o confiar en tests existentes de NAMING-07 (si los hay, ejecutarlos
    como regression check).
  </behavior>
  <action>
    Crear `tests/routers/test_forbidden_redirect.py`.

    1. Reusar fixtures de login existentes (mismo patrón que 05-02
       Task 3).

    2. Tests principales:
       ```python
       def test_html_403_redirects_with_flash_cookie(client, rrhh_session):
           r = client.get("/ajustes", cookies=rrhh_session,
                          headers={"Accept": "text/html"},
                          follow_redirects=False)
           assert r.status_code == 302
           assert r.headers["location"] == "/"
           set_cookie = r.headers.get("set-cookie", "")
           assert "nexo_flash=" in set_cookie
           assert "No tienes permiso para acceder a" in set_cookie

       def test_next_html_request_renders_flash_toast(client, rrhh_session):
           # Disparar 403 primero
           r1 = client.get("/ajustes", cookies=rrhh_session,
                           follow_redirects=False)
           assert r1.status_code == 302
           flash_cookie = _extract_cookie(r1, "nexo_flash")
           # Request 2: GET / con la cookie
           r2 = client.get("/",
                           cookies={**rrhh_session, "nexo_flash": flash_cookie},
                           follow_redirects=False)
           assert r2.status_code == 200
           assert "showToast('info', 'Aviso'" in r2.text

       def test_api_json_403_preserves_contract(client, rrhh_session):
           r = client.get("/ajustes", cookies=rrhh_session,
                          headers={"Accept": "application/json"},
                          follow_redirects=False)
           assert r.status_code == 403
           body = r.json()
           assert body["detail"].startswith("Permiso requerido:")
           assert "nexo_flash" not in r.headers.get("set-cookie", "")

       def test_hx_request_403_preserves_contract(client, rrhh_session):
           r = client.get("/ajustes", cookies=rrhh_session,
                          headers={"HX-Request": "true"},
                          follow_redirects=False)
           assert r.status_code == 403
       ```

    3. Test adicional de read-and-clear:
       ```python
       def test_flash_cookie_is_cleared_after_read(client, rrhh_session):
           # Request con cookie flash
           r1 = client.get("/",
                           cookies={**rrhh_session, "nexo_flash": "hola"},
                           follow_redirects=False)
           assert r1.status_code == 200
           # Response borra la cookie
           set_cookie = r1.headers.get("set-cookie", "")
           assert "nexo_flash=" in set_cookie
           assert "Max-Age=0" in set_cookie or 'expires=Thu, 01 Jan 1970' in set_cookie.lower()
       ```

    4. Marcar `pytestmark = pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/routers/test_forbidden_redirect.py -x -q</automated>
  </verify>
  <done>
    - ≥ 5 tests, todos verdes.
    - Suite completa sin regresión: `pytest tests/ -x -q`.
    - `ruff check tests/routers/test_forbidden_redirect.py` OK.
  </done>
  <commit>test(routers): integration tests for 403 HTML redirect + JSON contract</commit>
</task>

</tasks>

<success_criteria>
- `nexo/middleware/flash.py` existe y es consumido por `api.main.app` entre Audit y Auth.
- Navegar como non-propietario a `/ajustes` (HTML) devuelve 302 con cookie flash; siguiente request muestra toast.
- Navegar como non-propietario a `/ajustes` con `Accept: application/json` o `HX-Request: true` devuelve 403 JSON intacto.
- `global_exception_handler` de NAMING-07 sin regresión (404/500 siguen el comportamiento previo).
- 5 commits atómicos (middleware, handler+wiring, render+toast, test flash, test forbidden).
- Suite completa verde.
</success_criteria>

<output>
`.planning/phases/05-ui-por-roles/05-03-SUMMARY.md` con:
- Confirmación explícita del orden del middleware stack tras el cambio.
- Prueba de no-regresión del handler NAMING-07 (log del test relevante si existe, o smoke manual documentado).
- Confirmación de que `_wants_json` NO se triplicó.
- Flag "ready for 05-04 / Wave 4 (split de /ajustes hub)".
</output>
