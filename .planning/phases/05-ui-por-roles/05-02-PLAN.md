---
phase: 05-ui-por-roles
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - templates/base.html
  - tests/routers/test_sidebar_filtering.py
autonomous: true
requirements: [UIROL-02]
tags: [templates, rbac, sidebar, phase-5]
must_haves:
  truths:
    - "El `nav_items` de `base.html` deja de tener el campo `visible_to` (strings `\"*\"`/`\"propietario\"`) y pasa a tener `permission` (string de permiso o `None`)."
    - "El filtro del loop usa `can(current_user, permission)` — única fuente de verdad compartida con `require_permission` (D-01)."
    - "Propietario ve los 11 items del sidebar (incluye separador `_sep1`) + link a Solicitudes."
    - "Un usuario de `rrhh` (no propietario) NO ve `/ajustes` ni `/bbdd` ni `/pipeline`, SÍ ve `/historial` y `/operarios`."
    - "Un directivo/usuario de `ingenieria` SÍ ve `/pipeline`, `/recursos`, `/ciclos-calc`, `/bbdd`; NO ve `/ajustes` ni `/operarios`."
    - "El badge de Solicitudes (propietario-only, HTMX 30s) se conserva funcionalmente; solo cambia el gate de `role == 'propietario'` a `can(current_user, \"aprobaciones:manage\")`."
  artifacts:
    - path: templates/base.html
      provides: "Sidebar filtrado por permisos usando `can()` (D-01) + badge Solicitudes refactorizado."
      contains: "can(current_user"
    - path: tests/routers/test_sidebar_filtering.py
      provides: "Integration tests con TestClient: propietario ve 11 items, ingeniería ve N, rrhh ve M."
      min_lines: 80
  key_links:
    - from: "templates/base.html nav loop"
      to: "nexo.services.auth.can (via Jinja global)"
      via: "llamada `{% if permission is none or can(current_user, permission) %}`"
      pattern: "can\\(current_user, permission\\)"
    - from: "templates/base.html solicitudes block"
      to: "can(current_user, \"aprobaciones:manage\")"
      via: "refactor del `if current_user and current_user.role == 'propietario'`"
      pattern: 'can\\(current_user, "aprobaciones:manage"\\)'
---

<objective>
Wave 2 — sidebar refactor. Reemplazar el filtro grueso por rol
(`visible_to` == `"*"` / `"propietario"`) por filtro fino por permiso
(`can(current_user, "modulo:accion")`). Resultado: el sidebar y el backend
usan la misma fuente de verdad (`PERMISSION_MAP` vía `can()`), imposibilitando
el caso "link visible que devuelve 403 al hacer click".

Purpose: D-01 (sidebar fine-grained) + D-02 (zero-trust DOM) aplicados al
único consumidor que queda con gating grueso.

Output: `templates/base.html` con `nav_items` de 5-tuplas (permission en vez
de visible_to) + badge Solicitudes consistente + tests de integración con
TestClient que verifican el set visible por rol/dept.

Reuse register consumed (from 05-PATTERNS.md):
- #1 `can(user, permission)` — recién creado en 05-01.
- #2 `PERMISSION_MAP` — fuente de los strings de permiso (auth.py:200-232).
- #18 route `/ajustes` ya gatea con `require_permission("ajustes:manage")`.

Pitfalls prevenidos:
- Pitfall 3: NO se registra `current_user` como Jinja global — se sigue
  pasando por contexto vía `render()`. Solo `can` va a globals.
- Arity del tuple: se reemplaza `visible_to` in-place (sigue siendo 5 slots),
  NO se añade un 6º slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-por-roles/05-CONTEXT.md
@.planning/phases/05-ui-por-roles/05-RESEARCH.md
@.planning/phases/05-ui-por-roles/05-PATTERNS.md
@.planning/phases/05-ui-por-roles/05-01-PLAN.md
@templates/base.html
@nexo/services/auth.py

<interfaces>
<!-- Permission strings que usa el sidebar (extraídos de auth.py:200-232). -->

Sidebar → permission mapping (per 05-RESEARCH.md §Pattern 5 + 05-PATTERNS.md):
- `/`            → None (dashboard siempre visible post-login)
- `/datos`       → "datos:read"
- `/pipeline`    → "pipeline:read"
- `/historial`   → "historial:read"
- `/capacidad`   → "capacidad:read"
- `_sep1`        → None (separador siempre visible)
- `/recursos`    → "recursos:read"
- `/ciclos-calc` → "ciclos:read"
- `/operarios`   → "operarios:read"
- `/bbdd`        → "bbdd:read"
- `/ajustes`     → "ajustes:manage"   (lista vacía → propietario-only)
- Solicitudes    → "aprobaciones:manage" (lista vacía → propietario-only)

Visibilidad esperada por rol (per CONTEXT.md success criteria 1-3):
- propietario: TODO (bypass).
- directivo de ingenieria: dashboard, datos, pipeline, historial, capacidad,
  sep, recursos, ciclos_calc, bbdd. NO ajustes, NO operarios.
- usuario de produccion: dashboard, datos, pipeline, historial, capacidad,
  sep, recursos, ciclos_calc. NO bbdd, NO operarios, NO ajustes.
- usuario de rrhh: dashboard, historial (rrhh está en historial:read), sep,
  operarios. NO pipeline, NO bbdd, NO ajustes, NO datos.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor nav_items to carry `permission` instead of `visible_to`</name>
  <files>templates/base.html</files>
  <behavior>
    - El loop `{% for key, href, label, icon_paths, ... in nav_items %}`
      sigue desempacando 5 elementos. La 5ª posición cambia de string
      (`"*"`, `"propietario"`) a string de permiso o `None`.
    - El `{% if %}` del loop cambia de
      `visible_to == "*" or (current_user and current_user.role == visible_to)`
      a `permission is none or can(current_user, permission)`.
    - Items del sidebar visibles por rol coinciden con el mapping declarado
      en <interfaces>.
  </behavior>
  <action>
    Editar `templates/base.html` en la región lines 48-79.

    1. Reemplazar cada tupla de `nav_items` (lines 49-59) para que el 5º
       slot sea un permission string o `None`. Texto literal a aplicar
       (preservando los SVG paths `icon_paths` exactamente como están —
       NO modificar las cadenas SVG):

       ```jinja
       {% set nav_items = [
         ("dashboard",   "/",            "Centro Mando",    "<MISMO SVG>", None),
         ("datos",       "/datos",       "Datos",           "<MISMO SVG>", "datos:read"),
         ("pipeline",    "/pipeline",    "Analisis",        "<MISMO SVG>", "pipeline:read"),
         ("historial",   "/historial",   "Historial",       "<MISMO SVG>", "historial:read"),
         ("capacidad",   "/capacidad",   "Capacidad",       "<MISMO SVG>", "capacidad:read"),
         ("_sep1",       "",             "",                "",            None),
         ("recursos",    "/recursos",    "Recursos",        "<MISMO SVG>", "recursos:read"),
         ("ciclos_calc", "/ciclos-calc", "Calcular Ciclos", "<MISMO SVG>", "ciclos:read"),
         ("operarios",   "/operarios",   "Operarios",       "<MISMO SVG>", "operarios:read"),
         ("bbdd",        "/bbdd",        "BBDD",            "<MISMO SVG>", "bbdd:read"),
         ("ajustes",     "/ajustes",     "Ajustes",         "<MISMO SVG>", "ajustes:manage"),
       ] %}
       ```

    2. Reemplazar el unpack y el filtro (líneas 61-62). Antes:
       ```jinja
       {% for key, href, label, icon_paths, visible_to in nav_items %}
         {% if visible_to == "*" or (current_user and current_user.role == visible_to) %}
       ```
       Después:
       ```jinja
       {% for key, href, label, icon_paths, permission in nav_items %}
         {% if permission is none or can(current_user, permission) %}
       ```

    3. NO tocar el body del loop (líneas 63-78): separador, `<a>` con icon,
       tag `x-show`, etc. Quedan idénticos.

    4. Smoke manual mental: comprobar que `{% if permission is none or
       can(current_user, permission) %}` es tolerante a `current_user = None`
       (rutas públicas como `/login`). Respuesta: sí — `can(None, x)` devuelve
       False, y `permission is none` rescata dashboard+separador.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from jinja2 import Environment, FileSystemLoader; e = Environment(loader=FileSystemLoader('templates')); e.parse(open('templates/base.html').read()); print('OK: base.html parses')" && grep -n "can(current_user, permission)" templates/base.html && grep -c '"visible_to"\|visible_to ==' templates/base.html | grep -q '^0$' && echo "OK: no residual visible_to"</automated>
  </verify>
  <done>
    - `grep -n "can(current_user, permission)" templates/base.html` matches.
    - `grep -c 'visible_to ==' templates/base.html` devuelve 0.
    - `grep -c '"\*"' templates/base.html` en la región del nav_items
      devuelve 0 (el `"*"` como rol desaparece; el `"*"` puede aparecer en
      otros sitios no relacionados — inspección manual del diff).
    - Template parsea sin error de sintaxis Jinja.
  </done>
  <commit>feat(sidebar): filter nav_items by permission using can() (D-01, UIROL-02)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Solicitudes badge gate from role check to can()</name>
  <files>templates/base.html</files>
  <behavior>
    - Línea 82: `{% if current_user and current_user.role == 'propietario' %}`
      → `{% if can(current_user, "aprobaciones:manage") %}`.
    - Funcionalmente equivalente HOY (PERMISSION_MAP["aprobaciones:manage"] == []
      → propietario-only).
    - El bloque del `<a>` con HTMX `hx-get="/api/approvals/count"` queda
      intacto.
    - La convención del sidebar es 100% `can()` — no quedan mixed gates
      (anti-patrón documentado en 05-PATTERNS.md).
  </behavior>
  <action>
    Editar `templates/base.html` línea 82. Reemplazar:
    ```jinja
    {% if current_user and current_user.role == 'propietario' %}
    ```
    por:
    ```jinja
    {% if can(current_user, "aprobaciones:manage") %}
    ```

    No tocar el resto del bloque (líneas 83-94: el `<a>`, el SVG, el HTMX
    badge, el `{% endif %}`).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && grep -c "current_user.role == 'propietario'" templates/base.html | grep -q '^0$' && echo "OK: no role-string check in base.html" && grep -n 'can(current_user, "aprobaciones:manage")' templates/base.html</automated>
  </verify>
  <done>
    - `grep -c "current_user.role == 'propietario'" templates/base.html` devuelve 0.
    - `grep -n 'can(current_user, "aprobaciones:manage")' templates/base.html` matches 1 línea.
    - Template parsea correctamente.
  </done>
  <commit>refactor(sidebar): replace role-check with can() for solicitudes badge</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests for sidebar visibility per role/dept</name>
  <files>tests/routers/test_sidebar_filtering.py</files>
  <behavior>
    Tests E2E ligeros que hacen login como 3 usuarios distintos, GET `/`
    (dashboard post-login), y verifican que el HTML de respuesta contiene/
    no contiene los hrefs del sidebar según el rol/dept.

    Casos mínimos:
    - test_sidebar_propietario_sees_all_items: login propietario → HTML
      contiene todos los hrefs: `/`, `/datos`, `/pipeline`, `/historial`,
      `/capacidad`, `/recursos`, `/ciclos-calc`, `/operarios`, `/bbdd`,
      `/ajustes`, `/ajustes/solicitudes`.
    - test_sidebar_ingenieria_directivo_sees_engineering_modules: HTML
      contiene `/pipeline`, `/recursos`, `/ciclos-calc`, `/bbdd`; NO
      contiene `/ajustes` ni `/operarios` ni `/ajustes/solicitudes`.
    - test_sidebar_produccion_usuario_sees_production_modules: HTML
      contiene `/pipeline`, `/recursos`, `/historial`; NO contiene `/bbdd`
      ni `/ajustes` ni `/ajustes/solicitudes` ni `/operarios`.
    - test_sidebar_rrhh_usuario_sees_operarios_only_for_hr: HTML contiene
      `/operarios` y `/historial`; NO contiene `/pipeline` ni `/bbdd` ni
      `/ajustes` ni `/datos`.
    - test_sidebar_anon_login_page_renders_without_error: GET `/login`
      (no auth) — response 200, sin excepción por `can(None, ...)`.
  </behavior>
  <action>
    Crear `tests/routers/test_sidebar_filtering.py`.

    1. Reusar las fixtures/factories existentes en
       `tests/routers/conftest.py` y/o `tests/conftest.py` para:
       - Construir un `TestClient(app)`.
       - Crear usuarios con rol + departments, autenticarlos (reusar el
         mismo helper que usan los tests de Plan 04-03 o Plan 02-04 para
         login — NO re-implementar). Si no hay helper, el primer test
         debe hacer POST `/login` con credenciales seed, capturar cookie,
         y reutilizarla para el GET `/`.

    2. Para cada caso, assert sobre el body HTML:
       ```python
       resp = client.get("/", cookies=session_cookies)
       assert resp.status_code == 200
       body = resp.text
       # Items que DEBEN estar:
       assert 'href="/pipeline"' in body
       assert 'href="/recursos"' in body
       # Items que NO deben estar:
       assert 'href="/ajustes"' not in body
       assert 'href="/operarios"' not in body
       ```

    3. Importante (Pitfall defensivo): los asserts deben buscar el `href="/x"`
       EXACTO (con comillas), no substring `/x` — si buscas `/ajustes`
       también haría match de `/ajustes/solicitudes`.

    4. Para el caso "propietario ve Solicitudes", asserts sobre
       `href="/ajustes/solicitudes"` Y `hx-get="/api/approvals/count"`.

    5. Marcar `pytestmark = pytest.mark.integration` si aplica la convención
       del repo.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && pytest tests/routers/test_sidebar_filtering.py -x -q</automated>
  </verify>
  <done>
    - ≥ 5 tests, todos verdes.
    - `ruff check tests/routers/test_sidebar_filtering.py` OK.
    - Sin duplicación de login helper (el test reutiliza fixture existente).
  </done>
  <commit>test(sidebar): integration tests for per-role sidebar visibility</commit>
</task>

</tasks>

<success_criteria>
- `templates/base.html` NO contiene `visible_to` ni `current_user.role == 'propietario'`.
- El sidebar filtra via `can(current_user, permission)` en 100% de los items.
- `tests/routers/test_sidebar_filtering.py` tiene ≥ 5 tests verdes.
- No regresión en suite completa: `pytest tests/ -x -q` verde.
- 3 commits atómicos (nav_items, solicitudes badge, tests).
</success_criteria>

<output>
`.planning/phases/05-ui-por-roles/05-02-SUMMARY.md` con:
- Diff resumido de `base.html` (11 tuplas migradas + 1 badge refactorizado).
- Lista concreta de items visibles observada por rol en los tests (p.ej.
  "rrhh usuario ve: dashboard, historial, sep, operarios" — para smoke
  manual posterior).
- Flag "ready for 05-03 / Wave 3 (forbidden UX + flash pipeline)".
</output>
