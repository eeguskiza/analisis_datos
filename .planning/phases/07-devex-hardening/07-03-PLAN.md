---
phase: 07-devex-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - docs/ARCHITECTURE.md
  - docs/RUNBOOK.md
  - docs/RELEASE.md
  - CHANGELOG.md
  - tests/infra/test_devex_docs.py
autonomous: true
requirements:
  - DEVEX-04
  - DEVEX-05
  - DEVEX-06
tags:
  - devex
  - docs
  - architecture
  - runbook
  - release
  - changelog

must_haves:
  truths:
    - "docs/ARCHITECTURE.md existe con diagrama Mermaid que muestra los 3 engines (engine_mes, engine_app, engine_nexo) y los servicios compose (web, db, caddy, mcp profile-gated)"
    - "docs/RUNBOOK.md tiene EXACTAMENTE 5 escenarios (MES caido, Postgres no arranca, cert Caddy expira, pipeline atascado, lockout propietario) siguiendo el patron Sintomas -> Diagnostico -> Remedio -> Prevencion"
    - "El escenario Pipeline atascado documenta explicitamente que NO existe un helper list_locks() y que el remedio es docker compose restart web"
    - "El escenario Lockout propietario documenta DELETE directo sobre nexo.login_attempts (emergency-only) + prevencion '>=2 propietarios siempre'"
    - "El escenario Certificado Caddy documenta que Caddy tls internal usa root CA auto-regenerada cada 10 anos e intermediate cada 7 dias"
    - "El escenario Postgres no arranca documenta pgdata volume persistence y NO usar docker compose down -v (Landmine 6 Phase 6)"
    - "docs/RELEASE.md tiene checklist con >=7 items, referencia a scripts/deploy.sh + tests/infra/deploy_smoke.sh, y define convencion semver vMAJOR.MINOR.PATCH"
    - "CHANGELOG.md sigue Keep a Changelog 1.1.0 con seccion [Unreleased] + [1.0.0] detallando Phases 1-7"
    - "Los 4 docs usan espanol para contenido user-facing y no contienen emojis (hard invariant CLAUDE.md)"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Mapa tecnico del repo para devs nuevos / IAs"
      contains: "mermaid"
      min_lines: 150
    - path: "docs/RUNBOOK.md"
      provides: "5 escenarios de incidencia con procedimiento completo"
      contains: "## Escenario 5:"
      min_lines: 250
    - path: "docs/RELEASE.md"
      provides: "Checklist de release versionado con semver + deploy + smoke"
      contains: "scripts/deploy.sh"
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog con historial Mark-III (Phase 1..7)"
      contains: "## [1.0.0]"
    - path: "tests/infra/test_devex_docs.py"
      provides: "Regresion sobre los 4 docs (estructura + palabras clave + landmines criticas)"
      contains: "def test_"
  key_links:
    - from: "docs/RUNBOOK.md Escenario 4"
      to: "nexo/services/pipeline_lock.py"
      via: "documentar ausencia de list_locks() y remedio via restart"
      pattern: "docker compose restart web"
    - from: "docs/RUNBOOK.md Escenario 5"
      to: "nexo.login_attempts"
      via: "DELETE manual via docker compose exec db psql"
      pattern: "DELETE FROM nexo.login_attempts"
    - from: "docs/RELEASE.md"
      to: "scripts/deploy.sh + tests/infra/deploy_smoke.sh"
      via: "checklist referencia los artefactos Phase 6"
      pattern: "deploy_smoke.sh"
    - from: "docs/ARCHITECTURE.md"
      to: "CLAUDE.md, DEPLOY_LAN.md, AUTH_MODEL.md, BRANDING.md, GLOSSARY.md, RUNBOOK.md, RELEASE.md"
      via: "cross-links en seccion Enlaces rapidos"
      pattern: "DEPLOY_LAN.md"
---

<objective>
Producir el quartet de docs de cierre de Mark-III: `docs/ARCHITECTURE.md` (mapa
tecnico), `docs/RUNBOOK.md` (5 escenarios de incidencia), `docs/RELEASE.md`
(checklist versionado) y `CHANGELOG.md` (Keep a Changelog con Phases 1-7).

Los 4 son copy-pega con adaptacion desde RESEARCH.md (§Code Examples), pero
tres escenarios del RUNBOOK estan basados en hallazgos de lectura directa del codigo:
pipeline_lock sin helper list_locks, auth sin helper unlock_user, Caddy tls
internal con rotacion intermediate/root 7d/10a, pgdata volume frente a `down -v`.

Purpose: DEVEX-04 + DEVEX-05 + DEVEX-06 aterrizan. Segundo admin puede operar
incidencia comun sin contexto del autor; un release reproducible tiene checklist
ejecutable.
Output: 4 docs nuevos + 1 test de regresion que valida estructura minima.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-devex-hardening/07-RESEARCH.md
@.planning/phases/07-devex-hardening/07-VALIDATION.md
@CLAUDE.md
@docs/DEPLOY_LAN.md
@docs/AUTH_MODEL.md
@docs/BRANDING.md
@docs/GLOSSARY.md
@nexo/services/pipeline_lock.py
@nexo/services/auth.py
@api/main.py
@docker-compose.yml
@scripts/deploy.sh
@scripts/backup_nightly.sh

<interfaces>
<!-- 4 hallazgos criticos verificados en Phase 7 research, deben reflejarse
     palabra por palabra en RUNBOOK.md: -->

1. Pipeline atascado (Escenario 4): nexo/services/pipeline_lock.py usa
   asyncio.Semaphore(3) in-process. NO hay funcion list_locks() ni endpoint admin.
   Remedio oficial: docker compose restart web. Diagnostico indirecto
   accediendo a atributo privado pipeline_semaphore._value (advertencia: fragil).

2. Lockout propietario (Escenario 5): nexo/services/auth.py tiene clear_attempts(db,
   email, ip) pero NO hay helper unlock_user ni endpoint admin. Si el unico propietario
   se lockea, recuperacion via DELETE FROM nexo.login_attempts WHERE email con
   docker compose exec -T db psql. Prevencion literal: siempre crear >=2 propietarios.

3. Certificado Caddy (Escenario 3): Caddy tls internal con rotacion documentada
   en Phase 6 DEPLOY_LAN.md: root CA cada 10 anos, intermediate cada 7 dias. Root CA
   vive en volumen nombrado caddy_data — NO borrar con docker compose down -v
   (Landmine 6 de Phase 6).

4. Postgres no arranca (Escenario 2): pgdata volumen en docker-compose.yml,
   backup reciente en /var/backups/nexo/<latest>.sql.gz (backup_nightly.sh de Phase 6,
   retencion 7d). Recovery: gunzip < file | docker compose exec -T db psql -U user -d db.

<!-- Invariantes CLAUDE.md que se aplican a TODOS los docs: -->

- No emojis en los archivos generados (hard invariant)
- Espanol para user-facing (RUNBOOK, ARCHITECTURE, RELEASE); las referencias tecnicas
  (nombres de archivos, comandos, env vars) quedan en ingles natural
- Placeholders literales tipo IP_NEXO, email, POSTGRES_USER con angle brackets
  (patron heredado de DEPLOY_LAN.md de Phase 6)
- NO credenciales reales en los docs

<!-- Referencias cruzadas esperadas en ARCHITECTURE.md seccion Enlaces rapidos: -->
CLAUDE.md, AUTH_MODEL.md, BRANDING.md, GLOSSARY.md, DEPLOY_LAN.md, RUNBOOK.md, RELEASE.md,
SECURITY_AUDIT.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear docs/ARCHITECTURE.md (mapa tecnico + Mermaid 3 engines)</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - .planning/phases/07-devex-hardening/07-RESEARCH.md §5 "ARCHITECTURE.md (DEVEX-04)" + Code Examples (lines 1078-1288)
    - docs/DEPLOY_LAN.md (patron de tono + secciones para consistencia)
    - docker-compose.yml (confirmar nombres servicios: web, db, caddy, mcp)
    - api/main.py (confirmar orden middleware stack: Auth -> Audit -> Flash -> QueryTiming)
    - nexo/data/engines.py (confirmar nombres engine_mes / engine_app / engine_nexo)
  </read_first>
  <action>
    Crear docs/ARCHITECTURE.md copiando LITERAL el esqueleto de RESEARCH.md §Code Examples
    (lines 1080-1288). Ajustes a aplicar:

    - Header: "Ultima revision: 2026-MM-DD (Sprint 6 / Phase 7)." con MM-DD de hoy.
    - Seccion 1 "Que es Nexo": 1 parrafo remitiendo a CLAUDE.md.
    - Seccion 2 "Stack": tabla con versiones leidas de requirements.txt (FastAPI 0.135.3,
      Jinja2 3.1.6, SQLAlchemy 2.0.49, psycopg2-binary 2.9.11, pyodbc 5.3.0, argon2-cffi 25.1.0,
      slowapi 0.1.9, matplotlib 3.10.8, pandas 3.0.2). Runtime: python:3.11-slim-bookworm.
    - Seccion 3 "Los 3 engines": diagrama Mermaid literal del RESEARCH (lines 1128-1163)
      mostrando Browser LAN -> Caddy (443 TLS) -> Web FastAPI (:8000) -> (engine_nexo -> Postgres 16
      container db, engine_app -> SQL Server ecs_mobility, engine_mes -> SQL Server dbizaro
      READ-ONLY) + subgrafo opcional MCP (profile-gated). Seguido por tabla de responsabilidades
      por engine (3 filas, con env vars y casos de uso).
    - Seccion 4 "Layout del repo": arbol anotado con api/, nexo/ (incluir nexo/data/,
      nexo/middleware/, nexo/services/), OEE/ (marcado "LEGACY — NO TOCAR"), templates/,
      static/, scripts/, tests/, caddy/, docs/, .planning/, Dockerfile,
      docker-compose.yml, docker-compose.prod.yml, Makefile, pyproject.toml (Phase 7).
    - Seccion 5 "Flujo de una request": middleware stack literal verificado en api/main.py.
      Listar orden: (1) AuthMiddleware outer, (2) AuditMiddleware, (3) FlashMiddleware,
      (4) QueryTimingMiddleware inner.
    - Seccion 6 "Schedulers": tabla con 3 tasks lifespan (thresholds_cache listener,
      cleanup_scheduler con approvals_cleanup/query_log_cleanup/factor_auto_refresh). Cadencias
      exactas del STATE.md Plan 04-04.
    - Seccion 7 "Deployment": 3 lineas redirigiendo a docs/DEPLOY_LAN.md.
    - Seccion 8 "Enlaces rapidos": cross-links a CLAUDE.md, AUTH_MODEL.md, BRANDING.md,
      GLOSSARY.md, DEPLOY_LAN.md, RUNBOOK.md, RELEASE.md, SECURITY_AUDIT.md.
    - Seccion 9 "Si tocas algo estructural, actualiza esto": warning de drift (1 parrafo).

    Reglas:
    - NO emojis.
    - Espanol para prosa, ingles para nombres tecnicos (engine_mes, make up, docker compose).
    - El bloque Mermaid DEBE estar con triple backtick mermaid para que GitHub/VSCode
      lo renderice.
    - Tamano objetivo: ~300-500 lineas.
  </action>
  <verify>
    <automated>test -f docs/ARCHITECTURE.md && grep -q 'mermaid' docs/ARCHITECTURE.md && grep -q 'engine_mes' docs/ARCHITECTURE.md && grep -q 'engine_app' docs/ARCHITECTURE.md && grep -q 'engine_nexo' docs/ARCHITECTURE.md && [ $(wc -l < docs/ARCHITECTURE.md) -ge 150 ]</automated>
  </verify>
  <acceptance_criteria>
    - [x] test -f docs/ARCHITECTURE.md exit 0
    - [x] grep -c 'engine_mes\|engine_app\|engine_nexo' docs/ARCHITECTURE.md >= 3
    - [x] grep -q 'mermaid' docs/ARCHITECTURE.md exit 0
    - [x] grep -q 'Caddy' docs/ARCHITECTURE.md exit 0 (aparece en el diagrama)
    - [x] grep -q 'profile' docs/ARCHITECTURE.md exit 0 (mcp profile-gated mencionado)
    - [x] grep -q 'AuthMiddleware' docs/ARCHITECTURE.md && grep -q 'AuditMiddleware' docs/ARCHITECTURE.md && grep -q 'FlashMiddleware' docs/ARCHITECTURE.md && grep -q 'QueryTimingMiddleware' docs/ARCHITECTURE.md exit 0
    - [x] grep -q 'DEPLOY_LAN.md' docs/ARCHITECTURE.md && grep -q 'RUNBOOK.md' docs/ARCHITECTURE.md && grep -q 'RELEASE.md' docs/ARCHITECTURE.md exit 0
    - [x] wc -l docs/ARCHITECTURE.md >= 150
    - [x] No emojis (python3 regex check sobre rangos unicode emoji-heavy)
  </acceptance_criteria>
  <done>ARCHITECTURE.md publicado con Mermaid + middleware stack + schedulers + cross-links.</done>
</task>

<task type="auto">
  <name>Task 2: Crear docs/RUNBOOK.md con 5 escenarios exactos (hallazgos criticos incluidos)</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/07-devex-hardening/07-RESEARCH.md §6 "RUNBOOK.md (DEVEX-05) — 5 escenarios concretos" (lines 369-467) + Code Examples (lines 1290-1402)
    - nexo/services/pipeline_lock.py (confirmar Semaphore(3) in-process, sin list_locks)
    - nexo/services/auth.py (confirmar clear_attempts, sin unlock_user)
    - docs/DEPLOY_LAN.md (patron de placeholders IP_NEXO, POSTGRES_USER, Landmines numerados)
    - scripts/backup_nightly.sh (confirmar ruta /var/backups/nexo/ y retencion)
  </read_first>
  <action>
    Crear docs/RUNBOOK.md con EXACTAMENTE 5 escenarios, cada uno en seccion "## Escenario N:"
    (con dos puntos al final para que el test de regresion los cuente). Estructura obligatoria
    por escenario: "### Sintomas" -> "### Diagnostico" -> "### Remedio" -> "### Prevencion".

    Escenario 1: MES caido (SQL Server dbizaro inaccesible) — copia literal del RESEARCH
    Code Examples lines 1307-1384. Sintomas: /bbdd 503 + pyodbc.OperationalError. Diagnostico:
    docker compose exec web python -c "from nexo.data.engines import engine_mes...". Remedio
    3 casos (red/credenciales/MES down). Prevencion: /api/health monitoreo.

    Escenario 2: Postgres no arranca — basado en §6 RESEARCH lines 387-398:
    - Sintomas: docker compose ps muestra db unhealthy/exited, web en dependency failed.
    - Diagnostico: docker compose logs db + df -h /var/lib/docker.
    - Remedio: (A) espacio, docker system prune -a (WARNING: NO docker volume rm pgdata),
      (B) corrupcion, restore desde /var/backups/nexo/<latest>.sql.gz con gunzip + psql,
      (C) downgrade imagen.
    - Prevencion: backup_nightly.sh en cron (Phase 6). Alerta a disco >80%. Warning explicito
      "NO usar docker compose down -v — borra pgdata (Landmine 6 Phase 6)".

    Escenario 3: Certificado Caddy expira / warning en browsers — basado en §6 RESEARCH
    lines 401-413:
    - Sintomas: ERR_CERT_AUTHORITY_INVALID o NET::ERR_CERT_DATE_INVALID en browser.
    - Contexto: Caddy tls internal con root CA cada 10 anos, intermediate cada 7 dias
      (VERIFIED en Phase 6 DEPLOY_LAN.md). Root CA distribuida manualmente a clientes LAN.
    - Diagnostico: docker compose exec caddy caddy list-certificates + openssl s_client
      -connect nexo.ecsmobility.local:443 -showcerts con grep issuer/subject/Not After.
    - Remedio: (A) intermediate caducado con Caddy vivo -> docker compose restart caddy.
      (B) root CA cambiada tras reinstalacion limpia -> re-extraer de volumen caddy_data
      y redistribuir. (C) auto-renovacion rota -> docker compose logs caddy, evaluar
      cert-manager o certbot DNS-01.
    - Prevencion: Root CA vive en volumen caddy_data — NO borrar con down -v. Backup de
      caddy_data junto a pgdata.

    Escenario 4: Pipeline atascado (HALLAZGO CRITICO: no hay list_locks helper) — basado
    en §6 RESEARCH lines 415-430:
    - Sintomas: /api/pipeline/run 504 tras 15 min, 3 slots ocupados, usuarios esperando.
      Logs con asyncio.wait_for timeout.
    - Diagnostico (HALLAZGO PRIMARIO): NO existe endpoint directo para listar locks. Indicador
      indirecto accediendo a atributo privado: docker compose exec web python -c "from
      nexo.services.pipeline_lock import pipeline_semaphore; print('value:',
      pipeline_semaphore._value)" (fragil, solo diagnostico). Ver threads vivos:
      docker compose exec web python -c "import threading; [print(t.name, t.is_alive()) for t
      in threading.enumerate()]". Ver ultimo pipeline en nexo.query_log.
    - Remedio:
      - Nuclear: docker compose restart web — mata threads colgados, reinicia event loop,
        libera semaphore. ~30s (lifespan re-inicializa). Usuarios pierden sesion si no cookie
        persistente.
      - No-nuclear: esperar timeout duro 15 min (el thread matplotlib puede seguir 30-60s
        tras timeout, Pitfall 1 documentado en pipeline_lock.py docstring).
    - Prevencion: Phase 4 preflight bloquea pipelines caros antes de tomar slot. Monitor
      nexo.query_log alerta actual_ms > warn_ms * 3. Mark-IV: pool workers + supervisor.

    Escenario 5: Lockout del unico propietario (HALLAZGO CRITICO: no hay unlock_user) —
    basado en §6 RESEARCH lines 432-463:
    - Sintomas: unico usuario propietario intenta loguear, responde "Usuario y contrasena
      incorrectos o cuenta bloqueada. Intentalo en 15 min.". Otros usuarios (directivo, usuario)
      no gestionan porque solo propietario tiene ese permiso.
    - Diagnostico: docker compose exec -T db psql -U POSTGRES_USER -d POSTGRES_DB -c
      "SELECT email, ip, failed_at FROM nexo.login_attempts WHERE email='<email>' AND
      failed_at > now() - interval '15 minutes';".
    - Remedio (DELETE manual — EMERGENCY ONLY):
      docker compose exec -T db psql -U POSTGRES_USER -d POSTGRES_DB -c
      "DELETE FROM nexo.login_attempts WHERE email='<email-propietario>';"
      Variante Python si psql no disponible: docker compose exec web python -c "from
      nexo.data.engines import SessionLocalNexo; from nexo.services.auth import clear_attempts;
      db = SessionLocalNexo(); clear_attempts(db, '<email>', '<ip>')". ATENCION:
      clear_attempts(db, email, ip) requiere tupla; si IP desconocido, DELETE directo solo por
      email como primera linea.
    - Prevencion (HARD RULE):
      1. SIEMPRE crear >=2 usuarios propietario (principal + break-glass con password fuerte
         en sobre sellado).
      2. Documentar en CLAUDE.md procedimiento break-glass.
      3. Mark-IV: make unlock-user EMAIL=x + endpoint /api/admin/unlock con token.

    Header del doc:

    # RUNBOOK.md — Procedimientos de incidencia para Nexo

    > Audiencia: operador (admin IT) respondiendo a incidencia en produccion.
    > Complementa DEPLOY_LAN.md (instalacion) con respuesta runtime.
    > Ver ARCHITECTURE.md para contexto del sistema.

    Ultima revision: 2026-MM-DD (Sprint 6 / Phase 7).

    Cada escenario tiene: Sintomas -> Diagnostico -> Remedio -> Prevencion.
    Comandos asumen ssh IP_NEXO en /opt/nexo salvo indicacion contraria.

    Reglas:
    - NO emojis (hard invariant).
    - EXACTAMENTE 5 escenarios (ni mas ni menos).
    - Cada escenario con sus 4 sub-secciones literal.
    - Placeholders IP_NEXO, email, email-propietario, ip, POSTGRES_USER, POSTGRES_DB, latest
      con angle brackets (coherente con DEPLOY_LAN.md).
    - El escenario 4 DEBE incluir literal "NO existe" o "NO hay" respecto a list_locks()
      (Landmine 7 del RESEARCH).
    - El escenario 5 DEBE incluir prevencion ">= 2 propietarios" (Landmine 8 del RESEARCH).
    - Tamano: ~400-500 lineas totales.
  </action>
  <verify>
    <automated>test -f docs/RUNBOOK.md && [ $(grep -cE '^## Escenario [1-5]:' docs/RUNBOOK.md) -eq 5 ] && grep -q 'MES' docs/RUNBOOK.md && grep -q 'Postgres' docs/RUNBOOK.md && grep -q 'Caddy' docs/RUNBOOK.md && grep -q 'pipeline_semaphore' docs/RUNBOOK.md && grep -q 'DELETE FROM nexo.login_attempts' docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - [x] test -f docs/RUNBOOK.md exit 0
    - [x] Exactamente 5 escenarios: grep -cE '^## Escenario [1-5]:' docs/RUNBOOK.md == 5
    - [x] Cada escenario tiene Sintomas/Diagnostico/Remedio/Prevencion: grep -cE '^### (Sintomas|Diagnostico|Remedio|Prevencion)' docs/RUNBOOK.md >= 20 (5*4)
    - [x] Palabras clave: MES, Postgres, Caddy, Pipeline, propietario todas presentes
    - [x] Escenario 4 (HALLAZGO): grep -qE 'pipeline_semaphore|list_locks' docs/RUNBOOK.md + grep -qE 'docker compose restart web' docs/RUNBOOK.md
    - [x] Escenario 5 (HALLAZGO): grep -q 'DELETE FROM nexo.login_attempts' docs/RUNBOOK.md + grep -qE '(>= ?2|>=2|dos|al menos 2) propietarios' docs/RUNBOOK.md
    - [x] Escenario 3 (Caddy): grep -q 'caddy_data' docs/RUNBOOK.md + grep -qE '7 d|10 a|intermediate|root CA' docs/RUNBOOK.md
    - [x] Escenario 2 (Postgres): grep -qE 'pgdata|down -v|backup_nightly' docs/RUNBOOK.md
    - [x] wc -l docs/RUNBOOK.md >= 250
    - [x] No emojis (python3 regex check)
  </acceptance_criteria>
  <done>RUNBOOK.md con 5 escenarios exactos, hallazgos criticos explicitos, placeholders consistentes.</done>
</task>

<task type="auto">
  <name>Task 3: Crear docs/RELEASE.md + CHANGELOG.md + tests/infra/test_devex_docs.py</name>
  <files>
    docs/RELEASE.md,
    CHANGELOG.md,
    tests/infra/test_devex_docs.py
  </files>
  <read_first>
    - .planning/phases/07-devex-hardening/07-RESEARCH.md §7 "RELEASE.md (DEVEX-06) — checklist" + Code Examples (lines 1406-1575)
    - scripts/deploy.sh (confirmar flujo pre-backup + build + up + smoke)
    - tests/infra/deploy_smoke.sh (confirmar existe, 11 checks Phase 6)
    - .planning/ROADMAP.md (listado completo de Phases para el CHANGELOG)
    - .planning/STATE.md (para citas concretas de Plan 04-04, 05-*, 06-* en CHANGELOG)
  </read_first>
  <action>
    Step A: Crear docs/RELEASE.md copiando literal de RESEARCH.md §Code Examples
    docs/RELEASE.md (lines 1406-1507). Adaptaciones:

    - Header con audiencia: "quien corta una release de Nexo (tipicamente dev lead o
      responsable de la milestone)".
    - Seccion "Versionado": Semver literal, mapa v1.0.0 = Mark-III close, v1.0.x = fixes,
      v2.0.0 = Mark-IV. Warning "Tags son inmutables".
    - Seccion "Checklist pre-release": 7 items como checkboxes markdown. Referencias literales
      a: .planning/ROADMAP.md, pytest --cov, pre-commit run --all-files, .planning/STATE.md
      "Blockers/Concerns", CHANGELOG.md.
    - Seccion "Checklist de release (ejecucion)": 7 pasos numerados con comandos literales:
      (1) git merge --no-ff feature/Mark-III, (2) git tag -a v1.0.0, (3) gh release create,
      (4) ssh IP_NEXO 'cd /opt/nexo && git pull && make deploy', (5) ssh IP_NEXO 'bash
      tests/infra/deploy_smoke.sh' (referencia explicita a Phase 6), (6) verificacion manual
      LAN, (7) anuncio interno.
    - Seccion "Rollback": comandos git checkout tag anterior + make prod-down/up + restore
      desde /var/backups/nexo/predeploy/.

    Step B: Crear CHANGELOG.md en raiz del repo siguiendo Keep a Changelog 1.1.0. Copia
    literal de RESEARCH.md §Code Examples (lines 1511-1575), adaptado con:

    - Header "# Changelog" + link Keep a Changelog + Semver.
    - Seccion [Unreleased] con subsecciones vacias Added/Changed/Fixed.
    - Seccion "## [1.0.0] - 2026-MM-DD" con introduccion "Cierre de Mark-III. Primera release
      productiva de Nexo como plataforma interna de ECS Mobility."
    - Bajo Added, un bullet por cada Phase 1-7 con resumen de entregables (extraidos de
      ROADMAP.md + STATE.md).
    - Bajo Changed: notas sobre OEE/ sin renombrar + repo sin renombrar (coste alto).
    - Bajo Security: rotacion SA SQL Server (Sprint 0) + exception handler UUID + audit
      append-only Postgres.
    - Bajo Deprecated: OEE_* env vars como fallback Mark-III.
    - Links al final: [Unreleased] y [1.0.0] con placeholder <org> para que el mantenedor
      rellene tras merge.

    Step C: Crear tests/infra/test_devex_docs.py con 12+ tests de regresion:

    - test_architecture_md_exists_and_has_mermaid: ARCHITECTURE.md existe + grep mermaid.
    - test_architecture_md_has_three_engines: grep engine_mes + engine_app + engine_nexo.
    - test_architecture_md_has_middleware_stack: Auth/Audit/Flash/QueryTiming.
    - test_architecture_md_cross_links: DEPLOY_LAN.md + RUNBOOK.md + RELEASE.md mencionados.
    - test_runbook_md_has_exactly_5_scenarios: regex '^## Escenario [1-5]:' cuenta 5.
    - test_runbook_md_scenario_structure: 5 escenarios * 4 secciones (Sintomas/Diagnostico/
      Remedio/Prevencion) >= 20.
    - test_runbook_md_pipeline_scenario_has_restart_remedy: 'docker compose restart web'
      + 'pipeline_semaphore' presentes.
    - test_runbook_md_lockout_scenario_has_delete_remedy: 'DELETE FROM nexo.login_attempts'
      + '>= 2 propietarios' (regex flex).
    - test_runbook_md_caddy_scenario_has_rotation_info: 'caddy_data' + '7 d' o '10 a'
      o 'intermediate' presente.
    - test_release_md_has_semver: regex 'v[0-9]+\.[0-9]+\.[0-9]+'.
    - test_release_md_has_checklist_with_min_7_items: grep -cE '^- \[ \]' >= 7.
    - test_release_md_references_deploy_script: 'scripts/deploy.sh' + 'deploy_smoke.sh'.
    - test_changelog_md_has_unreleased_and_1_0_0: '[Unreleased]' + '[1.0.0]'.
    - test_changelog_md_covers_all_phases: grep 'Phase 1' hasta 'Phase 7' cada una presente.
    - test_all_docs_have_no_emojis: regex unicode emoji-heavy contra los 4 archivos.

    Patron del test: usar pathlib.Path, abrir, leer texto, hacer asserts con mensajes claros
    en el estilo de tests/infra/test_deploy_lan_doc.py (Phase 6).

    Step D: Ejecutar `pytest tests/infra/test_devex_docs.py -v` y confirmar todos green.

    Reglas:
    - Los 2 docs (RELEASE, CHANGELOG) sin emojis.
    - RELEASE.md placeholders IP_NEXO, org (para gh release URL) consistentes con patron
      DEPLOY_LAN.md.
    - CHANGELOG.md: Phase entries concisos (1-2 lineas cada uno); no duplicar con
      SUMMARY.md de cada plan.
    - En los comandos literales de RELEASE.md, conservar `git tag -a v1.0.0 -m "..."` +
      `git push origin v1.0.0` (no hacer force push, no amend — consistente con CLAUDE.md).
  </action>
  <verify>
    <automated>test -f docs/RELEASE.md && test -f CHANGELOG.md && test -f tests/infra/test_devex_docs.py && grep -qE 'v[0-9]+\.[0-9]+\.[0-9]+' docs/RELEASE.md && grep -q 'scripts/deploy.sh' docs/RELEASE.md && grep -q 'deploy_smoke.sh' docs/RELEASE.md && [ $(grep -cE '^- \[ \]' docs/RELEASE.md) -ge 7 ] && grep -q '## \[Unreleased\]' CHANGELOG.md && grep -q '## \[1.0.0\]' CHANGELOG.md && pytest tests/infra/test_devex_docs.py -v</automated>
  </verify>
  <acceptance_criteria>
    - [x] test -f docs/RELEASE.md && test -f CHANGELOG.md && test -f tests/infra/test_devex_docs.py
    - [x] RELEASE.md tiene semver: grep -qE 'v[0-9]+\.[0-9]+\.[0-9]+' docs/RELEASE.md
    - [x] RELEASE.md referencia deploy.sh: grep -q 'scripts/deploy.sh' docs/RELEASE.md
    - [x] RELEASE.md referencia smoke Phase 6: grep -q 'deploy_smoke.sh' docs/RELEASE.md
    - [x] RELEASE.md checklist >=7: grep -cE '^- \[ \]' docs/RELEASE.md >= 7
    - [x] CHANGELOG sigue Keep a Changelog: grep 'Keep a Changelog' && grep 'semver\|Semantic Versioning'
    - [x] CHANGELOG tiene Unreleased + 1.0.0: grep -q '## \[Unreleased\]' + grep -q '## \[1.0.0\]'
    - [x] CHANGELOG cubre Phase 1..7: grep -c 'Phase [1-7]' >= 7
    - [x] No emojis en RELEASE.md ni CHANGELOG.md (regex unicode)
    - [x] pytest tests/infra/test_devex_docs.py -v todos green (>= 12 tests)
  </acceptance_criteria>
  <done>RELEASE.md + CHANGELOG.md publicados, test regresion valida los 4 docs del quartet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs -> operador humano | Docs expuestos a anyone con acceso al repo (dev, operador, IA). Contienen comandos que tocan BD y servicios. |
| RUNBOOK.md -> produccion | Operador ejecuta procedimientos en LAN productiva. Comandos son literales, no review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | RUNBOOK.md contiene comandos con placeholders que podrian ejecutarse con credenciales reales si el operador los copia mal | mitigate | Todos los placeholders en angle brackets (IP_NEXO, email, POSTGRES_USER, latest). Patron heredado de DEPLOY_LAN.md Phase 6. Ningun credential real en el doc. Review manual al ejecutar. |
| T-07-12 | Elevation of Privilege | Escenario 5 documenta DELETE FROM nexo.login_attempts — si se usa sin control, permite desbloquear cualquier cuenta | accept | Marcado "EMERGENCY ONLY". Requiere acceso root al host Ubuntu + `docker compose exec db`. Audit log retiene la accion (schema nexo.login_attempts tiene timestamp; el DELETE no se audita intencionalmente porque es recovery manual). Prevencion documentada: >=2 propietarios. Backlog Mark-IV: endpoint admin con token. |
| T-07-13 | Tampering | Un operador podria editar RUNBOOK.md con comandos maliciosos (supply chain interno) | accept | Repo tiene control de acceso ECS Mobility. PRs revisables. No se mitiga a nivel doc. |
| T-07-14 | Denial of Service | Escenario 4 recomienda `docker compose restart web` — si se ejecuta en horario pico, tira a los usuarios activos | mitigate | Documentado explicitamente: "~30s (lifespan re-inicializa). Usuarios pierden sesion si no cookie persistente." Operador toma decision informada. Alternativa no-nuclear documentada tambien. |
| T-07-15 | Information Disclosure | CHANGELOG.md podria accidentalmente incluir CVE IDs privados o internal incident details | accept | Seccion Security en CHANGELOG menciona rotacion SA SQL Server (Sprint 0) — ya es publico via SECURITY_AUDIT.md. No se anaden CVE nuevos en Phase 7. Guideline: futuras entradas de Security en CHANGELOG deben referenciar SECURITY_AUDIT.md, no detallar in-line. |
</threat_model>

<verification>
## Phase-level checks

```bash
# Los 4 docs existen
test -f docs/ARCHITECTURE.md
test -f docs/RUNBOOK.md
test -f docs/RELEASE.md
test -f CHANGELOG.md

# ARCHITECTURE: Mermaid + 3 engines + middleware
grep -q 'mermaid' docs/ARCHITECTURE.md
grep -c 'engine_mes\|engine_app\|engine_nexo' docs/ARCHITECTURE.md
grep -q 'AuthMiddleware' docs/ARCHITECTURE.md
grep -q 'FlashMiddleware' docs/ARCHITECTURE.md

# RUNBOOK: exactamente 5 escenarios + hallazgos criticos
[ $(grep -cE '^## Escenario [1-5]:' docs/RUNBOOK.md) -eq 5 ]
grep -q 'pipeline_semaphore' docs/RUNBOOK.md
grep -q 'DELETE FROM nexo.login_attempts' docs/RUNBOOK.md
grep -q 'caddy_data' docs/RUNBOOK.md
grep -q 'pgdata' docs/RUNBOOK.md

# RELEASE: semver + checklist + deploy references
grep -qE 'v[0-9]+\.[0-9]+\.[0-9]+' docs/RELEASE.md
[ $(grep -cE '^- \[ \]' docs/RELEASE.md) -ge 7 ]
grep -q 'scripts/deploy.sh' docs/RELEASE.md
grep -q 'deploy_smoke.sh' docs/RELEASE.md

# CHANGELOG: Keep a Changelog + 1.0.0 + phases
grep -q 'Keep a Changelog' CHANGELOG.md
grep -q '## \[Unreleased\]' CHANGELOG.md
grep -q '## \[1.0.0\]' CHANGELOG.md
[ $(grep -c 'Phase [1-7]' CHANGELOG.md) -ge 7 ]

# No emojis en los 4 docs
python3 -c "
import re
for f in ['docs/ARCHITECTURE.md', 'docs/RUNBOOK.md', 'docs/RELEASE.md', 'CHANGELOG.md']:
    c = open(f, encoding='utf-8').read()
    assert not re.search(r'[\U0001F300-\U0001FAFF\U0001F600-\U0001F64F\u2600-\u27BF]', c), f'emoji in {f}'
print('no emojis')
"

# Tests regresion
pytest tests/infra/test_devex_docs.py -v

# No regresion suite existente
pytest tests/ -q --no-cov
```
</verification>

<success_criteria>
- [ ] docs/ARCHITECTURE.md con Mermaid + 3 engines + middleware stack + schedulers + cross-links (>=150 lineas)
- [ ] docs/RUNBOOK.md con EXACTAMENTE 5 escenarios, cada uno con 4 sub-secciones
- [ ] Escenario 4 (Pipeline atascado): documenta ausencia list_locks() + remedio docker compose restart web
- [ ] Escenario 5 (Lockout propietario): documenta DELETE FROM nexo.login_attempts + prevencion >=2 propietarios
- [ ] Escenario 3 (Caddy): documenta root CA 10a / intermediate 7d + caddy_data volumen
- [ ] Escenario 2 (Postgres): documenta pgdata volume + NO down -v (Landmine 6)
- [ ] docs/RELEASE.md con >=7 items de checklist + referencia a scripts/deploy.sh + tests/infra/deploy_smoke.sh + semver
- [ ] CHANGELOG.md Keep a Changelog 1.1.0 con [Unreleased] + [1.0.0] + cobertura Phase 1-7
- [ ] Ningun emoji en los 4 docs (hard invariant CLAUDE.md)
- [ ] tests/infra/test_devex_docs.py con >= 12 tests green
- [ ] Suite existente sin regresion
</success_criteria>

<output>
After completion, create `.planning/phases/07-devex-hardening/07-03-SUMMARY.md` with:
- Lineas finales por cada doc (ARCHITECTURE, RUNBOOK, RELEASE, CHANGELOG)
- Numero de escenarios del RUNBOOK (debe ser 5)
- Lista de palabras clave verificadas por test (pipeline_semaphore, DELETE FROM nexo.login_attempts, caddy_data, pgdata)
- Test count anadido en tests/infra/ (esperado: >= 12)
- Referencias cruzadas ARCHITECTURE.md -> otros docs (lista)
- Cualquier deviation (si algun hallazgo critico no se pudo confirmar por lectura del codigo, anotarlo)
</output>
