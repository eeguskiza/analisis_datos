---
phase: 07-devex-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - CLAUDE.md
  - tests/infra/test_claude_md.py
autonomous: true
requirements:
  - DEVEX-07
tags:
  - devex
  - claude-md
  - conventions
  - phase-wrap-up

must_haves:
  truths:
    - "CLAUDE.md Ultima revision bumpeada a 2026-MM-DD (cierre Mark-III / Sprint 6)"
    - "CLAUDE.md incluye seccion nueva 'Tooling DevEx (Phase 7)' con pre-commit, ruff, make lint/format, coverage gate 60%, y referencias a ARCHITECTURE.md + RUNBOOK.md + RELEASE.md"
    - "CLAUDE.md incluye seccion nueva 'Despliegue productivo (Phase 6)' con make prod-*, hostname .local, root CA interna, runbook docs/DEPLOY_LAN.md"
    - "CLAUDE.md seccion 'Que NO hacer' anade: 'No commitear con --no-verify (pre-commit Sprint 6)' + 'No bajar cobertura por debajo de 60% en PRs (CI bloquea)'"
    - "CLAUDE.md seccion 'Fuente de verdad del plan' lista ARCHITECTURE.md, RUNBOOK.md, RELEASE.md, CHANGELOG.md como nuevos artefactos docs"
    - "CLAUDE.md seccion 'Convenciones de naming' + 'Politica de commits' intactas (NO regresion de decisiones previas)"
    - "Test de regresion tests/infra/test_claude_md.py congela los 5 puntos anteriores via asserts grep-verificables"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Fuente de verdad para IAs/devs, actualizada tras cierre Mark-III"
      contains: "Tooling DevEx"
    - path: "tests/infra/test_claude_md.py"
      provides: "Regresion sobre CLAUDE.md"
      contains: "def test_"
  key_links:
    - from: "CLAUDE.md seccion Tooling DevEx"
      to: "docs/ARCHITECTURE.md + docs/RUNBOOK.md + docs/RELEASE.md + .pre-commit-config.yaml"
      via: "cross-references explicitas al quartet Phase 7"
      pattern: "ARCHITECTURE.md"
    - from: "CLAUDE.md seccion Despliegue productivo"
      to: "docs/DEPLOY_LAN.md + make prod-*"
      via: "apunta al runbook Phase 6 y targets Makefile"
      pattern: "make prod-"
    - from: "CLAUDE.md seccion Que NO hacer"
      to: ".pre-commit-config.yaml + pyproject.toml --cov-fail-under"
      via: "nuevas prohibiciones refieren a los gates Phase 7"
      pattern: "no-verify"
---

<objective>
Cerrar Phase 7 actualizando CLAUDE.md para reflejar todas las convenciones Mark-III
posteriores a Sprints 5-6: deploy productivo (Phase 6: make prod-*, Caddy tls internal,
DEPLOY_LAN.md) + DevEx (Phase 7: pre-commit, ruff, coverage gate, ARCHITECTURE.md,
RUNBOOK.md, RELEASE.md, CHANGELOG.md).

Esto cierra DEVEX-07 literal ("CLAUDE.md actualizado con convenciones Mark-III
completas tras Sprint 6") y es el cierre natural de la phase — ya que CLAUDE.md es
la fuente de verdad para cualquier IA/dev que arranque en este repo post-Mark-III.

Purpose: Ultimo merge de Phase 7. Tras este plan, cualquier dev/IA que clone el repo
ve (a) plan completo hasta Phase 7, (b) herramientas activas, (c) docs de referencia
(quartet), (d) prohibiciones adicionales (no --no-verify).
Output: CLAUDE.md actualizado + test de regresion que congela las convenciones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-devex-hardening/07-RESEARCH.md
@.planning/phases/07-devex-hardening/07-VALIDATION.md
@.planning/phases/07-devex-hardening/07-01-SUMMARY.md
@.planning/phases/07-devex-hardening/07-02-SUMMARY.md
@.planning/phases/07-devex-hardening/07-03-SUMMARY.md
@CLAUDE.md
@docs/ARCHITECTURE.md
@docs/RUNBOOK.md
@docs/RELEASE.md
@CHANGELOG.md
@.pre-commit-config.yaml
@pyproject.toml
@Makefile
@.github/workflows/ci.yml

<interfaces>
<!-- Estado CLAUDE.md actual (verificado 2026-04-21): -->

- Linea 7: "Ultima revision: 2026-04-18 (sesion de arranque de Sprint 0, antes de
  `/gsd-execute-phase 1`)."
- Seccion "Stack" linea 18-19 ya lista el stack actual.
- Seccion "Fuente de verdad del plan" enumera docs/ actuales (MARK_III_PLAN, OPEN_QUESTIONS,
  CURRENT_STATE_AUDIT, AUTH_MODEL, BRANDING, GLOSSARY, SECURITY_AUDIT, DATA_MIGRATION_NOTES).
  NO incluye ARCHITECTURE.md, RUNBOOK.md, RELEASE.md, CHANGELOG.md.
- Seccion "Decisiones cerradas de Mark-III" cubre Phase 1-5 essentials. Phase 6 y 7 quedan fuera.
- Seccion "Politica de commits" OK intacta.
- Seccion "Flujo de trabajo GSD" OK intacta.
- Seccion "Que NO hacer" tiene 11 items; anadir 2 nuevos relacionados con Phase 7.

<!-- Output del quartet Phase 7 que este plan referencia: -->

- docs/ARCHITECTURE.md (Phase 7 / Plan 03 Task 1)
- docs/RUNBOOK.md (Phase 7 / Plan 03 Task 2)
- docs/RELEASE.md (Phase 7 / Plan 03 Task 3)
- CHANGELOG.md (Phase 7 / Plan 03 Task 3)
- .pre-commit-config.yaml (Phase 7 / Plan 01)
- pyproject.toml (Phase 7 / Plan 01)
- Makefile targets test/lint/format/migrate (Phase 7 / Plan 02)

<!-- Invariante: este plan NO modifica ningun otro archivo. Solo CLAUDE.md y el test infra. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Actualizar CLAUDE.md con 3 secciones nuevas + anotaciones + test de regresion</name>
  <files>
    CLAUDE.md,
    tests/infra/test_claude_md.py
  </files>
  <read_first>
    - CLAUDE.md (estado actual — confirmar linea 7 "Ultima revision" y secciones)
    - .planning/phases/07-devex-hardening/07-RESEARCH.md §8 "CLAUDE.md update (DEVEX-07)" (lines 528-551)
    - .planning/phases/07-devex-hardening/07-01-SUMMARY.md (para saber valor final de --cov-fail-under)
    - .planning/phases/07-devex-hardening/07-02-SUMMARY.md (confirmar CI matriz activa)
    - .planning/phases/07-devex-hardening/07-03-SUMMARY.md (confirmar quartet docs publicado)
    - tests/infra/test_deploy_lan_doc.py (patron de tests de docs a replicar)
  </read_first>
  <behavior>
    - Test 1 (test_claude_md.py::test_file_exists): CLAUDE.md existe y es legible.
    - Test 2 (test_claude_md.py::test_ultima_revision_bumped_post_phase7): regex "Ultima revision: 2026-0[4-9]" o mas reciente; la version 2026-04-18 (Sprint 0) ya no aparece como "Ultima revision" actual.
    - Test 3 (test_claude_md.py::test_has_tooling_devex_section): existe encabezado "## Tooling DevEx" (o "## Tooling DevEx (Phase 7)").
    - Test 4 (test_claude_md.py::test_tooling_devex_references_pre_commit): la seccion Tooling DevEx menciona "pre-commit install".
    - Test 5 (test_claude_md.py::test_tooling_devex_references_make_lint_format): menciona "make lint" y "make format".
    - Test 6 (test_claude_md.py::test_tooling_devex_cross_links_quartet): menciona ARCHITECTURE.md, RUNBOOK.md, RELEASE.md.
    - Test 7 (test_claude_md.py::test_has_deploy_production_section): existe encabezado relacionado con "## Despliegue productivo" o similar que lista make prod-*.
    - Test 8 (test_claude_md.py::test_deploy_section_references_lan_doc): menciona docs/DEPLOY_LAN.md.
    - Test 9 (test_claude_md.py::test_que_no_hacer_has_no_verify_rule): seccion "Que NO hacer" contiene regla sobre --no-verify.
    - Test 10 (test_claude_md.py::test_que_no_hacer_has_coverage_rule): seccion "Que NO hacer" contiene regla sobre cobertura 60%.
    - Test 11 (test_claude_md.py::test_fuente_de_verdad_lists_new_docs): seccion Fuente de verdad menciona ARCHITECTURE.md + RUNBOOK.md + RELEASE.md + CHANGELOG.md.
    - Test 12 (test_claude_md.py::test_no_emojis): ningun emoji en CLAUDE.md (regex unicode).
    - Test 13 (test_claude_md.py::test_preserves_existing_invariants): conserva literalmente las decisiones criticas pre-existentes (make up NO arranca mcp, NO renombrar OEE/, NO rotar credenciales fuera de Sprint 0).
  </behavior>
  <action>
    Step A (RED): Crear tests/infra/test_claude_md.py implementando los 13 tests. Patron:
    abrir CLAUDE.md via pathlib, leer texto, regex asserts. Ejemplo de estructura:

    ```python
    import re
    from pathlib import Path

    ROOT = Path(__file__).parents[2]
    CLAUDE_MD = ROOT / "CLAUDE.md"

    def test_file_exists():
        assert CLAUDE_MD.exists()

    def test_ultima_revision_bumped_post_phase7():
        text = CLAUDE_MD.read_text(encoding="utf-8")
        # Busca "Ultima revision: 2026-0X-YY" donde X >= 5 (mes >= Mayo 2026)
        # o el literal de hoy (fecha de ejecucion). Acepta cualquier 2026-05.. o 2026-06..
        # pero rechaza si sigue en 2026-04-18 como ultima revision.
        m = re.search(r'[ÚUú]ltima revisi[óo]n:\s*(\d{4}-\d{2}-\d{2})', text)
        assert m, "No se encontro linea 'Ultima revision'"
        # La fecha debe ser >= 2026-04-21 (fecha de este plan)
        assert m.group(1) >= "2026-04-21", f"Ultima revision no actualizada: {m.group(1)}"

    # ... resto de tests
    ```

    Correr pytest tests/infra/test_claude_md.py -v -> algunos deben fallar (la seccion nueva
    no existe).

    Step B (GREEN): Editar CLAUDE.md aplicando 4 cambios (via Edit):

    1. **Linea 7 (header "Ultima revision")**: bumpear a fecha de hoy.
       FROM: `Ultima revision: 2026-04-18 (sesion de arranque de Sprint 0, antes de
       `/gsd-execute-phase 1`).`
       TO: `Ultima revision: 2026-MM-DD (cierre Mark-III / Sprint 6 / Phase 7).`

    2. **Seccion "Fuente de verdad del plan"** (alrededor de linea 27 — tras el bullet
       "DATA_MIGRATION_NOTES.md"): anadir 4 bullets nuevos referenciando el quartet Phase 7:
       ```markdown
       - `docs/ARCHITECTURE.md` — mapa tecnico del repo (3 engines, middleware stack,
         schedulers, layout). Phase 7 entregable.
       - `docs/RUNBOOK.md` — 5 escenarios de incidencia runtime (MES caido, Postgres
         no arranca, cert Caddy expira, pipeline atascado, lockout propietario). Phase 7
         entregable.
       - `docs/RELEASE.md` — checklist reproducible de release versionado (semver,
         deploy, smoke). Phase 7 entregable.
       - `CHANGELOG.md` — Keep a Changelog 1.1.0 con historial Mark-III (Phases 1-7).
       ```

    3. **Nueva seccion despues de "Flujo de trabajo GSD"** (antes de "Que NO hacer"):
       anadir dos secciones bloque:

       ```markdown
       ## Tooling DevEx (Phase 7)

       Pre-commit + ruff + mypy + coverage gate activos. Cada dev tras clonar debe:

       ```bash
       pip install -r requirements-dev.txt
       pre-commit install
       ```

       Targets Makefile para el dev loop:

       | Target | Proposito |
       |--------|-----------|
       | `make test` | pytest con cobertura gate (--cov-fail-under=<N>) |
       | `make lint` | ruff check + ruff format --check + mypy |
       | `make format` | ruff check --fix + ruff format (auto-fix seguro) |
       | `make migrate` | docker compose exec web python scripts/init_nexo_schema.py |
       | `make backup` | bash scripts/backup_nightly.sh |

       CI (`.github/workflows/ci.yml`) corre matriz Python 3.11+3.12 con coverage gate
       bloqueante. Smoke test `docker compose up + curl /api/health` en cada PR.

       Referencias:
       - `docs/ARCHITECTURE.md` — como esta hecho Nexo por dentro (3 engines, middleware,
         schedulers).
       - `docs/RUNBOOK.md` — que hacer cuando algo se rompe (5 escenarios).
       - `docs/RELEASE.md` — como cortar un release (semver + deploy + smoke).
       - `.pre-commit-config.yaml` — hooks scoped a api/+nexo/ (OEE/ intacto).
       - `pyproject.toml` — ruff + mypy + pytest + coverage config consolidada.

       ---

       ## Despliegue productivo (Phase 6)

       Deploy LAN HTTPS con Caddy `tls internal`. Hostname `nexo.ecsmobility.local`
       resuelto via hosts-file + root CA interna distribuida manualmente a clientes.

       Targets Makefile para prod (usan `docker-compose.yml` + `docker-compose.prod.yml`):

       | Target | Proposito |
       |--------|-----------|
       | `make prod-up` | Arranca stack prod (requiere `.env` con secretos reales) |
       | `make prod-down` | Para stack prod (SIN -v — pgdata persiste) |
       | `make prod-logs` | Logs stack prod en tiempo real |
       | `make prod-status` | Estado de containers prod |
       | `make prod-health` | Health check via /api/health |
       | `make deploy` | Ejecuta scripts/deploy.sh (git pull + build + up + smoke) |
       | `make backup` | Ejecuta scripts/backup_nightly.sh (pg_dump + rotacion 7d) |

       Runbook completo de instalacion + recovery: `docs/DEPLOY_LAN.md`.
       Smoke post-deploy: `bash tests/infra/deploy_smoke.sh` (11 checks).

       ```

    4. **Seccion "Que NO hacer"** (linea aprox 150-170): anadir 2 bullets al final de la lista:
       ```markdown
       - No commitear con `--no-verify` (pre-commit entro en Phase 7). Si el hook falla,
         `git diff`, `git add .`, commit de nuevo — el hook auto-fixea la mayoria de issues.
       - No bajar la cobertura por debajo del gate configurado en pyproject.toml
         (`--cov-fail-under=<N>`). CI bloquea PRs que regresionen cobertura. Si tu PR
         anade logica nueva, anade tests.
       ```

    Step C (GREEN verify): correr pytest tests/infra/test_claude_md.py -v y confirmar los
    13 tests green.

    Step D: Tambien correr la suite completa para descartar regresion cruzada en otros
    tests infra:
    ```bash
    pytest tests/infra/ -v --no-cov
    ```

    Reglas:
    - Usar Edit (no Write) para preservar el resto de CLAUDE.md exactamente como esta.
    - NO tocar las decisiones cerradas pre-existentes (Mark-III naming, pre-commit "entra en
      Sprint 6", politica de commits, flujo GSD, invariantes OEE/make up sin mcp, etc.).
      Solo ANADIR secciones nuevas.
    - NO emojis.
    - Espanol para prosa, ingles para comandos/nombres tecnicos.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && grep -qE '[ÚUú]ltima revisi[óo]n:\s*2026-0[4-9]' CLAUDE.md && grep -q 'Tooling DevEx' CLAUDE.md && grep -q 'Despliegue productivo' CLAUDE.md && grep -q 'pre-commit install' CLAUDE.md && grep -q 'ARCHITECTURE.md' CLAUDE.md && grep -q 'RUNBOOK.md' CLAUDE.md && grep -q 'RELEASE.md' CLAUDE.md && grep -q 'CHANGELOG.md' CLAUDE.md && grep -q 'no-verify' CLAUDE.md && grep -q 'make prod-' CLAUDE.md && pytest tests/infra/test_claude_md.py -v</automated>
  </verify>
  <acceptance_criteria>
    - [x] test -f CLAUDE.md && test -f tests/infra/test_claude_md.py
    - [x] Ultima revision bumpeada: grep -qE 'Ultima revisi[óo]n:\s*2026-0[4-9]' CLAUDE.md con fecha >= 2026-04-21
    - [x] Seccion Tooling DevEx: grep -q 'Tooling DevEx' CLAUDE.md
    - [x] Menciona pre-commit install: grep -q 'pre-commit install' CLAUDE.md
    - [x] Menciona make lint/format/test: grep -E 'make (lint|format|test)' CLAUDE.md con >=3 ocurrencias
    - [x] Referencias quartet: grep -q 'ARCHITECTURE.md' && grep -q 'RUNBOOK.md' && grep -q 'RELEASE.md' && grep -q 'CHANGELOG.md'
    - [x] Seccion Despliegue productivo: grep -q 'Despliegue productivo' CLAUDE.md
    - [x] Referencias make prod-*: grep -c 'make prod-' CLAUDE.md >= 3
    - [x] Referencia DEPLOY_LAN.md: grep -q 'DEPLOY_LAN.md' CLAUDE.md
    - [x] Prohibicion no-verify: grep -q 'no-verify' CLAUDE.md (en seccion Que NO hacer)
    - [x] Prohibicion cobertura: grep -qE 'cov-fail-under|60%' CLAUDE.md
    - [x] No emojis: python3 regex unicode check
    - [x] Conservacion invariantes: grep -q 'make up.*mcp\|mcp.*make up' CLAUDE.md (sigue documentando la regla) + grep -q 'carpeta `OEE/`' CLAUDE.md
    - [x] pytest tests/infra/test_claude_md.py -v: 13 tests green
    - [x] pytest tests/infra/ -v --no-cov: toda la suite infra green (no regresion cruzada)
  </acceptance_criteria>
  <done>CLAUDE.md actualizado con convenciones Mark-III completas, test regresion verde, no regresion en suite infra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md -> futuras IAs/devs | El doc sera leido automaticamente en cada sesion Claude Code. Sus instrucciones son autoritativas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Tampering | Un PR podria silenciosamente modificar CLAUDE.md para relajar reglas (permitir --no-verify, etc.) | mitigate | Tests de regresion tests/infra/test_claude_md.py congelan los 5 invariantes criticos (Ultima revision fresh, secciones presentes, prohibiciones presentes). Cualquier PR que relaje las reglas rompe el test y es visible en CI. |
| T-07-17 | Information Disclosure | CLAUDE.md podria accidentalmente incluir decisiones internas confidenciales (credenciales rotadas, timelines internos) | accept | CLAUDE.md es parte del repo, visible a quien tenga acceso. No contiene credenciales ni timelines sensibles — solo convenciones + decisiones de arquitectura. Patron heredado desde Phase 1. |
| T-07-18 | Repudiation | Cambios a CLAUDE.md sin commit message descriptivo dificultan auditar por que se relajo una regla | mitigate | Conventional Commits + Phase 7 policy "un commit = un cambio coherente". Los tests de regresion forzaran al autor del cambio a actualizar EL test si cambia la regla — trail explicito. |
</threat_model>

<verification>
## Phase-level checks

```bash
# Archivo existe y actualizado
test -f CLAUDE.md
test -f tests/infra/test_claude_md.py

# Ultima revision bumpeada
grep -qE '[ÚUú]ltima revisi[óo]n:\s*2026-0[4-9]' CLAUDE.md

# Las 3 secciones nuevas existen
grep -q 'Tooling DevEx' CLAUDE.md
grep -q 'Despliegue productivo' CLAUDE.md

# Referencias al quartet
grep -q 'ARCHITECTURE.md' CLAUDE.md
grep -q 'RUNBOOK.md' CLAUDE.md
grep -q 'RELEASE.md' CLAUDE.md
grep -q 'CHANGELOG.md' CLAUDE.md

# Referencias DevEx tooling
grep -q 'pre-commit install' CLAUDE.md
grep -qE 'make (lint|format|test|migrate)' CLAUDE.md
grep -qE 'cov-fail-under|60%' CLAUDE.md

# Referencias prod
grep -q 'make prod-' CLAUDE.md
grep -q 'DEPLOY_LAN.md' CLAUDE.md

# Prohibiciones nuevas
grep -q 'no-verify' CLAUDE.md

# No regresion: invariantes preservados
grep -qE 'make up.*mcp|mcp.*make up|profiles.*mcp' CLAUDE.md
grep -q 'carpeta `OEE/`' CLAUDE.md
grep -q 'filter-repo' CLAUDE.md

# No emojis
python3 -c "
import re
c = open('CLAUDE.md', encoding='utf-8').read()
assert not re.search(r'[\U0001F300-\U0001FAFF\U0001F600-\U0001F64F\u2600-\u27BF]', c), 'emoji in CLAUDE.md'
print('ok')
"

# Tests regresion
pytest tests/infra/test_claude_md.py -v
pytest tests/infra/ -v --no-cov
```
</verification>

<success_criteria>
- [ ] CLAUDE.md "Ultima revision" bumpeada a fecha >= 2026-04-21 con label "cierre Mark-III / Sprint 6 / Phase 7"
- [ ] CLAUDE.md tiene seccion nueva "Tooling DevEx" con tabla de Makefile targets + referencias al quartet
- [ ] CLAUDE.md tiene seccion nueva "Despliegue productivo" con tabla de make prod-* + referencia a DEPLOY_LAN.md
- [ ] CLAUDE.md seccion "Fuente de verdad del plan" lista ARCHITECTURE.md + RUNBOOK.md + RELEASE.md + CHANGELOG.md
- [ ] CLAUDE.md seccion "Que NO hacer" tiene 2 prohibiciones nuevas: --no-verify + cobertura <60%
- [ ] Invariantes pre-existentes preservados (make up sin mcp, OEE/ sin rename, filter-repo prohibido)
- [ ] Ningun emoji en CLAUDE.md
- [ ] tests/infra/test_claude_md.py con 13 tests green
- [ ] Toda la suite tests/infra/ sin regresion (incluye tests Phase 6 + tests Plan 07-01/02/03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-devex-hardening/07-04-SUMMARY.md` with:
- Lineas finales de CLAUDE.md (esperado delta +80 a +120 lineas)
- Fecha bumpeada de "Ultima revision"
- Lista de las 2 secciones nuevas anadidas + los 2 bullets prohibitivos nuevos
- Test count anadido en tests/infra/test_claude_md.py (esperado: 13)
- Total cumulativo tests/infra/ tras Phase 7 completo (esperado: ~73 Phase 6 + 11 Plan 07-01 + 19 Plan 07-02 + 12 Plan 07-03 + 13 Plan 07-04 ~= 128)
- Confirmacion explicita de que las decisiones Mark-III pre-existentes NO fueron modificadas (lista)
- Estado de gates Phase 7 cerrables:
  - [ ] DEVEX-01 (Plan 07-01)
  - [ ] DEVEX-02 (Plan 07-02)
  - [ ] DEVEX-03 (Plan 07-02)
  - [ ] DEVEX-04 (Plan 07-03)
  - [ ] DEVEX-05 (Plan 07-03)
  - [ ] DEVEX-06 (Plan 07-03)
  - [ ] DEVEX-07 (Plan 07-04, este)
</output>
