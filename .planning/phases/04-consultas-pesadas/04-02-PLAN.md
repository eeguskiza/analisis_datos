---
phase: 04-consultas-pesadas
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
autonomous: false
requirements: [QUERY-03, QUERY-04, QUERY-05, QUERY-07, QUERY-08]
tags: [nexo, phase-4, preflight, middleware, asyncio, semaphore, pipeline, modal-amber, modal-red]
files_modified:
  - nexo/services/preflight.py
  - nexo/services/pipeline_lock.py
  - nexo/middleware/__init__.py
  - nexo/middleware/query_timing.py
  - api/main.py
  - api/routers/pipeline.py
  - api/routers/bbdd.py
  - api/routers/capacidad.py
  - api/routers/operarios.py
  - templates/pipeline.html
  - templates/bbdd.html
  - static/js/app.js
  - .env.example
  - tests/services/test_preflight.py
  - tests/services/test_pipeline_lock.py
  - tests/middleware/test_query_timing.py
  - tests/routers/test_preflight_endpoints.py

must_haves:
  truths:
    # ROADMAP-verbatim success-criteria truths (PC-04-08):
    - "Pipeline no congela la UI (asyncio.to_thread + semáforo)"
    - "Usuario ve toast amber al disparar pipeline estimado >warn_ms"
    - "nexo.query_log contiene estimated_ms y actual_ms para cada ejecución"
    # Implementation-level truths:
    - "POST /api/pipeline/preflight devuelve Estimation JSON con level/estimated_ms/breakdown"
    - "POST /api/pipeline/run con level amber SIN force devuelve HTTP 428 con payload estimation"
    - "POST /api/pipeline/run con level red SIN approval_id devuelve HTTP 403"
    - "POST /api/pipeline/run green ejecuta directo dentro de async with semaphore + asyncio.to_thread"
    - "QueryTimingMiddleware escribe fila en nexo.query_log con endpoint/user_id/estimated_ms/actual_ms/status/approval_id tras cada request a los 4 endpoints timed"
    - "actual_ms > warn_ms * 1.5 graba fila con status=slow Y emite log.warning"
    - "Paths NO listados en _TIMED_PATHS no escriben en query_log"
    - "Pipeline corre en asyncio.to_thread bajo asyncio.Semaphore(3)"
    - "Pipeline que excede 900s devuelve HTTP 504 y query_log status=timeout"
    - "/capacidad y /operarios con rango > 90 dias invocan preflight; con rango <= 90 dias saltan preflight completamente"
    - "Modal amber aparece con Continuar/Cancelar cuando endpoint devuelve 428; Continuar re-dispara con force=true"
    - "Modal red aparece con Solicitar aprobacion/Cancelar cuando endpoint devuelve 403 con level=red; Solicitar POSTea a /api/approvals (stub hasta 04-03)"
  artifacts:
    - path: "nexo/services/preflight.py"
      provides: "estimate_cost(endpoint, params) pure function"
      exports: ["estimate_cost"]
    - path: "nexo/services/pipeline_lock.py"
      provides: "pipeline_semaphore(3) + PIPELINE_TIMEOUT_SEC + run_with_lock helper"
      exports: ["pipeline_semaphore", "PIPELINE_TIMEOUT_SEC", "MAX_CONCURRENT", "run_with_lock"]
    - path: "nexo/middleware/query_timing.py"
      provides: "QueryTimingMiddleware BaseHTTPMiddleware"
      contains: "class QueryTimingMiddleware"
    - path: "api/routers/pipeline.py"
      provides: "POST /preflight + POST /run async con semaforo/to_thread/force/approval_id"
      contains: "async def run"
    - path: "api/routers/bbdd.py"
      provides: "POST /query con preflight ANTES de _validate_sql"
      contains: "estimate_cost"
    - path: "static/js/app.js"
      provides: "Alpine component preflightModal con openAmber/openRed/humanize_ms"
      contains: "preflightModal"
  key_links:
    - from: "nexo/services/preflight.py"
      to: "nexo/services/thresholds_cache.py (get)"
      via: "thresholds_cache.get(endpoint)"
      pattern: "thresholds_cache.get"
    - from: "nexo/middleware/query_timing.py"
      to: "nexo.query_log via SessionLocalNexo + NexoQueryLog"
      via: "best-effort INSERT"
      pattern: "NexoQueryLog"
    - from: "api/routers/pipeline.py"
      to: "nexo/services/pipeline_lock.py"
      via: "async with pipeline_semaphore + asyncio.wait_for(asyncio.to_thread)"
      pattern: "asyncio.to_thread"
    - from: "api/main.py"
      to: "nexo/middleware/query_timing.py"
      via: "app.add_middleware(QueryTimingMiddleware) registrado PRIMERO (inner-most LIFO)"
      pattern: "add_middleware.QueryTimingMiddleware"
---

<objective>
Implementar preflight + postflight + asyncio.to_thread + modal frontend. Este plan cumple el 80% del trabajo funcional de Phase 4.

Qué introduce:
1. nexo/services/preflight.py: función pura estimate_cost que calcula Estimation(level, estimated_ms, breakdown, ...) consumiendo thresholds_cache (Plan 04-01).
2. nexo/services/pipeline_lock.py: asyncio.Semaphore(3) + constants para timeout 15min.
3. nexo/middleware/query_timing.py: middleware que mide actual_ms y graba fila en nexo.query_log para los 4 endpoints TIMED.
4. Refactor api/routers/pipeline.py: nuevo POST /preflight + POST /run acepta force/approval_id + async con semaforo + to_thread + wait_for.
5. Refactor api/routers/bbdd.py /query: preflight inyectado ANTES de _validate_sql.
6. Refactor api/routers/capacidad.py y operarios.py: preflight condicional a rango_dias > 90.
7. Modal AMBER (D-05, D-07) + Modal RED (D-06) en frontend via Alpine.js en static/js/app.js + templates pipeline/bbdd.
8. Wiring en api/main.py: add QueryTimingMiddleware como INNER-MOST (primero en registrar = más cercano al handler por LIFO Starlette).

Decisiones implementadas: D-01/D-02/D-03 (umbrales ya en DB via 04-01; preflight los consume), D-04 (factor 2000ms; sin recalc manual aún — va en 04-04), D-05 (modal amber bloqueante Continuar/Cancelar), D-06 (modal red Solicitar aprobacion/Cancelar), D-07 (texto jerárquico 3 líneas), D-08 (per-request, sin sticky), D-09 (params_json sanitizado), D-17 (postflight slow status + log.warning), D-18 (semáforo + timeout soft + env vars).

Landmine crítico respetado: asyncio.wait_for(asyncio.to_thread(...)) NO cancela el thread. Timeout es soft (documentado en código + .env.example).

Plan 04-03 corre en paralelo (aprobaciones). Plan 04-04 corre después (UI observabilidad + LISTEN/NOTIFY + edición umbrales). El import de consume_approval en pipeline.py queda con guard condicional hasta que 04-03 entregue el módulo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-consultas-pesadas/04-CONTEXT.md
@.planning/phases/04-consultas-pesadas/04-RESEARCH.md
@.planning/phases/04-consultas-pesadas/04-PATTERNS.md
@.planning/phases/04-consultas-pesadas/04-VALIDATION.md
@.planning/phases/04-consultas-pesadas/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From Plan 04-01 (already merged):

```python
# nexo/data/dto/query.py
class Estimation(BaseModel):
    endpoint: str
    estimated_ms: int
    level: Literal["green", "amber", "red"]
    reason: str
    breakdown: str
    factor_used_ms: Optional[float]
    warn_ms: int
    block_ms: int
    # frozen=True via ConfigDict

# nexo/services/thresholds_cache.py
@dataclass(frozen=True)
class ThresholdEntry:
    endpoint: str
    warn_ms: int
    block_ms: int
    factor_ms: Optional[float]
    loaded_at: datetime

def get(endpoint: str) -> Optional[ThresholdEntry]: ...
def full_reload() -> None: ...

# nexo/data/models_nexo.py
class NexoQueryLog(NexoBase): ...
# columns: id, ts, user_id, endpoint, params_json, estimated_ms, actual_ms,
# rows, status, approval_id, ip

# nexo/data/engines.py (from Phase 3)
SessionLocalNexo: sessionmaker  # context-manager compatible
engine_nexo: Engine
```

From api/main.py current middleware order (lines 138-139):

```python
app.add_middleware(AuditMiddleware)    # inner — 2do en ejecutar
app.add_middleware(AuthMiddleware)     # outer — 1ro en ejecutar
# LIFO: el PRIMERO registrado corre ÚLTIMO (innermost, más cercano al handler).
# Tras 04-02:
# app.add_middleware(QueryTimingMiddleware)  # innermost — última capa antes del handler
# app.add_middleware(AuditMiddleware)
# app.add_middleware(AuthMiddleware)          # outermost — primera capa
# Cadena efectiva: request -> Auth -> Audit -> QueryTiming -> handler -> QueryTiming -> Audit -> Auth -> response
```

From api/middleware/audit.py (analog para query_timing):

```python
class AuditMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        user = getattr(request.state, "user", None)
        if user is None:
            return await call_next(request)
        response = await call_next(request)
        try:
            db = SessionLocalNexo()
            try:
                db.add(NexoAuditLog(...))
                db.commit()
            finally:
                db.close()
        except Exception:
            logger.exception("Error escribiendo nexo.audit_log ...")
        return response
```

From api/routers/pipeline.py (forma actual, a refactorizar):

```python
router = APIRouter(prefix="/pipeline", tags=["pipeline"],
    dependencies=[Depends(require_permission("pipeline:read"))])

@router.post("/run", dependencies=[Depends(require_permission("pipeline:run"))])
def run(req: PipelineRequest):
    def event_stream():
        for msg in run_pipeline(...):
            yield f"data: {msg}\n\n"
    return StreamingResponse(event_stream(), media_type="text/event-stream")
```

From api/routers/bbdd.py (validator existente a extender):

```python
def _validate_sql(sql: str) -> None:
    if not sql or not sql.strip():
        raise HTTPException(400, "Falta el campo 'sql'")
    sql_stripped = sql.lstrip(" \t\r\n;(")
    first_word = sql_stripped.split(None, 1)[0].upper() if sql_stripped else ""
    if first_word not in {"SELECT", "WITH"}:
        raise HTTPException(400, "Solo se permiten SELECT/WITH")

@router.post("/query")
async def query(req: QueryRequest, ...): ...
```

From api/routers/capacidad.py (a extender):

```python
@router.get("")
def capacidad(
    engine_mes: EngineMes,
    fecha_inicio: date = Query(...),
    fecha_fin: date = Query(...),
):
    if fecha_fin < fecha_inicio:
        raise HTTPException(400, "fecha_fin < fecha_inicio")
```

From nexo/services/auth.py (require_permission factory):

```python
def require_permission(permission: str):
    async def _check(request: Request) -> NexoUser: ...
    return _check
```

Stub Plan 04-03 interface (este plan usa import-guard):

```python
# Plan 04-03 entrega nexo/services/approvals.py con:
# def consume_approval(db, *, approval_id, user_id, current_params) -> NexoQueryApproval
# Plan 04-02 wrapea import en try/except ImportError para que el código
# compile standalone y active la verificación cuando 04-03 aterrice.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nexo/services/preflight.py + nexo/services/pipeline_lock.py</name>
  <read_first>
    - nexo/services/auth.py (módulo-level pure functions + singleton pattern lines 47-170)
    - nexo/services/thresholds_cache.py (de 04-01 — usa get(endpoint) + ThresholdEntry)
    - nexo/data/dto/query.py (Estimation de 04-01)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 2 (Preflight Service lines 509-656)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 3 (Semaphore + to_thread + timeout lines 660-760)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-04, D-18
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 341-475
  </read_first>
  <behavior>
    nexo/services/preflight.py:
    - `from __future__ import annotations`. Imports: date, Literal, thresholds_cache (módulo), Estimation (de nexo.data.dto.query).
    - `def estimate_cost(endpoint: str, params: dict) -> Estimation`: dispatches a `_estimate_pipeline`, `_estimate_bbdd`, `_estimate_rango`. Si thresholds_cache.get(endpoint) es None -> devuelve defensive Estimation(level='green', estimated_ms=0, reason='threshold no configurado — default green', breakdown='', factor_used_ms=None, warn_ms=0, block_ms=0). Si endpoint no en pipeline/run|bbdd/query|capacidad|operarios -> mismo defensive green.
    - `_estimate_pipeline(params, t)`: n_recursos = int(params.get('n_recursos', 0) or len(params.get('recursos', []) or [])); n_dias = int(params.get('n_dias', 0) or _calc_days(params)); factor = t.factor_ms or 2000.0; estimated = int(n_recursos * n_dias * factor); breakdown = f'{n_recursos} recursos × {n_dias} días × ~{factor/1000:.1f}s/run'.
    - `_estimate_bbdd(params, t)`: factor = t.factor_ms or 1000.0; estimated = int(factor); breakdown = f'baseline ~{factor/1000:.1f}s (sin EXPLAIN)'.
    - `_estimate_rango(params, t, endpoint)`: n_dias = int(params.get('rango_dias', 0) or _calc_days(params)); si n_dias <= 90 -> defensive green reason='rango <=90d — preflight desactivado'; else factor = t.factor_ms or 50.0; estimated = int(n_dias * factor); breakdown = f'{n_dias} días × ~{factor}ms/día'.
    - `_classify(estimated_ms, t)`: green si estimated < t.warn_ms; amber si < t.block_ms; red en caso contrario (strict less-than; 600000 exacto -> red).
    - `_reason_for(level, t)`: green='Ejecución estándar'; amber=f'Supera umbral de aviso ({t.warn_ms}ms)'; red=f'Excede límite configurado de {t.block_ms/1000/60:.0f} min'.
    - `_calc_days(params)`: extract fecha_desde/fecha_inicio + fecha_hasta/fecha_fin; convertir str -> date con fromisoformat; devolver (ff - fi).days + 1; si falta devolver 0.
    - Todos los Estimation construidos incluyen warn_ms=t.warn_ms, block_ms=t.block_ms, factor_used_ms=factor, endpoint=endpoint.

    nexo/services/pipeline_lock.py:
    - import asyncio, os.
    - MAX_CONCURRENT = int(os.environ.get('NEXO_PIPELINE_MAX_CONCURRENT', '3')).
    - PIPELINE_TIMEOUT_SEC = float(os.environ.get('NEXO_PIPELINE_TIMEOUT_SEC', '900')).
    - pipeline_semaphore = asyncio.Semaphore(MAX_CONCURRENT) (módulo-level, 1 por proceso).
    - `async def run_with_lock(fn, /, *args, **kwargs)`: async with pipeline_semaphore: return await asyncio.wait_for(asyncio.to_thread(fn, *args, **kwargs), timeout=PIPELINE_TIMEOUT_SEC).
    - Docstring módulo (bilingüe para que grep de acceptance lo encuentre tanto en ES como en EN): debe incluir estas 2 líneas explícitas como comentarios dentro del docstring:
      ```
      # NOTE: asyncio.wait_for does NOT kill the underlying thread.
      # Timeout is UX-soft — matplotlib keeps running until it completes.
      # (ES) NO mata el thread subyacente. Ver 04-RESEARCH.md Pitfall 1.
      ```
    - Export via __all__.
  </behavior>
  <action>
    Escribir nexo/services/preflight.py y nexo/services/pipeline_lock.py según behavior. Probar: python -c "from nexo.services.preflight import estimate_cost; print(estimate_cost('pipeline/run', {'n_recursos': 2, 'n_dias': 5}))" debe devolver Estimation.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.services.preflight import estimate_cost; from nexo.services.pipeline_lock import pipeline_semaphore, PIPELINE_TIMEOUT_SEC; assert PIPELINE_TIMEOUT_SEC == 900.0; assert pipeline_semaphore._value == 3; e = estimate_cost('pipeline/run', {'n_recursos': 2, 'n_dias': 5}); assert e.estimated_ms == 20000; print('OK', e.level)"</automated>
  </verify>
  <acceptance_criteria>
    - from nexo.services.preflight import estimate_cost OK.
    - from nexo.services.pipeline_lock import pipeline_semaphore, PIPELINE_TIMEOUT_SEC, run_with_lock OK.
    - estimate_cost('pipeline/run', {'n_recursos': 2, 'n_dias': 5}) -> Estimation estimated_ms=20000 level=green.
    - estimate_cost('pipeline/run', {'n_recursos': 10, 'n_dias': 30}) -> estimated_ms=600000 level=red (600000 >= block_ms).
    - estimate_cost('capacidad', {'rango_dias': 30}) -> level=green, reason menciona 'rango <=90d'.
    - estimate_cost('unknown_endpoint', {}) -> level=green defensive.
    - pipeline_semaphore._value == 3 con NEXO_PIPELINE_MAX_CONCURRENT sin setear.
    - grep "wait_for does NOT kill" nexo/services/pipeline_lock.py -> true.
  </acceptance_criteria>
  <done>
    Ambos services importables, puros, testeables sin DB. estimate_cost clasifica correctamente contra los seeds D-01/D-02/D-03. pipeline_semaphore es módulo-level.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create nexo/middleware/query_timing.py + wire in api/main.py</name>
  <read_first>
    - api/middleware/audit.py (analog exacto — BaseHTTPMiddleware, dispatch, SessionLocalNexo)
    - api/main.py lines 125-170 (middleware registration + router includes)
    - nexo/data/models_nexo.py (NexoQueryLog de 04-01)
    - nexo/services/thresholds_cache.py (para lookup warn_ms)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 1 lines 352-508
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 513-594
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-17 (slow status + warning)
  </read_first>
  <behavior>
    Crear nexo/middleware/__init__.py (vacío).

    Crear nexo/middleware/query_timing.py implementando class QueryTimingMiddleware(BaseHTTPMiddleware):
    - Constantes módulo:
      - _TIMED_PATHS: dict[str, str] = {'/api/pipeline/run': 'pipeline/run', '/api/bbdd/query': 'bbdd/query', '/api/capacidad': 'capacidad', '/api/operarios': 'operarios'}
      - _EXCLUDED = frozenset({'/api/health', '/api/approvals/count'})
    - dispatch(self, request, call_next):
      1. endpoint_key = _TIMED_PATHS.get(request.url.path). Si None o path en _EXCLUDED -> return await call_next(request).
      2. user = getattr(request.state, 'user', None). Si None -> return await call_next(request).
      3. Leer estimated_ms/approval_id/params_json de request.state.
      4. SHORT-CIRCUIT extra: si estimated_ms is None AND endpoint_key in ('capacidad', 'operarios') -> return await call_next(request) (rango <=90d sin preflight -> skip log).
      5. t0 = time.monotonic().
      6. try: response = await call_next(request); actual_ms = int((time.monotonic() - t0) * 1000); query_status = _classify_status(actual_ms, endpoint_key, response.status_code); _persist(...); si slow -> log.warning con ratio. Return response.
      7. except Exception: actual_ms = int((time.monotonic() - t0) * 1000); _persist(..., status='error'); raise.
    - _classify_status(actual_ms, endpoint_key, http_status): 504->timeout; >=400->error; else t = thresholds_cache.get(endpoint_key); t None -> ok; actual_ms > t.warn_ms * 1.5 -> slow; else ok.
    - _persist(user_id, endpoint, params_json, estimated_ms, actual_ms, rows, status, approval_id, ip): abre SessionLocalNexo(); db.add(NexoQueryLog(ts=datetime.now(timezone.utc), ...)); db.commit(); finally close. Wrap try/except; logger.exception on failure; NUNCA re-raise (copia discipline de AuditMiddleware).
    - logger = logging.getLogger('nexo.query_timing').

    Wiring en api/main.py:
    - Add import: from nexo.middleware.query_timing import QueryTimingMiddleware.
    - ANTES de app.add_middleware(AuditMiddleware) insertar app.add_middleware(QueryTimingMiddleware).
    - Actualizar comentario arriba (~lines 125-140): cadena es request -> Auth -> Audit -> QueryTiming -> handler.
  </behavior>
  <action>
    Crear nexo/middleware/__init__.py vacío. Escribir nexo/middleware/query_timing.py full implementation (no stub). Editar api/main.py añadiendo el import y la línea app.add_middleware(QueryTimingMiddleware) ANTES del add_middleware(AuditMiddleware) existente. Actualizar comentario docstring del orden middleware.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.middleware.query_timing import QueryTimingMiddleware; from nexo.middleware.query_timing import _TIMED_PATHS, _EXCLUDED; assert '/api/pipeline/run' in _TIMED_PATHS; assert '/api/health' in _EXCLUDED; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import de QueryTimingMiddleware OK.
    - _TIMED_PATHS contiene las 4 rutas y _EXCLUDED contiene /api/health + /api/approvals/count.
    - grep "add_middleware(QueryTimingMiddleware)" api/main.py -> coincide.
    - grep "add_middleware(QueryTimingMiddleware)" api/main.py aparece ANTES que "add_middleware(AuditMiddleware)" (verificable con awk sobre número de línea).
    - docker compose up web arranca sin error (smoke: curl /api/health -> 200 y la middleware chain no rompe).
    - Middleware NO re-raises cuando la escritura a DB falla (test integration en Task 6).
  </acceptance_criteria>
  <done>
    Middleware registrado en main.py como innermost. Al llegar request a los 4 paths timed, escribe fila en query_log con actual_ms correcto y clasifica slow/ok/error/timeout según status code + thresholds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor api/routers/pipeline.py + extend api/models.py (preflight endpoint + run async + force/approval_id)</name>
  <read_first>
    - api/routers/pipeline.py (forma actual completa)
    - api/services/pipeline.py (run_pipeline generator — NO modificar, solo wrap)
    - api/models.py (PipelineRequest — ya extendido por 04-01 con force/approval_id)
    - nexo/services/preflight.py (nuevo en esta plan task 1)
    - nexo/services/pipeline_lock.py (nuevo en esta plan task 1)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Code Examples lines 1316-1406 (preflight endpoint + run con force)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pipeline Wrapping lines 680-745 (Opción A recomendada)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-15 (semántica force + approval_id single-use)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 597-645
  </read_first>
  <behavior>
    api/routers/pipeline.py:
    - Nuevos imports: import asyncio, json; from fastapi import Request, HTTPException; from nexo.services.preflight import estimate_cost; from nexo.services.pipeline_lock import pipeline_semaphore, PIPELINE_TIMEOUT_SEC.
    - Import-guard para 04-03 (activado al aterrizar):
      ```python
      try:
          from nexo.services.approvals import consume_approval
          _APPROVALS_AVAILABLE = True
      except ImportError:
          _APPROVALS_AVAILABLE = False
      ```
    - Añadir endpoint POST /preflight:
      ```python
      @router.post("/preflight")
      def preflight(req: PipelineRequest, user=Depends(require_permission("pipeline:run"))) -> Estimation:
          params = _build_pipeline_params(req)
          return estimate_cost("pipeline/run", params)
      ```
    - Helper `_build_pipeline_params(req) -> dict`:
      ```python
      return {
          "fecha_desde": req.fecha_inicio.isoformat(),
          "fecha_hasta": req.fecha_fin.isoformat(),
          "recursos": req.recursos or [],
          "n_recursos": len(req.recursos or []),
          "n_dias": (req.fecha_fin - req.fecha_inicio).days + 1,
      }
      ```
    - Cambiar run() a async def run(req: PipelineRequest, request: Request, db: DbNexo, user=Depends(require_permission('pipeline:run'))):
      1. params = _build_pipeline_params(req).
      2. params_json_str = json.dumps(params, sort_keys=True, ensure_ascii=False).
      3. est = estimate_cost('pipeline/run', params).
      4. request.state.estimated_ms = est.estimated_ms; request.state.params_json = params_json_str.
      5. Gate logic:
         - Si est.level == 'red':
           - Si no (req.force and req.approval_id): raise HTTPException(403, detail={'estimation': est.model_dump(), 'action': 'request_approval'}).
           - Si _APPROVALS_AVAILABLE: approval = consume_approval(db, approval_id=req.approval_id, user_id=user.id, current_params=params); request.state.approval_id = approval.id.
           - Si NOT _APPROVALS_AVAILABLE: raise HTTPException(503, 'Flujo de aprobación no disponible aún — esperar Plan 04-03').
         - Si est.level == 'amber':
           - Si no req.force: raise HTTPException(428, detail={'estimation': est.model_dump(), 'action': 'confirm_amber'}).
           - (Si force=true para amber: se permite sin approval_id — amber no requiere aprobación, solo confirmación).
      6. Ejecutar con semáforo:
         ```python
         def _worker() -> list[str]:
             return list(run_pipeline(
                 fecha_inicio=req.fecha_inicio, fecha_fin=req.fecha_fin,
                 modulos=req.modulos, source=req.source, recursos=req.recursos,
             ))
         async with pipeline_semaphore:
             try:
                 messages = await asyncio.wait_for(
                     asyncio.to_thread(_worker), timeout=PIPELINE_TIMEOUT_SEC,
                 )
             except asyncio.TimeoutError:
                 raise HTTPException(504, f"Pipeline timeout ({PIPELINE_TIMEOUT_SEC}s)")
         ```
      7. Opción A del research: collect todos los messages + replay via SSE:
         ```python
         def event_stream():
             for msg in messages:
                 yield f"data: {msg}\n\n"
         return StreamingResponse(event_stream(), media_type="text/event-stream")
         ```
      8. Nota docstring: SSE en tiempo real se pierde (Opción A research); si UX demanda progress real, planner futuro cambia a Opción B. Aceptado por A1 del research.
  </behavior>
  <action>
    Editar api/routers/pipeline.py según behavior: añadir imports, helper _build_pipeline_params, endpoint POST /preflight, y refactorizar run() a async con gate logic + semáforo + to_thread + wait_for + collect-then-replay. Registrar POST /preflight con el mismo require_permission('pipeline:run') que /run (porque estimar es una acción autorizada).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "import inspect; from api.routers import pipeline; src = inspect.getsource(pipeline); assert 'estimate_cost' in src; assert 'pipeline_semaphore' in src; assert 'asyncio.to_thread' in src; assert 'POST' in src or 'router.post' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "router.post" api/routers/pipeline.py muestra al menos 2 rutas (preflight + run).
    - grep "async def run" api/routers/pipeline.py coincide.
    - grep "pipeline_semaphore" api/routers/pipeline.py coincide.
    - grep "asyncio.to_thread" api/routers/pipeline.py coincide.
    - grep "consume_approval" api/routers/pipeline.py coincide (import-guard activado).
    - grep "_APPROVALS_AVAILABLE" api/routers/pipeline.py coincide.
    - pytest tests/routers/test_preflight_endpoints.py::test_preflight_returns_estimation_json -x (tras Task 6) -> green.
    - pytest tests/routers/test_preflight_endpoints.py::test_run_amber_no_force_returns_428 -x -> green.
    - Smoke: docker compose up web, curl -X POST http://localhost/api/pipeline/preflight -d '{...}' devuelve Estimation JSON 200.
  </acceptance_criteria>
  <done>
    pipeline.py tiene endpoint /preflight funcional y /run refactorizado (async + semáforo + to_thread + gate force/approval). El import-guard permite Plan 04-02 mergear standalone sin romper compile; cuando 04-03 aterriza, _APPROVALS_AVAILABLE=True y consume_approval funciona.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Refactor bbdd.py + capacidad.py + operarios.py (preflight injection)</name>
  <read_first>
    - api/routers/bbdd.py (_validate_sql + POST /query)
    - api/routers/capacidad.py lines 42-50 (handler actual)
    - api/routers/operarios.py (shape análogo a capacidad)
    - nexo/services/preflight.py (nuevo)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md lines 646-700 (endpoint retrofit)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 647-705
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-03 (preflight capacidad/operarios solo si rango > 90d)
  </read_first>
  <behavior>
    api/routers/bbdd.py POST /query:
    - Añadir imports: import json; from nexo.services.preflight import estimate_cost.
    - Al inicio del handler (tras parse del body, ANTES de _validate_sql):
      ```python
      params = {"sql": req.sql, "database": req.database, "user_provided": True}
      params_json_str = json.dumps(params, sort_keys=True, ensure_ascii=False)
      est = estimate_cost("bbdd/query", params)
      request.state.estimated_ms = est.estimated_ms
      request.state.params_json = params_json_str
      if est.level == "red":
          if not (req.force and req.approval_id):
              raise HTTPException(403, detail={"estimation": est.model_dump(), "action": "request_approval"})
          # approval consume via import-guard (mismo patrón que pipeline.py)
          if not _APPROVALS_AVAILABLE:
              raise HTTPException(503, "Flujo aprobación no disponible aún")
          approval = consume_approval(db, approval_id=req.approval_id, user_id=user.id, current_params=params)
          request.state.approval_id = approval.id
      elif est.level == "amber":
          if not req.force:
              raise HTTPException(428, detail={"estimation": est.model_dump(), "action": "confirm_amber"})
      ```
    - Después: llamar _validate_sql(req.sql) tal como antes.
    - Extender BbddQueryRequest (en api/models.py si no existe, o en bbdd.py si es local) con force: bool = False, approval_id: Optional[int] = None (o leer via Query params; consistente con pipeline).
    - IMPORTANTE: preflight PRIMERO, validación después (user-facing feedback antes de seguridad técnica) — research §Architecture Diagram lines 221-227 confirma este orden.

    api/routers/capacidad.py handler capacidad(...):
    - Añadir params al signature: request: Request, force: bool = Query(False), approval_id: Optional[int] = Query(None).
    - Inmediatamente después del guard fecha_fin < fecha_inicio:
      ```python
      rango_dias = (fecha_fin - fecha_inicio).days
      if rango_dias > 90:
          params = {"fecha_desde": fi, "fecha_hasta": ff, "rango_dias": rango_dias}
          params_json_str = json.dumps(params, sort_keys=True, ensure_ascii=False)
          est = estimate_cost("capacidad", params)
          request.state.estimated_ms = est.estimated_ms
          request.state.params_json = params_json_str
          if est.level == "red":
              if not (force and approval_id):
                  raise HTTPException(403, detail={"estimation": est.model_dump(), "action": "request_approval"})
              # consume_approval via import-guard idéntico a pipeline.py
              ...
          elif est.level == "amber" and not force:
              raise HTTPException(428, detail={"estimation": est.model_dump(), "action": "confirm_amber"})
      # else: rango <= 90d → sin preflight, request.state.estimated_ms sigue None
      ```
    - Resto del handler (consulta MES) igual que antes.

    api/routers/operarios.py:
    - Idéntico patrón a capacidad, usando endpoint='operarios' en estimate_cost.

    Todos los cambios mantienen retrocompatibilidad: sin force/approval_id explícitos, rango <=90d sigue funcionando igual; rango >90d con coste green (raro) pasa directamente.
  </behavior>
  <action>
    Editar los 3 routers aplicando el patrón "preflight-antes-de-ejecutar" según behavior. Añadir el mismo import-guard de consume_approval que en pipeline.py (reusar el módulo, no duplicar try/except — opcional: extraer el guard a nexo/services/approvals_guard.py si los 4 routers lo necesitan). Extender BbddQueryRequest con force/approval_id si no están.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "import inspect; from api.routers import bbdd, capacidad, operarios; assert 'estimate_cost' in inspect.getsource(bbdd); assert 'rango_dias > 90' in inspect.getsource(capacidad); assert 'estimate_cost' in inspect.getsource(operarios); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "estimate_cost" api/routers/bbdd.py, api/routers/capacidad.py, api/routers/operarios.py coincide en los 3.
    - grep "rango_dias > 90" api/routers/capacidad.py y operarios.py coincide (D-03).
    - grep "_validate_sql" api/routers/bbdd.py sigue existiendo (no se borra) y está DESPUÉS del preflight call.
    - curl -X POST /api/bbdd/query con SQL trivial -> 200 (green level, passes through).
    - curl -X POST /api/bbdd/query con sql que dispara red + sin approval -> 403 con detail.estimation.level='red'.
    - curl /api/capacidad?fecha_inicio=2025-01-01&fecha_fin=2025-02-01 (rango 31d) -> 200 sin preflight (request.state.estimated_ms None).
    - curl /api/capacidad?fecha_inicio=2025-01-01&fecha_fin=2025-06-01 (rango 151d) -> preflight trigger; si level green -> 200; si amber -> 428.
    - pytest tests/routers/test_preflight_endpoints.py::test_preflight_scope (tras Task 6) -> green.
  </acceptance_criteria>
  <done>
    Los 3 routers inyectan preflight con la semántica correcta (bbdd siempre; capacidad/operarios solo rango>90d). request.state está poblado cuando aplica, y QueryTimingMiddleware escribe en query_log automáticamente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Frontend modal (static/js/app.js + templates pipeline.html + bbdd.html) + .env.example</name>
  <read_first>
    - static/js/app.js (extender — ver patrón usuariosPanel() en templates/ajustes_usuarios.html lines 240-299)
    - templates/pipeline.html (forma actual — añadir modales + Alpine wrapper)
    - templates/bbdd.html (forma actual — añadir lógica similar)
    - templates/base.html (Alpine 3.14.8 + HTMX 2.0.4 ya cargados line 8-11)
    - .env.example (añadir 2 env vars pipeline)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Alpine modal amber component lines 1408-1518
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-05, D-06, D-07 (texto exacto del modal)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 996-1056
  </read_first>
  <behavior>
    static/js/app.js — añadir al final:
    - `function humanize_ms(ms)`: helper puro. `ms < 1000` → `~${ms}ms`; `< 60_000` → `~${Math.round(ms/1000)}s`; `< 3_600_000` → `~${Math.floor(ms/60000)} min ${Math.round((ms%60000)/1000)}s`; else → `~${Math.floor(ms/3_600_000)}h ${Math.floor((ms%3_600_000)/60_000)} min`.
    - Alpine component global `Alpine.data('preflightModal', () => ({ ... }))` con:
      - state: `modalLevel: null, estimation: null, currentParams: null, currentEndpoint: null, currentExecuteFn: null`.
      - `async attempt(endpoint, params, executeFn)`: POST a `/api/${endpoint}/preflight` con params -> estimation; si level='green' -> await executeFn(false, null); else store currentParams/currentEndpoint/currentExecuteFn + set modalLevel=level.
      - `async confirmRun()`: modalLevel=null; await this.currentExecuteFn(true, null).
      - `async requestApproval()`: POST /api/approvals con {endpoint: currentEndpoint, params: currentParams, estimated_ms: estimation.estimated_ms}; recibe {approval_id}; modalLevel=null; mostrar toast (ver showToast existente o consola fallback): "Solicitud enviada. Ver en Mis Solicitudes (#${approval_id})." + link /mis-solicitudes.
      - `cancel()`: modalLevel=null; estimation=null; currentParams=null.
      - `humanize(ms)`: returns humanize_ms(ms).
      - `init()` (Alpine lifecycle hook — auto-invoked on mount): **lee `?approval_id=<N>` de `window.location.search` usando `URLSearchParams`** (per D-15 user re-dispatch flow). Si presente, lo guarda en `this.pendingApprovalId` (state adicional inicializado a `null`). El primer `attempt()` siguiente detecta `this.pendingApprovalId` y, si el preflight devuelve level `red`, **auto-llama a `executeFn(true, this.pendingApprovalId)` sin abrir modal** (the user already approved their intent by clicking "Ejecutar ahora" in /mis-solicitudes). Tras el primer uso, resetea `this.pendingApprovalId = null`. Ejemplo explícito:
        ```js
        init() {
          const params = new URLSearchParams(window.location.search);
          const aid = params.get('approval_id');
          this.pendingApprovalId = aid ? parseInt(aid, 10) : null;
        },
        async attempt(endpoint, params, executeFn) {
          // ... existing preflight logic ...
          if (this.pendingApprovalId && estimation.level === 'red') {
            const aid = this.pendingApprovalId;
            this.pendingApprovalId = null;
            return await executeFn(true, aid);
          }
          // ... rest of existing logic (green → direct, amber/red → modal) ...
        }
        ```

    templates/pipeline.html:
    - Envolver la zona con `<div x-data="preflightModal">`.
    - Al final, añadir 2 `<div x-show="modalLevel === 'amber'">` y `<div x-show="modalLevel === 'red'">` con overlay + modal card (markup per research §Alpine modal amber component lines 1408-1518, adaptado):
      - AMBER: título "Esta operación puede tardar"; `<p class="text-2xl font-bold" x-text="humanize(estimation.estimated_ms)">`; `<p class="text-sm text-gray-500" x-text="estimation.breakdown">`; `<p class="text-sm text-gray-500 mt-2">La UI seguirá respondiendo mientras la operación corre. Los demás módulos no se bloquearán.</p>`; botones Cancelar (@click=cancel()) + Continuar (@click=confirmRun()).
      - RED: título "Operación requiere aprobación" (texto rojo); duración estimada + estimation.reason; botones Cancelar (@click=cancel()) + Solicitar aprobación (@click=requestApproval()).
    - Cambiar el botón "Ejecutar pipeline" actual para que en @click llame `attempt('pipeline', paramsFromForm(), async (force, approvalId) => { ... doRun ... })`.

    templates/bbdd.html:
    - Mismo patrón: wrapper x-data="preflightModal", 2 modales (amber/red), el botón "Ejecutar consulta" llama `attempt('bbdd', {sql, database}, executeFn)`.
    - El modal red en bbdd permite solicitar aprobación con params={sql, database}.

    .env.example — añadir al final una sección nueva:
    ```
    # ── Phase 4: Consultas pesadas ─────────────────────────────────────
    # Pipeline OEE concurrency + soft timeout (D-18).
    # El timeout es UX soft: matplotlib sigue corriendo en background aunque
    # la corrutina cancele. Ver docs MARK_III_PLAN.md Sprint 3.
    NEXO_PIPELINE_MAX_CONCURRENT=3
    NEXO_PIPELINE_TIMEOUT_SEC=900
    ```
  </behavior>
  <action>
    Editar static/js/app.js, templates/pipeline.html, templates/bbdd.html per behavior. Añadir 2 env vars a .env.example. Verificar smoke en browser: cargar /pipeline, inspeccionar consola no-errors; intentar ejecutar pipeline con rango corto -> green directo (no modal); rango 2 años × 50 recursos -> 428 amber -> modal amber aparece con textos correctos.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && grep -q "preflightModal" static/js/app.js && grep -q "modalLevel" templates/pipeline.html && grep -q "humanize_ms" static/js/app.js && grep -q "NEXO_PIPELINE_MAX_CONCURRENT" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep preflightModal static/js/app.js -> coincide.
    - grep humanize_ms static/js/app.js -> coincide.
    - grep "approval_id" static/js/app.js -> coincide (URL re-dispatch per D-15/PC-04-02).
    - grep "URLSearchParams" static/js/app.js -> coincide (URL parsing present).
    - grep "x-data=\"preflightModal\"" templates/pipeline.html templates/bbdd.html -> ambos coinciden.
    - grep "x-show=\"modalLevel === 'amber'\"" templates/pipeline.html -> coincide; idem 'red'.
    - grep "La UI seguirá respondiendo" templates/pipeline.html -> coincide (D-07 reassurance literal).
    - grep NEXO_PIPELINE_MAX_CONCURRENT .env.example -> coincide; idem NEXO_PIPELINE_TIMEOUT_SEC.
    - Browser smoke (modal amber + UI no-congela + user re-dispatch desde /mis-solicitudes?approval_id=) está AHORA en Task 7 (autonomous=false manual smoke). No repetir aquí.
  </acceptance_criteria>
  <done>
    Modales AMBER y RED operan per D-05/D-06/D-07. Alpine component reusable entre pipeline y bbdd. humanize_ms produce texto humano. Env vars documentadas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Implementar tests services/middleware/routers (convertir stubs Wave 0 en tests verdes)</name>
  <read_first>
    - tests/services/test_preflight.py (stub Wave 0 de Plan 04-01 — convertir en tests reales)
    - tests/services/test_pipeline_lock.py (stub Wave 0)
    - tests/middleware/test_query_timing.py (stub Wave 0)
    - tests/routers/test_preflight_endpoints.py (stub Wave 0)
    - tests/auth/test_rbac_smoke.py (patrón TestClient + cookie + _create_test_user + _login)
    - tests/data/conftest.py (_postgres_reachable + db_nexo)
    - .planning/phases/04-consultas-pesadas/04-VALIDATION.md (lista exacta de tests esperados)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Validation Architecture
  </read_first>
  <behavior>
    tests/services/test_preflight.py (convertir skip en tests puros):
    - test_estimate_cost_pipeline_green: monkeypatch thresholds_cache.get para retornar ThresholdEntry(warn_ms=120_000, block_ms=600_000, factor_ms=2000.0, loaded_at=...); llamar estimate_cost con n_recursos=2, n_dias=5 -> estimated_ms=20000, level=green.
    - test_estimate_cost_pipeline_amber: n_recursos=10, n_dias=10 -> 200000 (entre warn 120k y block 600k) -> amber.
    - test_estimate_cost_pipeline_red: n_recursos=10, n_dias=30 -> 600000 >= block -> red.
    - test_estimate_cost_bbdd_green_default: factor fallback 1000ms -> level green (vs warn 3000).
    - test_estimate_cost_rango_short_skips_preflight: rango_dias=30 -> level green con reason "<=90d".
    - test_estimate_cost_rango_long_classifies: rango_dias=180 con factor 50 -> 9000ms vs warn 3000 -> amber (o red si excede block 30000 -> 9000 < 30000 -> amber).
    - test_estimate_cost_unknown_endpoint_defensive_green: endpoint='foobar' -> level green.
    - test_factor_recalc_skipped (skip): marcado TODO Plan 04-04.

    tests/services/test_pipeline_lock.py:
    - Usar @pytest.mark.asyncio (instalado via requirements-dev.txt en 04-01).
    - test_semaphore_limits_to_max: crear 5 corrutinas que hagan async with pipeline_semaphore: await asyncio.sleep(0.1); medir que solo 3 corren concurrentemente usando un counter + asyncio.Lock para observar el max simultáneo.
    - test_timeout_raises: await asyncio.wait_for(asyncio.to_thread(time.sleep, 1.0), timeout=0.1) lanza TimeoutError.
    - test_run_with_lock_completes_green: await run_with_lock(lambda: 42) devuelve 42.
    - Nota: no se usa envVar PIPELINE_TIMEOUT_SEC reducido aquí (módulo-level constante); si se quiere probar timeout short, se hace via asyncio.wait_for con timeout pequeño directo.

    tests/middleware/test_query_timing.py:
    - pytestmark [pytest.mark.integration, pytest.mark.skipif(not _postgres_reachable(), reason='...')].
    - Fixtures: TestClient, _create_test_user, _login (copy-paste de test_rbac_smoke.py).
    - test_timing_writes_row_for_timed_path: login usuario; hacer POST a /api/bbdd/query con SQL trivial ("SELECT 1"); verificar con session Postgres que existe al menos 1 fila en nexo.query_log con endpoint='bbdd/query', user_id=user.id, actual_ms > 0.
    - test_timing_excludes_non_timed_paths: llamar GET /api/health; verificar que NO se añadió fila (compare count antes/después).
    - test_slow_status_written: monkeypatch thresholds_cache.get para devolver ThresholdEntry con warn_ms=1 block_ms=2 (muy bajo); hacer query que tome >1.5ms (trivial); verificar fila con status='slow' + log warning (capturar via caplog).
    - test_error_status_on_exception: simular handler que raise; verificar fila con status='error'.

    tests/routers/test_preflight_endpoints.py:
    - pytestmark integration + skipif Postgres.
    - test_preflight_returns_estimation_json: login; POST /api/pipeline/preflight con payload mínimo; response 200; body tiene keys level/estimated_ms/breakdown/factor_used_ms/warn_ms/block_ms.
    - test_run_amber_no_force_returns_428: monkeypatch thresholds o usar payload pesado para forzar amber; POST /api/pipeline/run sin force -> status 428; body.detail.estimation.level='amber'.
    - test_run_red_without_approval_returns_403: payload que fuerza red; sin approval_id -> 403.
    - test_preflight_scope_bbdd_always: POST /api/bbdd/query SQL trivial con thresholds que lo marquen amber -> 428.
    - test_preflight_scope_capacidad_short_bypass: GET /api/capacidad rango 30d -> 200 (sin 428 aunque threshold bajo — D-03 rango<=90 short-circuit).
    - test_preflight_scope_capacidad_long_triggers: GET rango 180d -> preflight dispara; si amber con force=false -> 428.
    - test_run_red_with_valid_approval_executes: requires Plan 04-03 — marcar xfail con reason="Requires approval flow from Plan 04-03" hasta que 04-03 aterrice.
  </behavior>
  <action>
    Re-escribir los 4 archivos de tests convirtiendo stubs en tests funcionales. Ejecutar pytest tests/services/test_preflight.py tests/services/test_pipeline_lock.py -x (unit, no DB) -> green. Con docker compose up -d db y init_nexo_schema ejecutado, pytest tests/middleware/test_query_timing.py tests/routers/test_preflight_endpoints.py -x -> green excepto test_run_red_with_valid_approval_executes (xfail hasta 04-03).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -m pytest tests/services/test_preflight.py tests/services/test_pipeline_lock.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/services/test_preflight.py tiene al menos 7 funciones test_* todas verdes (unit, no DB needed).
    - tests/services/test_pipeline_lock.py tiene al menos 3 tests verdes con @pytest.mark.asyncio.
    - tests/middleware/test_query_timing.py tiene al menos 4 tests; con Postgres up + init_schema -> verdes.
    - tests/routers/test_preflight_endpoints.py tiene al menos 6 tests; 1 marcado xfail para 04-03.
    - pytest tests/services/test_preflight.py tests/services/test_pipeline_lock.py -x -> green (sin Postgres).
    - pytest tests/middleware/ tests/routers/test_preflight_endpoints.py -x -> green con Postgres up.
    - Sin regresión: pytest tests/data/ tests/auth/ -x -> green (Phase 2/3 no afectados).
  </acceptance_criteria>
  <done>
    Wave 0 tests convertidos en tests reales verdes. Cobertura efectiva de QUERY-03/04/05/07/08 vía los tests; QUERY-06 parcial (red+approval) pendiente de 04-03 (xfail).
  </done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7 (MANUAL SMOKE): Modal amber E2E + UI no-congela + user re-dispatch desde URL approval_id</name>
  <what-built>
    Plan 04-02 entregó preflight + semáforo + middleware + modal Alpine. Tests automáticos cubren API/contract, pero los 3 comportamientos observables críticos requieren ojo humano: (1) que el modal amber aparece con breakdown correcto en browser; (2) que `asyncio.to_thread` + `pipeline_semaphore` efectivamente NO congelan la UI mientras el pipeline ejecuta; (3) que `?approval_id=<id>` en la URL (link generado por Plan 04-03 en /mis-solicitudes) se lee y auto-re-dispara con `force=true + approval_id` sin abrir el modal rojo.
  </what-built>
  <how-to-verify>
    1. **Modal amber E2E:** Login como usuario con permiso `pipeline:run`. En /pipeline introducir rango "2025-01-01 → 2026-12-31" (≈24 meses) y seleccionar ~20 recursos. Click "Ejecutar pipeline". Verificar:
       - El modal amber aparece con título "Esta operación puede tardar".
       - La duración renderizada coincide con `humanize(estimated_ms)` (formato `~X min Ys` o similar).
       - `estimation.breakdown` aparece ("N recursos × M días × ~2.0s/run").
       - Párrafo "La UI seguirá respondiendo mientras la operación corre..." visible literal.
       - Botones [Cancelar] y [Continuar] responden: Cancelar cierra sin request; Continuar dispara POST /api/pipeline/run con `force=true`.
    2. **UI no-congela (2 pestañas):** Desde la pestaña A disparar un pipeline amber grande. Inmediatamente abrir pestaña B en paralelo y navegar a /recursos, /historial, /capacidad. Cada una debe responder en <500ms. La pestaña A sigue mostrando SSE progreso o spinner sin bloquear. Confirmar en DevTools Network que las requests de B NO quedan pendientes detrás de A.
    3. **User re-dispatch desde URL approval_id (PC-04-02):** Requiere que Plan 04-03 esté mergeado. Como usuario: (a) disparar un pipeline red → solicitar aprobación → aparece en /mis-solicitudes como pending. (b) Login propietario → aprobar la solicitud en /ajustes/solicitudes. (c) Usuario regresa a /mis-solicitudes → fila ahora status=approved + link "Ejecutar ahora" visible. (d) Click en el link: la URL destino es `/pipeline?approval_id=<N>`. (e) En /pipeline, **sin volver a llenar el formulario**, click en "Ejecutar pipeline" con los mismos params que generaron la solicitud: el pipeline se ejecuta directamente, SIN modal rojo (`pendingApprovalId` detectado + `force=true + approval_id` auto-enviados). (f) psql `SELECT status, consumed_at FROM nexo.query_approvals WHERE id=<N>;` → status='consumed', consumed_at poblado.
    4. **Cancelación:** En cualquier punto probar [Cancelar] del modal: no se hace request y el UI vuelve al estado de formulario.
    5. Abrir DevTools Console y confirmar cero errores JS Alpine/fetch durante (1)-(3).
  </how-to-verify>
  <resume-signal>Reanudar con "amber-ok + ui-no-blocks + url-redispatch-ok" o describir desviaciones concretas (elemento x-show no oculto, breakdown missing, UI tab B con latencia >500ms, link approval_id no existe / no funciona).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API (preflight POST) | Usuario envía params que el backend estimate_cost interpreta. Valores no se ejecutan (sólo clasifican). |
| Browser -> API (run con force/approval_id) | force=true debe exigir approval_id cuando level=red (D-15). Approval_id pertenece al usuario y coincide con params. |
| Middleware -> query_log | Writes best-effort; fallo de escritura NO tumba la response (pattern audit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation of Privilege | POST /api/pipeline/run | mitigate | /preflight es informativo; /run SIEMPRE re-ejecuta estimate_cost y bloquea amber/red sin force/approval. Research §Preflight bypass. |
| T-04-02-02 | Tampering | request.state.params_json (leído por middleware) | mitigate | Router construye params_json_str localmente; middleware sólo lee. Browser no puede escribir a request.state. |
| T-04-02-03 | Information Disclosure | Timing side-channel (actual_ms en query_log) | accept | Log solo visible a propietario vía /ajustes/rendimiento (Plan 04-04). LAN-only. |
| T-04-02-04 | Denial of Service | Pipeline concurrency | mitigate | Semáforo(3) limita worker CPU/RAM. Timeout 15min evita infinite-loop. |
| T-04-02-05 | Denial of Service | Timeout soft (thread sigue ejecutando) | accept | Landmine documentado en RESEARCH §Pitfall 1 y en pipeline_lock.py docstring. Pipeline cleanup existe en finally de run_pipeline (tmp_root rmtree). |
| T-04-02-06 | Repudiation | Quién ejecutó pipeline con force | mitigate | audit_middleware (Phase 2) graba POST /api/pipeline/run con user_id; query_timing graba la misma fila con estimated_ms + actual_ms + approval_id — correlación completa. |
</threat_model>

<verification>
- python smoke: from nexo.services.preflight import estimate_cost; from nexo.services.pipeline_lock import pipeline_semaphore; from nexo.middleware.query_timing import QueryTimingMiddleware — todos importan.
- docker compose up web -> arranque limpio (schema_guard OK + middleware chain OK).
- curl -X POST /api/pipeline/preflight con payload JSON -> 200 + Estimation body.
- curl -X POST /api/pipeline/run con payload que provoca amber sin force -> 428 + estimation detail.
- curl -X POST /api/pipeline/run con green payload + login cookie -> 200 SSE con mensajes del pipeline.
- psql: SELECT endpoint, user_id, estimated_ms, actual_ms, status FROM nexo.query_log ORDER BY ts DESC LIMIT 5 -> filas con actual_ms > 0.
- pytest tests/services/test_preflight.py tests/services/test_pipeline_lock.py -x -> green.
- pytest tests/middleware/test_query_timing.py tests/routers/test_preflight_endpoints.py -x -> green (Postgres up required).
- Smoke browser: /pipeline con rango grande -> modal amber aparece, Cancelar cierra, Continuar ejecuta y UI no se congela (abrir /historial en otra pestaña responde rápido).
</verification>

<success_criteria>
1. POST /api/pipeline/preflight devuelve Estimation correcta para green/amber/red.
2. POST /api/pipeline/run acepta force/approval_id y aplica gate logic según level.
3. QueryTimingMiddleware escribe en nexo.query_log con los 4 endpoints timed; paths no listados no escriben.
4. Pipeline ejecuta en asyncio.to_thread dentro de semáforo(3); no congela event loop.
5. Timeout 900s se respeta (soft UX); status=timeout en query_log cuando dispara.
6. Modal AMBER y RED funcionan en /pipeline y /bbdd con los textos D-05/D-06/D-07.
7. /capacidad y /operarios con rango <=90d no invocan preflight (backward-compat); rango >90d sí.
8. Tests services/middleware/routers verdes.
9. Sin regresión en Phase 2 y Phase 3 (tests auth/data siguen green).
10. .env.example documenta las 2 env vars del pipeline.
</success_criteria>

<output>
Crear .planning/phases/04-consultas-pesadas/04-02-SUMMARY.md documentando:
- Endpoints creados/modificados y sus status codes por level.
- Middleware registrado y su posición LIFO.
- Decisiones implementadas (D-01..D-09, D-17, D-18) con referencias concretas.
- Tests convertidos de stub Wave 0 a verdes (contadores).
- Landmines documentados en código (soft timeout, import-guard approvals).
- Commits landed (1 por task recomendado; ~6 commits).
- Cualquier descubrimiento que afecte a Plan 04-03 o 04-04.
</output>
