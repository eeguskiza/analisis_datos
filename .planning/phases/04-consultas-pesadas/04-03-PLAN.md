---
phase: 04-consultas-pesadas
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
autonomous: false
requirements: [QUERY-06]
tags: [nexo, phase-4, approvals, cas, cancel, sidebar-badge, htmx-polling, mis-solicitudes]
files_modified:
  - nexo/services/approvals.py
  - nexo/services/approvals_cleanup.py
  - nexo/services/cleanup_scheduler.py
  - nexo/services/auth.py
  - api/routers/approvals.py
  - api/routers/pages.py
  - api/main.py
  - templates/ajustes_solicitudes.html
  - templates/mis_solicitudes.html
  - templates/ajustes.html
  - templates/base.html
  - .env.example
  - tests/services/test_approvals.py
  - tests/routers/test_approvals_api.py
  - tests/routers/test_preflight_endpoints.py  # PC-04-07: remove xfail marker only

must_haves:
  truths:
    # ROADMAP-verbatim success-criteria truth (PC-04-08):
    - "Query en /bbdd (u otro endpoint) con coste estimado >block_ms abre flujo de aprobación y no ejecuta hasta que propietario aprueba"
    # Implementation-level truths:
    - "POST /api/approvals crea fila en nexo.query_approvals con status=pending, created_at=now, ttl_days=NEXO_APPROVAL_TTL_DAYS"
    - "POST /api/approvals/{id}/approve (propietario) cambia status a approved, graba approved_by + approved_at"
    - "POST /api/approvals/{id}/reject (propietario) cambia status a rejected, graba rejected_at"
    - "POST /api/approvals/{id}/cancel (dueño) verifica ownership y pending, cambia status a cancelled"
    - "POST /api/approvals/{id}/cancel rechaza con 403 si user != dueño"
    - "consume_approval hace UPDATE atómico WHERE consumed_at IS NULL AND status=approved AND user_id=uid AND params_json=pj RETURNING id; segunda llamada con mismo approval_id devuelve None"
    - "consume_approval falla con 403 si params cambian entre solicitud y consumo (equality check D-15)"
    - "GET /api/approvals/count devuelve HTML fragment vacío si 0 pendientes o <span>(N)</span> si N>0"
    - "GET /ajustes/solicitudes (solo propietario) renderiza tabla con pendientes + histórico 30d + HTMX auto-refresh cada 30s"
    - "GET /mis-solicitudes (todo usuario logeado) renderiza solicitudes propias con botón Cancelar sobre pending"
    - "cleanup_scheduler arranca en lifespan y marca expired las solicitudes >7 días (NEXO_APPROVAL_TTL_DAYS)"
    - "Badge sidebar propietario hace hx-get /api/approvals/count every 30s"
  artifacts:
    - path: "nexo/services/approvals.py"
      provides: "create_approval + consume_approval (CAS) + approve/reject/cancel + expire_stale"
      exports: ["create_approval", "consume_approval", "approve", "reject", "cancel", "expire_stale"]
    - path: "nexo/services/approvals_cleanup.py"
      provides: "run_once + async run_loop (Monday 03:05) que llama expire_stale"
      exports: ["run_once", "run_loop"]
    - path: "nexo/services/cleanup_scheduler.py"
      provides: "async cleanup_loop en lifespan; llama cada jobs registrados"
      exports: ["cleanup_loop"]
    - path: "api/routers/approvals.py"
      provides: "Router /api/approvals/* endpoints (create/approve/reject/cancel/count) + GET /ajustes/solicitudes"
      contains: "router = APIRouter"
    - path: "templates/ajustes_solicitudes.html"
      provides: "Tabla propietario con Aprobar/Rechazar + histórico + HTMX 30s"
    - path: "templates/mis_solicitudes.html"
      provides: "Tabla propia con Cancelar sobre pending"
    - path: "templates/base.html"
      provides: "Badge sidebar con hx-get /api/approvals/count every 30s (solo propietario)"
      contains: "hx-get=\"/api/approvals/count\""
  key_links:
    - from: "api/routers/approvals.py POST /{id}/cancel"
      to: "nexo/services/approvals.py cancel()"
      via: "ownership check (approval.user_id == user.id)"
      pattern: "cancel.*user_id"
    - from: "api/routers/pipeline.py (de 04-02) + bbdd.py + capacidad.py + operarios.py"
      to: "nexo/services/approvals.py consume_approval"
      via: "import consume_approval; import-guard en 04-02 se activa aquí"
      pattern: "consume_approval"
    - from: "api/main.py lifespan"
      to: "nexo/services/cleanup_scheduler.py"
      via: "asyncio.create_task(cleanup_loop())"
      pattern: "cleanup_loop"
    - from: "templates/base.html sidebar"
      to: "/api/approvals/count"
      via: "hx-get every 30s (solo nav item propietario)"
      pattern: "hx-get=\"/api/approvals/count\""
---

<objective>
Implementa el flujo de aprobación asíncrona end-to-end: tabla query_approvals usada + service layer (create/consume/approve/reject/cancel/expire_stale) + router /api/approvals/* + 2 páginas UI (/ajustes/solicitudes para propietario, /mis-solicitudes para usuario) + badge sidebar HTMX + job cleanup 7d TTL + scheduler asyncio para jobs en lifespan.

Decisiones implementadas: D-06 (modal red -> POST /api/approvals), D-13 (badge HTMX 30s, sin email/banner), D-14 (TTL 7d + Monday 03:05 job + histórico 30d), D-15 (CAS single-use + equality params_json), D-16 (usuario cancela propia pending).

Paralelizable con 04-02 (disjoint files: este plan toca approvals.py/router/pages/templates solicitudes+mis-solicitudes/base.html badge; 04-02 toca preflight/middleware/timing/routers pipeline/bbdd/capacidad/operarios).

Critical finding del research: CAS atómico = UPDATE ... WHERE consumed_at IS NULL AND status='approved' AND user_id=:uid AND params_json=:pj RETURNING id. Una sola query; si devuelve 0 filas -> 403 con diagnóstico.

LISTEN/NOTIFY listener real + scheduler para query_log cleanup + factor auto-refresh viven en Plan 04-04. Este plan arranca el scheduler con UN job (approvals_cleanup); Plan 04-04 añade los otros 2 jobs al mismo scheduler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-consultas-pesadas/04-CONTEXT.md
@.planning/phases/04-consultas-pesadas/04-RESEARCH.md
@.planning/phases/04-consultas-pesadas/04-PATTERNS.md
@.planning/phases/04-consultas-pesadas/04-VALIDATION.md
@.planning/phases/04-consultas-pesadas/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From Plan 04-01:

```python
# nexo/data/models_nexo.py
class NexoQueryApproval(NexoBase):
    __tablename__ = "query_approvals"
    # cols: id(PK), user_id(FK users CASCADE), endpoint, params_json, estimated_ms,
    # status default='pending', created_at, ttl_days default=7,
    # approved_at, approved_by, rejected_at, cancelled_at, expired_at,
    # consumed_at, consumed_run_id

# nexo/data/repositories/nexo.py
class ApprovalRepo:
    def __init__(self, db): ...
    def create(self, *, user_id, endpoint, params_json, estimated_ms, ttl_days=7) -> NexoQueryApproval
    def get(self, approval_id) -> QueryApprovalRow | None
    def list_by_user(self, user_id, *, statuses=None) -> list[QueryApprovalRow]
    def list_pending(self) -> list[QueryApprovalRow]
    def count_pending(self) -> int
    def approve(self, approval_id, approved_by) -> None
    def reject(self, approval_id, decided_by) -> None
    def cancel(self, approval_id, user_id) -> bool  # False si no es owner o no pending
    def consume(self, *, approval_id, user_id, current_params_json) -> QueryApprovalRow | None  # CAS
    def expire_stale(self, cutoff) -> int  # rows moved pending->expired
```

From Phase 2:

```python
# nexo/services/auth.py
PERMISSION_MAP: dict[str, list[str]] = {
    "pipeline:run":       ["ingenieria", "produccion"],
    "pipeline:read":      ["ingenieria", "produccion", "gerencia"],
    ...
}
# EXTEND with 3 new perms in this plan:
# "aprobaciones:manage": []  # propietario-only (empty list = propietario bypass)
# ... limites:manage and rendimiento:read go in Plan 04-04.

def require_permission(permission: str):
    async def _check(request: Request) -> NexoUser: ...
    return _check

# api/routers/usuarios.py — template-match pattern for CRUD router
router = APIRouter(
    prefix="/ajustes/usuarios", tags=["ajustes"],
    dependencies=[Depends(require_permission("usuarios:manage"))],
)
```

From api/routers/pages.py:

```python
@router.get("/ajustes", dependencies=[Depends(require_permission("ajustes:manage"))])
def ajustes_page(request: Request):
    return render("ajustes.html", request, _common_extra("ajustes"))
```

From api/main.py lifespan (extend):

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    schema_guard.verify(engine_nexo)
    try:
        init_db()
    except Exception as exc:
        logger.error(f"Error inicializando BD: {exc}")
    # ADD cleanup_scheduler task here (this plan)
    yield
    # ADD cancellation of task here
```

From Plan 04-02 (parallelizable — files DISJOINT from this plan):

```python
# api/routers/pipeline.py contains an import-guard:
try:
    from nexo.services.approvals import consume_approval
    _APPROVALS_AVAILABLE = True
except ImportError:
    _APPROVALS_AVAILABLE = False
# When Plan 04-03 merges, the import succeeds and the feature activates.
```

From templates/base.html (sidebar structure to extend with badge):

```html
{% for item in nav_items %}
  <a href="{{ item.href }}" class="...">{{ item.label }}</a>
{% endfor %}
```
The template/router loop filters by permission; the new nav item "Solicitudes" sólo aparece para propietario (aprobaciones:manage vacío -> sólo propietario).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nexo/services/approvals.py (service layer wrapper over ApprovalRepo) + extend PERMISSION_MAP</name>
  <read_first>
    - nexo/services/auth.py (PERMISSION_MAP at ~line 200 — extend with aprobaciones:manage)
    - nexo/data/repositories/nexo.py (ApprovalRepo from 04-01)
    - nexo/data/models_nexo.py (NexoQueryApproval)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 4 (Approval Flow lines 762-938 — especialmente consume_approval)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 415-447
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-14, D-15, D-16
  </read_first>
  <behavior>
    nexo/services/approvals.py — pure service wrapping ApprovalRepo:
    - imports: json, from datetime import datetime, timezone, timedelta; from fastapi import HTTPException; from sqlalchemy.orm import Session; from nexo.data.repositories.nexo import ApprovalRepo; from nexo.data.models_nexo import NexoQueryApproval.
    - Helper `_canonical_json(obj) -> str`: `return json.dumps(obj, sort_keys=True, ensure_ascii=False)`. Docstring menciona que garantiza equality bit-a-bit entre creación y consumo (A7 del research).
    - `def create_approval(db: Session, *, user_id: int, endpoint: str, params: dict, estimated_ms: int, ttl_days: int = 7) -> int`:
      - params_json = _canonical_json(params).
      - repo = ApprovalRepo(db).
      - row = repo.create(user_id=user_id, endpoint=endpoint, params_json=params_json, estimated_ms=estimated_ms, ttl_days=ttl_days).
      - return row.id.
    - `def consume_approval(db: Session, *, approval_id: int, user_id: int, current_params: dict) -> NexoQueryApproval`:
      - current_json = _canonical_json(current_params).
      - result = ApprovalRepo(db).consume(approval_id=approval_id, user_id=user_id, current_params_json=current_json).
      - if result is None: diagnóstico detallado (mismo patrón RESEARCH lines 920-935):
        - row = db.get(NexoQueryApproval, approval_id).
        - if row is None: raise HTTPException(403, "Approval no existe").
        - if row.user_id != user_id: raise HTTPException(403, "Approval pertenece a otro usuario").
        - if row.status != 'approved': raise HTTPException(403, f"Approval está en estado {row.status}").
        - if row.consumed_at is not None: raise HTTPException(403, "Approval ya fue consumido").
        - raise HTTPException(403, "Parámetros cambiaron respecto a la solicitud aprobada").
      - return result (QueryApprovalRow). Llamador puede convertir a ORM via db.get si necesita.
    - `def approve(db, approval_id, approved_by) -> None`: repo.approve(approval_id, approved_by).
    - `def reject(db, approval_id, decided_by) -> None`: repo.reject(approval_id, decided_by).
    - `def cancel(db, approval_id, user_id) -> bool`: return repo.cancel(approval_id, user_id).
    - `def expire_stale(db, ttl_days: int) -> int`: cutoff = datetime.now(timezone.utc) - timedelta(days=ttl_days); return repo.expire_stale(cutoff).
    - NO emite email / banner / websocket — D-13 + SMTP Out of Scope.

    Extender nexo/services/auth.py PERMISSION_MAP:
    - Localizar el dict PERMISSION_MAP (~line 200) y añadir una línea:
      ```python
      "aprobaciones:manage": [],   # Propietario only (lista vacía = solo propietario via bypass hardcoded)
      ```
    - NO añadir "limites:manage" ni "rendimiento:read" aquí — esos los añade Plan 04-04.
    - Actualizar docs/AUTH_MODEL.md §Apéndice PERMISSION_MAP si existe sección dedicada a Phase 4 (opcional — si el patrón es actualizar docs al cerrar phase, dejar para SUMMARY).
  </behavior>
  <action>
    Crear nexo/services/approvals.py per behavior. Editar nexo/services/auth.py añadiendo aprobaciones:manage al PERMISSION_MAP (ubicado entre los bloques existentes, preferiblemente tras usuarios:manage para agrupación administrativa).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.services.approvals import create_approval, consume_approval, approve, reject, cancel, expire_stale; from nexo.services.auth import PERMISSION_MAP; assert 'aprobaciones:manage' in PERMISSION_MAP; assert PERMISSION_MAP['aprobaciones:manage'] == []; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - from nexo.services.approvals import todas las 6 funciones OK.
    - PERMISSION_MAP['aprobaciones:manage'] == [] (propietario-only).
    - grep "_canonical_json" nexo/services/approvals.py -> coincide (helper existe).
    - grep "sort_keys=True" nexo/services/approvals.py -> coincide (equality D-15).
    - consume_approval con approval inexistente -> HTTPException(403, "Approval no existe") (verificable via pytest con Postgres up en Task 5).
    - La import-guard de Plan 04-02 (si ya mergeado) ahora activa: verificar en pipeline.py que _APPROVALS_AVAILABLE = True tras este task: `python -c "from api.routers.pipeline import _APPROVALS_AVAILABLE; assert _APPROVALS_AVAILABLE"` (opcional, solo si 04-02 ya está en main).
  </acceptance_criteria>
  <done>
    Service layer completo para approvals. PERMISSION_MAP extendido. La dependencia de 04-02 al consume_approval resuelve sin error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create api/routers/approvals.py (endpoints + pages) + register in api/main.py</name>
  <read_first>
    - api/routers/usuarios.py (analog de CRUD router propietario-only con form-posts y redirects)
    - api/routers/auditoria.py (analog de página con filtros + tabla + pagination)
    - api/routers/pages.py (cómo extender /ajustes/* o registrar nuevos)
    - api/deps.py (DbNexo, render helper)
    - nexo/services/approvals.py (recién creado)
    - nexo/data/repositories/nexo.py (ApprovalRepo — list_pending, list_by_user, count_pending)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Endpoints per D-13/D-14/D-15/D-16
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 710-770 (approvals.py router adaptation)
  </read_first>
  <behavior>
    api/routers/approvals.py:
    - imports: APIRouter, Depends, Form, HTMLResponse, HTTPException, Request, Response, RedirectResponse; from api.deps import DbNexo, render; from nexo.services.auth import require_permission; from nexo.services import approvals as svc; from nexo.data.repositories.nexo import ApprovalRepo; Pydantic body model CreateApprovalBody.

    - `class CreateApprovalBody(BaseModel)`: endpoint: str; params: dict; estimated_ms: int.

    - Router root — un solo APIRouter sin prefix global (los paths de /api/* y /ajustes/* y /mis-solicitudes tienen prefixes distintos; registrar múltiples sub-routers o usar paths absolutos en cada decorator).

    - Endpoints propietario-only (dependency require_permission("aprobaciones:manage")):
      1. POST /api/approvals (SIN require_permission — cualquier usuario autenticado puede crear su propia solicitud). Signature:
         ```python
         @router.post("/api/approvals")
         async def create(body: CreateApprovalBody, request: Request, db: DbNexo):
             user = request.state.user
             if user is None:
                 raise HTTPException(401)
             approval_id = svc.create_approval(db, user_id=user.id, endpoint=body.endpoint, params=body.params, estimated_ms=body.estimated_ms)
             return {"approval_id": approval_id, "status": "pending"}
         ```
      2. GET /api/approvals/count (propietario-only) -> HTMLResponse:
         ```python
         @router.get("/api/approvals/count", dependencies=[Depends(require_permission("aprobaciones:manage"))])
         def count(db: DbNexo) -> HTMLResponse:
             n = ApprovalRepo(db).count_pending()
             if n == 0:
                 return HTMLResponse("")
             return HTMLResponse(f'<span class="ml-2 text-xs bg-yellow-400 text-yellow-900 px-1.5 py-0.5 rounded-full font-bold">{n}</span>')
         ```
      3. POST /api/approvals/{id}/approve (propietario): fetch user; svc.approve(db, id, user.id); RedirectResponse("/ajustes/solicitudes?ok=approved", 303).
      4. POST /api/approvals/{id}/reject (propietario): svc.reject(db, id, user.id); RedirectResponse(303).
      5. POST /api/approvals/{id}/cancel (cualquier auth user):
         ```python
         @router.post("/api/approvals/{id}/cancel")
         def cancel(id: int, request: Request, db: DbNexo):
             user = request.state.user
             if user is None: raise HTTPException(401)
             ok = svc.cancel(db, id, user.id)
             if not ok:
                 raise HTTPException(403, "No puedes cancelar esta solicitud (no eres el dueño o no está pending)")
             return RedirectResponse("/mis-solicitudes?ok=cancelled", 303)
         ```
      6. GET /ajustes/solicitudes (propietario):
         ```python
         @router.get("/ajustes/solicitudes", response_class=HTMLResponse,
                     dependencies=[Depends(require_permission("aprobaciones:manage"))])
         def page_solicitudes(request: Request, db: DbNexo):
             repo = ApprovalRepo(db)
             pending = repo.list_pending()
             # Histórico 30d: approved+rejected+expired+cancelled+consumed dentro de los últimos 30 días
             from datetime import datetime, timedelta, timezone
             cutoff = datetime.now(timezone.utc) - timedelta(days=30)
             historico = repo.list_recent_non_pending(cutoff)  # método provisto por Plan 04-01 Task 3 (ya existe — solo consumir)
             return render("ajustes_solicitudes.html", request, {"pending": pending, "historico": historico})
         ```
         NOTE (PC-04-06 fix): `list_recent_non_pending(cutoff)` **ya existe** en `nexo/data/repositories/nexo.py` como parte de Plan 04-01 Task 3. Este plan solo lo CONSUME — NO lo reimplementa ni lo modifica. Si durante ejecución se detecta que el método no está, **detener** y escalar a revisión de Plan 04-01 (gap upstream).
      7. GET /mis-solicitudes (cualquier auth user):
         ```python
         @router.get("/mis-solicitudes", response_class=HTMLResponse)
         def page_mis_solicitudes(request: Request, db: DbNexo):
             user = request.state.user
             if user is None: raise HTTPException(401)
             repo = ApprovalRepo(db)
             mias = repo.list_by_user(user.id)
             return render("mis_solicitudes.html", request, {"solicitudes": mias})
         ```

    Registrar en api/main.py:
    - Añadir import: `from api.routers import approvals as approvals_router`.
    - Al final del bloque de include_router: `app.include_router(approvals_router.router)` (sin prefix='/api' global porque los decorators ya son absolutos).
  </behavior>
  <action>
    Escribir api/routers/approvals.py per behavior. El método `list_recent_non_pending` YA EXISTE en `nexo/data/repositories/nexo.py` (entregado por Plan 04-01 Task 3 — PC-04-06); este task lo IMPORTA y CONSUME, no lo modifica. Registrar en api/main.py. Smoke-test: curl con cookie propietario a /api/approvals/count -> respuesta HTML fragment.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from api.routers import approvals; from api.routers.approvals import CreateApprovalBody; b = CreateApprovalBody(endpoint='pipeline/run', params={'n':1}, estimated_ms=1000); assert b.endpoint == 'pipeline/run'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "router\\.(post|get)" api/routers/approvals.py -> >= 7 rutas.
    - grep "/api/approvals/count" api/routers/approvals.py -> coincide.
    - grep "/ajustes/solicitudes" api/routers/approvals.py -> coincide.
    - grep "/mis-solicitudes" api/routers/approvals.py -> coincide.
    - grep "list_recent_non_pending" nexo/data/repositories/nexo.py -> coincide (método entregado por Plan 04-01; 04-03 NO lo añade).
    - grep "list_recent_non_pending" api/routers/approvals.py -> coincide (Plan 04-03 lo CONSUME).
    - grep "approvals_router" api/main.py -> coincide.
    - Smoke: docker compose up web arranca sin error.
    - Curl (con cookie propietario): GET /api/approvals/count -> 200 + HTML (""="" si 0 pendientes).
    - Curl (sin auth): GET /ajustes/solicitudes -> 401/redirect a /login.
    - Curl (usuario no-propietario): GET /ajustes/solicitudes -> 403.
    - Curl (cualquier auth user): POST /api/approvals con {endpoint:..., params:..., estimated_ms:...} -> {approval_id: N, status: 'pending'}.
  </acceptance_criteria>
  <done>
    Router approvals.py operativo con 7 endpoints. ApprovalRepo consume `list_recent_non_pending` (provisto por Plan 04-01). Permisos correctos (propietario-only para manage, self-service para create/cancel/mis-solicitudes).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Templates ajustes_solicitudes.html + mis_solicitudes.html + update ajustes.html hub + base.html sidebar badge</name>
  <read_first>
    - templates/ajustes.html (hub con nav cards — añadir card Solicitudes)
    - templates/ajustes_auditoria.html (analog filters + tabla + badges paginación)
    - templates/ajustes_usuarios.html (form inline + onsubmit confirm pattern)
    - templates/base.html (sidebar nav loop + HTMX ya cargado)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 878-976 (template patterns)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-13 (badge HTMX)
  </read_first>
  <behavior>
    templates/ajustes_solicitudes.html (nueva; propietario):
    - `{% extends "base.html" %}{% block content %}`.
    - Feedback banner idéntico a ajustes_auditoria.html (ok/error param).
    - Sección "Pendientes" con tabla:
      - columnas: # (id), Usuario (email), Endpoint, Estimado (humanize_ms como helper JS o pre-format en Python con `format_ms(ms)`), Creado (ts relativa "hace 2h"), Acciones (2 botones inline form).
      - para cada pending: form POST /api/approvals/{id}/approve con onsubmit confirm -> [Aprobar]; otro form POST /api/approvals/{id}/reject -> [Rechazar].
    - Sección "Histórico (30d)" con tabla más compacta: id, user, endpoint, status badge (colores: approved=green, rejected=red, cancelled=gray, expired=gray, consumed=blue), fechas.
    - HTMX auto-refresh: `<div hx-get="/ajustes/solicitudes" hx-trigger="every 30s" hx-select="#tabla-pending" hx-swap="outerHTML">` — o más simple: envolver la tabla pending con `<div id="tabla-pending" hx-get="/api/approvals/pending-table" hx-trigger="every 30s" hx-swap="outerHTML">` (requiere endpoint extra; OMITIR si la simpleza prima — preferir refresh de la página entera cada 30s vía meta refresh NO, mejor HTMX de la tabla). Simple approach: documentar que el badge (global en base.html) ya hace polling; para la tabla, aceptar que propietario refresca manualmente o recarga la página. Decisión planner: mantener simple — SIN HTMX interno en esta tabla, confiar en el badge + usuario recarga manualmente. Aceptado en 04-VALIDATION.md (auto-refresh es nice-to-have).

    templates/mis_solicitudes.html (nueva; cualquier user):
    - `{% extends "base.html" %}{% block content %}`.
    - Tabla única: #, Endpoint, Estimado, Creado, Status badge (colores según status), Acción.
    - Para filas con status=pending: botón inline form POST /api/approvals/{id}/cancel con onsubmit confirm.
    - Para filas con status=approved + consumed_at IS NULL: badge verde "Aprobado — pendiente de ejecutar" **+ link "Ejecutar ahora" REQUERIDO** (PC-04-02 + D-15). El link target depende del endpoint almacenado en la fila approval:
      - Si `s.endpoint == 'pipeline/run'` → href `/pipeline?approval_id={{ s.id }}`.
      - Si `s.endpoint == 'bbdd/query'` → href `/bbdd?approval_id={{ s.id }}`.
      - Si `s.endpoint == 'capacidad'` → href `/capacidad?approval_id={{ s.id }}`.
      - Si `s.endpoint == 'operarios'` → href `/operarios?approval_id={{ s.id }}`.
      El frontend `preflightModal` de Plan 04-02 Task 5 lee `?approval_id=` del URL en `init()` y auto-re-dispara con `force=true + approval_id` al siguiente attempt (ver Plan 04-02 Task 5 behavior). Este link NO es opcional — es el único flujo que cierra D-15 user-consume.

    templates/ajustes.html (hub) — añadir card Solicitudes (propietario):
    - Copiar una de las existentes (Usuarios / Auditoría).
    - Envolver en `{% if request.state.user.role == 'propietario' %}`.
    - href="/ajustes/solicitudes", título "Solicitudes", descripción "Aprobar/rechazar queries pesadas pendientes.".

    templates/base.html (sidebar badge):
    - Localizar el nav item de Ajustes (o añadir item dedicado "Solicitudes" sólo para propietario).
    - Opción simple: añadir item nuevo en el loop nav_items solo para propietario con estructura:
      ```html
      {% if request.state.user and request.state.user.role == 'propietario' %}
      <a href="/ajustes/solicitudes" class="nav-item flex items-center justify-between">
        <span class="flex items-center gap-2">
          <svg>...</svg>
          <span>Solicitudes</span>
        </span>
        <span hx-get="/api/approvals/count" hx-trigger="load, every 30s" hx-swap="innerHTML"></span>
      </a>
      {% endif %}
      ```
    - El `hx-get` renderiza el fragment que devuelve /api/approvals/count (vacío o `<span class="ml-2 text-xs bg-yellow-400 ...">(3)</span>`).
    - Verificar que HTMX está cargado en base.html (sí, line 8).
  </behavior>
  <action>
    Crear templates/ajustes_solicitudes.html y templates/mis_solicitudes.html per behavior. Editar templates/ajustes.html añadiendo card "Solicitudes" protegida con {% if propietario %}. Editar templates/base.html insertando el nav item "Solicitudes" con HTMX badge en el bloque de nav propietario-only.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && grep -q "hx-get=\"/api/approvals/count\"" templates/base.html && grep -q "{% extends \"base.html\" %}" templates/ajustes_solicitudes.html && grep -q "{% extends \"base.html\" %}" templates/mis_solicitudes.html && grep -q "/ajustes/solicitudes" templates/ajustes.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - templates/ajustes_solicitudes.html existe, extiende base, contiene pending + histórico tables.
    - templates/mis_solicitudes.html existe, contiene tabla propia + botón Cancelar en pending.
    - templates/ajustes.html contiene link a /ajustes/solicitudes dentro de bloque {% if propietario %}.
    - templates/base.html contiene item "Solicitudes" con hx-get /api/approvals/count + hx-trigger every 30s.
    - Smoke browser: login propietario + ir a /ajustes/solicitudes -> página renderiza con tabla pending vacía (si ninguna solicitud) + histórico vacío.
    - Smoke browser: login usuario + ir a /mis-solicitudes -> página renderiza (vacía en primera visita).
    - Smoke browser: propietario ve "Solicitudes" en sidebar; usuario NO ve el item (permission bypass hardcoded; el {% if %} oculta el link para no-propietarios).
    - `grep "approval_id={{" templates/mis_solicitudes.html` -> coincide (PC-04-02: link "Ejecutar ahora" con ?approval_id=... es REQUERIDO).
    - `grep "Ejecutar ahora" templates/mis_solicitudes.html` -> coincide (texto literal del link visible para filas approved+consumed_at IS NULL).
  </acceptance_criteria>
  <done>
    Templates completos. Badge sidebar operativo (aunque sin solicitudes pendientes renderiza span vacío). Hub /ajustes muestra acceso a solicitudes para propietario.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create cleanup_scheduler + approvals_cleanup + wire in lifespan</name>
  <read_first>
    - api/main.py (lifespan — extender con asyncio.create_task + cancel en finally)
    - nexo/services/approvals.py (expire_stale)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 6 (Cleanup Scheduler lines 1133-1193) — usar asyncio.sleep + next Monday calc
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 477-503
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-14 (Monday 03:05 cada semana + NEXO_APPROVAL_TTL_DAYS=7)
  </read_first>
  <behavior>
    nexo/services/cleanup_scheduler.py:
    - `from __future__ import annotations`. imports: asyncio, logging, from datetime import datetime, time, timedelta, timezone.
    - `log = logging.getLogger("nexo.cleanup_scheduler")`.
    - Helper `_seconds_until(target_time: time, *, dow: int | None = None) -> float`:
      - now_utc. target = now.replace(hour, minute, second=0, microsecond=0). If dow given: days_ahead = (dow - now.weekday()) % 7; if days_ahead == 0 and target <= now: days_ahead = 7; target += timedelta(days=days_ahead). Else: if target <= now: target += timedelta(days=1). Return (target - now).total_seconds().
    - `async def cleanup_loop() -> None`:
      - Loop forever:
        1. Calcular seconds_until cada job registrado (en este plan sólo approvals cleanup Monday 03:05).
        2. Esperar el menor con asyncio.sleep (cancellable).
        3. Disparar el job que corresponda (tolerancia ±1 min).
        4. Repetir.
      - Usa try/except asyncio.CancelledError para shutdown limpio; excepciones de jobs se loggean y el loop continúa.
      - Jobs registrados:
        - approvals_cleanup Monday 03:05.
      - Plan 04-04 extenderá este archivo con query_log_cleanup (Monday 03:00) y factor_auto_refresh (1er Monday del mes 03:10).

    nexo/services/approvals_cleanup.py:
    - `def run_once(db_factory) -> int`: opens session via db_factory; ttl_days = int(os.environ.get('NEXO_APPROVAL_TTL_DAYS', '7')); n = approvals.expire_stale(db, ttl_days); log.info("approvals_cleanup: %d solicitudes expiradas", n); grabar en audit_log via AuditRepo.append(user_id=None, ip='127.0.0.1', method='DELETE', path='__cleanup_approvals__', status=200, details_json=json.dumps({"rows_expired": n, "cutoff_ts": ..., "ttl_days": ttl_days})); db.commit(); close; return n.
    - `async def run_loop()`: wrapper simple; calls run_once inside asyncio.to_thread (para no bloquear event loop). — ALTERNATIVA: este file expone run_once (sync) y cleanup_scheduler.py lo llama directamente via asyncio.to_thread. Decisión: exportar SOLO run_once desde este archivo; el scheduler lo wrappea. Deja run_loop como alias trivial en caso de que scheduler tenga que llamarlo en background standalone.

    Wire en api/main.py lifespan:
    - Añadir imports en la cabecera: from nexo.services.cleanup_scheduler import cleanup_loop.
    - En lifespan, después de schema_guard.verify y init_db:
      ```python
      cleanup_task = asyncio.create_task(cleanup_loop())
      ```
    - En finally (tras yield):
      ```python
      cleanup_task.cancel()
      try:
          await cleanup_task
      except (asyncio.CancelledError, Exception):
          pass
      ```

    Añadir env var a .env.example (sección Phase 4):
    ```
    # Approval TTL — tras N días sin approve/reject/cancel, marca expired (D-14).
    NEXO_APPROVAL_TTL_DAYS=7
    ```
  </behavior>
  <action>
    Crear nexo/services/cleanup_scheduler.py y nexo/services/approvals_cleanup.py per behavior. Editar api/main.py lifespan con create_task + cancel. Añadir NEXO_APPROVAL_TTL_DAYS a .env.example.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.services.cleanup_scheduler import cleanup_loop; from nexo.services.approvals_cleanup import run_once; import inspect; assert inspect.iscoroutinefunction(cleanup_loop); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - from nexo.services.cleanup_scheduler import cleanup_loop OK; cleanup_loop es coroutine.
    - from nexo.services.approvals_cleanup import run_once OK.
    - grep "cleanup_loop" api/main.py coincide (registrado en lifespan).
    - grep "cleanup_task.cancel" api/main.py coincide (shutdown limpio).
    - grep "NEXO_APPROVAL_TTL_DAYS" .env.example coincide.
    - docker compose up web arranca sin error; logs muestran "cleanup_scheduler next run in Xs".
    - Smoke manual: crear una solicitud con created_at artificial de hace 8 días (SQL directo); triggear run_once manualmente (pytest or python -c): status cambia a 'expired' + expired_at poblado + audit_log tiene fila de path=__cleanup_approvals__.
  </acceptance_criteria>
  <done>
    Scheduler asyncio arranca en lifespan; un job (approvals_cleanup) registrado. Monday 03:05 expirará pending >7d. Shutdown cancela tasks limpiamente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement tests services/test_approvals.py + routers/test_approvals_api.py (convertir Wave 0 stubs en verdes)</name>
  <read_first>
    - tests/services/test_approvals.py (stub de 04-01)
    - tests/routers/test_approvals_api.py (stub de 04-01)
    - tests/auth/test_rbac_smoke.py (helpers _create_test_user/_login + TestClient + cookie)
    - tests/data/conftest.py (_postgres_reachable, db_nexo fixture)
    - nexo/services/approvals.py
    - api/routers/approvals.py
    - .planning/phases/04-consultas-pesadas/04-VALIDATION.md Wave 0 list
  </read_first>
  <behavior>
    tests/services/test_approvals.py — convertir skip en tests reales (pytestmark integration + skipif Postgres):
    - test_create_sets_status_pending: svc.create_approval(...) devuelve id entero; fetch ORM row; assert row.status == 'pending', created_at is not None.
    - test_create_sets_ttl_days_default_7: sin ttl_days -> row.ttl_days == 7. Con ttl_days=3 -> row.ttl_days == 3.
    - test_consume_success_marks_consumed: crear approval, repo.approve manual, call consume con mismos params -> devuelve row con consumed_at is not None + status='consumed'.
    - test_consume_race_second_call_returns_403: crear approved approval; primera consume OK; segunda consume mismos params -> HTTPException(403, "Approval ya fue consumido").
    - test_consume_wrong_user_returns_403: approval para user A, consume llamado con user B -> HTTPException(403, "otro usuario").
    - test_consume_wrong_status_returns_403: status='pending' (no approved), consume -> HTTPException(403, "está en estado pending").
    - test_consume_params_tamper_returns_403: crear approved con params={'a':1}; consume con params={'a':2} -> HTTPException(403, "Parámetros cambiaron").
    - test_cancel_own_pending_returns_true: svc.cancel(db, approval_id, owner_user_id) -> True; row.status=='cancelled'.
    - test_cancel_others_returns_false: svc.cancel(db, approval_id, other_user_id) -> False; row.status sigue 'pending'.
    - test_expire_stale_moves_pending_to_expired: crear 3 approvals con created_at artificial: -1d, -5d, -10d. Llamar expire_stale(db, ttl_days=7) -> devuelve 1; row de -10d tiene status='expired', expired_at poblado; rows -1d/-5d siguen pending.

    tests/routers/test_approvals_api.py — convertir skip en contract tests (integration + Postgres):
    - Fixtures: TestClient, _create_test_user (propietario + user), _login.
    - test_create_approval_pending: login usuario; POST /api/approvals {endpoint, params, estimated_ms} -> 200 + body.approval_id integer; verificar en DB fila pending propia del user.
    - test_propietario_approve_flow: usuario crea approval; propietario logea; POST /api/approvals/{id}/approve -> 303 redirect; DB row.status=='approved' + approved_by=propietario.id.
    - test_propietario_reject_flow: análogo pero /reject -> status=rejected.
    - test_user_cancel_own_pending: usuario crea approval; POST /api/approvals/{id}/cancel (mismo user) -> 303; DB status=cancelled.
    - test_user_cannot_cancel_others: userA crea; userB POST /cancel -> 403.
    - test_count_badge_returns_html_fragment_empty: propietario GET /api/approvals/count sin pendientes -> 200 + body="".
    - test_count_badge_returns_html_fragment_with_count: crear 3 pending; propietario GET /count -> body contiene "(3)".
    - test_non_propietario_cannot_see_count: usuario no-propietario GET /count -> 403.
    - test_mis_solicitudes_shows_only_own: userA y userB ambos crean; userA GET /mis-solicitudes -> solo ve las suyas.
    - test_ajustes_solicitudes_is_propietario_only: usuario GET /ajustes/solicitudes -> 403; propietario -> 200.

    Re-export / adjust fixtures según necesidad. Usar @pytest.mark.asyncio si algún test es async (TestClient es sync — no necesario).

    **Sub-task 5.1 (PC-04-07): Flip xfail en test_preflight_endpoints.py**
    - Editar `tests/routers/test_preflight_endpoints.py` y ELIMINAR el decorador `@pytest.mark.xfail(reason="Requires approval flow from Plan 04-03")` que cubre `test_run_red_with_valid_approval_executes` (introducido por Plan 04-02 Task 6). Ahora que `consume_approval` existe (Task 1 de este plan), ese test debe ejecutarse y pasar en verde.
    - Verificar que el test pasa tras remover el marker:
      ```
      pytest tests/routers/test_preflight_endpoints.py::test_run_red_with_valid_approval_executes -x
      ```
    - Si el test no pasa, diagnosticar la integración `/api/pipeline/run` (force=True + approval_id) → consume_approval → ejecutar pipeline. NO reintroducir el xfail sin escalar.
  </behavior>
  <action>
    Editar tests/services/test_approvals.py y tests/routers/test_approvals_api.py para convertir stubs en tests funcionales per behavior. Ejecutar con Postgres up + docker compose web + init_nexo_schema.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -m pytest tests/services/test_approvals.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/services/test_approvals.py tiene al menos 10 funciones test_*.
    - tests/routers/test_approvals_api.py tiene al menos 10 funciones test_*.
    - pytest tests/services/test_approvals.py -x (con Postgres up) -> green.
    - pytest tests/routers/test_approvals_api.py -x (con Postgres up + web running) -> green.
    - Sin regresión: pytest tests/auth/ tests/data/ -x -> green.
    - **PC-04-07:** `grep -c "@pytest.mark.xfail.*test_run_red_with_valid_approval_executes" tests/routers/test_preflight_endpoints.py` → `0` (marker removido).
    - **PC-04-07:** `pytest tests/routers/test_preflight_endpoints.py::test_run_red_with_valid_approval_executes -x` → PASSED (no longer xfail).
  </acceptance_criteria>
  <done>
    Wave 0 stubs convertidos en cobertura real; CAS race + params tampering + ownership + cleanup expire_stale todos verificados. El xfail marker de `test_run_red_with_valid_approval_executes` (introducido por 04-02 Task 6) ha sido removido y el test ahora pasa en verde (PC-04-07).
  </done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (MANUAL SMOKE): User cancel flow E2E + badge propietario no cuenta cancelled</name>
  <what-built>
    Plan 04-03 entregó aprobaciones end-to-end: service + router + templates + badge HTMX + scheduler cleanup. Tests automáticos cubren contract-level, pero el flujo inter-usuario (usuario dispara → solicita → propietario ve badge → usuario cancela → badge baja) requiere inspección ocular en dos sesiones simultáneas (2 navegadores o ventana incógnito).
  </what-built>
  <how-to-verify>
    1. **Setup dos sesiones:**
       - Ventana A: login usuario normal (rol `usuario`).
       - Ventana B (incógnito): login propietario (rol `propietario`).
    2. **Disparar red + solicitar aprobación (Ventana A):**
       - Navegar a /bbdd. Introducir una query SQL que fuerce level red (editar threshold warn/block_ms bajo primero desde /ajustes/limites, o usar una query conocida pesada).
       - Click "Ejecutar consulta" → modal rojo aparece con [Solicitar aprobación] y [Cancelar].
       - Click [Solicitar aprobación] → toast "Solicitud enviada. Ver en Mis Solicitudes (#N)." + link.
    3. **Ver solicitud propia (Ventana A):**
       - Navegar a /mis-solicitudes → aparece fila con status=pending, el endpoint `bbdd/query`, estimated_ms, y botón [Cancelar].
    4. **Verificar badge propietario (Ventana B):**
       - Esperar ≤30s (HTMX polling) o recargar /ajustes → sidebar "Solicitudes" muestra badge "(1)".
       - Navegar a /ajustes/solicitudes → fila pending visible con datos del usuario de Ventana A.
    5. **Usuario cancela (Ventana A):**
       - En /mis-solicitudes, click [Cancelar] → confirmación → POST /api/approvals/{id}/cancel → redirect con `?ok=cancelled` → fila pasa a status=cancelled (badge gris).
    6. **Verificar badge baja (Ventana B):**
       - Esperar ≤30s → sidebar badge desaparece (count=0 → HTML fragment vacío) O cambia a count menor si había otras pendientes.
       - /ajustes/solicitudes → fila cancelled aparece en sección "Histórico (30d)" con badge gris; NO aparece en "Pendientes".
    7. **Verificar ownership enforcement (negative):**
       - En Ventana B (propietario), intentar POST manual `/api/approvals/{id}/cancel` con cookie propietario sobre una solicitud de Ventana A aún pending (si creas otra primero). Esperado: 403 "No puedes cancelar esta solicitud (no eres el dueño o no está pending)".
    8. **Verificar scheduler arranca:**
       - `docker compose logs web | grep "cleanup_scheduler next run"` → muestra Xs hasta Monday 03:05 o posterior.
  </how-to-verify>
  <resume-signal>Reanudar con "cancel-flow-ok + badge-ok + ownership-ok" o describir desviaciones (badge no actualizó en 30s, cancel sin redirect 303, fila cancelled no aparece en histórico, 403 incorrecto).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> POST /api/approvals | Cualquier user autenticado puede crear solicitud para sí mismo. Sin rate-limit en Mark-III (aceptado — preflight+login-limit cubren DoS). |
| Browser -> POST /api/approvals/{id}/cancel | Ownership check server-side (svc.cancel rechaza si user.id != approval.user_id). |
| Browser (propietario) -> POST /api/approvals/{id}/approve|reject | require_permission("aprobaciones:manage") bloquea no-propietario. |
| pipeline.run/bbdd.query/capacidad/operarios -> consume_approval | CAS atómico; si params cambian entre solicitud y consumo -> 403. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | POST /api/approvals/{id}/approve | mitigate | require_permission("aprobaciones:manage") + session cookie (Phase 2). No bypass. |
| T-04-03-02 | Tampering | Approval params_json between create+consume | mitigate | consume_approval equality check en CAS SQL (params_json = :pj). Error 403 explícito. |
| T-04-03-03 | Repudiation | Quién aprobó/rechazó | mitigate | approved_by/rejected_by columnas pobladas + audit_middleware loggea POST /api/approvals/* con user_id. |
| T-04-03-04 | Information Disclosure | /mis-solicitudes | mitigate | Filtrado server-side WHERE user_id = current_user; no hay endpoint público que lista todas. |
| T-04-03-05 | DoS | Spam de POST /api/approvals | accept | LAN-only, ~20 users. Low risk. Mark-IV podría añadir slowapi rate-limit. |
| T-04-03-06 | Elevation of Privilege | Usuario aprueba su propia solicitud | mitigate | approve endpoint requiere aprobaciones:manage (propietario-only via empty-list bypass). |
| T-04-03-07 | Race (double-consume) | consume_approval | mitigate | UPDATE ... WHERE consumed_at IS NULL RETURNING id (atomic). Tested via integration test. |
</threat_model>

<verification>
- python smoke: from nexo.services.approvals import create_approval, consume_approval, approve, reject, cancel, expire_stale -> OK.
- docker compose up web -> logs muestran "cleanup_scheduler next run in Xs" + schema_guard OK.
- curl con cookie usuario: POST /api/approvals {endpoint: "pipeline/run", params: {...}, estimated_ms: 700000} -> 200 + approval_id.
- psql: SELECT id, user_id, status, created_at FROM nexo.query_approvals -> filas visibles.
- curl con cookie propietario: GET /api/approvals/count -> HTML fragment.
- curl con cookie propietario: POST /api/approvals/{id}/approve -> 303 redirect.
- curl con cookie usuario original: /api/pipeline/run con force=true + approval_id (via browser) -> pipeline corre (requiere que 04-02 haya mergeado).
- pytest tests/services/test_approvals.py -x -> green.
- pytest tests/routers/test_approvals_api.py -x -> green.
- Browser smoke: propietario ve badge "(N)" en sidebar actualizado cada 30s; usuario ve /mis-solicitudes con solo sus filas.
</verification>

<success_criteria>
1. Tabla nexo.query_approvals se usa vía 6 funciones service + 7 endpoints router.
2. CAS atómico previene double-consume; params tampering rechazado con mensaje claro.
3. Ownership enforcement: /cancel solo permite al dueño sobre filas pending.
4. propietario-only: /approve, /reject, /count, /ajustes/solicitudes bloqueados a no-propietario.
5. Self-service: POST /api/approvals y /mis-solicitudes acceden cualquier user auth.
6. Badge sidebar refresh cada 30s vía HTMX.
7. TTL 7d aplicado por job Monday 03:05.
8. Lifespan arranca cleanup_scheduler asyncio.create_task; shutdown cancela limpiamente.
9. Tests services + routers verdes (~20 tests nuevos).
10. Integration con 04-02: import consume_approval activa _APPROVALS_AVAILABLE=True en pipeline.py + bbdd.py + capacidad.py + operarios.py.
</success_criteria>

<output>
Crear .planning/phases/04-consultas-pesadas/04-03-SUMMARY.md documentando:
- 6 funciones service + 7 endpoints router + 2 templates + 1 badge + 1 job + 1 scheduler.
- Decisiones implementadas (D-06, D-13, D-14, D-15, D-16) con test IDs.
- Landmines documentados (CAS race resuelto, sin email D-13).
- Integración con 04-02 verificada (_APPROVALS_AVAILABLE=True).
- Commits landed (~5-6 commits).
</output>
