---
phase: 04-consultas-pesadas
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [QUERY-01, QUERY-02]
tags: [nexo, phase-4, foundation, schema, orm, postgres, wave-0-tests]
files_modified:
  - nexo/data/models_nexo.py
  - nexo/data/dto/query.py
  - nexo/data/dto/__init__.py
  - nexo/data/repositories/nexo.py
  - nexo/data/schema_guard.py
  - nexo/services/thresholds_cache.py
  - nexo/services/__init__.py
  - api/models.py
  - scripts/init_nexo_schema.py
  - tests/services/__init__.py
  - tests/services/test_preflight.py
  - tests/services/test_thresholds_cache.py
  - tests/services/test_approvals.py
  - tests/services/test_pipeline_lock.py
  - tests/middleware/__init__.py
  - tests/middleware/test_query_timing.py
  - tests/routers/__init__.py
  - tests/routers/test_preflight_endpoints.py
  - tests/routers/test_approvals_api.py
  - tests/data/test_schema_query_log.py
  - tests/data/test_schema_guard_extended.py
  - requirements-dev.txt

must_haves:
  truths:
    - "Tabla nexo.query_log existe con columnas (id, ts, user_id, endpoint, params_json, estimated_ms, actual_ms, rows, status, approval_id, ip) y los 4 índices prescritos"
    - "Tabla nexo.query_thresholds existe con PK endpoint y columnas (warn_ms, block_ms, factor_ms, factor_updated_at, updated_at, updated_by)"
    - "Tabla nexo.query_approvals existe con state machine completa (pending/approved/rejected/cancelled/expired/consumed) y los índices prescritos"
    - "schema_guard.CRITICAL_TABLES incluye las 3 tablas nuevas y el arranque falla si alguna falta"
    - "scripts/init_nexo_schema.py siembra 4 filas por defecto en query_thresholds (pipeline/run, bbdd/query, capacidad, operarios) con umbrales D-01/D-02/D-03"
    - "thresholds_cache.get('pipeline/run') devuelve ThresholdEntry válido tras full_reload()"
    - "Los 10 archivos de test (Wave 0) existen con stubs xfail/skip que documentan los contratos; el suite pytest pasa (sin verdes falsos — stubs marcados skip/xfail hasta que llegue la implementación)"
  artifacts:
    - path: "nexo/data/models_nexo.py"
      provides: "3 nuevas ORM classes: NexoQueryLog, NexoQueryThreshold, NexoQueryApproval"
      contains: "class NexoQueryLog"
    - path: "nexo/data/dto/query.py"
      provides: "Row DTOs + Estimation response model"
      exports: ["QueryLogRow", "QueryThresholdRow", "QueryApprovalRow", "Estimation"]
    - path: "nexo/data/repositories/nexo.py"
      provides: "QueryLogRepo + ThresholdRepo + ApprovalRepo (skeleton methods; ApprovalRepo incl. list_recent_non_pending for histórico 30d)"
      contains: "class QueryLogRepo"
    - path: "nexo/data/schema_guard.py"
      provides: "CRITICAL_TABLES extendido 8 → 11"
      contains: "\"query_log\""
    - path: "nexo/services/thresholds_cache.py"
      provides: "In-memory dict + full_reload + get (SIN listen loop todavía — eso va en 04-04)"
      exports: ["get", "full_reload", "reload_one", "ThresholdEntry"]
    - path: "api/models.py"
      provides: "Estimation Pydantic response model + PipelineRequest extended con force/approval_id"
      contains: "class Estimation"
    - path: "scripts/init_nexo_schema.py"
      provides: "Seed de 4 thresholds por defecto + create_all de 3 tablas nuevas"
      contains: "query_thresholds"
    - path: "tests/services/test_preflight.py"
      provides: "Wave 0 stubs para estimate_cost (xfail hasta Plan 04-02)"
    - path: "tests/services/test_thresholds_cache.py"
      provides: "Wave 0 stubs para cache roundtrip (parte xfail hasta 04-04 LISTEN)"
    - path: "tests/data/test_schema_query_log.py"
      provides: "Tests verdes en 04-01: tablas existen, columnas, índices"
  key_links:
    - from: "nexo/data/schema_guard.py"
      to: "nexo/data/models_nexo.py (NexoQueryLog, NexoQueryThreshold, NexoQueryApproval)"
      via: "CRITICAL_TABLES tuple"
      pattern: "query_log.*query_thresholds.*query_approvals"
    - from: "scripts/init_nexo_schema.py"
      to: "nexo.query_thresholds"
      via: "INSERT INTO ... ON CONFLICT (endpoint) DO NOTHING"
      pattern: "pipeline/run.*120000.*600000"
    - from: "nexo/services/thresholds_cache.py"
      to: "nexo/data/repositories/nexo.py (ThresholdRepo)"
      via: "repo.list_all() dentro de full_reload"
      pattern: "ThresholdRepo.*list_all"
---

<objective>
Foundation bloqueante para toda Phase 4: crea las 3 tablas Postgres (`query_log`, `query_thresholds`, `query_approvals`) con sus ORM models + DTOs + repositorios + extensión de schema_guard + seeds iniciales + el cache skeleton + TODOS los test stubs de Wave 0.

Por qué PRIMERO: los Plans 04-02 (preflight+middleware), 04-03 (approvals) y 04-04 (observability UI) consumen estos modelos/repos/tablas. Sin este plan, el resto no arranca.

Decisiones de CONTEXT.md implementadas: D-01 (umbrales pipeline 120k/600k), D-02 (umbrales bbdd 3k/30k), D-03 (umbrales capacidad/operarios iguales a bbdd), D-04 (factor seed 2000ms), D-10 (retention infra — la columna status habilita el cleanup de 04-04), D-14 (TTL approval — columnas ttl_days/created_at/expired_at), D-15 (CAS semantics — columnas consumed_at/consumed_run_id), D-16 (cancelación usuario — columna cancelled_at), D-19 (cache skeleton; listen loop diferido a 04-04).

Output: schema Postgres + capa ORM operativa + thresholds cache básico + 10 archivos Wave 0 de tests listos para que los planes siguientes implementen contra ellos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-consultas-pesadas/04-CONTEXT.md
@.planning/phases/04-consultas-pesadas/04-RESEARCH.md
@.planning/phases/04-consultas-pesadas/04-PATTERNS.md
@.planning/phases/04-consultas-pesadas/04-VALIDATION.md
@.planning/phases/03-capa-de-datos/03-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos existentes que este plan consume o extiende. Extraídos del codebase. -->
<!-- El executor NO necesita explorar: todo está aquí. -->

From nexo/data/models_nexo.py (existing — this plan APPENDS to it):
```python
NEXO_SCHEMA = "nexo"

class NexoBase(DeclarativeBase): ...

def _utcnow() -> datetime:
    return datetime.now(timezone.utc)

# Existing tables (do NOT modify; just add 3 new classes after them):
class NexoUser(NexoBase): ...          # nexo.users (PK id Integer)
class NexoRole(NexoBase): ...
class NexoSession(NexoBase): ...
class NexoLoginAttempt(NexoBase): ...
class NexoAuditLog(NexoBase):
    __tablename__ = "audit_log"
    __table_args__ = {"schema": NEXO_SCHEMA}
    id = Column(Integer, primary_key=True)
    ts = Column(DateTime(timezone=True), nullable=False, default=_utcnow, index=True)
    user_id = Column(Integer, ForeignKey("nexo.users.id", ondelete="SET NULL"), nullable=True)
    ip = Column(String(64), nullable=False)
    method = Column(String(10), nullable=False)
    path = Column(String(500), nullable=False)
    status = Column(Integer, nullable=False)
    details_json = Column(Text, nullable=True)

__all__ = [..., "NexoAuditLog"]  # extend with 3 new names
```

From nexo/data/dto/base.py:
```python
ROW_CONFIG: ConfigDict = ConfigDict(frozen=True, from_attributes=True)
```

From nexo/data/repositories/nexo.py (AuditRepo is the canonical analog for QueryLogRepo):
```python
class AuditRepo:
    def __init__(self, db: Session):
        self._db = db

    def append(self, *, user_id, ip, method, path, status, details_json) -> None:
        """No commit here. Caller orchestrates."""
        self._db.add(NexoAuditLog(...))
    # NOTE: contract test_audit_repo_append_source_has_no_commit asserts
    # that ".commit()" NOT in inspect.getsource(append). Same rule for QueryLogRepo.append.

    def list_filtered(self, *, ..., page, limit) -> list[AuditLogRow]: ...
    def count_filtered(self, *, ...) -> int: ...
    def iter_filtered(self, *, ...): ...  # yield_per=500
```

From nexo/data/schema_guard.py (line 33-42 — extend this):
```python
CRITICAL_TABLES: tuple[str, ...] = (
    "users", "roles", "departments", "user_departments",
    "permissions", "sessions", "login_attempts", "audit_log",
)  # EXTEND to 11 — append query_log, query_thresholds, query_approvals
```

From api/models.py (PipelineRequest existing — extend with 2 fields):
```python
class PipelineRequest(BaseModel):
    fecha_inicio: date
    fecha_fin: date
    modulos: list[str] = ...
    recursos: Optional[list[int]] = None
    source: str = "csv"
    # ADD: force: bool = False
    # ADD: approval_id: Optional[int] = None
```

From scripts/init_nexo_schema.py (existing — extend seed block). Pattern for seeding:
```python
# After create_all, insert seed rows with ON CONFLICT DO NOTHING for idempotency.
```

From requirements-dev.txt (existing):
```
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
# ADD: pytest-asyncio==0.24.0   # needed for test_pipeline_lock.py
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 ORM models to nexo/data/models_nexo.py</name>
  <read_first>
    - nexo/data/models_nexo.py (append; current file has NexoAuditLog at ~lines 169-180 and NexoLoginAttempt at ~lines 154-164 — use these as analog shapes)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md (D-14, D-15, D-16 define columns for query_approvals)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Pattern 4 Schema ORM (lines 783-849 show exact Column definitions including index names)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 85-161 (adaptation notes for the 3 tables)
  </read_first>
  <behavior>
    - Import Float from sqlalchemy (existing imports only bring Integer/String — add Float).
    - Add `NexoQueryLog` class after `NexoAuditLog` with columns: id(Integer PK), ts(DateTime tz=True default=_utcnow), user_id(FK nexo.users.id nullable, SET NULL), endpoint(String(100) NOT NULL), params_json(Text nullable), estimated_ms(Integer nullable), actual_ms(Integer NOT NULL), rows(Integer nullable), status(String(20) NOT NULL), approval_id(FK nexo.query_approvals.id nullable, SET NULL), ip(String(64) nullable). `__table_args__` = tuple with 4 named Indexes (`ix_query_log_ts` on ts, `ix_query_log_endpoint_ts` on (endpoint, ts), `ix_query_log_user_ts` on (user_id, ts), `ix_query_log_status_slow` partial Index on ts where status='slow' via `postgresql_where=text("status = 'slow'")`) + `{"schema": NEXO_SCHEMA}`.
    - Add `NexoQueryThreshold` class with PK `endpoint(String(100) primary_key=True)`, warn_ms(Integer NOT NULL), block_ms(Integer NOT NULL), factor_ms(Float nullable), updated_at(DateTime tz=True default=_utcnow), updated_by(FK users.id SET NULL nullable), factor_updated_at(DateTime tz=True nullable). `__table_args__` = `{"schema": NEXO_SCHEMA}` (no composite indices — PK suffices for a 4-row lookup table).
    - Add `NexoQueryApproval` class with columns: id(Integer PK), user_id(FK users.id CASCADE NOT NULL), endpoint(String(100) NOT NULL), params_json(Text NOT NULL), estimated_ms(Integer NOT NULL), status(String(20) default='pending' NOT NULL), created_at(DateTime tz=True default=_utcnow), ttl_days(Integer default=7 NOT NULL), approved_at(DateTime tz=True nullable), approved_by(FK users.id SET NULL nullable), rejected_at(DateTime tz=True nullable), cancelled_at(DateTime tz=True nullable), expired_at(DateTime tz=True nullable), consumed_at(DateTime tz=True nullable), consumed_run_id(Integer nullable — soft FK to query_log.id, NOT declared as ForeignKey to avoid circular). `__table_args__` with 3 named Indexes (`ix_approvals_status` on status, `ix_approvals_user_status` on (user_id, status), `ix_approvals_created_at` on created_at) + `{"schema": NEXO_SCHEMA}`.
    - Append the 3 class names to the module `__all__` list.
    - Use `default=_utcnow` (callable), never `default=_utcnow()` (called once at import). Never `datetime.utcnow()`.
    - Define classes in the order: NexoQueryApproval FIRST, then NexoQueryThreshold, then NexoQueryLog. Rationale: NexoQueryLog has `approval_id` FK to query_approvals, so query_approvals must be declared before query_log for ORM resolver clarity (SQLAlchemy accepts string references so order is technically irrelevant, but declaration-order matches dependency).
  </behavior>
  <action>
    Open nexo/data/models_nexo.py. Add `Float` to the sqlalchemy imports. At the end of the file (after NexoAuditLog and before the `__all__`), add the 3 classes per the behavior block. Update `__all__` to append `"NexoQueryApproval"`, `"NexoQueryThreshold"`, `"NexoQueryLog"` in that order. Do NOT create any Alembic migrations — Phase 4 relies on `NEXO_AUTO_MIGRATE=true` + `create_all()` via scripts/init_nexo_schema.py (same pattern as Phase 2).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.data.models_nexo import NexoQueryLog, NexoQueryThreshold, NexoQueryApproval; assert NexoQueryLog.__tablename__ == 'query_log'; assert NexoQueryThreshold.__tablename__ == 'query_thresholds'; assert NexoQueryApproval.__tablename__ == 'query_approvals'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import of the 3 new classes succeeds (`python -c "from nexo.data.models_nexo import NexoQueryLog, NexoQueryThreshold, NexoQueryApproval"`).
    - Each class has `__table_args__` containing `"schema": "nexo"`.
    - NexoQueryLog has 4 named indices (`grep -c "ix_query_log" nexo/data/models_nexo.py` == 4).
    - NexoQueryApproval has 3 named indices (`grep -c "ix_approvals" nexo/data/models_nexo.py` == 3).
    - `grep -q "datetime.utcnow()" nexo/data/models_nexo.py` returns false (no deprecated utcnow).
    - `grep -q "from sqlalchemy import.*Float" nexo/data/models_nexo.py` returns true.
    - `__all__` contains the 3 new class names (`python -c "from nexo.data.models_nexo import __all__; assert 'NexoQueryLog' in __all__ and 'NexoQueryThreshold' in __all__ and 'NexoQueryApproval' in __all__"`).
  </acceptance_criteria>
  <done>
    The 3 classes import cleanly, have correct table names, schema, indexes, and are exported via `__all__`. No runtime DB interaction yet — that happens in Task 5 (schema_guard + init_nexo_schema).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DTOs (QueryLogRow, QueryThresholdRow, QueryApprovalRow, Estimation) + extend api/models.py</name>
  <read_first>
    - nexo/data/dto/nexo.py (existing analog — use same Row shape with ROW_CONFIG)
    - nexo/data/dto/base.py (ROW_CONFIG definition lines 17-24)
    - nexo/data/dto/__init__.py (re-exports; extend)
    - api/models.py (PipelineRequest currently lives here — extend with force/approval_id)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 165-200 (DTO adaptation notes)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md lines 535-545 (Estimation dataclass — but we use Pydantic to match codebase convention)
  </read_first>
  <behavior>
    - Create nexo/data/dto/query.py with imports: `from datetime import datetime`, `from typing import Literal, Optional`, `from pydantic import BaseModel`, `from nexo.data.dto.base import ROW_CONFIG`.
    - Define `QueryLogRow(BaseModel)` with `model_config = ROW_CONFIG` and fields: `id: int`, `ts: datetime`, `user_id: Optional[int]`, `endpoint: str`, `params_json: Optional[str]`, `estimated_ms: Optional[int]`, `actual_ms: int`, `rows: Optional[int]`, `status: str`, `approval_id: Optional[int]`, `ip: Optional[str]`.
    - Define `QueryThresholdRow(BaseModel)` with `model_config = ROW_CONFIG` and fields: `endpoint: str`, `warn_ms: int`, `block_ms: int`, `factor_ms: Optional[float]`, `updated_at: datetime`, `updated_by: Optional[int]`, `factor_updated_at: Optional[datetime]`.
    - Define `QueryApprovalRow(BaseModel)` with `model_config = ROW_CONFIG` and fields mapping the full NexoQueryApproval (id, user_id, endpoint, params_json, estimated_ms, status, created_at, ttl_days, approved_at, approved_by, rejected_at, cancelled_at, expired_at, consumed_at, consumed_run_id). All date/id columns Optional where the model nullable.
    - Define `Estimation(BaseModel)` (response DTO for preflight, NOT a Row — still `frozen=True`): `endpoint: str`, `estimated_ms: int`, `level: Literal["green", "amber", "red"]`, `reason: str`, `breakdown: str`, `factor_used_ms: Optional[float]`, `warn_ms: int`, `block_ms: int`. Use `model_config = ConfigDict(frozen=True)` (NOT ROW_CONFIG — no `from_attributes` needed since there is no ORM backing row).
    - Export all 4 in `__all__` of the new file.
    - Update `nexo/data/dto/__init__.py` to re-export the 4 new names (follow the pattern already used for nexo.py/app.py re-exports).
    - In `api/models.py`, extend `PipelineRequest` by adding `force: bool = False` and `approval_id: Optional[int] = None` at the end of the class (preserve field order of existing fields).
  </behavior>
  <action>
    Write nexo/data/dto/query.py with the 4 classes per behavior. Update nexo/data/dto/__init__.py to expose `QueryLogRow, QueryThresholdRow, QueryApprovalRow, Estimation` following the existing re-export style. Edit api/models.py to append `force: bool = False` and `approval_id: Optional[int] = None` to PipelineRequest. Do NOT add `Estimation` to api/models.py — it lives canonically in nexo/data/dto/query.py and is imported where needed.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.data.dto.query import QueryLogRow, QueryThresholdRow, QueryApprovalRow, Estimation; e = Estimation(endpoint='pipeline/run', estimated_ms=5000, level='amber', reason='test', breakdown='x', factor_used_ms=2000.0, warn_ms=3000, block_ms=30000); assert e.level == 'amber'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from nexo.data.dto.query import QueryLogRow, QueryThresholdRow, QueryApprovalRow, Estimation` succeeds.
    - `Estimation(level='yellow', ...)` raises ValidationError (Literal enforced).
    - `Estimation(endpoint='x', estimated_ms=1, level='green', reason='r', breakdown='b', factor_used_ms=None, warn_ms=1, block_ms=2)` succeeds.
    - PipelineRequest accepts `force=True, approval_id=42` without error.
    - PipelineRequest default `force=False, approval_id=None` (no regression — existing tests pass).
    - `grep -q "force: bool = False" api/models.py` returns true.
  </acceptance_criteria>
  <done>
    DTOs are importable, frozen, typed correctly. Estimation can be constructed and serialized to JSON. PipelineRequest is backwards-compatible with existing callers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add QueryLogRepo + ThresholdRepo + ApprovalRepo to nexo/data/repositories/nexo.py</name>
  <read_first>
    - nexo/data/repositories/nexo.py (append; AuditRepo at ~lines 114-261 is the canonical analog — copy its shape exactly)
    - tests/data/test_nexo_repository.py (contract test pattern `test_audit_repo_append_source_has_no_commit` at lines 67-77 — must be replicated for QueryLogRepo.append)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 204-314 (detailed repo adaptation notes + CAS SQL)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md lines 894-938 (consume_approval canonical SQL)
  </read_first>
  <behavior>
    - All 3 repos take `db: Session` in constructor (match AuditRepo shape).
    - **QueryLogRepo:**
      - `append(*, user_id, ip, endpoint, params_json, estimated_ms, actual_ms, rows, status, approval_id) -> None`: `self._db.add(NexoQueryLog(...))`. NO `.commit()` / `.flush()` in source (contract test will assert this via `inspect.getsource`).
      - `list_filtered(*, endpoint=None, user_id=None, status=None, date_from=None, date_to=None, page=1, limit=100) -> list[QueryLogRow]`: mirror `AuditRepo.list_filtered` with `select(NexoQueryLog, NexoUser).outerjoin(NexoUser, NexoUser.id == NexoQueryLog.user_id)`, apply conditional where, `order_by(ts.desc()).limit(limit).offset((page-1)*limit)`, return list comprehension of `QueryLogRow.model_validate({...})`.
      - `count_filtered(*, ...)` / `iter_filtered(*, ..., yield_per=500)`: mirror AuditRepo trio.
      - `summary(endpoint: str, date_from: datetime, date_to: datetime) -> dict`: executes `SELECT COUNT(*) AS n_runs, AVG(estimated_ms) AS avg_est, AVG(actual_ms) AS avg_actual, percentile_cont(0.95) WITHIN GROUP (ORDER BY actual_ms) AS p95, SUM(CASE WHEN status='slow' THEN 1 ELSE 0 END) AS n_slow FROM nexo.query_log WHERE endpoint=:ep AND ts BETWEEN :df AND :dt` via `text()` with bindparams. Returns dict with those 5 keys plus a computed `divergence_pct = (avg_actual - avg_est) / avg_est * 100` (handle avg_est=None → 0).
    - **ThresholdRepo:**
      - `get(endpoint: str) -> QueryThresholdRow | None`: `self._db.query(NexoQueryThreshold).filter_by(endpoint=endpoint).first()` → `QueryThresholdRow.model_validate(row)` if found else None.
      - `list_all() -> list[QueryThresholdRow]`: full scan (only 4 rows expected) → list comprehension.
      - `update(*, endpoint, warn_ms, block_ms, factor_ms=None, updated_by, factor_touched=False) -> None`: fetch row; update fields; if `factor_touched=True` also set `factor_updated_at = _utcnow()`. Set `updated_at = _utcnow()`. DOES commit (consumer pattern: `usuarios.editar` commits inline — cf. research §Pattern on CRUD).
    - **ApprovalRepo:**
      - `create(*, user_id, endpoint, params_json, estimated_ms, ttl_days=7) -> NexoQueryApproval`: add + commit + refresh; returns the ORM row (NOT a DTO — caller may need `.id` back).
      - `get(approval_id: int) -> QueryApprovalRow | None`: fetch + model_validate.
      - `list_by_user(user_id: int, *, statuses=None) -> list[QueryApprovalRow]`: filter by user_id + optional status IN (...) + order by created_at desc.
      - `list_pending() -> list[QueryApprovalRow]`: `filter(status='pending').order_by(created_at desc)`.
      - `list_recent_non_pending(cutoff: datetime, limit: int = 100) -> list[QueryApprovalRow]`: `filter(NexoQueryApproval.status != 'pending', NexoQueryApproval.created_at >= cutoff).order_by(NexoQueryApproval.created_at.desc()).limit(limit)` → list of QueryApprovalRow. Consumed by Plan 04-03 for the histórico 30d table in /ajustes/solicitudes.
      - `count_pending() -> int`: scalar COUNT of pending.
      - `approve(approval_id, approved_by) -> None`: fetch + set status='approved', approved_at=_utcnow(), approved_by=... + commit.
      - `reject(approval_id, decided_by) -> None`: analogous with status='rejected', rejected_at.
      - `cancel(approval_id, user_id) -> bool`: fetch row; verify `row.user_id == user_id` (return False if mismatch); verify status is 'pending' (return False if not); set status='cancelled', cancelled_at. Commit. Return True on success.
      - `consume(*, approval_id, user_id, current_params_json) -> QueryApprovalRow | None`: **CAS atomic UPDATE ... WHERE ... RETURNING**. Use `text("UPDATE nexo.query_approvals SET consumed_at = (now() AT TIME ZONE 'UTC'), status = 'consumed' WHERE id = :id AND user_id = :uid AND status = 'approved' AND consumed_at IS NULL AND params_json = :pj RETURNING id, endpoint, params_json, estimated_ms, user_id, created_at, ttl_days, approved_at, approved_by, rejected_at, cancelled_at, expired_at, consumed_at, status")`. Execute with bindparams; if `scalar` is None → return None; else commit and refetch `QueryApprovalRow`.
      - `expire_stale(cutoff: datetime) -> int`: `UPDATE nexo.query_approvals SET status='expired', expired_at=now() WHERE status='pending' AND created_at < :cutoff RETURNING id` — return count via rowcount. Commit.
    - Import `NexoQueryLog, NexoQueryThreshold, NexoQueryApproval` at the top of the repository file (after existing imports). Import `QueryLogRow, QueryThresholdRow, QueryApprovalRow` from `nexo.data.dto.query`.
    - Do NOT add `_utcnow` locally — import it from `nexo.data.models_nexo` (already defined there at line ~51).
  </behavior>
  <action>
    Open nexo/data/repositories/nexo.py. After the existing `AuditRepo` class, add the 3 new repos exactly as specified. The critical `consume` method uses a single `text()` statement — this is the only place in the entire phase where raw SQL via `text()` is acceptable because SQLAlchemy's ORM does not expose UPDATE...RETURNING atomically. Export the 3 new classes at module bottom if `__all__` exists (otherwise skip).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "import inspect; from nexo.data.repositories.nexo import QueryLogRepo, ThresholdRepo, ApprovalRepo; src = inspect.getsource(QueryLogRepo.append); assert '.commit()' not in src and '.flush()' not in src; assert 'RETURNING id' in inspect.getsource(ApprovalRepo.consume); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import of QueryLogRepo, ThresholdRepo, ApprovalRepo succeeds.
    - `inspect.getsource(QueryLogRepo.append)` contains NO `.commit()` or `.flush()` (contract-test parity with AuditRepo).
    - `inspect.getsource(ApprovalRepo.consume)` contains `RETURNING` and `consumed_at IS NULL` (CAS invariants).
    - `inspect.getsource(ApprovalRepo.consume)` contains `params_json = :pj` or equivalent `:params_json` bindparam (params equality check per D-15 point 5).
    - `ThresholdRepo.list_all()` signature returns `list[QueryThresholdRow]` (inspect signature annotation).
    - No direct pyodbc imports, no SQL Server references (nexo.* is Postgres).
    - `inspect.getsource(ApprovalRepo.list_recent_non_pending)` exists and filters `status != 'pending'` + `created_at >= cutoff`.
  </acceptance_criteria>
  <done>
    Three new repos exist with skeleton methods matching AuditRepo discipline (append is commit-free; CRUD ops commit inline; CAS is atomic UPDATE...RETURNING). Implementations may be minimal for now — full behavior is exercised by Plans 04-02/03/04; this plan provides the contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend schema_guard + scripts/init_nexo_schema.py (tables + seeds)</name>
  <read_first>
    - nexo/data/schema_guard.py (CRITICAL_TABLES tuple at ~lines 33-42, verify() at ~lines 100+)
    - scripts/init_nexo_schema.py (existing — review the create_all block + any seed sections)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md (D-01, D-02, D-03, D-04 have the exact seed values)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 318-338 (schema_guard extension adaptation)
  </read_first>
  <behavior>
    - In nexo/data/schema_guard.py: extend the `CRITICAL_TABLES` tuple by appending `"query_log"`, `"query_thresholds"`, `"query_approvals"` in that exact order (post-existing 8). Tuple becomes 11 entries. NO other changes — the `verify()` signature and kwarg-injection behavior must remain intact (Phase 3 tests depend on it).
    - In scripts/init_nexo_schema.py: after the existing `Base.metadata.create_all(...)` (or equivalent call that materializes the ORM classes), add an idempotent seed block for `nexo.query_thresholds` with 4 rows:
      1. endpoint='pipeline/run', warn_ms=120000, block_ms=600000, factor_ms=2000.0 (D-01 + D-04 seed).
      2. endpoint='bbdd/query', warn_ms=3000, block_ms=30000, factor_ms=1000.0 (D-02; factor is baseline default for bbdd queries without EXPLAIN per RESEARCH §_estimate_bbdd fallback).
      3. endpoint='capacidad', warn_ms=3000, block_ms=30000, factor_ms=50.0 (D-03; factor=ms/día per RESEARCH §_estimate_rango default).
      4. endpoint='operarios', warn_ms=3000, block_ms=30000, factor_ms=50.0 (D-03; same factor default).
    - All seeds: `updated_at = now()`, `updated_by = NULL`, `factor_updated_at = NULL`. Use `ON CONFLICT (endpoint) DO NOTHING` to make re-runs idempotent.
    - Seeds MUST be done via SQLAlchemy text() with bindparams — NOT via ORM insert (avoids importing model ordering complexity in a script). Example:
      ```python
      from sqlalchemy import text
      seeds = [
          {"endpoint": "pipeline/run", "warn_ms": 120000, "block_ms": 600000, "factor_ms": 2000.0},
          ...
      ]
      for s in seeds:
          engine_nexo.execute(text("""
              INSERT INTO nexo.query_thresholds (endpoint, warn_ms, block_ms, factor_ms, updated_at, updated_by, factor_updated_at)
              VALUES (:endpoint, :warn_ms, :block_ms, :factor_ms, now(), NULL, NULL)
              ON CONFLICT (endpoint) DO NOTHING
          """), s)
      ```
    - Script must be re-runnable (currently is — keep that invariant).
  </behavior>
  <action>
    Edit nexo/data/schema_guard.py: locate the `CRITICAL_TABLES: tuple[str, ...] = (...)` literal and append 3 entries. Edit scripts/init_nexo_schema.py: after create_all() add a seed block per behavior. Run `python scripts/init_nexo_schema.py` inside the web container (or locally if Postgres is up) to validate — the script should emit "seed: pipeline/run inserted" / "seed: bbdd/query inserted" etc on first run, and "seed: pipeline/run already present" / silent on subsequent runs.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.data.schema_guard import CRITICAL_TABLES; assert len(CRITICAL_TABLES) == 11; assert 'query_log' in CRITICAL_TABLES and 'query_thresholds' in CRITICAL_TABLES and 'query_approvals' in CRITICAL_TABLES; print('OK, tables:', len(CRITICAL_TABLES))"</automated>
  </verify>
  <acceptance_criteria>
    - `CRITICAL_TABLES` is a tuple of length 11 containing the 3 new table names.
    - Running `python scripts/init_nexo_schema.py` against a Postgres with nexo schema creates the 3 new tables and inserts 4 seed rows in `nexo.query_thresholds`.
    - Re-running the script is idempotent (0 new seeds on second run — ON CONFLICT DO NOTHING).
    - `schema_guard.verify(engine_nexo)` completes without RuntimeError after the script runs (all 11 tables present).
    - Without the 3 tables present, `schema_guard.verify(engine_nexo)` raises RuntimeError mentioning at least one of the 3 new names (test in Task 6).
  </acceptance_criteria>
  <done>
    schema_guard enforces the new tables at startup. init_nexo_schema.py creates them + seeds defaults. Phase 4 planning decisions D-01/D-02/D-03/D-04 are materialized in the database on clean install.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create thresholds_cache.py skeleton (in-memory dict + full_reload + get with fallback; NO LISTEN loop)</name>
  <read_first>
    - nexo/services/auth.py (module-level singleton `_ph`, `_serializer` pattern at ~lines 47-77)
    - nexo/data/repositories/nexo.py (ThresholdRepo just created — use its `list_all()`)
    - nexo/data/engines.py (SessionLocalNexo factory — existing from Phase 3)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md lines 940-1050 (full_reload + reload_one + get + FALLBACK_REFRESH_SECONDS; IGNORE the listen_loop / _blocking_listen_forever sections — those go in Plan 04-04)
    - .planning/phases/04-consultas-pesadas/04-PATTERNS.md lines 396-412 (cache adaptation notes)
    - .planning/phases/04-consultas-pesadas/04-CONTEXT.md D-19 (cache + invalidate on edit — listen integration is for 04-04, but the safety-net re-read after 5 min stale MUST be in this plan)
  </read_first>
  <behavior>
    - Create nexo/services/thresholds_cache.py with:
      - `from __future__ import annotations`, imports: `logging`, `threading.Lock`, `datetime`, `timezone`, `dataclass`, `Optional`.
      - `log = logging.getLogger("nexo.thresholds_cache")`.
      - Constant `FALLBACK_REFRESH_SECONDS = 300` (5 min — D-19 safety net).
      - `@dataclass(frozen=True) class ThresholdEntry`: fields endpoint(str), warn_ms(int), block_ms(int), factor_ms(Optional[float]), loaded_at(datetime).
      - Module-level: `_cache: dict[str, ThresholdEntry] = {}`, `_cache_lock = Lock()`, `_loaded_at_global: datetime | None = None`.
      - `def full_reload() -> None`: opens a `SessionLocalNexo()` session, calls `ThresholdRepo(session).list_all()`, clears the cache under the lock, rebuilds it with `ThresholdEntry(endpoint=r.endpoint, warn_ms=r.warn_ms, block_ms=r.block_ms, factor_ms=r.factor_ms, loaded_at=now)`, sets `_loaded_at_global = now`. Logs `info` with count. Closes session.
      - `def reload_one(endpoint: str) -> None`: opens session, uses ThresholdRepo.get(endpoint). If None → pop from cache (under lock). Else → overwrite that key in cache (under lock). Log info.
      - `def get(endpoint: str) -> Optional[ThresholdEntry]`: checks `_loaded_at_global`; if None OR `(now - _loaded_at_global).total_seconds() > FALLBACK_REFRESH_SECONDS` → logs warning "thresholds_cache stale — forcing full_reload" and calls `full_reload()`. Then under lock returns `_cache.get(endpoint)`.
      - `def notify_changed(endpoint: str) -> None`: opens an engine_nexo connection with `isolation_level="AUTOCOMMIT"` (required for NOTIFY in psycopg2), executes `NOTIFY nexo_thresholds_changed, :endpoint` via `text()` with bindparam; commits. This is called by the thresholds CRUD endpoint in Plan 04-04 — put the function here so the interface is stable across plans.
    - Do NOT implement `listen_loop` / `_blocking_listen_forever` / `start_listener` in this plan. Those are Plan 04-04 scope. Add a stub comment: `# NOTE: LISTEN/NOTIFY listener is implemented in Plan 04-04.`
    - Create nexo/services/__init__.py if not already an empty file (it exists per grep — just verify; do not modify if already present).
  </behavior>
  <action>
    Write nexo/services/thresholds_cache.py per behavior. Confirm `engine_nexo` is the right import path (`from nexo.data.engines import engine_nexo, SessionLocalNexo`). Verify `SessionLocalNexo` is exported from nexo.data.engines (it is per Phase 3 summary — Phase 3 moved session factories there).
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -c "from nexo.services import thresholds_cache; assert hasattr(thresholds_cache, 'get') and hasattr(thresholds_cache, 'full_reload') and hasattr(thresholds_cache, 'reload_one') and hasattr(thresholds_cache, 'notify_changed'); from nexo.services.thresholds_cache import ThresholdEntry; e = ThresholdEntry(endpoint='x', warn_ms=1, block_ms=2, factor_ms=None, loaded_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc)); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from nexo.services.thresholds_cache import get, full_reload, reload_one, notify_changed, ThresholdEntry, FALLBACK_REFRESH_SECONDS` succeeds.
    - ThresholdEntry is frozen (attempting `e.warn_ms = 99` raises `FrozenInstanceError`).
    - `FALLBACK_REFRESH_SECONDS == 300`.
    - `grep -q "LISTEN" nexo/services/thresholds_cache.py` returns false (no LISTEN code yet — listener lives in 04-04).
    - `get()` without prior `full_reload()` triggers a full reload (reads from DB) — this is the stale-fallback path.
    - Thread-safety: the `_cache_lock` protects both reads and writes.
  </acceptance_criteria>
  <done>
    Cache skeleton is operational for pure in-memory access + DB re-read. Listener integration is deferred to Plan 04-04. Public interface `get(endpoint)` is stable so preflight service in Plan 04-02 can consume it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Wave 0 test stubs (10 files) + pytest-asyncio + test_schema_guard_extended + test_schema_query_log implemented</name>
  <read_first>
    - tests/data/test_nexo_repository.py (existing — canonical shape for integration tests + `pytestmark = pytest.mark.integration` + contract tests via `inspect.getsource`)
    - tests/data/test_schema_guard.py (kwarg-injection pattern at lines 39-55 — use this for test_schema_guard_extended.py)
    - tests/data/conftest.py (existing — `_postgres_reachable`, `db_nexo` fixture with rollback)
    - tests/auth/test_rbac_smoke.py (TestClient + cookie + `_create_test_user` + `_login` helpers)
    - requirements-dev.txt (add pytest-asyncio)
    - .planning/phases/04-consultas-pesadas/04-VALIDATION.md (exact list of 10 Wave 0 files + commands)
    - .planning/phases/04-consultas-pesadas/04-RESEARCH.md §Validation Architecture (behaviors each test must cover — lines 1642-1687)
  </read_first>
  <behavior>
    - Create `tests/services/__init__.py`, `tests/middleware/__init__.py`, `tests/routers/__init__.py` (empty files).
    - Append `pytest-asyncio==0.24.0` to requirements-dev.txt (after httpx line).
    - **FULL implementation (must pass in this plan):**
      1. `tests/data/test_schema_query_log.py` — imports ThresholdRepo, ApprovalRepo, QueryLogRepo + their DTOs. Uses `pytestmark = [pytest.mark.integration, pytest.mark.skipif(not _postgres_reachable(), reason="...")]` + `db_nexo` fixture. Tests:
         - `test_query_log_repo_append_source_has_no_commit` — `inspect.getsource(QueryLogRepo.append)` MUST NOT contain `.commit()` or `.flush()` (parity with AuditRepo).
         - `test_query_log_repo_append_adds_to_session` — appends one row, asserts `db_nexo.new` delta == +1, transaction still open.
         - `test_threshold_repo_list_all_returns_dtos` — asserts all items are `QueryThresholdRow`, and at least the 4 seeded endpoints appear.
         - `test_threshold_repo_get_returns_dto_or_none` — valid endpoint returns QueryThresholdRow; unknown endpoint returns None.
         - `test_approval_repo_create_returns_row_with_id` — creates approval; row.id is not None; row.status == 'pending'.
         - `test_approval_repo_consume_returns_none_if_status_not_approved` — creates pending approval, calls consume with same params — returns None (status != 'approved').
      2. `tests/data/test_schema_guard_extended.py` — imports `schema_guard` and `engine_nexo`. `pytestmark` with postgres skipif. Tests:
         - `test_verify_accepts_all_11_tables_when_present` — after `init_nexo_schema.py` the call `schema_guard.verify(engine_nexo)` does not raise.
         - `test_verify_raises_when_query_log_missing` — monkeypatch CRITICAL_TABLES to include a `__missing_query_log_sim__` name → raises RuntimeError mentioning it. (Use kwarg-injection pattern from existing test_schema_guard.py — do NOT monkeypatch module constants directly if the verify signature accepts `critical_tables=` kwarg).
    - **STUB implementation (skip markers with reason pointing to the plan that implements them):**
      3. `tests/services/test_preflight.py` — Skipped with `pytest.skip("Implemented in Plan 04-02")` at module level. Include a top docstring listing the 5 test names planned: `test_pipeline_classify_green`, `test_pipeline_classify_amber`, `test_pipeline_classify_red`, `test_bbdd_classify_*`, `test_factor_recalc_median`.
      4. `tests/services/test_thresholds_cache.py` — Full suite with 2 test groups:
         - **Unit group (runs in this plan):** `test_get_returns_none_for_unknown_endpoint` (calls full_reload via a stub SessionLocalNexo or reach a fresh DB with seeds; if Postgres down, skip). `test_entry_is_frozen` (attempt mutation → error).
         - **Integration group (skip in this plan):** `test_notify_triggers_reload_under_1s` — `pytest.skip("LISTEN integration implemented in Plan 04-04")`.
      5. `tests/services/test_approvals.py` — Implement `test_create_sets_status_pending`, `test_create_sets_ttl_days_default_7`. Skip `test_consume_cas_race`, `test_cancel_wrong_user_returns_false`, `test_expire_stale_moves_pending_to_expired` with `pytest.skip("Implemented in Plan 04-03")`.
      6. `tests/services/test_pipeline_lock.py` — Module-level `pytest.skip("Implemented in Plan 04-02", allow_module_level=True)`. Docstring lists: `test_semaphore_limits_to_max`, `test_timeout_raises_504`.
      7. `tests/middleware/test_query_timing.py` — Module-level skip until Plan 04-02. Docstring lists: `test_writes_query_log_on_timed_path`, `test_slow_status_written_when_actual_exceeds_warn_1_5x`, `test_excluded_paths_bypass`.
      8. `tests/routers/test_preflight_endpoints.py` — Module-level skip until Plan 04-02. Docstring lists: `test_preflight_returns_estimation_json`, `test_run_without_force_on_amber_returns_428`, `test_run_red_without_approval_returns_403`, `test_run_red_with_valid_approval_executes`.
      9. `tests/routers/test_approvals_api.py` — Module-level skip until Plan 04-03. Docstring lists: `test_create_approval_pending`, `test_propietario_approve_flow`, `test_user_cancel_own_pending`, `test_user_cannot_cancel_others`, `test_count_badge_returns_html_fragment`.
      10. `tests/conftest.py` — Check if it exists; if yes, extend it with a minimal `@pytest.fixture thresholds_cache_empty` that calls `_cache.clear()` and resets `_loaded_at_global = None` (access via the module). If it doesn't exist (it does — confirmed by grep), append the fixture without removing existing content.
    - Each stub file must still be syntactically valid Python + importable without side effects.
    - For the 2 fully implemented files, assert at minimum that they are `pytest -x` clean when Postgres is up and `pytest -x -k "not integration"` clean when Postgres is down (thanks to skipif).
  </behavior>
  <action>
    Create the 10 test files per the behavior. Append `pytest-asyncio==0.24.0` to requirements-dev.txt. Run `pytest tests/data/test_schema_query_log.py tests/data/test_schema_guard_extended.py -x` locally or in container after Task 4 has seeded the DB — these 2 files MUST be green. The other 8 files should report as SKIPPED (not xfail, not failure) in the pytest summary.
  </action>
  <verify>
    <automated>cd /home/eeguskiza/analisis_datos && python -m pytest tests/services/test_preflight.py tests/services/test_pipeline_lock.py tests/middleware/test_query_timing.py tests/routers/test_preflight_endpoints.py tests/routers/test_approvals_api.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 10 test files exist at the paths specified in VALIDATION.md Wave 0.
    - `pytest --collect-only tests/services/ tests/middleware/ tests/routers/` collects every file without ImportError.
    - `tests/data/test_schema_query_log.py` has at least 6 test functions defined (grep `def test_` count ≥ 6).
    - `tests/data/test_schema_guard_extended.py` has at least 2 test functions.
    - Stub files use `pytest.skip(reason="Implemented in Plan 04-XX", allow_module_level=True)` so `pytest` reports them as SKIPPED, not FAILED.
    - `grep -q "pytest-asyncio" requirements-dev.txt` returns true.
    - `tests/services/__init__.py`, `tests/middleware/__init__.py`, `tests/routers/__init__.py` all exist and are empty.
    - When Postgres is reachable AND after Task 4 seeded the DB: `pytest tests/data/test_schema_query_log.py tests/data/test_schema_guard_extended.py -x` is GREEN.
  </acceptance_criteria>
  <done>
    Wave 0 contract is in place: every downstream plan (04-02, 04-03, 04-04) inherits a file that documents the tests it must make pass. The implemented tests (test_schema_query_log, test_schema_guard_extended) prove Plan 04-01 delivered the schema correctly.
  </done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7 (MANUAL SMOKE): Schema + seeds verification en Postgres</name>
  <what-built>
    Plan 04-01 creó 3 tablas nuevas (`nexo.query_log`, `nexo.query_thresholds`, `nexo.query_approvals`) + seeds de umbrales + `schema_guard` extendido. Todas las verificaciones automáticas (Task 6) han corrido. Falta ojo humano sobre la forma real del schema en Postgres (constraints, nombres exactos de índices, columnas nullable vs NOT NULL) porque los tests automated importan ORM classes pero no siempre detectan drift entre model y DDL emitido por `create_all`.
  </what-built>
  <how-to-verify>
    1. `docker compose up -d db` y esperar que Postgres esté listo (`docker compose logs db | grep "ready to accept"`).
    2. Ejecutar `docker compose exec web python scripts/init_nexo_schema.py` (o local si Postgres local). Verificar output: "seed: pipeline/run inserted", "seed: bbdd/query inserted", "seed: capacidad inserted", "seed: operarios inserted".
    3. Re-ejecutar el mismo script; verificar que NO se insertan nuevas filas (idempotencia ON CONFLICT).
    4. Conectar con psql: `docker compose exec db psql -U $NEXO_PG_USER -d $NEXO_PG_DB`.
    5. `\d nexo.query_log` — confirmar 11 columnas y 4 índices (`ix_query_log_ts`, `ix_query_log_endpoint_ts`, `ix_query_log_user_ts`, `ix_query_log_status_slow`). El último debe ser partial index con `WHERE status = 'slow'`.
    6. `\d nexo.query_thresholds` — PK en `endpoint`, 7 columnas, sin índices adicionales.
    7. `\d nexo.query_approvals` — 15 columnas y 3 índices (`ix_approvals_status`, `ix_approvals_user_status`, `ix_approvals_created_at`).
    8. `SELECT endpoint, warn_ms, block_ms, factor_ms FROM nexo.query_thresholds ORDER BY endpoint;` — confirmar 4 filas con valores D-01/D-02/D-03/D-04: `bbdd/query|3000|30000|1000`, `capacidad|3000|30000|50`, `operarios|3000|30000|50`, `pipeline/run|120000|600000|2000`.
    9. `docker compose up web` — arranque limpio, logs muestran `schema_guard OK` sin `RuntimeError`.
  </how-to-verify>
  <resume-signal>Reanudar con "schema-ok" o describir desviaciones (nombre de índice distinto, columna nullable inesperada, seed mismatch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Postgres (init_nexo_schema) | Seed inserts via text() + bindparams. Not user-controlled input but still MUST use parameterized SQL (no string interpolation of values). |
| ORM → Postgres (repo methods) | All existing repo patterns use parameterized SQL (text+bindparams) or SQLAlchemy ORM. No string concatenation anywhere in new repos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | nexo.query_thresholds | mitigate | `updated_by` column captures who wrote — audit_middleware (Phase 2) already logs the write. Actual RBAC for /api/thresholds is Plan 04-04 (only propietario via require_permission). This plan just stores the column. |
| T-04-01-02 | Information Disclosure | nexo.query_log.params_json | accept | params_json may contain SQL; retention 90d (D-10) + only propietario reads /ajustes/rendimiento. LAN-only deployment. Acceptable per CONTEXT §D-09 rationale. |
| T-04-01-03 | Tampering | ApprovalRepo.consume (CAS) | mitigate | Single `UPDATE ... WHERE consumed_at IS NULL AND params_json = :pj RETURNING id` atomic — race prevented by DB-level row lock. Tested in Plan 04-03. |
| T-04-01-04 | Repudiation | nexo.query_approvals state transitions | mitigate | Every state change sets a `*_at` timestamp column + caller-bound `approved_by`/`user_id` (set in downstream plans). audit_middleware logs each POST /api/approvals/* call. |
| T-04-01-05 | Denial of Service | query_log growth | mitigate | Index `ix_query_log_ts` enables fast purge; D-10 retention job in Plan 04-04 keeps table bounded at ~90k rows. |
| T-04-01-06 | Denial of Service | Approval spam | accept | Each user creates 1 approval per amber/red attempt. LAN, ~20 users. Worst case ~200 rows/day. Trivial for Postgres. slowapi rate-limit opcional Mark-IV. |
</threat_model>

<verification>
- `docker compose up -d db` then run `python scripts/init_nexo_schema.py` → output shows 4 seeds inserted + ON CONFLICT no-op on re-run.
- `psql nexo -c "\d nexo.query_log"` shows all 11 columns + 4 indexes.
- `psql nexo -c "\d nexo.query_thresholds"` shows PK on endpoint.
- `psql nexo -c "\d nexo.query_approvals"` shows 3 indexes.
- `psql nexo -c "SELECT endpoint, warn_ms, block_ms, factor_ms FROM nexo.query_thresholds ORDER BY endpoint"` returns exactly 4 rows with D-01/D-02/D-03/D-04 values.
- `pytest tests/data/test_schema_query_log.py tests/data/test_schema_guard_extended.py -v` → GREEN (all test functions pass).
- `pytest tests/services/ tests/middleware/ tests/routers/ --co -q` → collects all stubs without ImportError; stubs report SKIPPED in summary.
- Application boot: `docker compose up web` → no schema_guard errors (all 11 tables verified).
</verification>

<success_criteria>
1. 11 critical tables verified at startup by schema_guard.
2. 4 threshold seeds in nexo.query_thresholds matching D-01/D-02/D-03/D-04.
3. 3 new ORM classes importable, 3 new DTOs importable, `Estimation` importable.
4. PipelineRequest accepts `force` + `approval_id` without breaking existing POST /api/pipeline/run calls.
5. thresholds_cache.get('pipeline/run') returns ThresholdEntry(warn_ms=120000, block_ms=600000, factor_ms=2000.0) after full_reload.
6. 10 Wave 0 test files exist, 2 pass green, 8 are skipped with clear reasons.
7. `pip install -r requirements-dev.txt` installs pytest-asyncio without conflict.
8. No regression: `pytest tests/data/test_nexo_repository.py tests/data/test_schema_guard.py -x` (Phase 3 tests) still green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-consultas-pesadas/04-01-SUMMARY.md` documenting:
- Files created/modified with line counts.
- Seeds inserted (4 rows with values).
- Wave 0 test coverage status (2 green / 8 skipped with pointers).
- Any discoveries about ORM constraints or DB behavior worth propagating to Plans 04-02/03/04.
- Commits landed (1 per task recommended; 6 commits total expected).
</output>
