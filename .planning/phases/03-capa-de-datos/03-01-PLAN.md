---
phase: 03-capa-de-datos
plan: 01
slug: foundation-engines-loader-schema-guard
type: execute
wave: 1
depends_on: []
files_modified:
  - nexo/db/engine.py
  - api/main.py
  - api/deps.py
  - Makefile
  - .env.example
files_created:
  - nexo/data/__init__.py
  - nexo/data/engines.py
  - nexo/data/sql/__init__.py
  - nexo/data/sql/loader.py
  - nexo/data/dto/__init__.py
  - nexo/data/dto/base.py
  - nexo/data/schema_guard.py
  - tests/data/__init__.py
  - tests/data/conftest.py
  - tests/data/test_engines.py
  - tests/data/test_sql_loader.py
  - tests/data/test_schema_guard.py
  - tests/data/test_dto_immutable.py
  - tests/data/test_fixtures.py
autonomous: true
requirements: [DATA-01, DATA-05, DATA-06, DATA-08, DATA-10, DATA-11]
requirements_addressed: [DATA-01, DATA-05, DATA-06, DATA-08, DATA-10, DATA-11]
tags: [data-layer, engines, schema-guard, test-fixtures]
must_haves:
  truths:
    - "`nexo/data/engines.py` exporta `engine_nexo`, `engine_app`, `engine_mes`, `SessionLocalNexo`, `SessionLocalApp` — tres engines coexisten en runtime sin conflicto"
    - "`nexo/db/engine.py` sigue funcionando como shim re-exportando de `nexo.data.engines` — consumidores Phase 2 (`nexo/services/auth.py`, `api/routers/auditoria.py`, `api/routers/usuarios.py`, `tests/auth/*`) sin cambios"
    - "`nexo.data.sql.loader.load_sql(name)` lee `.sql` del paquete vía `importlib.resources` con `lru_cache`"
    - "`nexo.data.schema_guard.verify(engine_nexo)` aborta arranque (`RuntimeError`) si falta cualquiera de las 8 tablas críticas de schema `nexo`; con `NEXO_AUTO_MIGRATE=true` las crea en su lugar"
    - "`schema_guard.verify` acepta kwarg `critical_tables: tuple[str, ...] = CRITICAL_TABLES` — tests inyectan tablas ficticias sin monkeypatch del módulo"
    - "`api/deps.py` expone `get_db_app`, `get_db_nexo`, `get_engine_mes` + aliases `DbApp`, `DbNexo`, `EngineMes`"
    - "`pytest tests/data/ -q -m 'not integration'` exit 0; `pytest tests/auth/ -q` sigue 11 passed / 1 skipped (regression IDENT-06 verde)"
    - "`make test-data` target existe y arranca compose db + corre tests/data/"
    - "`docker compose exec web python -c 'from nexo.data.engines import engine_nexo, engine_mes, engine_app; print(\"OK\")'` imprime OK sin traceback"
  artifacts:
    - path: "nexo/data/engines.py"
      provides: "3 engines (nexo/app/mes) + 2 session factories + MES DSN builder con DATABASE=dbizaro"
      min_lines: 60
      contains: "engine_mes"
    - path: "nexo/data/sql/loader.py"
      provides: "load_sql(name) con lru_cache e importlib.resources"
      min_lines: 15
      exports: ["load_sql"]
    - path: "nexo/data/schema_guard.py"
      provides: "verify(engine, critical_tables=CRITICAL_TABLES) + NEXO_AUTO_MIGRATE gate"
      min_lines: 30
      exports: ["verify", "CRITICAL_TABLES"]
    - path: "nexo/data/dto/__init__.py"
      provides: "package marker"
      min_lines: 1
    - path: "nexo/db/engine.py"
      provides: "shim re-export desde nexo.data.engines"
      contains: "from nexo.data.engines import"
    - path: "tests/data/conftest.py"
      provides: "fixtures db_nexo, db_app, engine_mes_mock con skipifs"
      exports: ["db_nexo", "db_app", "engine_mes_mock"]
    - path: "api/deps.py"
      provides: "get_db_app, get_db_nexo, get_engine_mes + Annotated aliases"
      contains: "def get_db_nexo"
    - path: "api/main.py"
      provides: "lifespan con schema_guard.verify() antes de init_db()"
      contains: "schema_guard.verify(engine_nexo)"
  key_links:
    - from: "nexo/db/engine.py"
      to: "nexo/data/engines.py"
      via: "shim re-export"
      pattern: "from nexo\\.data\\.engines import"
    - from: "api/main.py:lifespan"
      to: "nexo.data.schema_guard.verify"
      via: "llamada directa antes de init_db()"
      pattern: "schema_guard\\.verify\\(engine_nexo\\)"
    - from: "nexo/data/engines.py:engine_app"
      to: "api/database.py:engine"
      via: "re-export sin duplicar _mssql_creator (Landmine #3)"
      pattern: "from api\\.database import engine as engine_app"
    - from: "nexo/data/engines.py:engine_nexo"
      to: "api/config.py:settings.effective_pg_user"
      via: "DSN builder usa effective_* (Landmine #12, preserva IDENT-06)"
      pattern: "settings\\.effective_pg_user"
---

<objective>
Sentar la base de la capa de datos sin tocar ningún router. Mover `engine_nexo` a `nexo/data/engines.py`, añadir `engine_mes` con pool DATA-11 y DSN con `DATABASE=dbizaro` (habilita DATA-09 del plan siguiente), re-exportar `engine_app` desde `api/database.py` sin duplicar el creador pyodbc, crear el SQL loader (`lru_cache` + `importlib.resources`), el paquete DTO vacío, el `schema_guard` cableado en `lifespan`, las dependencias FastAPI y las fixtures pytest para `tests/data/`.

Purpose: 03-02 y 03-03 dependen de tener los 3 engines + loader + fixtures + DTO skeleton disponibles ANTES de tocar repos o routers. Sin esta base no pueden paralelizarse.

Output: 14 archivos nuevos + 5 modificados. Gate IDENT-06 debe seguir verde (`tests/auth/` 11 passed / 1 skipped). `tests/data/` queda con fixtures + 5 tests unit/integration que validan la base.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-datos/03-CONTEXT.md
@.planning/phases/03-capa-de-datos/03-RESEARCH.md
@.planning/phases/03-capa-de-datos/03-PATTERNS.md
@.planning/phases/03-capa-de-datos/03-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor va a consumir o crear. Extraídos del codebase. -->

From nexo/db/engine.py (será movido a nexo/data/engines.py y shim-eado):
```python
from sqlalchemy import create_engine
from sqlalchemy.engine import Engine
from sqlalchemy.orm import sessionmaker

engine_nexo: Engine  # Postgres, uses settings.effective_pg_user/password
SessionLocalNexo    # sessionmaker(bind=engine_nexo, autoflush=False, expire_on_commit=False)
```

From api/database.py (engine legacy SQL Server ecs_mobility que re-exportamos):
```python
def _mssql_creator():  # pyodbc raw connection — NO DUPLICAR (Landmine #3)
    ...
engine = create_engine("mssql+pyodbc://", creator=_mssql_creator, pool_pre_ping=True,
                       pool_size=5, max_overflow=10, pool_recycle=3600)
SessionLocal = sessionmaker(bind=engine, expire_on_commit=False)
```

From api/config.py (settings ya disponible; effective_* son mandatorios por Landmine #12):
```python
settings.effective_pg_user      # rol nexo_app si existe, sino oee (owner)
settings.effective_pg_password
settings.pg_host, settings.pg_port, settings.pg_db
settings.mes_server, settings.mes_port, settings.mes_user, settings.mes_password, settings.mes_db
settings.debug
```

From nexo/db/models.py (se importan NEXO_SCHEMA + NexoBase durante 03-01; en 03-03 el path cambia):
```python
NEXO_SCHEMA = "nexo"
class NexoBase(DeclarativeBase): ...
```

From api/main.py:25-34 (el lifespan actual a modificar):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    try:
        init_db()
        logger.info("Base de datos inicializada OK")
    except Exception as exc:
        logger.error(f"Error inicializando BD: {exc}")
        logger.error(traceback.format_exc())
    yield
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear engines.py + loader + DTO skeleton + schema_guard + shim (wave 1, foundation code)</name>
  <files>
    nexo/data/__init__.py (new, empty marker),
    nexo/data/engines.py (new),
    nexo/data/sql/__init__.py (new, empty marker),
    nexo/data/sql/loader.py (new),
    nexo/data/dto/__init__.py (new, empty marker),
    nexo/data/dto/base.py (new, opcional; solo si necesitas compartir Config),
    nexo/data/schema_guard.py (new),
    nexo/db/engine.py (modified → shim)
  </files>
  <read_first>
    - nexo/db/engine.py (fuente del engine_nexo a mover; líneas 1-55 completas — copia verbatim)
    - api/database.py:17-42 (engine SQL Server con _mssql_creator — NO duplicar, solo re-exportar)
    - api/config.py:1-150 (ver settings.effective_pg_user, settings.mes_*; Landmine #12)
    - nexo/db/models.py:37-41 (constante NEXO_SCHEMA + clase NexoBase; el schema_guard las importa)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones "nexo/data/engines.py" (líneas 91-168), "nexo/data/sql/loader.py" (líneas 185-214), "nexo/data/schema_guard.py" (líneas 218-259), "nexo/db/engine.py (modified → SHIM)" (líneas 172-182)
    - .planning/phases/03-capa-de-datos/03-RESEARCH.md §SQLAlchemy Multi-Engine Patterns, §Lifespan + schema_guard
  </read_first>
  <behavior>
    - Test 1: `nexo.data.engines` expone `engine_nexo`, `engine_app`, `engine_mes`, `SessionLocalNexo`, `SessionLocalApp`.
    - Test 2: `engine_mes.pool._recycle == 3600` y `engine_mes.pool._pre_ping is True` (DATA-11).
    - Test 3: `engine_mes.url.database == settings.mes_db` (debe ser `dbizaro` per CLAUDE.md) — habilita DATA-09.
    - Test 4: `nexo.data.sql.loader.load_sql` usa `lru_cache` (mismo string devuelto en 2 llamadas con el mismo nombre; `lru_cache` tiene atributo `cache_info`).
    - Test 5: `load_sql` acepta nombre con o sin `.sql`, resuelve subdirs con `/`.
    - Test 6: `nexo.data.schema_guard.verify(engine)` sobre un engine con todas las tablas devuelve None y loguea INFO; con `critical_tables=("users", "__nonexistent__")` lanza `RuntimeError` con mensaje que contiene "missing" o "faltan" y menciona la tabla; con `NEXO_AUTO_MIGRATE=true` (monkeypatch env) + `critical_tables` incluyendo una faltante ejecuta `NexoBase.metadata.create_all` y loguea WARNING.
    - Test 7: `nexo/db/engine.py` después del cambio solo contiene import/re-export desde `nexo.data.engines`; `nexo/services/auth.py` (que importa `from nexo.db.engine import engine_nexo, SessionLocalNexo`) sigue funcionando sin modificación.
    - Test 8: DTO base skeleton — paquete `nexo.data.dto` importable; `nexo.data.dto.base` (si existe) expone utilidades comunes o está vacío placeholder.
    - Test 9: `schema_guard.verify` signature — `inspect.signature(verify).parameters` incluye `critical_tables` como kwarg con default apuntando a `CRITICAL_TABLES`.
  </behavior>
  <action>
1. Crear `nexo/data/__init__.py` vacío (package marker).

2. Crear `nexo/data/engines.py`:

   ```python
   """3 engines + 3 session factories (Phase 3, plan 03-01).

   - engine_nexo: Postgres schema `nexo` (movido desde nexo/db/engine.py).
   - engine_app: SQL Server ecs_mobility (re-export desde api/database.py; NO duplicar
     el creator pyodbc — Landmine #3 del research).
   - engine_mes: SQL Server dbizaro read-only por convencion (DATA-01, DATA-11).
     DATABASE=dbizaro en la connection string habilita kill de 3-part names (DATA-09
     en plan 03-02).
   """
   from __future__ import annotations

   from sqlalchemy import create_engine
   from sqlalchemy.engine import Engine
   from sqlalchemy.orm import sessionmaker

   from api.config import settings


   # ── engine_nexo (Postgres, schema nexo) ──────────────────────────────────
   # Copiado verbatim de nexo/db/engine.py. Mantener settings.effective_pg_user/
   # effective_pg_password (Landmine #12 — rompe IDENT-06 si se usa pg_user a secas).
   def _build_pg_dsn() -> str:
       user = settings.effective_pg_user
       password = settings.effective_pg_password
       return (
           f"postgresql+psycopg2://{user}:{password}"
           f"@{settings.pg_host}:{settings.pg_port}/{settings.pg_db}"
       )


   engine_nexo: Engine = create_engine(
       _build_pg_dsn(),
       pool_size=5,
       max_overflow=5,
       pool_timeout=10,
       pool_recycle=1800,
       pool_pre_ping=True,
       echo=settings.debug,
   )

   SessionLocalNexo = sessionmaker(
       bind=engine_nexo,
       autoflush=False,
       autocommit=False,
       expire_on_commit=False,
   )


   # ── engine_app (SQL Server ecs_mobility) — RE-EXPORT ─────────────────────
   # NO duplicar _mssql_creator. El engine legacy vive en api/database.py y
   # el pipeline OEE lo consume tal cual; aqui solo lo exponemos con el nombre
   # nuevo para no duplicar la connection string.
   from api.database import engine as engine_app, SessionLocal as SessionLocalApp  # noqa: E402


   # ── engine_mes (SQL Server dbizaro, read-only convencion) ────────────────
   # DATA-11 pool params; DATA-01 DSN dedicado.
   def _build_mes_dsn() -> str:
       # Escape de + en el password (ODBC lo consume como encoded param).
       pwd = settings.mes_password.replace("+", "%2B")
       return (
           f"mssql+pyodbc://{settings.mes_user}:{pwd}"
           f"@{settings.mes_server}:{settings.mes_port}/{settings.mes_db}"
           "?driver=ODBC+Driver+18+for+SQL+Server"
           "&TrustServerCertificate=yes&Encrypt=yes"
       )


   engine_mes: Engine = create_engine(
       _build_mes_dsn(),
       pool_pre_ping=True,        # DATA-11
       pool_recycle=3600,         # DATA-11
       pool_size=3,
       max_overflow=2,
       pool_timeout=15,
       connect_args={"timeout": 15},  # DATA-11 (connect_timeout segundos)
   )
   ```

3. Crear `nexo/data/sql/__init__.py` vacío (package marker — necesario para `importlib.resources`).

4. Crear `nexo/data/sql/loader.py` verbatim del PATTERNS.md:

   ```python
   """Loader con lru_cache para archivos .sql versionados (D-01)."""
   from __future__ import annotations

   from functools import lru_cache
   from importlib.resources import files

   _PACKAGE = "nexo.data.sql"


   @lru_cache(maxsize=128)
   def load_sql(name: str) -> str:
       """Carga 'mes/extraer_datos_produccion.sql' desde el paquete.

       El nombre incluye el subdirectorio (engine) y extension opcional.
       Normaliza: 'mes/estado' -> 'mes/estado.sql'.
       """
       if not name.endswith(".sql"):
           name = f"{name}.sql"
       ref = files(_PACKAGE)
       for part in name.split("/"):
           ref = ref / part
       return ref.read_text(encoding="utf-8")
   ```

5. Crear `nexo/data/dto/__init__.py` vacío (package marker — DTOs concretos llegan en 03-02/03-03).

6. (Opcional) Crear `nexo/data/dto/base.py` vacío o con un `BaseRow = BaseModel` compartido si lo ves útil; si no, omítelo. El test `test_dto_immutable.py` define un DTO ad-hoc para no depender de DTOs reales.

7. Crear `nexo/data/schema_guard.py` — **CAMBIO RESPECTO A PATTERNS.md**: `verify()` acepta `critical_tables: tuple[str, ...] = CRITICAL_TABLES` como kwarg para testabilidad (sin monkeypatch del módulo, fragil si CRITICAL_TABLES es frozenset/tuple computado):

   ```python
   """schema_guard.verify — corre en lifespan, aborta o auto-migra (D-06, D-07)."""
   from __future__ import annotations

   import logging
   import os

   from sqlalchemy import inspect
   from sqlalchemy.engine import Engine

   # Durante 03-01 los modelos siguen en nexo/db/models.py; en 03-03 se mueven
   # a nexo/data/models_nexo.py pero el shim los expondra igual.
   from nexo.db.models import NEXO_SCHEMA, NexoBase

   log = logging.getLogger("nexo.schema_guard")

   CRITICAL_TABLES: tuple[str, ...] = (
       "users", "roles", "departments", "user_departments",
       "permissions", "sessions", "login_attempts", "audit_log",
   )


   def _auto_migrate_enabled() -> bool:
       return os.environ.get("NEXO_AUTO_MIGRATE", "").lower() in {"1", "true", "yes"}


   def verify(
       engine: Engine,
       critical_tables: tuple[str, ...] = CRITICAL_TABLES,
   ) -> None:
       """Chequea tablas criticas schema nexo. Si faltan → abort o auto-migrate.

       Args:
           engine: engine Postgres (normalmente engine_nexo) a inspeccionar.
           critical_tables: tupla de nombres de tabla esperados en schema nexo.
               Por defecto, las 8 tablas criticas de Phase 2 (CRITICAL_TABLES).
               Tests pueden pasar una tupla distinta (p. ej. incluyendo un
               nombre ficticio) sin monkeypatch del modulo — esto sustituye
               la fragilidad de `monkeypatch.setattr(schema_guard, "CRITICAL_TABLES", ...)`
               si la constante se convierte en frozenset/tuple inmutable.
       """
       insp = inspect(engine)
       existing = set(insp.get_table_names(schema=NEXO_SCHEMA))
       missing = [t for t in critical_tables if t not in existing]

       if not missing:
           log.info(
               "schema_guard OK — %d tablas nexo.* presentes", len(critical_tables)
           )
           return

       if not _auto_migrate_enabled():
           raise RuntimeError(
               f"Schema guard: faltan tablas en nexo.* -> {missing}. "
               "Ejecuta `make nexo-init` o define NEXO_AUTO_MIGRATE=true "
               "(solo dev) para crearlas automaticamente."
           )

       log.warning(
           "NEXO_AUTO_MIGRATE activo — creando %d tablas faltantes: %s",
           len(missing), missing,
       )
       NexoBase.metadata.create_all(bind=engine)
       log.warning("auto-migracion completada. NO usar en produccion.")
   ```

8. Sustituir contenido de `nexo/db/engine.py` por un shim (preservar docstring original como referencia):

   ```python
   """SHIM — movido a ``nexo/data/engines.py`` en Plan 03-01.

   Durante Phase 3 este modulo re-exporta los simbolos originales para no
   romper consumidores (``nexo/services/auth.py``,
   ``api/routers/auditoria.py``, ``api/routers/usuarios.py``,
   ``tests/auth/*``). Se elimina al final de Plan 03-03, una vez los
   consumers migren sus imports a ``nexo.data.engines``.
   """
   from nexo.data.engines import engine_nexo, SessionLocalNexo  # noqa: F401
   ```

9. Verifica que `python -c "from nexo.db.engine import engine_nexo, SessionLocalNexo"` sigue funcionando sin warnings (no debería haber ciclos; `nexo.data.engines` importa `api.database` pero no viceversa).
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from nexo.data.engines import engine_nexo, engine_app, engine_mes, SessionLocalNexo, SessionLocalApp; from nexo.db.engine import engine_nexo as e2; assert e2 is engine_nexo; print('OK shim+engines')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.sql.loader import load_sql; import functools; assert hasattr(load_sql, 'cache_info'); print('OK loader cached')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.schema_guard import verify, CRITICAL_TABLES; import inspect; assert len(CRITICAL_TABLES) == 8; sig = inspect.signature(verify); assert 'critical_tables' in sig.parameters; print('OK schema_guard signature')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.engines import engine_mes; assert engine_mes.pool._recycle == 3600; assert engine_mes.pool._pre_ping is True; print('OK DATA-11')"</automated>
  </verify>
  <done>
    - `nexo/data/engines.py` exporta 5 símbolos; `engine_mes` tiene `pool_recycle=3600`, `pool_pre_ping=True`, DSN con `DATABASE={settings.mes_db}`.
    - `nexo/db/engine.py` es shim puro (≤10 líneas efectivas).
    - `nexo/services/auth.py` y `api/routers/auditoria.py`, `api/routers/usuarios.py` siguen importando de `nexo.db.engine` sin cambios y arrancan.
    - `nexo/data/sql/loader.py`, `nexo/data/schema_guard.py`, `nexo/data/dto/__init__.py` existen.
    - `schema_guard.verify(engine, critical_tables=...)` acepta kwarg `critical_tables` con default a `CRITICAL_TABLES`.
    - `pytest tests/auth/ -q` sigue 11 passed / 1 skipped (gate IDENT-06 intacto).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear schema_guard en lifespan + dependencias DB en api/deps.py + Makefile + .env.example</name>
  <files>
    api/main.py,
    api/deps.py,
    Makefile,
    .env.example
  </files>
  <read_first>
    - api/main.py:25-34 (lifespan actual — modificar preservando el try/except de init_db)
    - api/main.py:107-124 (orden LIFO de middlewares — NO tocar, Landmine #11)
    - api/deps.py completo (hoy solo tiene templates y render — añadir DB deps al final)
    - api/database.py:281-287 (get_db yield-pattern — referencia)
    - api/routers/auditoria.py:46-51 (get_nexo_db local — el patrón generalizado)
    - Makefile (targets existentes nexo-init, nexo-owner líneas 53-60; añadir test-data)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones "api/main.py (modified)", "api/deps.py (modified)", "Makefile (modified — test-data target)", ".env.example (modified)"
  </read_first>
  <behavior>
    - Test 1: `api.deps` expone `get_db_app`, `get_db_nexo`, `get_engine_mes` como callables; los `get_db_*` son generators (yield + finally close).
    - Test 2: `api.deps.DbApp`, `api.deps.DbNexo`, `api.deps.EngineMes` son aliases `Annotated[...]` válidos para usar en signatures FastAPI.
    - Test 3: Arrancar `api.main.app` con `TestClient` llama `schema_guard.verify(engine_nexo)` antes de `init_db()` (verificar con monkeypatch que cambiamos verify → spy y comprobar orden de llamada; si Postgres está verde y tablas existen, verify devuelve None y el resto del lifespan sigue).
    - Test 4: Si `schema_guard.verify` lanza `RuntimeError`, `TestClient(app)` lo propaga (el `yield` nunca se ejecuta). Simularlo con `monkeypatch.setattr("nexo.data.schema_guard.verify", lambda e: (_ for _ in ()).throw(RuntimeError("boom")))` y confirmar excepción en startup.
    - Test 5: `make test-data` target aparece en `make help` / `.PHONY`.
  </behavior>
  <action>
1. Modificar `api/main.py` lifespan:

   - Imports (añadir cerca de los otros, ANTES de `logging.basicConfig`):
     ```python
     from nexo.data import schema_guard
     from nexo.data.engines import engine_nexo
     ```

   - Sustituir el bloque `async def lifespan(...)` actual (líneas ~25-34) por:
     ```python
     @asynccontextmanager
     async def lifespan(app: FastAPI):
         """Inicializa la BBDD al arrancar.

         1. schema_guard ANTES de init_db: si falla aqui, abortamos claro
            (RuntimeError sube hasta uvicorn y la app NO arranca). D-06/D-07.
         2. init_db() preserva su try/except legacy — bootstrap ecs_mobility
            es best-effort historicamente.
         """
         # 1. Schema guard (Phase 3, plan 03-01) — aborta arranque si faltan
         # tablas en nexo.* y NEXO_AUTO_MIGRATE != true.
         schema_guard.verify(engine_nexo)

         # 2. init_db legacy (bootstrap ecs_mobility) — mantener best-effort.
         try:
             init_db()
             logger.info("Base de datos inicializada OK")
         except Exception as exc:
             logger.error(f"Error inicializando BD: {exc}")
             logger.error(traceback.format_exc())
         yield
     ```

   - NO tocar las líneas 107-124 (orden LIFO de middlewares — Landmine #11).

2. Modificar `api/deps.py` — añadir al final del archivo (después del `render()` actual):

   ```python
   # ── Dependencias de base de datos (Plan 03-01) ─────────────────────────
   # Tres engines distintos: app (ecs_mobility), nexo (Postgres), mes (dbizaro
   # read-only). Cada uno con su propio generator yield-pattern para que
   # FastAPI cierre la sesion al terminar la request.

   from typing import Annotated, Iterator  # noqa: E402

   from fastapi import Depends  # noqa: E402
   from sqlalchemy.engine import Engine  # noqa: E402
   from sqlalchemy.orm import Session  # noqa: E402

   from nexo.data.engines import (  # noqa: E402
       SessionLocalApp,
       SessionLocalNexo,
       engine_mes as _engine_mes,
   )


   def get_db_app() -> Iterator[Session]:
       """Sesion SQL Server ecs_mobility. Caller comitea."""
       db = SessionLocalApp()
       try:
           yield db
       finally:
           db.close()


   def get_db_nexo() -> Iterator[Session]:
       """Sesion Postgres nexo.*. Caller comitea."""
       db = SessionLocalNexo()
       try:
           yield db
       finally:
           db.close()


   def get_engine_mes() -> Engine:
       """MES es read-only; entregamos el Engine directamente (no session stateful)."""
       return _engine_mes


   # Aliases Annotated (PEP 593) — firmas de router mas limpias.
   DbApp = Annotated[Session, Depends(get_db_app)]
   DbNexo = Annotated[Session, Depends(get_db_nexo)]
   EngineMes = Annotated[Engine, Depends(get_engine_mes)]
   ```

3. Modificar `Makefile` — añadir el target `test-data` y registrarlo en `.PHONY`:

   - En la línea `.PHONY: ...` (primera línea), añadir `test-data` al listado.
   - Tras `nexo-owner:` (o donde esté el bloque de targets nexo-*) añadir:

     ```makefile
     test-data: ## Arranca compose db y corre pytest tests/data/
     	docker compose up -d db
     	@echo "Esperando Postgres..."
     	@until docker compose exec -T db pg_isready -U $${NEXO_PG_USER:-oee} >/dev/null 2>&1; do sleep 1; done
     	docker compose exec -T web pytest tests/data/ -q
     ```

   No apagamos compose — dev normal lo quiere arriba. Si falla `pytest`, el target falla (exit != 0).

4. Modificar `.env.example` — añadir al final (antes de cualquier bloque de comentarios de cierre, si existen):

   ```
   # ── Schema guard (Phase 3) ──────────────────────────────────────────────
   # Si está en true y al arrancar faltan tablas en nexo.*, las crea
   # automáticamente via NexoBase.metadata.create_all. SOLO dev / primer
   # deploy. NUNCA en producción (usa `make nexo-init` en su lugar).
   # Tras cambiar .env: `docker compose up -d --force-recreate web`.
   NEXO_AUTO_MIGRATE=false
   ```
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from api.deps import get_db_app, get_db_nexo, get_engine_mes, DbApp, DbNexo, EngineMes; import inspect; assert inspect.isgeneratorfunction(get_db_app); assert inspect.isgeneratorfunction(get_db_nexo); print('OK deps')"</automated>
    <automated>docker compose exec -T web python -c "import api.main as m; import inspect; src = inspect.getsource(m.lifespan); assert 'schema_guard.verify(engine_nexo)' in src; print('OK lifespan wired')"</automated>
    <automated>grep -qE "^test-data:" Makefile && echo "OK test-data target defined" || echo "MISSING: test-data target in Makefile"</automated>
    <automated>grep -q "NEXO_AUTO_MIGRATE" .env.example && echo "OK NEXO_AUTO_MIGRATE entry" || echo "MISSING: NEXO_AUTO_MIGRATE in .env.example"</automated>
  </verify>
  <done>
    - `api/main.py:lifespan` llama `schema_guard.verify(engine_nexo)` como primera sentencia; `init_db()` sigue en try/except.
    - `api/deps.py` expone 3 funciones + 3 aliases `Annotated`.
    - `make test-data` target funcional (arranca compose db + pytest tests/data/).
    - `.env.example` documenta `NEXO_AUTO_MIGRATE=false` con comentario "solo dev".
    - Orden de middlewares en `api/main.py` inalterado (Landmine #11).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixtures + 5 tests en tests/data/ (W0 validation scaffolding)</name>
  <files>
    tests/data/__init__.py (new, empty),
    tests/data/conftest.py (new),
    tests/data/test_engines.py (new, DATA-01 + DATA-11),
    tests/data/test_sql_loader.py (new, DATA-05 loader),
    tests/data/test_schema_guard.py (new, DATA-06),
    tests/data/test_dto_immutable.py (new, DATA-08),
    tests/data/test_fixtures.py (new, DATA-10 meta-test)
  </files>
  <read_first>
    - tests/auth/test_audit_append_only.py:32-97 (_connected_as_nexo_app + _owner_engine + pytestmark skipif — patrón canónico)
    - tests/auth/test_rbac_smoke.py:37-74 (_postgres_reachable + TestClient pattern)
    - tests/conftest.py (registro global del marker `integration`)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md sección "tests/data/conftest.py" (líneas 790-869) — fixture pattern verbatim
    - .planning/phases/03-capa-de-datos/03-VALIDATION.md secciones "Per-Task Verification Map" y "Wave 0 Requirements" (líneas 41-99)
  </read_first>
  <behavior>
    - Test 1 (test_engines.py DATA-01): importa los 5 símbolos; `engine_nexo`, `engine_app`, `engine_mes` son `sqlalchemy.engine.Engine`.
    - Test 2 (test_engines.py DATA-11): `engine_mes.pool._recycle == 3600` y `engine_mes.pool._pre_ping is True`.
    - Test 3 (test_engines.py DATA-09 smoke, marcado `integration`, skipif sin SQL Server): `engine_mes.connect().execute(text("SELECT DB_NAME()")).scalar() == settings.mes_db` (debería ser `"dbizaro"`).
    - Test 4 (test_sql_loader.py DATA-05): `load_sql` normaliza extensión, resuelve subdirs, usa `lru_cache` (cache_info.hits crece tras segunda llamada).
    - Test 5 (test_sql_loader.py DATA-05): lanza `FileNotFoundError` (o equivalente de `importlib.resources`) si el archivo no existe.
    - Test 6 (test_schema_guard.py DATA-06, integration): `verify(engine_nexo)` con las 8 tablas presentes no lanza nada y loguea INFO.
    - Test 7 (test_schema_guard.py DATA-06, integration): con `critical_tables=("users", "__nonexistent_table__")` (kwarg, no monkeypatch) lanza `RuntimeError` con mensaje que contiene "missing"/"faltan" + nombre de la tabla y NO se ejecuta `create_all`.
    - Test 8 (test_schema_guard.py DATA-06, integration): con `NEXO_AUTO_MIGRATE=true` (monkeypatch env) + `critical_tables` incluyendo una faltante, registra log WARNING y termina sin raise (no verificamos side effect real; con monkeypatch de `NexoBase.metadata.create_all` podemos spy la llamada).
    - Test 9 (test_dto_immutable.py DATA-08): definir un `RowDummy(BaseModel)` con `model_config = ConfigDict(frozen=True, from_attributes=True)`; intentar mutar campo lanza `ValidationError` de Pydantic v2.
    - Test 10 (test_fixtures.py DATA-10): las fixtures `db_nexo`, `db_app`, `engine_mes_mock` importan y son callables; `db_nexo` yieldea una Session válida (si Postgres está up — skipif sino); `engine_mes_mock` devuelve un `MagicMock`.
  </behavior>
  <action>
1. Crear `tests/data/__init__.py` vacío.

2. Crear `tests/data/conftest.py` verbatim del patrón en PATTERNS.md líneas 808-867:

   ```python
   """Fixtures comunes para tests/data/ (Plan 03-01, DATA-10)."""
   from __future__ import annotations

   from typing import Iterator
   from unittest.mock import MagicMock

   import pytest
   from sqlalchemy import text
   from sqlalchemy.orm import Session

   from nexo.data.engines import (
       SessionLocalApp,
       SessionLocalNexo,
       engine_app,
       engine_mes,
       engine_nexo,
   )


   def _postgres_reachable() -> bool:
       try:
           with engine_nexo.connect() as c:
               c.execute(text("SELECT 1"))
           return True
       except Exception:
           return False


   def _mssql_reachable() -> bool:
       try:
           with engine_app.connect() as c:
               c.execute(text("SELECT 1"))
           return True
       except Exception:
           return False


   @pytest.fixture
   def db_nexo() -> Iterator[Session]:
       """Session Postgres nexo. Rollback al final (no truncate).

       Skip si Postgres no esta arriba (docker compose up -d db).
       """
       if not _postgres_reachable():
           pytest.skip("Postgres no arriba: docker compose up -d db")
       db = SessionLocalNexo()
       try:
           yield db
       finally:
           db.rollback()
           db.close()


   @pytest.fixture
   def db_app() -> Iterator[Session]:
       """Session SQL Server ecs_mobility. Skip si no hay SQL Server (CI)."""
       if not _mssql_reachable():
           pytest.skip("SQL Server ecs_mobility no arriba (esperable en CI)")
       db = SessionLocalApp()
       try:
           yield db
       finally:
           db.rollback()
           db.close()


   @pytest.fixture
   def engine_mes_mock(monkeypatch):
       """Engine MES mockeado (CI no tiene SQL Server; queries MES se testean con mocks)."""
       mock_engine = MagicMock()
       monkeypatch.setattr("nexo.data.engines.engine_mes", mock_engine)
       return mock_engine
   ```

3. Crear `tests/data/test_engines.py`:

   ```python
   """DATA-01 + DATA-11: engines + pool config."""
   from __future__ import annotations

   import pytest
   from sqlalchemy import text
   from sqlalchemy.engine import Engine

   from api.config import settings
   from nexo.data.engines import (
       SessionLocalApp,
       SessionLocalNexo,
       engine_app,
       engine_mes,
       engine_nexo,
   )


   def test_three_engines_exported():
       """DATA-01: engine_nexo, engine_app, engine_mes son Engine."""
       for e in (engine_nexo, engine_app, engine_mes):
           assert isinstance(e, Engine)


   def test_two_session_factories_exported():
       """DATA-01: session factories para nexo y app."""
       assert SessionLocalNexo is not None
       assert SessionLocalApp is not None


   def test_mes_pool_config():
       """DATA-11: pool_recycle=3600, pool_pre_ping=True."""
       # SQLAlchemy expone estos via pool atributos privados; consistente con
       # la introspeccion usada en tests/auth/.
       assert engine_mes.pool._recycle == 3600, (
           f"engine_mes pool_recycle esperado 3600, got {engine_mes.pool._recycle}"
       )
       assert engine_mes.pool._pre_ping is True, (
           "engine_mes pool_pre_ping debe ser True"
       )


   def test_mes_url_has_mes_db_as_default_catalog():
       """DATA-09 preparacion: DSN tiene DATABASE={mes_db} para kill 3-part names en 03-02."""
       assert engine_mes.url.database == settings.mes_db, (
           f"engine_mes debe apuntar a {settings.mes_db}, got {engine_mes.url.database}"
       )


   @pytest.mark.integration
   def test_engine_mes_database_is_dbizaro():
       """DATA-09: smoke real contra SQL Server — skip si no hay servidor."""
       try:
           with engine_mes.connect() as conn:
               db = conn.execute(text("SELECT DB_NAME()")).scalar()
       except Exception as exc:
           pytest.skip(f"SQL Server no alcanzable: {exc}")
       assert db == "dbizaro", f"engine_mes default catalog != dbizaro (got {db})"
   ```

4. Crear `tests/data/test_sql_loader.py`:

   ```python
   """DATA-05: loader con lru_cache + importlib.resources."""
   from __future__ import annotations

   import pytest

   from nexo.data.sql.loader import load_sql


   def test_load_sql_has_cache():
       """lru_cache expone cache_info()."""
       assert hasattr(load_sql, "cache_info")
       assert hasattr(load_sql, "cache_clear")


   def test_load_sql_normalizes_extension():
       """Acepta con o sin .sql; resuelve al mismo archivo."""
       # Durante 03-01 no hay archivos .sql aun; colocamos uno de prueba
       # ad-hoc via tmp dir seria overkill. En su lugar, verificamos que
       # con un nombre inexistente lanza error *igual* con ambas formas.
       with pytest.raises(Exception):
           load_sql("nonexistent/fake")
       with pytest.raises(Exception):
           load_sql("nonexistent/fake.sql")


   def test_load_sql_missing_raises():
       """Archivo no existe → FileNotFoundError (o equivalente)."""
       load_sql.cache_clear()
       with pytest.raises((FileNotFoundError, Exception)):
           load_sql("mes/definitely_not_here_0xdeadbeef")
   ```

5. Crear `tests/data/test_schema_guard.py` — **usa kwarg `critical_tables=`, no monkeypatch del modulo** (D-01 + nota testabilidad):

   ```python
   """DATA-06: schema_guard.verify comportamiento.

   Nota: inyectamos `critical_tables` como kwarg en lugar de monkeypatchear
   la constante del modulo. Esto evita fragilidad si `CRITICAL_TABLES` se
   convierte en frozenset/tuple computada en import time.
   """
   from __future__ import annotations

   import pytest

   from nexo.data import schema_guard
   from nexo.data.engines import engine_nexo

   from tests.data.conftest import _postgres_reachable

   pytestmark = [
       pytest.mark.integration,
       pytest.mark.skipif(
           not _postgres_reachable(),
           reason="Postgres no arriba: docker compose up -d db",
       ),
   ]


   def test_verify_ok_with_all_tables(caplog):
       """Todas las 8 tablas existen → None + log INFO."""
       with caplog.at_level("INFO", logger="nexo.schema_guard"):
           schema_guard.verify(engine_nexo)
       # El log INFO debe mencionar "OK".
       assert any("OK" in r.message for r in caplog.records)


   def test_verify_raises_when_table_missing(monkeypatch):
       """Tabla ficticia faltante + auto_migrate=false → RuntimeError.

       Inyectamos `critical_tables` como kwarg (parametro publico). NO
       monkeypatch de schema_guard.CRITICAL_TABLES — eso es fragil.
       """
       monkeypatch.delenv("NEXO_AUTO_MIGRATE", raising=False)
       with pytest.raises(RuntimeError) as exc_info:
           schema_guard.verify(
               engine_nexo,
               critical_tables=("users", "__nonexistent_table__"),
           )
       assert "__nonexistent_table__" in str(exc_info.value)


   def test_auto_migrate_creates_missing(monkeypatch, caplog):
       """NEXO_AUTO_MIGRATE=true + tabla faltante → create_all + log WARNING.

       Igual que el test anterior: `critical_tables` kwarg, no monkeypatch.
       """
       monkeypatch.setenv("NEXO_AUTO_MIGRATE", "true")

       called = {"create_all": False}

       def fake_create_all(bind):  # noqa: ARG001
           called["create_all"] = True

       monkeypatch.setattr(
           "nexo.data.schema_guard.NexoBase.metadata.create_all", fake_create_all
       )
       with caplog.at_level("WARNING", logger="nexo.schema_guard"):
           schema_guard.verify(
               engine_nexo,
               critical_tables=("users", "__nonexistent_table__"),
           )
       assert called["create_all"] is True
       assert any("auto-migrate" in r.message.lower() or "NEXO_AUTO_MIGRATE" in r.message
                  for r in caplog.records)
   ```

6. Crear `tests/data/test_dto_immutable.py`:

   ```python
   """DATA-08: DTOs frozen (Pydantic v2)."""
   from __future__ import annotations

   import pytest
   from pydantic import BaseModel, ConfigDict, ValidationError


   class _RowDummy(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       nombre: str


   def test_frozen_dto_rejects_mutation():
       row = _RowDummy(id=1, nombre="test")
       with pytest.raises(ValidationError):
           row.id = 2  # frozen → Pydantic v2 lanza ValidationError


   def test_frozen_dto_roundtrips_from_attributes():
       class _Src:
           id = 7
           nombre = "src"
       src = _Src()
       row = _RowDummy.model_validate(src)
       assert row.id == 7
       assert row.nombre == "src"
   ```

7. Crear `tests/data/test_fixtures.py`:

   ```python
   """DATA-10: fixtures de conftest.py funcionan."""
   from __future__ import annotations

   from unittest.mock import MagicMock

   import pytest
   from sqlalchemy.orm import Session


   def test_db_nexo_fixture_yields_session(db_nexo):
       """db_nexo yieldea una Session (skip si Postgres no arriba)."""
       assert isinstance(db_nexo, Session)


   def test_engine_mes_mock_is_magicmock(engine_mes_mock):
       """engine_mes_mock sustituye nexo.data.engines.engine_mes con un MagicMock."""
       assert isinstance(engine_mes_mock, MagicMock)


   def test_db_app_fixture_optional(db_app):
       """db_app skip si no hay SQL Server (CI); Session si lo hay."""
       assert isinstance(db_app, Session)
   ```
  </action>
  <verify>
    <automated>docker compose exec -T web pytest tests/data/test_engines.py tests/data/test_sql_loader.py tests/data/test_dto_immutable.py -q -m "not integration" -x</automated>
    <automated>docker compose exec -T web pytest tests/data/ -q -x</automated>
    <automated>docker compose exec -T web pytest tests/auth/ -q</automated>
  </verify>
  <done>
    - Los 5 test files existen en `tests/data/` + `conftest.py` + `__init__.py`.
    - `pytest tests/data/ -q -m "not integration"` exit 0 (unit tests verdes).
    - `pytest tests/data/ -q` exit 0 con Postgres arriba; skipea SQL Server si no está disponible.
    - `pytest tests/auth/ -q` sigue 11 passed / 1 skipped (regression IDENT-06).
    - `test_schema_guard.py` usa kwarg `critical_tables=` (no monkeypatch del modulo).
    - `make test-data` corre hasta el final con exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → schema_guard | `NEXO_AUTO_MIGRATE` var cruza de entorno al código que muta el schema Postgres |
| settings → engines | Credenciales Postgres/SQL Server cruzan de `.env` (vía `api.config.settings`) a factories |
| `nexo/db/*` shim → `nexo/data/*` | Consumidores Phase 2 cruzan via re-export; romper el shim rompe auth y audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (tampering / silent schema change) | `schema_guard.verify` con `NEXO_AUTO_MIGRATE=true` | mitigate | Default `false` en `.env.example` + comentario "SOLO dev"; log `WARNING` explícito "auto-migración completada. NO usar en producción"; mención explícita en PATTERNS.md Landmine #4 de que el flag es peligroso en prod. `scripts/init_nexo_schema.py` es la ruta canónica para prod (existe desde 02-01). |
| T-03-01-02 | I (info disclosure vía DSN) | `_build_mes_dsn()` y `_build_pg_dsn()` | mitigate | Leen de `settings.*` (pydantic-settings lee env y no persiste en log). No hacer `print(_build_*_dsn())` ni logguear la URL con credenciales. Pool `echo=settings.debug` solo muestra SQL, no credenciales (Landmine verificado: SQLAlchemy oculta password en repr del Engine). |
| T-03-01-03 | D (denial of service via pool exhaustion) | `engine_mes` con `pool_size=3, max_overflow=2, pool_timeout=15` | accept | 5 conexiones simultáneas máximas hacia dbizaro son suficientes para el tráfico interno LAN (verificado en RESEARCH §pool sizing); timeout=15s evita deadlock con cola infinita. |
| T-03-01-04 | E (elevation — rol oee vs nexo_app) | `engine_nexo` usa `settings.effective_pg_user` | mitigate | Landmine #12 documentado; test `tests/auth/test_audit_append_only.py` confirma que el engine runtime conecta como `nexo_app` cuando `NEXO_PG_APP_USER` está definido. Fallback a `oee` solo en deploys pre-02-04. |
| T-03-01-05 | T (lifespan abort bypass) | Try/except de `init_db()` podría tragarse `schema_guard` errors | mitigate | `schema_guard.verify()` se coloca FUERA del try/except; si lanza `RuntimeError`, el lifespan aborta antes de entrar al try. Test 4 del task 2 verifica explícitamente. |

**Block on HIGH severity:** ninguna. Todas las mitigaciones están implementadas o aceptadas con rationale.
</threat_model>

<verification>
Hard gate (ejecutar en orden; CADA comando exit 0):

```bash
# 1. Código importable
docker compose exec -T web python -c "from nexo.data.engines import engine_nexo, engine_app, engine_mes, SessionLocalNexo, SessionLocalApp; from nexo.db.engine import engine_nexo as legacy; assert legacy is engine_nexo; print('engines+shim OK')"

# 2. Pool + DSN
docker compose exec -T web python -c "from nexo.data.engines import engine_mes; from api.config import settings; assert engine_mes.pool._recycle == 3600; assert engine_mes.pool._pre_ping is True; assert engine_mes.url.database == settings.mes_db; print('DATA-11 + DATA-09 prep OK')"

# 3. Loader + schema_guard signature
docker compose exec -T web python -c "from nexo.data.sql.loader import load_sql; from nexo.data.schema_guard import verify, CRITICAL_TABLES; import inspect; assert len(CRITICAL_TABLES) == 8; assert hasattr(load_sql, 'cache_info'); assert 'critical_tables' in inspect.signature(verify).parameters; print('loader + schema_guard OK')"

# 4. deps wiring
docker compose exec -T web python -c "from api.deps import get_db_app, get_db_nexo, get_engine_mes, DbApp, DbNexo, EngineMes; print('deps OK')"

# 5. Lifespan wired (grep source)
docker compose exec -T web python -c "import inspect, api.main as m; src = inspect.getsource(m.lifespan); assert 'schema_guard.verify(engine_nexo)' in src; print('lifespan wired')"

# 6. App arranca (schema presente → verify pasa, init_db corre)
docker compose exec -T web python -c "from fastapi.testclient import TestClient; from api.main import app; c = TestClient(app); r = c.get('/login'); assert r.status_code == 200; print('app arranca, schema_guard verde')"

# 7. Tests
docker compose exec -T web pytest tests/data/ -q
docker compose exec -T web pytest tests/auth/ -q  # 11 passed / 1 skipped (IDENT-06 intacto)

# 8. Makefile + .env (dos checks separados — no encadenar grep -q | head, ver revision)
grep -qE "^test-data:" Makefile && echo "OK test-data target defined" || echo "MISSING: test-data target in Makefile"
grep -q "NEXO_AUTO_MIGRATE" .env.example && echo "OK NEXO_AUTO_MIGRATE entry" || echo "MISSING: NEXO_AUTO_MIGRATE in .env.example"
```

**Regression de IDENT-06 (bloqueante):**
```bash
docker compose exec -T web pytest tests/auth/ -q
# Esperado: `11 passed, 1 skipped in Xs` (el skip es por rol app no configurado en CI sin make nexo-app-role — mismo comportamiento que pre-03-01)
```
</verification>

<success_criteria>
- `nexo/data/engines.py` exporta `engine_nexo`, `engine_app`, `engine_mes`, `SessionLocalNexo`, `SessionLocalApp`.
- `engine_mes.pool._recycle == 3600`, `engine_mes.pool._pre_ping is True`, `engine_mes.url.database == settings.mes_db` (DATA-01, DATA-11).
- `nexo/db/engine.py` es shim (≤10 líneas efectivas, re-exporta desde `nexo.data.engines`); consumidores Phase 2 funcionan sin cambios.
- `nexo/data/sql/loader.py:load_sql` existe, tiene `lru_cache`, lee via `importlib.resources` (DATA-05).
- `nexo/data/schema_guard.py:verify(engine, critical_tables=CRITICAL_TABLES)` aborta con `RuntimeError` si falta tabla y `NEXO_AUTO_MIGRATE != true`; crea vía `create_all` si el flag está activo (DATA-06). Acepta kwarg `critical_tables` para testabilidad.
- `api/main.py:lifespan` ejecuta `schema_guard.verify(engine_nexo)` antes de `init_db()`, fuera del try/except.
- `api/deps.py` expone `get_db_app`, `get_db_nexo`, `get_engine_mes` + `DbApp`, `DbNexo`, `EngineMes`.
- `tests/data/` con 5 tests + `conftest.py` + `__init__.py`; `pytest tests/data/ -q` exit 0 (skipea integration sin SQL Server).
- `tests/data/test_schema_guard.py` pasa `critical_tables=` como kwarg (no monkeypatch del modulo — no frágil).
- `pytest tests/auth/ -q` sigue 11 passed / 1 skipped.
- `make test-data` target funcional.
- `.env.example` documenta `NEXO_AUTO_MIGRATE=false`.
- Orden LIFO de middlewares en `api/main.py` inalterado.
</success_criteria>

<output>
After completion, create `.planning/phases/03-capa-de-datos/03-01-SUMMARY.md` con:

- Files created (14) y files modified (5)
- Commits atómicos recomendados:
  1. `refactor(03-01): move engine_nexo to nexo/data/engines.py + add engine_mes + shim`
  2. `feat(03-01): add sql loader + schema_guard (verify with critical_tables kwarg) + dto package skeleton`
  3. `feat(03-01): wire schema_guard in lifespan + api/deps.py + Makefile test-data`
  4. `test(03-01): add tests/data/ with fixtures and 5 wave-0 validations`
- Regression confirmada: `pytest tests/auth/ -q` = 11 passed / 1 skipped post-refactor.
- Nota: `engine_mes` pool params verificados; DSN con `DATABASE=dbizaro` habilita DATA-09 en 03-02.
- Nota: `schema_guard.verify` acepta `critical_tables=` kwarg (default `CRITICAL_TABLES`) — tests inyectan sin monkeypatch.
- Nota: shim `nexo/db/engine.py` vive durante Phase 3; se elimina al final de 03-03.
- Riesgos conocidos para 03-02 / 03-03: ninguno abierto. Paralelización autorizada.
</output>
</content>
</invoke>