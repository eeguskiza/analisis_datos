---
phase: 03-capa-de-datos
plan: 03
slug: app-nexo-repositories-routers-refactor
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - api/routers/ciclos.py
  - api/routers/recursos.py
  - api/routers/historial.py
  - api/routers/auditoria.py
  - api/routers/usuarios.py
  - nexo/services/auth.py
  - api/services/pipeline.py
  - api/database.py
  - nexo/db/engine.py
  - nexo/db/models.py
  - tests/auth/test_audit_append_only.py
files_created:
  - nexo/data/repositories/app.py
  - nexo/data/repositories/nexo.py
  - nexo/data/models_app.py
  - nexo/data/models_nexo.py
  - nexo/data/dto/app.py
  - nexo/data/dto/nexo.py
  - tests/data/test_app_repository.py
  - tests/data/test_nexo_repository.py
  - tests/data/test_routers_smoke.py
  - tests/data/test_no_raw_pyodbc_in_routers.py
autonomous: true
requirements: [DATA-03, DATA-04, DATA-07, DATA-08, DATA-10]
requirements_addressed: [DATA-03, DATA-04, DATA-07, DATA-08, DATA-10]
tags: [data-layer, app, nexo, repository, refactor, orm-migration]
must_haves:
  truths:
    - "`nexo/data/repositories/app.py` expone 6 repos: `RecursoRepo`, `CicloRepo`, `EjecucionRepo`, `MetricaRepo`, `LukRepo`, `ContactoRepo` (DATA-03)"
    - "`nexo/data/repositories/nexo.py` expone 3 repos: `UserRepo`, `RoleRepo`, `AuditRepo` (DATA-04)"
    - "`AuditRepo.append(...)` NO comitea internamente (caller owns transaction, IDENT-06 compat)"
    - "ORM models APP migrados a `nexo/data/models_app.py`; `api/database.py` mantiene re-export para `api/services/pipeline.py` (Landmine #9)"
    - "ORM models NEXO migrados a `nexo/data/models_nexo.py`; `nexo/db/models.py` re-exporta como shim transicional"
    - "`nexo/services/auth.py` consume `UserRepo` para `get_user_by_email` (refactor interno, firma estable)"
    - "`api/routers/auditoria.py` y `api/routers/usuarios.py` usan `get_db_nexo` de `api.deps` (eliminado el `get_nexo_db` duplicado)"
    - "`api/routers/{historial,recursos,ciclos}.py` consumen repos APP en lugar de queries ORM inline"
    - "DTOs frozen en `nexo/data/dto/{app,nexo}.py` para todo lo que cruza router↔repo"
    - "Meta-test `test_no_raw_pyodbc_in_routers` verde sobre los 10 routers"
    - "`pytest tests/auth/ -q` sigue 11 passed / 1 skipped tras la migración ORM"
    - "`pytest tests/data/ -q` exit 0 (incluye 4 tests nuevos: app_repo, nexo_repo, routers_smoke, no_pyodbc)"
    - "`api/main.py` arranca sin errores; los 10 routers responden 200 con cookie"
    - "`api/services/pipeline.py` es propiedad EXCLUSIVA de este plan (ver `<context_handoff>`): todos los edits de pipeline.py (migración ORM + swap de imports MES si 03-02 lo dejó pendiente) se consolidan en una sola tarea atómica aquí"
  artifacts:
    - path: "nexo/data/repositories/app.py"
      provides: "6 repos APP con Session inyectada"
      min_lines: 120
      exports: ["RecursoRepo", "CicloRepo", "EjecucionRepo", "MetricaRepo", "LukRepo", "ContactoRepo"]
    - path: "nexo/data/repositories/nexo.py"
      provides: "UserRepo, RoleRepo, AuditRepo"
      min_lines: 70
      exports: ["UserRepo", "RoleRepo", "AuditRepo"]
    - path: "nexo/data/models_app.py"
      provides: "Ciclo, Recurso, Ejecucion, InformeMeta, DatosProduccion, Contacto, MetricaOEE, ReferenciaStats, IncidenciaResumen, Base"
      min_lines: 150
      exports: ["Base", "Ciclo", "Recurso", "Ejecucion", "InformeMeta", "DatosProduccion", "Contacto", "MetricaOEE", "ReferenciaStats", "IncidenciaResumen", "SECTION_MAP"]
    - path: "nexo/data/models_nexo.py"
      provides: "NexoUser, NexoRole, NexoDepartment, NexoPermission, NexoSession, NexoLoginAttempt, NexoAuditLog + user_departments + NexoBase + NEXO_SCHEMA"
      min_lines: 150
      exports: ["NEXO_SCHEMA", "NexoBase", "NexoUser", "NexoRole", "NexoDepartment", "NexoPermission", "NexoSession", "NexoLoginAttempt", "NexoAuditLog", "user_departments"]
    - path: "nexo/data/dto/app.py"
      provides: "RecursoRow, CicloRow, EjecucionRow, MetricaRow, ReferenciaRow, IncidenciaRow, InformeRow, ContactoRow, LukRow"
      min_lines: 80
    - path: "nexo/data/dto/nexo.py"
      provides: "UserRow, RoleRow, DepartmentRow, AuditLogRow"
      min_lines: 40
    - path: "tests/data/test_nexo_repository.py"
      provides: "AuditRepo no-commit contract + UserRepo + RoleRepo CRUD"
      min_lines: 80
      contains: "test_audit_repo_no_commits"
    - path: "tests/data/test_routers_smoke.py"
      provides: "TestClient smokes parametrizado sobre 10 routers"
      min_lines: 40
    - path: "tests/data/test_no_raw_pyodbc_in_routers.py"
      provides: "meta grep final sobre api/routers/"
      min_lines: 15
  key_links:
    - from: "nexo/services/auth.py:get_user_by_email"
      to: "UserRepo.get_by_email"
      via: "delegación interna; firma externa estable"
      pattern: "UserRepo\\(db\\)\\.get_by_email"
    - from: "api/routers/auditoria.py"
      to: "nexo.data.repositories.nexo.AuditRepo"
      via: "AuditRepo(db_nexo).list_filtered(...)"
      pattern: "AuditRepo\\("
    - from: "api/routers/historial.py"
      to: "nexo.data.repositories.app.EjecucionRepo"
      via: "Depends(DbApp) → EjecucionRepo(db).list_recent()"
      pattern: "EjecucionRepo\\("
    - from: "api/database.py"
      to: "nexo.data.models_app"
      via: "re-export para api/services/pipeline.py (Landmine #9, ~15 referencias)"
      pattern: "from nexo\\.data\\.models_app import"
    - from: "nexo/db/models.py"
      to: "nexo.data.models_nexo"
      via: "shim transicional"
      pattern: "from nexo\\.data\\.models_nexo import"
    - from: "api/services/pipeline.py"
      to: "api/services/db.py (facade re-pointed en 03-02 a MesRepository) + nexo.data.models_app (re-export via api/database.py)"
      via: "ownership exclusiva 03-03: consolida migración ORM + swap MES legacy si 03-02 lo dejó pendiente"
      pattern: "from api\\.services\\.db|from api\\.database"
---

<objective>
Construir la capa APP + NEXO: migrar los modelos ORM a `nexo/data/models_{app,nexo}.py` (con shims de re-export), crear 9 repos (`RecursoRepo`, `CicloRepo`, `EjecucionRepo`, `MetricaRepo`, `LukRepo`, `ContactoRepo` para APP; `UserRepo`, `RoleRepo`, `AuditRepo` para NEXO), crear DTOs frozen en `nexo/data/dto/{app,nexo}.py`, refactorizar 3 routers APP (`ciclos`, `recursos`, `historial`), refactorizar `nexo/services/auth.py` + `api/routers/{auditoria,usuarios}.py` para consumir repos, y cerrar los meta-tests finales.

Purpose: cerrar DATA-03, DATA-04, DATA-07, DATA-08 y DATA-10. Dejar el schema `nexo` y el schema `ecs_mobility` detrás de repos limpios, con auth middleware y audit middleware apoyándose en la capa nueva sin cambiar contratos externos.

Output: 10 archivos nuevos, 11 modificados. Gate IDENT-06 intacto (`tests/auth/` 11 passed / 1 skipped). Meta-tests verdes. Smoke HTTP sobre 10 routers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context_handoff>
## Ownership handoff: `api/services/pipeline.py` (propiedad EXCLUSIVA de 03-03)

**Contexto** (Blocker-1 de revision): 03-02 y 03-03 corren en paralelo (ambos Wave 2, `depends_on: [03-01]`). Asignar `api/services/pipeline.py` a los dos planes produce conflicto de merge. La revisión movió TODOS los edits de pipeline.py aquí.

**Qué recibe 03-03 de 03-02:**
- `api/services/db.py` ya re-pointeado a `MesRepository` (03-02 Task 3.7). Este es el facade que pipeline.py consume; el swap del backend MES es transparente para pipeline.py via ese facade.
- `MesRepository`, `nexo/data/engines.py`, `api/deps.py` disponibles e importables.
- `api/services/pipeline.py` **sin modificar** por 03-02 (verificado con test de `git diff` en el verification gate de 03-02).

**Qué hace 03-03 con pipeline.py (consolidado en Task 4.7, un único commit atómico):**
1. **Migración ORM (Landmine #9, ~15 touch points)**: pipeline.py importa `MetricaOEE`, `ReferenciaStats`, `IncidenciaResumen`, `Ejecucion`, `InformeMeta`, `DatosProduccion` desde `api.database`. Al migrar esos modelos a `nexo/data/models_app.py` (Task 1) y dejar re-export en `api/database.py`, pipeline.py sigue funcionando sin cambios a sus imports. **No se requiere edit de pipeline.py por la migración ORM** — el re-export lo cubre.
2. **Swap import MES legacy (si aplica)**: si pipeline.py importa DIRECTAMENTE de `OEE.db.connector` (no via `api.services.db`), el executor decide:
   - **Opción A (preferida, minimum diff)**: dejar el import legacy tal cual. `OEE/db/connector.py` no es refactorizado en Mark-III (D-04); es legítimo seguir usándolo desde pipeline.py via `api.services.db` que ya delega a `MesRepository`.
   - **Opción B (cleanup)**: swap a `from nexo.data.repositories.mes import MesRepository` + `from nexo.data.engines import engine_mes` y usar el repo directamente. Solo si simplifica el código.
3. **Si 03-02 dejó pendiente el swap MES (Opción B y no lo hizo):** este plan lo hace aquí, atómicamente con la task ORM. Grep `api/services/pipeline.py` buscando `from OEE.db.connector` para confirmar.
4. **Una sola tarea (Task 4.7)** toca pipeline.py. Commit atómico: `refactor(03-03): pipeline.py — ORM migration + MES handoff from 03-02`.

**Verificación (automatizada en Task 4.7):**
- Tras este plan, `api/services/pipeline.py` debe seguir generando PDFs equivalentes (success criterion #5 ya validado en 03-02 via PDF regression gate; aquí solo verificamos que no rompemos el pipeline con la migración ORM).
- Grep `import pyodbc` en pipeline.py = 0 (si alguna vez estuvo, debe irse aquí).
</context_handoff>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-datos/03-CONTEXT.md
@.planning/phases/03-capa-de-datos/03-RESEARCH.md
@.planning/phases/03-capa-de-datos/03-PATTERNS.md
@.planning/phases/03-capa-de-datos/03-VALIDATION.md
@.planning/phases/03-capa-de-datos/03-01-SUMMARY.md
@.planning/phases/03-capa-de-datos/03-02-SUMMARY.md
@.planning/phases/02-identidad-auth-rbac-audit/02-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor va a consumir o crear. -->

From nexo/data/engines.py + api/deps.py (creados en 03-01):
```python
from nexo.data.engines import SessionLocalApp, SessionLocalNexo, engine_nexo, engine_app
from api.deps import DbApp, DbNexo, get_db_nexo, get_db_app
```

From api/database.py (modelos actuales a migrar):
```python
class Base(DeclarativeBase): pass
class Ciclo, Recurso, Ejecucion, InformeMeta, DatosProduccion, Contacto, MetricaOEE, ReferenciaStats, IncidenciaResumen(Base): ...
SECTION_MAP: dict[str, str]
def init_db(), _import_ciclos_csv(session), _import_recursos_json(session)
def get_db(): ...  # yield SessionLocal (sigue funcionando, no lo tocamos)
def check_db_health(): ...
```

From nexo/db/models.py (modelos NEXO actuales a migrar):
```python
NEXO_SCHEMA = "nexo"
class NexoBase(DeclarativeBase): pass
class NexoUser, NexoRole, NexoDepartment, NexoPermission, NexoSession, NexoLoginAttempt, NexoAuditLog(NexoBase): ...
user_departments: Table  # N:M
```

From nexo/services/auth.py (firmas a preservar; solo cambia el implementación):
```python
def get_user_by_email(db: Session, email: str) -> Optional[NexoUser]: ...
def create_session, get_session, extend_session, revoke_session, revoke_all_sessions  # KEEP AS-IS
def check_lockout, record_failed_attempt, clear_attempts  # KEEP AS-IS
def require_permission(permission: str) -> Callable: ...  # KEEP AS-IS
PERMISSION_MAP: dict  # KEEP AS-IS
```

Repos target shapes:

```python
# nexo/data/repositories/app.py
class RecursoRepo:
    def __init__(self, db: Session): ...
    def list_all(self) -> list[RecursoRow]: ...
    def list_activos(self) -> list[RecursoRow]: ...
    def get_by_nombre(self, nombre: str) -> RecursoRow | None: ...
    def replace_all(self, recursos: list[dict]) -> None: ...  # caller commits
    def add(self, *, centro_trabajo: int, nombre: str, seccion: str, activo: bool) -> RecursoRow: ...

class CicloRepo:
    def list_all(self) -> list[CicloRow]: ...
    def replace_all(self, rows: list[dict]) -> None: ...
    def add(self, *, maquina: str, referencia: str, tiempo_ciclo: float) -> CicloRow: ...

class EjecucionRepo:
    def list_recent(self, limit: int = 50) -> list[EjecucionRow]: ...
    def get_by_id(self, id: int) -> EjecucionRow | None: ...

class MetricaRepo:
    def tendencias_recurso(self, recurso: str, fecha_inicio: str | None, fecha_fin: str | None) -> list[MetricaRow]: ...

class LukRepo:
    # Wraps queries actualmente inline en luk4.py si se decide. Opcional.
    def latest_estado(self) -> LukRow | None: ...

class ContactoRepo:
    def list_all(self) -> list[ContactoRow]: ...

# nexo/data/repositories/nexo.py
class UserRepo:
    def __init__(self, db: Session): ...
    def get_by_email(self, email: str) -> NexoUser | None:
        """Devuelve ORM (not DTO) porque lo consume auth.py internamente."""

class RoleRepo:
    def list_all(self) -> list[RoleRow]: ...

class AuditRepo:
    def __init__(self, db: Session): ...
    def append(self, *, user_id: int | None, ip: str, method: str, path: str,
               status: int, details_json: str | None) -> None:
        """INSERT solo. NO commit (caller orquesta)."""
    def list_filtered(self, *, user_email=None, date_from=None, date_to=None,
                      path=None, status=None, page=1, limit=100) -> list[AuditLogRow]: ...
```

DTO targets:

```python
# nexo/data/dto/app.py — frozen Pydantic v2
class RecursoRow(BaseModel): ...  # from api/database.py:65-74
class CicloRow(BaseModel): ...
class EjecucionRow(BaseModel): ...
class MetricaRow(BaseModel): ...
class ReferenciaRow(BaseModel): ...
class IncidenciaRow(BaseModel): ...
class InformeRow(BaseModel): ...
class ContactoRow(BaseModel): ...
class LukRow(BaseModel): ...

# nexo/data/dto/nexo.py
class UserRow(BaseModel): ...
class RoleRow(BaseModel): ...
class DepartmentRow(BaseModel): ...
class AuditLogRow(BaseModel): ...
```

Current consumers that reference models through `api.database`:
- `api/services/pipeline.py` — ~15 referencias a `MetricaOEE`, `ReferenciaStats`, `IncidenciaResumen`, `Ejecucion`, `InformeMeta`, `DatosProduccion`. RESEARCH Landmine #9. **Propiedad exclusiva de este plan — ver `<context_handoff>`.**
- `api/routers/historial.py`, `recursos.py`, `ciclos.py`, `centro_mando.py` — usan `api.database.Recurso` + `get_db`.
</interfaces>

<research_notes>
- **D-01 scope DATA-05 (clarificado 2026-04-19 en CONTEXT.md)**: los archivos `.sql` versionados aplican SOLO a queries con `text()` (queries MES y queries APP/NEXO complejas o reutilizables). **Queries ORM puras vía `session.query(Model).filter(...)` NO requieren archivo `.sql`** — el modelo declarativo SQLAlchemy ES la representación canónica. Aplicación concreta en este plan:
  - `RecursoRepo`, `CicloRepo`, `EjecucionRepo`, `MetricaRepo`, `ContactoRepo` (APP) y `UserRepo`, `RoleRepo`, `AuditRepo` (NEXO) usan ORM puro en su implementación estándar → **DATA-05-compliant por D-01 scope clarification, NO se generan archivos `.sql`**.
  - Solo si algún método de repo APP/NEXO resulta más claro con `text()` (casos raros: queries analíticas con CTEs complejas, joins dinámicos), extraer a `nexo/data/sql/app/<method>.sql` o `nexo/data/sql/nexo/<method>.sql`. Expectativa: 0 archivos `.sql` APP/NEXO nuevos en este plan; si alguno aparece, documentar justificación en commit.
  - Cita explícita en el SUMMARY y en cualquier commit donde DATA-05 aplique: "Per CONTEXT.md D-01 (clarificación scope DATA-05): repos APP/NEXO con ORM puro cumplen DATA-05 sin `.sql` versionado; el modelo SQLAlchemy declarativo es la representación canónica."
</research_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar ORM models a nexo/data/models_{app,nexo}.py + shims (Wave 1)</name>
  <files>
    nexo/data/models_app.py (new),
    nexo/data/models_nexo.py (new),
    api/database.py (modified — re-export para preservar API),
    nexo/db/models.py (modified → shim)
  </files>
  <read_first>
    - api/database.py completo (líneas 44-190 = modelos + SECTION_MAP + init_db)
    - nexo/db/models.py completo (1-173)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones "nexo/data/models_app.py" (líneas 760-768) y "nexo/data/models_nexo.py" (líneas 770-784)
    - api/services/pipeline.py (grep `from api.database import` — identificar las 15 refs Landmine #9; ver `<context_handoff>` — pipeline.py se TOCA en Task 4.7, aquí solo se lee)
    - tests/auth/test_audit_append_only.py:30 (el import `from nexo.db.engine import engine_nexo` — tras 03-01 ya viene del shim; este plan puede actualizarlo al nuevo path en la task 6)
  </read_first>
  <behavior>
    - Test 1: `nexo.data.models_app` exporta `Ciclo`, `Recurso`, `Ejecucion`, `InformeMeta`, `DatosProduccion`, `Contacto`, `MetricaOEE`, `ReferenciaStats`, `IncidenciaResumen`, `Base`, `SECTION_MAP`.
    - Test 2: `api.database.Ciclo is nexo.data.models_app.Ciclo` — re-export preservado (Landmine #9).
    - Test 3: `nexo.data.models_nexo` exporta `NexoUser`, `NexoRole`, `NexoDepartment`, `NexoPermission`, `NexoSession`, `NexoLoginAttempt`, `NexoAuditLog`, `user_departments`, `NexoBase`, `NEXO_SCHEMA`.
    - Test 4: `nexo.db.models.NexoUser is nexo.data.models_nexo.NexoUser` — shim funcional.
    - Test 5: `nexo/services/auth.py` arranca sin errores tras el cambio (los imports de `nexo.db.models` resuelven al shim, que re-exporta).
    - Test 6: `api/services/pipeline.py` sigue funcionando sin tocar sus imports (se apoya en el re-export de `api/database.py`). **NOTA**: pipeline.py NO se edita en esta task — Task 4.7 es la que lo toca (ver `<context_handoff>`).
  </behavior>
  <action>
1. **Crear `nexo/data/models_nexo.py`** copiando verbatim el contenido actual de `nexo/db/models.py` (líneas 1-173). Preservar el header docstring y las 8 clases + `user_departments` + `NEXO_SCHEMA` + `NexoBase` + `_utcnow`. Sin cambios de comportamiento.

2. **Sustituir `nexo/db/models.py`** por un shim:
   ```python
   """SHIM — modelos ORM del schema `nexo` movidos a `nexo.data.models_nexo`
   en Plan 03-03. Este modulo re-exporta los simbolos originales para no
   romper consumidores (`nexo.services.auth`, `api.routers.auditoria`,
   `api.routers.usuarios`, `tests/auth/*`). Se elimina cuando todos los
   consumers migren sus imports a `nexo.data.models_nexo` (Mark-IV).
   """
   from nexo.data.models_nexo import (  # noqa: F401
       NEXO_SCHEMA,
       NexoAuditLog,
       NexoBase,
       NexoDepartment,
       NexoLoginAttempt,
       NexoPermission,
       NexoRole,
       NexoSession,
       NexoUser,
       user_departments,
   )
   ```

3. **Crear `nexo/data/models_app.py`** copiando el bloque ORM de `api/database.py` (líneas 44-190). Preservar:
   - `class Base(DeclarativeBase)`
   - `class Ciclo`, `Recurso`, `Ejecucion`, `InformeMeta`, `DatosProduccion`, `Contacto`, `MetricaOEE`, `ReferenciaStats`, `IncidenciaResumen`
   - `SECTION_MAP`
   
   NO mover `engine`, `SessionLocal`, `_mssql_creator`, `init_db`, `_import_*`, `get_db`, `check_db_health` — esas quedan en `api/database.py` (son infraestructura, no modelos).
   
   Nota: `datetime.utcnow` → opcional migrar a `datetime.now(timezone.utc)` (RESEARCH §State of the Art). Si el executor decide NO tocarlo para minimizar diff, OK. Si lo toca, hacerlo en commit separado `refactor(03-03): datetime.now(timezone.utc) en models_app`.

4. **Modificar `api/database.py`** — eliminar las clases ORM (líneas 44-190) y reemplazarlas con un re-export al final del archivo (tras `_mssql_creator`, engine, `SessionLocal`, `Base`):
   ```python
   # ... mantener engine, SessionLocal, _mssql_creator, init_db, _import_*, get_db, check_db_health ...

   # ── ORM models — migrados a nexo/data/models_app.py en Plan 03-03 ────────
   # Re-export para preservar consumidores existentes (Landmine #9 en
   # api/services/pipeline.py — 15 refs a MetricaOEE, ReferenciaStats,
   # IncidenciaResumen, Ejecucion, InformeMeta, DatosProduccion). Elimina
   # cuando todos los consumers migren al path nuevo (Mark-IV).
   from nexo.data.models_app import (  # noqa: E402, F401
       Base,
       Ciclo,
       Contacto,
       DatosProduccion,
       Ejecucion,
       IncidenciaResumen,
       InformeMeta,
       MetricaOEE,
       Recurso,
       ReferenciaStats,
       SECTION_MAP,
   )
   ```
   
   Clave: `Base` debe ser el mismo objeto importado (no un DeclarativeBase distinto), sino `init_db()` y los `session.query(Recurso)` rompen. Si surge conflicto de metadatas, es porque en `nexo/data/models_app.py` se instanció `Base` separadamente pero los modelos lo heredan; entonces `api/database.py:init_db` hará `session.query(Recurso)` contra el metadata nuevo — debe funcionar porque la tabla física es la misma. Verificar con el test 2.

5. **Caso edge**: si en `api/database.py` hay código que hace `Base.metadata.create_all(bind=engine)` o similar post-import, verificar que sigue apuntando al metadata correcto (el del nuevo módulo). El archivo actual usa un comentario "Las tablas ya existen en SQL Server, no hacemos create_all" y solo corre importaciones de CSV/JSON — no debería haber problema.
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from nexo.data.models_app import Recurso, Ciclo, MetricaOEE, Base, SECTION_MAP; from api.database import Recurso as R2, MetricaOEE as M2; assert Recurso is R2; assert MetricaOEE is M2; print('models_app OK + re-export')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.models_nexo import NexoUser, NexoAuditLog, NexoBase, NEXO_SCHEMA; from nexo.db.models import NexoUser as U2, NexoAuditLog as A2; assert NexoUser is U2; assert NexoAuditLog is A2; assert NEXO_SCHEMA == 'nexo'; print('models_nexo OK + shim')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.services.auth import get_user_by_email, require_permission; print('auth imports OK')"</automated>
    <automated>docker compose exec -T web python -c "from api.services.pipeline import run_pipeline; print('pipeline imports OK')"</automated>
    <automated>docker compose exec -T web pytest tests/auth/ -q</automated>
  </verify>
  <done>
    - `nexo/data/models_app.py` y `nexo/data/models_nexo.py` creados con copia verbatim de los ORM blocks.
    - `api/database.py` mantiene engine/infra + re-exporta clases.
    - `nexo/db/models.py` es shim.
    - Tests `tests/auth/` siguen 11 passed / 1 skipped (regression IDENT-06 intacta).
    - `api/services/pipeline.py` arranca sin errores pero **no ha sido editado aún** — su edición se consolida en Task 4.7 (ver `<context_handoff>`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear DTOs APP + NEXO (Wave 1)</name>
  <files>
    nexo/data/dto/app.py (new),
    nexo/data/dto/nexo.py (new)
  </files>
  <read_first>
    - api/database.py:65-190 (shapes de los modelos APP)
    - nexo/data/models_nexo.py (creado en task 1; shapes NEXO)
    - api/routers/recursos.py:24-31 (response shape actual de listar)
    - api/routers/ciclos.py:22-45 (response shape)
    - api/routers/historial.py:18-46 (EjecucionRow shape — incluye count aggregate)
    - api/routers/historial.py:49-97 (MetricaRow shape)
    - api/routers/auditoria.py (alrededor de líneas 130+ — serialización de audit rows)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md sección "nexo/data/dto/*.py" (líneas 374-418) — DTO pattern canónico
  </read_first>
  <behavior>
    - Test 1: Todos los DTOs tienen `model_config = ConfigDict(frozen=True, from_attributes=True)`.
    - Test 2: `RecursoRow.model_validate(Recurso(...))` roundtrips con id, centro_trabajo, nombre, seccion, activo.
    - Test 3: `UserRow.model_validate(NexoUser(...))` incluye id, email, role, active, must_change_password, departments (list[str] de codes).
    - Test 4: `AuditLogRow` incluye ts como `datetime` tz-aware.
  </behavior>
  <action>
1. Crear `nexo/data/dto/app.py`:

   ```python
   """DTOs frozen APP (Pydantic v2, from_attributes=True)."""
   from __future__ import annotations

   from datetime import date, datetime
   from typing import Optional

   from pydantic import BaseModel, ConfigDict


   class RecursoRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       centro_trabajo: int
       nombre: str
       seccion: str = "GENERAL"
       activo: bool = True


   class CicloRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       maquina: str
       referencia: str
       tiempo_ciclo: float


   class EjecucionRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       fecha_inicio: str
       fecha_fin: str
       source: str
       status: str
       modulos: Optional[str] = ""
       log: Optional[str] = ""
       n_pdfs: int = 0
       created_at: Optional[datetime] = None


   class MetricaRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       ejecucion_id: int
       seccion: Optional[str] = None
       recurso: Optional[str] = None
       fecha: Optional[date] = None
       turno: Optional[str] = None
       horas_brutas: float = 0.0
       horas_disponible: float = 0.0
       horas_operativo: float = 0.0
       disponibilidad_pct: float = 0.0
       rendimiento_pct: float = 0.0
       calidad_pct: float = 0.0
       oee_pct: float = 0.0
       piezas_totales: int = 0
       piezas_malas: int = 0


   class ReferenciaRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       ejecucion_id: int
       recurso: Optional[str] = None
       referencia: Optional[str] = None
       ciclo_ideal: Optional[float] = None
       ciclo_real: Optional[float] = None
       cantidad: int = 0
       horas: float = 0.0


   class IncidenciaRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       ejecucion_id: int
       recurso: Optional[str] = None
       nombre: Optional[str] = None
       tipo: Optional[str] = None
       horas: float = 0.0


   class InformeRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       ejecucion_id: Optional[int] = None
       fecha: str
       seccion: str
       maquina: str = ""
       modulo: str = ""
       pdf_path: str
       created_at: Optional[datetime] = None


   class ContactoRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       nombre: str
       email: str


   class LukRow(BaseModel):
       """Snapshot de luk4.estado — shape consumida por api/routers/luk4.py."""
       model_config = ConfigDict(frozen=True, from_attributes=True)
       estado_global: int = 0
       codigo_error: int = 0
       pct_auto: Optional[float] = None
       pct_error: Optional[float] = None
       ultimo_ts: Optional[datetime] = None
       alarma_componente: Optional[str] = None
       alarma_mensaje: Optional[str] = None
   ```

2. Crear `nexo/data/dto/nexo.py`:

   ```python
   """DTOs frozen NEXO schema (Pydantic v2)."""
   from __future__ import annotations

   from datetime import datetime
   from typing import Optional

   from pydantic import BaseModel, ConfigDict


   class UserRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       email: str
       role: str
       active: bool = True
       must_change_password: bool = True
       last_login: Optional[datetime] = None
       created_at: Optional[datetime] = None
       departments: tuple[str, ...] = ()  # codes; tuple para frozen


   class RoleRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       code: str
       name: str


   class DepartmentRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       code: str
       name: str


   class AuditLogRow(BaseModel):
       model_config = ConfigDict(frozen=True, from_attributes=True)
       id: int
       ts: datetime
       user_id: Optional[int] = None
       user_email: Optional[str] = None  # join con NexoUser si se resolvió
       ip: str
       method: str
       path: str
       status: int
       details_json: Optional[str] = None
   ```
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from nexo.data.dto.app import RecursoRow, CicloRow, EjecucionRow, MetricaRow, ReferenciaRow, IncidenciaRow, InformeRow, ContactoRow, LukRow; assert RecursoRow.model_config['frozen'] is True; print('DTOs APP OK')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.dto.nexo import UserRow, RoleRow, DepartmentRow, AuditLogRow; assert UserRow.model_config['frozen'] is True; print('DTOs NEXO OK')"</automated>
  </verify>
  <done>
    - 9 DTOs APP + 4 DTOs NEXO; todos frozen + from_attributes.
    - Roundtrip ORM → DTO funciona (test 2 en task 5 lo verifica con fixture).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear repos APP + NEXO (Wave 1) — ORM puro = DATA-05-compliant por D-01</name>
  <files>
    nexo/data/repositories/app.py (new),
    nexo/data/repositories/nexo.py (new)
  </files>
  <read_first>
    - nexo/services/auth.py:95-178 (patrones de ORM query — select, scalar_one_or_none)
    - api/routers/recursos.py (queries ORM inline — fuente para RecursoRepo methods)
    - api/routers/ciclos.py (queries ORM inline — fuente para CicloRepo)
    - api/routers/historial.py (queries ORM inline — fuente para EjecucionRepo + MetricaRepo)
    - api/routers/auditoria.py:70-130+ (query builder con filtros — fuente para AuditRepo.list_filtered)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones "nexo/data/repositories/app.py" y "nexo/data/repositories/nexo.py" (líneas 663-757)
    - .planning/phases/03-capa-de-datos/03-RESEARCH.md §Pattern 1 (sesión inyectada, no commit)
    - .planning/phases/03-capa-de-datos/03-CONTEXT.md §D-01 (scope DATA-05 clarificación 2026-04-19)
    - docs/AUTH_MODEL.md (contrato audit_log append-only)
  </read_first>
  <behavior>
    - Test 1 (RecursoRepo): `list_all()` devuelve `list[RecursoRow]` ordenado por seccion, nombre; roundtrip funciona.
    - Test 2 (RecursoRepo): `list_activos()` filtra por `activo=True`.
    - Test 3 (CicloRepo): `list_all()` devuelve `list[CicloRow]` ordenado por maquina, referencia.
    - Test 4 (EjecucionRepo): `list_recent(limit=5)` respeta el limit + order DESC.
    - Test 5 (UserRepo): `get_by_email("propietario@ecsmobility.com")` → UserRow con departments poblados (si existe) o None.
    - Test 6 (UserRepo, ORM variant): `get_by_email_orm` (alias interno) devuelve `NexoUser` ORM para consumo interno de `auth.py`.
    - Test 7 (AuditRepo.append): llamada con user_id, ip, method, path, status, details_json inserta fila; **no comitea** (verificar con `session.new` que la fila está pendiente).
    - Test 8 (AuditRepo.list_filtered): devuelve lista con user_email resuelto por join.
    - Test 9 (DATA-05 D-01 compliance): grep `nexo/data/sql/app/` y `nexo/data/sql/nexo/` → 0 archivos generados por este plan (ORM puro no requiere `.sql` per D-01). Si algún método de repo requiere `text()` por excepción, documentar en commit message + crear el `.sql` en el directorio correspondiente.
  </behavior>
  <action>
**Nota D-01 (scope DATA-05, CONTEXT.md 2026-04-19)**: los repos creados aquí usan ORM puro (`session.query(Model).filter(...)`, `select(Model)`). Per CONTEXT.md D-01 clarificación, **ORM puro NO requiere archivos `.sql` versionados** — el modelo declarativo SQLAlchemy ES la representación canónica. DATA-05 queda cumplido sin generar archivos bajo `nexo/data/sql/app/` o `nexo/data/sql/nexo/` en este plan. Citar esta justificación en el commit message del repo que introduce el patrón (típicamente Task 3).

1. Crear `nexo/data/repositories/app.py`:

   ```python
   """Repositorios APP (schema ecs_mobility) — Session inyectada, no commit.

   DATA-05 scope (per CONTEXT.md D-01): los métodos aquí usan ORM puro
   (select(), session.query(...)); NO requieren archivos .sql versionados.
   El modelo declarativo SQLAlchemy es la representación canónica de la
   query. Solo métodos que usen text() con SQL hardcoded requerirían
   nexo/data/sql/app/<method>.sql — no es el caso de este plan.
   """
   from __future__ import annotations

   from sqlalchemy import select
   from sqlalchemy.orm import Session

   from nexo.data.dto.app import (
       CicloRow,
       ContactoRow,
       EjecucionRow,
       IncidenciaRow,
       InformeRow,
       MetricaRow,
       RecursoRow,
       ReferenciaRow,
   )
   from nexo.data.models_app import (
       Ciclo,
       Contacto,
       Ejecucion,
       IncidenciaResumen,
       InformeMeta,
       MetricaOEE,
       Recurso,
       ReferenciaStats,
   )


   class RecursoRepo:
       def __init__(self, db: Session):
           self._db = db

       def list_all(self) -> list[RecursoRow]:
           rows = self._db.execute(
               select(Recurso).order_by(Recurso.seccion, Recurso.nombre)
           ).scalars().all()
           return [RecursoRow.model_validate(r) for r in rows]

       def list_activos(self) -> list[RecursoRow]:
           rows = self._db.execute(
               select(Recurso).where(Recurso.activo.is_(True))
               .order_by(Recurso.seccion, Recurso.nombre)
           ).scalars().all()
           return [RecursoRow.model_validate(r) for r in rows]

       def get_by_nombre(self, nombre: str) -> RecursoRow | None:
           row = self._db.execute(
               select(Recurso).where(Recurso.nombre == nombre)
           ).scalar_one_or_none()
           return RecursoRow.model_validate(row) if row else None

       def get_by_codigo(self, centro_trabajo: int) -> Recurso | None:
           """Devuelve ORM (uso interno para routers que escriben)."""
           return self._db.execute(
               select(Recurso).where(Recurso.centro_trabajo == centro_trabajo)
           ).scalar_one_or_none()

       def replace_all(self, recursos: list[dict]) -> None:
           """Borra todo + añade los nuevos. Caller debe comitear."""
           self._db.query(Recurso).delete()
           for r in recursos:
               self._db.add(Recurso(
                   centro_trabajo=r["centro_trabajo"],
                   nombre=r["nombre"],
                   seccion=r.get("seccion", "GENERAL"),
                   activo=r.get("activo", True),
               ))

       def add(self, *, centro_trabajo: int, nombre: str,
               seccion: str = "GENERAL", activo: bool = True) -> None:
           self._db.add(Recurso(
               centro_trabajo=centro_trabajo, nombre=nombre,
               seccion=seccion, activo=activo,
           ))


   class CicloRepo:
       def __init__(self, db: Session):
           self._db = db

       def list_all(self) -> list[CicloRow]:
           rows = self._db.execute(
               select(Ciclo).order_by(Ciclo.maquina, Ciclo.referencia)
           ).scalars().all()
           return [CicloRow.model_validate(r) for r in rows]

       def replace_all(self, rows: list[dict]) -> None:
           self._db.query(Ciclo).delete()
           for r in rows:
               self._db.add(Ciclo(
                   maquina=r["maquina"],
                   referencia=r["referencia"],
                   tiempo_ciclo=float(r.get("tiempo_ciclo", 0.0)),
               ))


   class EjecucionRepo:
       def __init__(self, db: Session):
           self._db = db

       def list_recent(self, limit: int = 50) -> list[EjecucionRow]:
           rows = self._db.execute(
               select(Ejecucion).order_by(Ejecucion.created_at.desc()).limit(limit)
           ).scalars().all()
           return [EjecucionRow.model_validate(r) for r in rows]

       def get_by_id(self, id: int) -> EjecucionRow | None:
           row = self._db.get(Ejecucion, id)
           return EjecucionRow.model_validate(row) if row else None


   class MetricaRepo:
       def __init__(self, db: Session):
           self._db = db

       def tendencias_recurso(
           self, recurso: str, fecha_inicio: str | None = None,
           fecha_fin: str | None = None,
       ) -> list[MetricaRow]:
           from datetime import date
           stmt = select(MetricaOEE).where(MetricaOEE.recurso.ilike(recurso))
           if fecha_inicio:
               stmt = stmt.where(MetricaOEE.fecha >= date.fromisoformat(fecha_inicio))
           if fecha_fin:
               stmt = stmt.where(MetricaOEE.fecha <= date.fromisoformat(fecha_fin))
           stmt = stmt.order_by(MetricaOEE.fecha)
           rows = self._db.execute(stmt).scalars().all()
           return [MetricaRow.model_validate(r) for r in rows]


   class LukRepo:
       """Wrapper opcional de queries luk4.* (en APP). Los routers pueden
       seguir inline en Mark-III si el executor decide no usarlo."""

       def __init__(self, db: Session):
           self._db = db

       # Métodos concretos los añadirá el executor si el refactor de
       # luk4.py lo justifica. Placeholder para DATA-03.


   class ContactoRepo:
       def __init__(self, db: Session):
           self._db = db

       def list_all(self) -> list[ContactoRow]:
           rows = self._db.execute(
               select(Contacto).order_by(Contacto.nombre)
           ).scalars().all()
           return [ContactoRow.model_validate(r) for r in rows]
   ```

2. Crear `nexo/data/repositories/nexo.py`:

   ```python
   """Repositorios NEXO (schema `nexo` en Postgres) — Session inyectada, no commit.

   DATA-05 scope (per CONTEXT.md D-01): ORM puro, no archivos .sql. El
   modelo SQLAlchemy declarativo es la representación canónica.
   """
   from __future__ import annotations

   from datetime import datetime
   from typing import Optional

   from sqlalchemy import select
   from sqlalchemy.orm import Session

   from nexo.data.dto.nexo import AuditLogRow, DepartmentRow, RoleRow, UserRow
   from nexo.data.models_nexo import (
       NexoAuditLog,
       NexoDepartment,
       NexoRole,
       NexoUser,
   )


   class UserRepo:
       def __init__(self, db: Session):
           self._db = db

       def get_by_email_orm(self, email: str) -> Optional[NexoUser]:
           """Devuelve ORM — uso interno por nexo.services.auth (necesita
           NexoUser completo para session management + password hashing)."""
           return self._db.execute(
               select(NexoUser).where(
                   NexoUser.email == email, NexoUser.active.is_(True)
               )
           ).scalar_one_or_none()

       def get_by_email(self, email: str) -> UserRow | None:
           """Devuelve DTO — uso desde routers / audit panel."""
           orm = self.get_by_email_orm(email)
           if not orm:
               return None
           return UserRow.model_validate({
               "id": orm.id,
               "email": orm.email,
               "role": orm.role,
               "active": orm.active,
               "must_change_password": orm.must_change_password,
               "last_login": orm.last_login,
               "created_at": orm.created_at,
               "departments": tuple(d.code for d in orm.departments),
           })

       def list_all(self) -> list[UserRow]:
           rows = self._db.execute(
               select(NexoUser).order_by(NexoUser.email)
           ).scalars().all()
           return [
               UserRow.model_validate({
                   "id": u.id, "email": u.email, "role": u.role,
                   "active": u.active,
                   "must_change_password": u.must_change_password,
                   "last_login": u.last_login,
                   "created_at": u.created_at,
                   "departments": tuple(d.code for d in u.departments),
               })
               for u in rows
           ]


   class RoleRepo:
       def __init__(self, db: Session):
           self._db = db

       def list_all(self) -> list[RoleRow]:
           rows = self._db.execute(
               select(NexoRole).order_by(NexoRole.code)
           ).scalars().all()
           return [RoleRow.model_validate(r) for r in rows]

       def list_departments(self) -> list[DepartmentRow]:
           rows = self._db.execute(
               select(NexoDepartment).order_by(NexoDepartment.code)
           ).scalars().all()
           return [DepartmentRow.model_validate(d) for d in rows]


   class AuditRepo:
       """Audit log repository. IDENT-06: append-only a nivel DB (rol nexo_app
       tiene INSERT/SELECT, no UPDATE/DELETE). Este repo SOLO hace INSERT."""

       def __init__(self, db: Session):
           self._db = db

       def append(
           self,
           *,
           user_id: int | None,
           ip: str,
           method: str,
           path: str,
           status: int,
           details_json: str | None,
       ) -> None:
           """INSERT — caller orquesta commit. NO comitea aquí (IDENT-06 compat)."""
           self._db.add(NexoAuditLog(
               user_id=user_id,
               ip=ip,
               method=method,
               path=path,
               status=status,
               details_json=details_json,
           ))

       def list_filtered(
           self,
           *,
           user_email: str | None = None,
           date_from: datetime | None = None,
           date_to: datetime | None = None,
           path: str | None = None,
           status: int | None = None,
           page: int = 1,
           limit: int = 100,
       ) -> list[AuditLogRow]:
           """Query con filtros para panel /ajustes/auditoria."""
           stmt = select(NexoAuditLog, NexoUser).outerjoin(
               NexoUser, NexoUser.id == NexoAuditLog.user_id
           )
           if user_email:
               stmt = stmt.where(NexoUser.email == user_email)
           if date_from:
               stmt = stmt.where(NexoAuditLog.ts >= date_from)
           if date_to:
               stmt = stmt.where(NexoAuditLog.ts <= date_to)
           if path:
               stmt = stmt.where(NexoAuditLog.path.ilike(f"%{path}%"))
           if status is not None:
               stmt = stmt.where(NexoAuditLog.status == status)
           stmt = stmt.order_by(NexoAuditLog.ts.desc()).limit(limit).offset((page - 1) * limit)

           rows = self._db.execute(stmt).all()
           return [
               AuditLogRow.model_validate({
                   "id": log.id, "ts": log.ts, "user_id": log.user_id,
                   "user_email": user.email if user else None,
                   "ip": log.ip, "method": log.method, "path": log.path,
                   "status": log.status, "details_json": log.details_json,
               })
               for log, user in rows
           ]
   ```
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from nexo.data.repositories.app import RecursoRepo, CicloRepo, EjecucionRepo, MetricaRepo, LukRepo, ContactoRepo; print('repos APP OK')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.repositories.nexo import UserRepo, RoleRepo, AuditRepo; print('repos NEXO OK')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.repositories.nexo import AuditRepo; import inspect; src = inspect.getsource(AuditRepo.append); assert 'commit' not in src.lower(); print('AuditRepo.append NO commit OK')"</automated>
    <automated>test ! -d nexo/data/sql/app || test -z "$(ls -A nexo/data/sql/app 2>/dev/null)" && echo "OK: no .sql APP generated (D-01 scope compliance)" || echo "WARN: unexpected .sql APP files; verify justification in commit"</automated>
    <automated>test ! -d nexo/data/sql/nexo || test -z "$(ls -A nexo/data/sql/nexo 2>/dev/null)" && echo "OK: no .sql NEXO generated (D-01 scope compliance)" || echo "WARN: unexpected .sql NEXO files; verify justification in commit"</automated>
  </verify>
  <done>
    - `nexo/data/repositories/app.py` con 6 repos (RecursoRepo con 6 métodos, CicloRepo, EjecucionRepo, MetricaRepo, LukRepo placeholder, ContactoRepo).
    - `nexo/data/repositories/nexo.py` con UserRepo (2 variantes ORM/DTO), RoleRepo, AuditRepo.
    - `AuditRepo.append` no contiene `commit()`.
    - **DATA-05 compliance per D-01**: 0 archivos `.sql` bajo `nexo/data/sql/app/` o `nexo/data/sql/nexo/` (ORM puro = representación canónica). Citar D-01 en commit message.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Refactor routers APP (ciclos → recursos → historial) + auth.py + auditoria + usuarios + pipeline.py handoff (Wave 2)</name>
  <files>
    api/routers/ciclos.py,
    api/routers/recursos.py,
    api/routers/historial.py,
    nexo/services/auth.py,
    api/routers/auditoria.py,
    api/routers/usuarios.py,
    api/services/pipeline.py
  </files>
  <read_first>
    - api/routers/ciclos.py completo (167 líneas)
    - api/routers/recursos.py completo (231 líneas)
    - api/routers/historial.py completo
    - nexo/services/auth.py (el módulo entero — el refactor es quirúrgico en get_user_by_email)
    - api/routers/auditoria.py completo (el handler principal y el export CSV)
    - api/routers/usuarios.py completo (CRUD)
    - api/services/pipeline.py completo (grep imports de `api.database` y de `OEE.db.connector` — propiedad exclusiva de este plan per `<context_handoff>`)
    - .planning/phases/03-capa-de-datos/03-02-SUMMARY.md (para confirmar qué dejó 03-02 respecto a `api/services/db.py` re-pointing)
    - nexo/data/repositories/app.py + nexo/data/repositories/nexo.py (creados en task 3)
  </read_first>
  <behavior>
    - Test 1 (ciclos): `GET /api/ciclos` devuelve `{"rows": [...], "grouped": {...}}` igual que antes.
    - Test 2 (recursos): `GET /api/recursos` devuelve lista con mismos campos.
    - Test 3 (historial): `GET /api/historial?limit=5` devuelve `{"ejecuciones": [...]}` con count per ejecución.
    - Test 4 (auth interno): `get_user_by_email(db, email)` sigue devolviendo `NexoUser` ORM (no rompe middleware).
    - Test 5 (auditoria): `GET /ajustes/auditoria` renderiza HTML con filas filtradas.
    - Test 6 (usuarios): `GET /ajustes/usuarios` renderiza lista de usuarios.
    - Test 7 (IDENT-06): `pytest tests/auth/` sigue 11 passed / 1 skipped.
    - Test 8 (pipeline.py edits atómicos): diff de `api/services/pipeline.py` en el commit de Task 4.7 contiene:
      - 0 imports eliminados/movidos relacionados con ORM (el re-export en `api/database.py` los cubre).
      - Si había `from OEE.db.connector import ...` directo, queda swappeado a la vía facade (`api.services.db.*`) o a `MesRepository` según Opción A/B (ver `<context_handoff>` punto 2).
  </behavior>
  <action>
### 4.1 — Refactor `api/routers/ciclos.py` (router más simple de APP, primero per D-06)

1. Cambiar imports:
   ```python
   # ANTES: from api.database import Ciclo, Recurso, SECTION_MAP, get_db
   # DESPUES:
   from api.database import SECTION_MAP, get_db  # SECTION_MAP sigue en api/database (o se puede migrar a models_app)
   from api.deps import DbApp
   from nexo.data.repositories.app import CicloRepo
   ```

2. Reemplazar handlers:
   ```python
   @router.get("")
   def listar(db: DbApp):
       repo = CicloRepo(db)
       rows = repo.list_all()  # list[CicloRow]
       flat = [r.model_dump() for r in rows]
       # grouped lo construye el router (transport logic)
       grouped: dict[str, dict[str, list]] = {}
       for r in rows:
           sec = SECTION_MAP.get(r.maquina.lower(), "GENERAL")
           grouped.setdefault(sec, {}).setdefault(r.maquina, []).append({
               "id": r.id, "referencia": r.referencia, "tiempo_ciclo": r.tiempo_ciclo,
           })
       return {"rows": flat, "grouped": grouped}

   @router.put("", dependencies=_edit)
   def guardar(payload: CiclosPayload, db: DbApp):
       repo = CicloRepo(db)
       repo.replace_all([r.model_dump() for r in payload.rows])
       db.commit()
       return {"ok": True, "count": len(payload.rows)}
   ```

3. Smoke: `GET /api/ciclos` → 200. Commit: `refactor(03-03): ciclos via CicloRepo (DATA-03)`.

### 4.2 — Refactor `api/routers/recursos.py`

1. Cambiar imports similares (`DbApp` + `RecursoRepo`).
2. Sustituir queries ORM inline por `RecursoRepo.list_all`, `list_activos`, `replace_all`, `add`, `get_by_codigo`.
3. Mantener `_sync_to_config(db)` — es service-layer helper, no refactorizable a repo.
4. Mantener `_auto_name` y `_NAME_RULES` (lógica de transporte).
5. Smoke: `GET /api/recursos` → 200. `PUT /api/recursos` roundtrips. Commit: `refactor(03-03): recursos via RecursoRepo (DATA-03)`.

### 4.3 — Refactor `api/routers/historial.py`

1. Imports nuevos: `from api.deps import DbApp`, `from nexo.data.repositories.app import EjecucionRepo, MetricaRepo`.
2. Sustituir queries inline. Para el count aggregate de `DatosProduccion`, mantener la query en el router (es un join, la composición queda en transport) o crear un método nuevo `EjecucionRepo.count_rows_by_ejecucion(ids: list[int]) -> dict[int, int]`.
3. Smoke: `GET /api/historial` → 200. Commit: `refactor(03-03): historial via repos (EjecucionRepo + MetricaRepo)`.

### 4.4 — Refactor `nexo/services/auth.py`

Cambio mínimo y quirúrgico para preservar firma externa:

1. En `get_user_by_email(db, email)`, delegar a `UserRepo`:
   ```python
   # NUEVO import al principio del módulo:
   # from nexo.data.repositories.nexo import UserRepo  # opcional, o lazy
   
   def get_user_by_email(db: Session, email: str) -> Optional[NexoUser]:
       """Helper público. Internamente usa UserRepo para cumplir DATA-04."""
       from nexo.data.repositories.nexo import UserRepo  # lazy import para evitar ciclo
       return UserRepo(db).get_by_email_orm(email)
   ```

2. Dejar `create_session`, `get_session`, `extend_session`, `revoke_session`, `revoke_all_sessions`, `check_lockout`, `record_failed_attempt`, `clear_attempts`, `PERMISSION_MAP`, `require_permission` SIN TOCAR. No son CRUD puro — son service/policy.

3. Smoke: `POST /login` con credenciales válidas → 302 (redirect). Commit: `refactor(03-03): auth.get_user_by_email uses UserRepo (DATA-04)`.

### 4.5 — Refactor `api/routers/auditoria.py`

1. Eliminar `get_nexo_db` local; importar `get_db_nexo` de `api.deps`.
2. Cambiar `Depends(get_nexo_db)` → `DbNexo` en handlers.
3. Eliminar la construcción inline del stmt con `_build_filtered_stmt`; delegar a `AuditRepo.list_filtered(...)`.
4. Import necesario: `from nexo.data.repositories.nexo import AuditRepo`.
5. El export CSV: iterar sobre `AuditLogRow` (DTOs) en lugar de tuplas ORM.
6. Smoke: `GET /ajustes/auditoria` → 200 renderiza HTML; `GET /ajustes/auditoria/export` → CSV. Commit: `refactor(03-03): auditoria via AuditRepo + DbNexo (DATA-04, DATA-07)`.

### 4.6 — Refactor `api/routers/usuarios.py`

1. Eliminar `get_nexo_db` local; importar `get_db_nexo` / `DbNexo` de `api.deps`.
2. Mantener la lógica de forms (validación de role/departments). Solo cambian los queries: sustituir `session.query(NexoUser)...` por `UserRepo(db).get_by_email(...)` / `UserRepo(db).list_all()` / etc.
3. El CRUD escribe directamente en ORM (add/edit/delete) — aquí el repo puede quedarse sin métodos de write; el router hace `db.add(NexoUser(...))` + `db.commit()`. Aceptar este diff mínimo — D-02 permite que el caller use ORM intra-services cuando es más claro.
4. Smoke: `GET /ajustes/usuarios` → 200. Commit: `refactor(03-03): usuarios via api.deps.DbNexo (DATA-07)`.

### 4.7 — `api/services/pipeline.py` (EDIT ATÓMICO — ownership exclusiva per `<context_handoff>`)

**Esta es la única task de este plan que toca pipeline.py.** Consolidar en un solo commit atómico: la migración ORM (vía re-export) Y el swap de imports MES legacy si 03-02 lo dejó pendiente.

1. **Diagnóstico inicial** — grep el archivo buscando patrones de import:
   ```bash
   grep -n "from api.database import" api/services/pipeline.py
   grep -n "from OEE.db.connector import" api/services/pipeline.py
   grep -n "from api.services.db import" api/services/pipeline.py
   grep -n "import pyodbc" api/services/pipeline.py
   ```

2. **Migración ORM (Landmine #9)**: si pipeline.py importa `MetricaOEE`, `ReferenciaStats`, `IncidenciaResumen`, `Ejecucion`, `InformeMeta`, `DatosProduccion` desde `api.database`:
   - **No cambiar nada** — el re-export establecido en Task 1 de este plan (`api/database.py` re-exporta desde `nexo.data.models_app`) cubre estos imports. Verificar con el verify gate.
   - **Alternativa opcional (cleanup)**: swap los imports a `from nexo.data.models_app import ...` directamente. Solo si mejora legibilidad. Decisión del executor; si se hace, documentar en commit message.

3. **Swap MES legacy (si 03-02 dejó pendiente)**: si pipeline.py importa DIRECTAMENTE de `OEE.db.connector`:
   - **Opción A (preferida, minimum diff)**: dejar el import legacy tal cual. `OEE/db/connector.py` no es refactorizado en Mark-III (D-04). Es legítimo seguir usándolo. En este caso, la task es NO-OP respecto al swap MES.
   - **Opción B (cleanup profundo)**: swap a `from nexo.data.repositories.mes import MesRepository` + `from nexo.data.engines import engine_mes`. Crear instancia `_mes_repo = MesRepository(engine=engine_mes)` a nivel módulo (o en cada función que lo necesita) y llamar `_mes_repo.detectar_recursos()` en lugar de `detectar_recursos(cfg)` directo. Solo si simplifica el código y no hay riesgo de tocar la lógica del pipeline.
   - Decidir A o B según inspección; si 03-02 ya había planteado el swap y lo dejó pendiente (posible si un commit intermedio tocó pipeline.py pero fue revertido por el handoff), completarlo aquí.

4. **Eliminar `import pyodbc`** si aparece directamente en pipeline.py (meta-test lo cubre implícitamente, pero es DATA-07 compliance). Si pipeline.py nunca tuvo pyodbc (normal — usa el facade), no hay cambio.

5. **NO tocar la lógica del pipeline** — ninguna función del módulo se reescribe. Solo imports + wiring.

6. **Commit atómico** — un solo commit cubre todo el edit de pipeline.py: `refactor(03-03): pipeline.py — ORM migration via re-export + MES handoff from 03-02`. Body del commit describe qué opción (A o B) se tomó respecto al swap MES.

7. **Verificación post-edit**:
   - `python -c "from api.services.pipeline import run_pipeline"` OK (sin errores).
   - `grep -c "import pyodbc" api/services/pipeline.py` == 0.
   - `grep -c "from OEE.db.connector" api/services/pipeline.py` == 0 (si se eligió Opción B) o == N (valor original, si Opción A).
   - Los tests `tests/test_oee_calc.py` + `tests/test_oee_helpers.py` (30 tests del safety net OEE) siguen verdes.
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from api.routers import ciclos, recursos, historial, auditoria, usuarios; print('routers APP+NEXO imports OK')"</automated>
    <automated>docker compose exec -T web pytest tests/auth/ -q</automated>
    <automated>docker compose exec -T web python -c "from fastapi.testclient import TestClient; from api.main import app; c = TestClient(app); r = c.get('/login'); assert r.status_code == 200; print('login page OK')"</automated>
    <automated>docker compose exec -T web python -c "from api.services.pipeline import run_pipeline; print('pipeline.py imports OK tras Task 4.7')"</automated>
    <automated>docker compose exec -T web bash -c 'grep -c "import pyodbc" api/services/pipeline.py | grep -q "^0$" && echo "OK pipeline pyodbc-free" || echo "FAIL: pyodbc still in pipeline.py"'</automated>
    <automated>docker compose exec -T web pytest tests/test_oee_calc.py tests/test_oee_helpers.py -q</automated>
  </verify>
  <done>
    - 3 routers APP (ciclos, recursos, historial) consumen repos APP.
    - `nexo/services/auth.py:get_user_by_email` delega a UserRepo.
    - `api/routers/auditoria.py` y `api/routers/usuarios.py` usan `DbNexo` (eliminado `get_nexo_db` local duplicado).
    - Commits atómicos por router.
    - **`api/services/pipeline.py` editado en UN SOLO commit atómico** (Task 4.7): migración ORM (via re-export) + swap MES legacy (Opción A o B documentada). Pyodbc eliminado si había alguno. Safety net `tests/test_oee_calc.py` intacto.
    - `pytest tests/auth/ -q` = 11 passed / 1 skipped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Tests de repos + smoke HTTP + meta-test pyodbc (Wave 3)</name>
  <files>
    tests/data/test_app_repository.py (new),
    tests/data/test_nexo_repository.py (new),
    tests/data/test_routers_smoke.py (new),
    tests/data/test_no_raw_pyodbc_in_routers.py (new)
  </files>
  <read_first>
    - nexo/data/repositories/app.py + nexo.py (creados en task 3)
    - tests/auth/test_rbac_smoke.py:37-74 (TestClient pattern + _postgres_reachable helper)
    - tests/auth/test_audit_append_only.py:60-97 (_owner_engine + _clean_test_rows — patrón para tests que insertan en audit_log)
    - tests/data/conftest.py (fixtures db_nexo, db_app creadas en 03-01)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md sección "tests/data/test_app_repository.py" + "tests/data/test_nexo_repository.py" + "tests/data/test_routers_smoke.py"
  </read_first>
  <behavior>
    - `test_app_repository.py` — integration (skipif sin SQL Server): RecursoRepo.list_all devuelve list[RecursoRow]; CicloRepo.list_all OK.
    - `test_nexo_repository.py` — integration con Postgres (fixture `db_nexo`):
      - UserRepo.get_by_email("propietario") devuelve UserRow o None sin error.
      - AuditRepo.append inserta; verify que `session.new` tiene el objeto antes de commit.
      - **`test_audit_repo_no_commits`**: llamar `AuditRepo.append(...)` y verificar que `session.in_transaction()` sigue igual, `session.new` contiene la fila, `session.dirty` vacío. NO llamar `commit` desde el test; verificar que el repo no lo hizo tampoco (rollback al final de la fixture limpia).
      - **Owner-engine cleanup**: si algún test comitea (para validar visibilidad cross-session), usar `_owner_engine()` para DELETE — `nexo_app` no puede DELETE audit_log (IDENT-06).
    - `test_routers_smoke.py` — TestClient parametrizado sobre 10 routers, cada uno con cookie válida, espera 200 o status documentado.
    - `test_no_raw_pyodbc_in_routers.py` — grep final sobre `api/routers/*.py`: 0 `import pyodbc` directo.
  </behavior>
  <action>
1. Crear `tests/data/test_app_repository.py`:

   ```python
   """DATA-03: integration tests contra SQL Server ecs_mobility."""
   from __future__ import annotations

   import pytest

   from nexo.data.dto.app import CicloRow, RecursoRow
   from nexo.data.repositories.app import CicloRepo, RecursoRepo

   pytestmark = pytest.mark.integration


   def test_recurso_repo_list_all_returns_dtos(db_app):
       repo = RecursoRepo(db_app)
       rows = repo.list_all()
       assert all(isinstance(r, RecursoRow) for r in rows)


   def test_recurso_repo_list_activos_filters(db_app):
       repo = RecursoRepo(db_app)
       activos = repo.list_activos()
       assert all(r.activo for r in activos)


   def test_ciclo_repo_list_all_returns_dtos(db_app):
       repo = CicloRepo(db_app)
       rows = repo.list_all()
       assert all(isinstance(r, CicloRow) for r in rows)
   ```

2. Crear `tests/data/test_nexo_repository.py`:

   ```python
   """DATA-04: integration tests contra Postgres nexo.*."""
   from __future__ import annotations

   import pytest
   from sqlalchemy import text

   from nexo.data.models_nexo import NexoAuditLog, NexoUser
   from nexo.data.repositories.nexo import AuditRepo, RoleRepo, UserRepo

   pytestmark = pytest.mark.integration


   def test_user_repo_get_by_email_returns_dto_or_none(db_nexo):
       repo = UserRepo(db_nexo)
       # Propietario suele existir tras Phase 2; si no, None es válido.
       result = repo.get_by_email("propietario@ecsmobility.com")
       assert result is None or result.email == "propietario@ecsmobility.com"


   def test_role_repo_lists_roles(db_nexo):
       repo = RoleRepo(db_nexo)
       roles = repo.list_all()
       codes = {r.code for r in roles}
       # Phase 2 seed incluye estos 3
       assert codes.issuperset({"propietario", "directivo", "usuario"})


   def test_audit_repo_append_does_not_commit(db_nexo):
       """IDENT-06 compat: AuditRepo.append NO comitea. Caller debe hacerlo.

       Dentro de la sesión, `session.new` contiene el objeto pendiente.
       Al cerrar la fixture con rollback, la fila desaparece.
       """
       repo = AuditRepo(db_nexo)

       before_new = len(list(db_nexo.new))
       repo.append(
           user_id=None, ip="127.0.0.1", method="GET", path="/_test_03_03_",
           status=200, details_json=None,
       )
       after_new = len(list(db_nexo.new))

       # Fila añadida pero no flushed ni comiteada
       assert after_new == before_new + 1
       # Verificar que no hay commit implícito (in_transaction sigue activo)
       assert db_nexo.in_transaction()


   def test_audit_repo_list_filtered_returns_dtos(db_nexo):
       """list_filtered devuelve AuditLogRow con user_email resuelto."""
       repo = AuditRepo(db_nexo)
       rows = repo.list_filtered(limit=5)
       # Puede ser lista vacía o poblada; lo importante es que no rompe.
       assert isinstance(rows, list)
   ```

3. Crear `tests/data/test_routers_smoke.py`:

   ```python
   """DATA-07: smoke HTTP sobre los 10 routers. Requiere cookie válida.

   Si no hay propietario en BD, skipea. En ese caso, los meta-tests cubren
   DATA-07 a nivel código (no import pyodbc, no dbizaro.).
   """
   from __future__ import annotations

   import os

   import pytest
   from fastapi.testclient import TestClient

   pytestmark = pytest.mark.integration


   @pytest.fixture(scope="module")
   def client_with_cookie():
       from api.main import app
       c = TestClient(app)
       email = os.environ.get("NEXO_TEST_EMAIL")
       password = os.environ.get("NEXO_TEST_PASSWORD")
       if not email or not password:
           pytest.skip(
               "NEXO_TEST_EMAIL / NEXO_TEST_PASSWORD no definidos — "
               "smoke HTTP requiere un propietario real."
           )
       r = c.post("/login", data={"email": email, "password": password})
       assert r.status_code in (302, 303), f"login falló: {r.status_code}"
       return c


   ROUTERS_OK = [
       ("/api/dashboard/summary", 200),
       ("/api/recursos", 200),
       ("/api/ciclos", 200),
       ("/api/historial?limit=5", 200),
       ("/api/operarios", 200),
       ("/api/bbdd/databases", 200),
       ("/ajustes", 200),
       ("/ajustes/usuarios", 200),
       ("/ajustes/auditoria", 200),
       ("/api/health", 200),
   ]


   @pytest.mark.parametrize("path,expected", ROUTERS_OK)
   def test_router_returns_expected_status(client_with_cookie, path, expected):
       r = client_with_cookie.get(path)
       # Algunos routers pueden devolver 502 si el MES no está alcanzable —
       # el smoke HTTP verifica que el HANDLER existe y la capa nueva no rompe.
       assert r.status_code in (expected, 502), (
           f"{path} → {r.status_code} (esperado {expected} o 502)"
       )
   ```

4. Crear `tests/data/test_no_raw_pyodbc_in_routers.py`:

   ```python
   """DATA-07: ningun router de api/routers/ importa pyodbc directo."""
   from __future__ import annotations

   import pathlib
   import re

   ROUTERS_DIR = pathlib.Path(__file__).resolve().parents[2] / "api" / "routers"


   def test_no_raw_pyodbc_imports():
       offenders: list[tuple[str, int, str]] = []
       pattern = re.compile(r"^\s*(import pyodbc|from pyodbc)")
       for py in ROUTERS_DIR.glob("*.py"):
           for i, line in enumerate(py.read_text(encoding="utf-8").splitlines(), 1):
               # ignorar comentarios
               if line.lstrip().startswith("#"):
                   continue
               if pattern.search(line):
                   offenders.append((py.name, i, line.strip()))
       assert not offenders, (
           f"Routers con pyodbc directo: {offenders}"
       )


   def test_no_pyodbc_connect_calls_in_routers():
       """Double-check: no `pyodbc.connect(...)` tampoco via import retardado."""
       offenders: list[tuple[str, int, str]] = []
       for py in ROUTERS_DIR.glob("*.py"):
           for i, line in enumerate(py.read_text(encoding="utf-8").splitlines(), 1):
               if "pyodbc.connect" in line and not line.lstrip().startswith("#"):
                   offenders.append((py.name, i, line.strip()))
       assert not offenders, f"Routers con pyodbc.connect: {offenders}"
   ```
  </action>
  <verify>
    <automated>docker compose exec -T web pytest tests/data/test_nexo_repository.py -q</automated>
    <automated>docker compose exec -T web pytest tests/data/test_app_repository.py -q</automated>
    <automated>docker compose exec -T web pytest tests/data/test_no_raw_pyodbc_in_routers.py -q -x</automated>
    <automated>docker compose exec -T web pytest tests/data/test_routers_smoke.py -q</automated>
    <automated>docker compose exec -T web pytest tests/auth/ -q</automated>
  </verify>
  <done>
    - 4 test files creados.
    - `test_audit_repo_no_commits` verde (DATA-04 IDENT-06 compat).
    - `test_no_raw_pyodbc_in_routers` verde (DATA-07).
    - `test_routers_smoke` pasa con credenciales o skipea limpiamente.
    - `pytest tests/auth/ -q` sigue 11 passed / 1 skipped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Actualizar imports de tests/auth/ al path nuevo (opcional cleanup)</name>
  <files>
    tests/auth/test_audit_append_only.py
  </files>
  <read_first>
    - tests/auth/test_audit_append_only.py:28-35 (imports from nexo.db.engine, nexo.db.models)
    - PATTERNS.md nota sobre eliminación del shim en Mark-IV
  </read_first>
  <behavior>
    - Tras el cambio: el test sigue pasando; shim `nexo/db/engine.py` y `nexo/db/models.py` siguen existiendo (los mantenemos durante Mark-III, eliminación diferida a Mark-IV).
  </behavior>
  <action>
**Decisión**: mantener los shims durante Mark-III (CLAUDE.md dice "no refactor ajeno sin justificación"). Solo actualizar `tests/auth/test_audit_append_only.py` si el equipo considera que el import via shim se siente sucio. Valor bajo — diff optativo.

Si el executor decide actualizar:

1. En `tests/auth/test_audit_append_only.py:30`:
   ```python
   # ANTES: from nexo.db.engine import engine_nexo
   # DESPUES:
   from nexo.data.engines import engine_nexo
   ```

2. Revisar otros tests/auth/ que importen de `nexo.db.*` y migrar si se quiere.

3. Los shims en `nexo/db/engine.py` y `nexo/db/models.py` PERMANECEN hasta Mark-IV. Mark-III los mantiene como "transition complete, cleanup diferido".

Si el executor decide NO tocar, ok — documentar en SUMMARY.md como deferred cleanup.
  </action>
  <verify>
    <automated>docker compose exec -T web pytest tests/auth/ -q</automated>
  </verify>
  <done>
    - Opcional: imports de tests/auth/ al path nuevo.
    - No-op si se decide mantener shim.
    - `tests/auth/` sigue 11 passed / 1 skipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → `/ajustes/auditoria` → `AuditRepo.list_filtered` | Usuario autenticado (propietario) consulta audit_log con filtros |
| HTTP → `/ajustes/usuarios` CRUD | Usuario autenticado (propietario) muta `nexo.users` — IDENT-06 no aplica aquí (users table no es append-only) |
| Middleware → `AuditRepo.append` | Cada request autenticada escribe una fila en audit_log; commit es del middleware, no del repo |
| ORM migration | Los 15 consumidores de `api/services/pipeline.py` siguen importando de `api/database` — el re-export debe preservar identidad del objeto (not just same fields) |
| pipeline.py handoff (03-02 → 03-03) | Single-owner edit atómico en Task 4.7 para evitar merge conflict entre planes paralelos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T (AuditRepo.append comitea silenciosamente) | `nexo/data/repositories/nexo.py:AuditRepo` | mitigate | Contract test `test_audit_repo_no_commits` (task 5) verifica que `session.new` crece sin que `session.commit` ocurra. Gate IDENT-06 bloquea UPDATE/DELETE a nivel DB como defense-in-depth — `tests/auth/test_audit_append_only.py` (Phase 2) lo confirma. |
| T-03-03-02 | I (filter bypass en /ajustes/auditoria) | `AuditRepo.list_filtered` | mitigate | Filtros construidos con SQLAlchemy expression language (no string interpolation); `path.ilike("%{value}%")` usa bindparam implícito. Propietario bypass en `require_permission` ya existe; no introducimos nueva vía de acceso. Tests de router smoke verifican 200 con cookie propietario. |
| T-03-03-03 | T (ORM migration breaks engine.metadata binding) | `nexo/data/models_app.py:Base` + `api/database.py:init_db()` | mitigate | Test `test_models_app_reexport_identity` verifica `api.database.Recurso is nexo.data.models_app.Recurso`. El re-export NO es clone — los modelos viven en `nexo/data/models_app.py` y `api/database.py` sólo los re-exporta. |
| T-03-03-04 | E (UserRepo.get_by_email_orm expone ORM a routers) | `UserRepo` | accept | Decisión D-02: ORM intra-services OK. `auth.py` necesita `NexoUser` completo para session management; routers consumen DTO (`UserRow`). El método `get_by_email_orm` está documentado como "uso interno". |
| T-03-03-05 | I (audit row leaks details_json sensitive) | `AuditRepo.list_filtered` + panel UI | accept | `details_json` ya viene sanitizado por el AuditMiddleware (02-04 — passwords SQL, tokens). Panel solo accesible a propietario. |
| T-03-03-06 | T (pipeline breaks por re-export ORM) | `api/services/pipeline.py` (~15 refs Landmine #9) | mitigate | Test 2 task 1 verifica identidad post-migración. Task 4.7 consolida cualquier edit de pipeline.py atómicamente. Smoke test del pipeline con `run_pipeline(fecha=YYYY-MM-DD)` cubierto por 03-02 PDF regression. |
| T-03-03-07 | E (RoleRepo.list_all expone catálogo completo) | `RoleRepo` | accept | El catálogo de roles es público por diseño (propietario, directivo, usuario). No hay info sensible. |
| T-03-03-08 | T (pipeline.py merge conflict con 03-02) | Ownership handoff `api/services/pipeline.py` | mitigate | Blocker-1 de revision: 03-02 NO edita pipeline.py; 03-03 es único owner. Verification gate en 03-02 (`git diff HEAD~..HEAD -- api/services/pipeline.py` debe ser vacío) y en 03-03 (Task 4.7 consolida todos los edits). |

**Block on HIGH severity:** T-03-03-01 (AuditRepo silent commit), T-03-03-03 (ORM identity broken) y T-03-03-08 (pipeline.py merge conflict) son HIGH. Tests/handoff los cubren como gates bloqueantes.
</threat_model>

<verification>
Hard gate:

```bash
# 1. ORM migration identity
docker compose exec -T web python -c "from nexo.data.models_app import Recurso, Ciclo, MetricaOEE; from api.database import Recurso as R2, Ciclo as C2, MetricaOEE as M2; assert Recurso is R2 and Ciclo is C2 and MetricaOEE is M2; print('models_app identity OK')"
docker compose exec -T web python -c "from nexo.data.models_nexo import NexoUser, NexoAuditLog; from nexo.db.models import NexoUser as U2, NexoAuditLog as A2; assert NexoUser is U2 and NexoAuditLog is A2; print('models_nexo identity OK')"

# 2. Repos importables
docker compose exec -T web python -c "from nexo.data.repositories.app import RecursoRepo, CicloRepo, EjecucionRepo, MetricaRepo, LukRepo, ContactoRepo; from nexo.data.repositories.nexo import UserRepo, RoleRepo, AuditRepo; print('repos OK')"

# 3. DTOs frozen
docker compose exec -T web python -c "from nexo.data.dto.app import RecursoRow, CicloRow, EjecucionRow, MetricaRow, ReferenciaRow, IncidenciaRow, InformeRow, ContactoRow, LukRow; from nexo.data.dto.nexo import UserRow, RoleRow, DepartmentRow, AuditLogRow; assert all(M.model_config['frozen'] for M in [RecursoRow, CicloRow, EjecucionRow, MetricaRow, ReferenciaRow, IncidenciaRow, InformeRow, ContactoRow, LukRow, UserRow, RoleRow, DepartmentRow, AuditLogRow]); print('DTOs frozen OK')"

# 4. AuditRepo no commit (source inspection)
docker compose exec -T web python -c "import inspect; from nexo.data.repositories.nexo import AuditRepo; src = inspect.getsource(AuditRepo.append); assert '.commit()' not in src and 'db.commit' not in src; print('AuditRepo.append NO commit OK')"

# 5. Routers arrancan
docker compose exec -T web python -c "from fastapi.testclient import TestClient; from api.main import app; c = TestClient(app); r = c.get('/login'); assert r.status_code == 200; print('app OK')"

# 6. Meta-tests
docker compose exec -T web pytest tests/data/test_no_raw_pyodbc_in_routers.py -q -x

# 7. Integration tests
docker compose exec -T web pytest tests/data/test_nexo_repository.py tests/data/test_app_repository.py -q

# 8. Regression IDENT-06
docker compose exec -T web pytest tests/auth/ -q
# Esperado: 11 passed / 1 skipped

# 9. Smoke HTTP (opcional — skipea si no hay NEXO_TEST_EMAIL)
docker compose exec -T web pytest tests/data/test_routers_smoke.py -q

# 10. Pipeline import (Landmine #9) + pyodbc-free
docker compose exec -T web python -c "from api.services.pipeline import run_pipeline; import inspect; print('pipeline imports OK')"
grep -c "import pyodbc" api/services/pipeline.py | grep -q "^0$" || { echo "FAIL: pyodbc in pipeline.py"; exit 1; }

# 11. DATA-05 D-01 compliance (no unexpected .sql APP/NEXO files)
test ! -d nexo/data/sql/app || test -z "$(ls -A nexo/data/sql/app 2>/dev/null)" || echo "NOTE: .sql APP files present — verify commit message cites text() usage per D-01"
test ! -d nexo/data/sql/nexo || test -z "$(ls -A nexo/data/sql/nexo 2>/dev/null)" || echo "NOTE: .sql NEXO files present — verify commit message cites text() usage per D-01"

# 12. OEE safety net (pipeline lógica no rota)
docker compose exec -T web pytest tests/test_oee_calc.py tests/test_oee_helpers.py -q
```
</verification>

<success_criteria>
- `nexo/data/models_app.py` + `nexo/data/models_nexo.py` contienen los ORM completos; `api/database.py` y `nexo/db/models.py` re-exportan.
- `api.database.Recurso is nexo.data.models_app.Recurso` (identity, no clone).
- `nexo.db.models.NexoUser is nexo.data.models_nexo.NexoUser` (shim funcional).
- 6 repos APP + 3 repos NEXO creados con Session inyectada.
- `AuditRepo.append` sin `commit()` (DATA-04 IDENT-06 compat).
- 9 DTOs APP + 4 DTOs NEXO frozen (DATA-08).
- 3 routers APP (ciclos, recursos, historial) consumen repos.
- `nexo/services/auth.py:get_user_by_email` delega a `UserRepo`.
- `api/routers/auditoria.py` y `api/routers/usuarios.py` usan `DbNexo` de `api/deps.py`.
- **`api/services/pipeline.py` editado en un solo commit atómico (Task 4.7)**: migración ORM (vía re-export de `api/database.py`) + swap MES handoff de 03-02 (Opción A minimum diff O Opción B cleanup, documentado). Sin pyodbc.
- **DATA-05 compliance per CONTEXT.md D-01**: APP/NEXO repos usan ORM puro → 0 archivos `.sql` bajo `nexo/data/sql/{app,nexo}/` generados por este plan. Cita de D-01 en commit message de Task 3 ("Per CONTEXT.md D-01 scope clarification: ORM puro cumple DATA-05 sin `.sql` versionado; el modelo SQLAlchemy declarativo es la representación canónica").
- 4 tests nuevos (`test_app_repository`, `test_nexo_repository`, `test_routers_smoke`, `test_no_raw_pyodbc_in_routers`); `test_audit_repo_no_commits` verde.
- `pytest tests/auth/ -q` = 11 passed / 1 skipped (regression IDENT-06 intacto).
- `pytest tests/data/ -q` verde (integration skipea sin DBs).
- `pytest tests/test_oee_calc.py tests/test_oee_helpers.py -q` verde (30 tests safety net OEE — asegura que pipeline.py Task 4.7 no rompió cálculos).
- 10 routers responden 200 OK con cookie propietario.
- Shims `nexo/db/engine.py` y `nexo/db/models.py` mantenidos (cleanup a Mark-IV).
- Autonomous: true (Postgres real en compose basta para todos los gates; SQL Server es opcional).
</success_criteria>

<output>
After completion, create `.planning/phases/03-capa-de-datos/03-03-SUMMARY.md` con:

- Files created (10) y files modified (11).
- Commits atómicos sugeridos (uno por paso; el orden respeta D-06):
  1. `refactor(03-03): migrate ORM models to nexo/data/models_{app,nexo}.py with shims`
  2. `feat(03-03): add DTOs frozen in nexo/data/dto/{app,nexo}.py (DATA-08)`
  3. `feat(03-03): add repositories for APP (6 repos) and NEXO (3 repos) (DATA-03, DATA-04) — ORM puro cumple DATA-05 per CONTEXT.md D-01 (scope clarification: modelo SQLAlchemy declarativo es la representación canónica; no se generan .sql versionados para repos ORM-only)`
  4. `refactor(03-03): ciclos via CicloRepo`
  5. `refactor(03-03): recursos via RecursoRepo`
  6. `refactor(03-03): historial via EjecucionRepo + MetricaRepo`
  7. `refactor(03-03): auth.get_user_by_email uses UserRepo`
  8. `refactor(03-03): auditoria via AuditRepo + DbNexo`
  9. `refactor(03-03): usuarios uses api.deps.DbNexo (remove local get_nexo_db)`
  10. `refactor(03-03): pipeline.py — ORM migration via re-export + MES handoff from 03-02 (single atomic commit, Opción A/B documented in body)`
  11. `test(03-03): repos + routers smoke + no_pyodbc meta-test`
- Estado de shims (`nexo/db/engine.py`, `nexo/db/models.py`): conservados en Mark-III, eliminación diferida a Mark-IV.
- **DATA-05 cierre**: los `.sql` versionados en Mark-III viven en `nexo/data/sql/mes/` (creados en 03-02). Los repos APP/NEXO usan ORM puro y son DATA-05-compliant por D-01 scope clarification (CONTEXT.md 2026-04-19). 0 archivos `.sql` APP/NEXO generados. Si en el futuro algún método APP/NEXO requiere `text()`, añadir bajo `nexo/data/sql/{app,nexo}/` con justificación.
- **pipeline.py ownership**: este plan fue único editor de `api/services/pipeline.py` (per `<context_handoff>`). Documentar opción elegida (A/B) y justificación en el SUMMARY.
- Gap abierto: LukRepo queda como placeholder (métodos específicos en Mark-IV si luk4.py gana nuevas queries).
- 03-verify checklist: ver bloque `<verification>`.
</output>
</content>
</invoke>