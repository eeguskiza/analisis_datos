---
phase: 03-capa-de-datos
plan: 02
slug: mes-repository-routers-refactor
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - api/routers/centro_mando.py
  - api/routers/capacidad.py
  - api/routers/operarios.py
  - api/routers/luk4.py
  - api/routers/bbdd.py
  - api/services/db.py
files_created:
  - nexo/data/repositories/__init__.py
  - nexo/data/repositories/mes.py
  - nexo/data/dto/mes.py
  - nexo/data/sql/mes/extraer_datos_produccion.sql
  - nexo/data/sql/mes/detectar_recursos.sql
  - nexo/data/sql/mes/calcular_ciclos_reales.sql
  - nexo/data/sql/mes/estado_maquina_live.sql
  - nexo/data/sql/mes/centro_mando_fmesmic.sql
  - nexo/data/sql/mes/capacidad_piezas_linea.sql
  - nexo/data/sql/mes/capacidad_ciclos_p10_180d.sql
  - nexo/data/sql/mes/operarios_listar.sql
  - nexo/data/sql/mes/operarios_ficha.sql
  - nexo/data/sql/mes/bbdd_list_databases.sql
  - nexo/data/sql/mes/bbdd_list_tables.sql
  - nexo/data/sql/mes/bbdd_list_columns.sql
  - scripts/gen_pdf_reference.py
  - scripts/pdf_regression_check.py
  - tests/data/reference/.gitkeep
  - tests/data/test_mes_repository.py
  - tests/data/test_mes_engine_context.py
  - tests/data/test_no_three_part_names.py
  - tests/data/test_bbdd_whitelist.py
autonomous: false
requirements: [DATA-02, DATA-05, DATA-07, DATA-09]
requirements_addressed: [DATA-02, DATA-05, DATA-07, DATA-09]
tags: [data-layer, mes, repository, refactor, pdf-regression]
user_setup:
  - service: ecs-mobility-sql-server-dbizaro
    why: "El gate de regresión de PDF (success criterion #5) requiere ejecutar `scripts/gen_pdf_reference.py --fecha=<fecha_estable>` ANTES del refactor contra datos reales de IZARO."
    env_vars:
      - name: NEXO_MES_SERVER
        source: ".env (LAN interna)"
      - name: NEXO_MES_USER
        source: ".env"
      - name: NEXO_MES_PASSWORD
        source: ".env"
      - name: NEXO_MES_DB
        source: ".env → debe ser `dbizaro`"
    dashboard_config:
      - task: "Operador ejecuta `python scripts/gen_pdf_reference.py --fecha=2026-03-15` (o fecha histórica con datos estables) DESPUÉS de crear los scripts (Task 1) y ANTES del refactor de routers (Task 3)"
        location: "Máquina con acceso LAN al servidor SQL Server dbizaro"
      - task: "Tras el refactor, operador ejecuta `python scripts/pdf_regression_check.py --fecha=2026-03-15` como gate final del plan"
        location: "Misma máquina"
must_haves:
  truths:
    - "Ningún router de `api/routers/{bbdd,capacidad,operarios,centro_mando,luk4}.py` importa `pyodbc` directamente"
    - "Ningún archivo Python bajo `api/` contiene la cadena `dbizaro.` (3-part names eliminados per DATA-09), excepto `OEE/db/connector.py` que queda tal cual (D-04)"
    - "`MesRepository` expone 5 métodos: `extraer_datos_produccion`, `detectar_recursos`, `calcular_ciclos_reales`, `estado_maquina_live`, `consulta_readonly`"
    - "Los 4 métodos legacy-wrapping delegan a `OEE.db.connector` (Pitfall: no reimplementar `load_config`; usar `api.services.db.get_config`)"
    - "`consulta_readonly(sql, database)` ejecuta SQL YA validado por `api/routers/bbdd.py` (whitelist queda en router, D-05)"
    - "Query centro_mando usa `admuser.fmesmic` (2-part) con `bindparam('codes', expanding=True)` — string interpolation `ct0,ct1,...` eliminada"
    - "Pipeline OEE genera PDFs equivalentes a pre-refactor (hash SHA-256 idéntico, o fallback a ±5% size + misma page count) — success criterion #5"
    - "El comentario `-- NOTA T3:` está presente en `extraer_datos_produccion.sql`"
    - "`pytest tests/data/test_mes_*.py tests/data/test_no_three_part_names.py tests/data/test_bbdd_whitelist.py -q` exit 0"
    - "Smoke HTTP con cookie de sesión válida: los 5 routers responden 200 OK"
    - "`pytest tests/auth/ -q` sigue 11 passed / 1 skipped (regression IDENT-06)"
    - "`api/services/pipeline.py` NO es tocado por este plan (propiedad exclusiva de 03-03 — ver `<context_handoff>`)"
  artifacts:
    - path: "nexo/data/repositories/mes.py"
      provides: "MesRepository con 5 métodos"
      min_lines: 80
      exports: ["MesRepository"]
    - path: "nexo/data/dto/mes.py"
      provides: "ProduccionRow, EstadoMaquinaRow, CicloRealRow, CapacidadRow, OperarioRow"
      min_lines: 40
      exports: ["ProduccionRow", "EstadoMaquinaRow", "CicloRealRow", "CapacidadRow", "OperarioRow"]
    - path: "nexo/data/sql/mes/centro_mando_fmesmic.sql"
      provides: "Query fmesmic con admuser.fmesmic (2-part names)"
      contains: "FROM admuser.fmesmic"
      does_not_contain: "dbizaro."
    - path: "nexo/data/sql/mes/extraer_datos_produccion.sql"
      provides: "Query extraer_datos con comentario T3 preservado"
      contains: "NOTA T3"
    - path: "scripts/gen_pdf_reference.py"
      provides: "CLI para grabar PDF de referencia + SHA-256"
      min_lines: 30
    - path: "scripts/pdf_regression_check.py"
      provides: "CLI que compara hash o size ±5% post-refactor"
      min_lines: 40
    - path: "tests/data/test_mes_repository.py"
      provides: "5 tests del repo (mocks + delegation)"
      min_lines: 80
    - path: "tests/data/test_no_three_part_names.py"
      provides: "meta-test grep sobre api/"
      contains: "dbizaro"
    - path: "tests/data/test_bbdd_whitelist.py"
      provides: "Contract test del anti-DDL whitelist (D-05 security mitigation)"
      min_lines: 20
  key_links:
    - from: "api/routers/centro_mando.py"
      to: "MesRepository.centro_mando_fmesmic"
      via: "Depends(EngineMes) → MesRepository(engine=engine_mes).centro_mando_fmesmic(ct_codes)"
      pattern: "MesRepository\\("
    - from: "api/routers/bbdd.py:POST /consulta"
      to: "MesRepository.consulta_readonly"
      via: "SQL ya validada por whitelist anti-DDL"
      pattern: "consulta_readonly"
    - from: "api/services/db.py"
      to: "MesRepository"
      via: "legacy wrapper re-pointed a MesRepository"
      pattern: "from nexo\\.data\\.repositories\\.mes"
    - from: "nexo/data/sql/mes/centro_mando_fmesmic.sql"
      to: "engine_mes"
      via: "DATABASE=dbizaro baked in DSN → 2-part names funcionan"
      pattern: "FROM admuser\\.fmesmic"
---

<objective>
Construir la capa MES: `MesRepository` con 5 métodos (4 wrappers delgados sobre `OEE/db/connector.py` per D-04 + `consulta_readonly` que corre SQL ya validada). Extraer queries inline de 5 routers (`centro_mando`, `capacidad`, `operarios`, `luk4`, `bbdd`) a `.sql` versionados bajo `nexo/data/sql/mes/`. Matar 3-part names `dbizaro.admuser.*` (DATA-09) gracias al DSN con `DATABASE=dbizaro` del plan 03-01. Preservar comentario T3 en la query de extracción. Blindar con hash-guard de PDF pre/post refactor (success criterion #5) y meta-tests grep-based.

Purpose: 03-03 necesita `MesRepository` listo (pipeline lo consume a través de `api/services/db.py`). Al cerrar 03-02 todos los routers MES tiran de la capa nueva; `OEE/db/connector.py` queda como único sitio con SQL inline (D-04). **NOTA DE HANDOFF (Blocker-1 de revision)**: `api/services/pipeline.py` es propiedad EXCLUSIVA de 03-03. Este plan deja `api/services/db.py` apuntando a `MesRepository`, pero NO edita `pipeline.py`. 03-03 se encarga del swap completo de imports en pipeline.py como parte de su migración ORM + auth/repo refactor.

Output: 22 archivos nuevos, 6 modificados (pipeline.py NO — moved a 03-03). Gate: PDF regression check pasa; los 5 routers smoke HTTP 200; `grep "import pyodbc" api/routers/{bbdd,capacidad,operarios,centro_mando,luk4}.py` = 0 coincidencias; `grep "dbizaro\." api/` = 0 coincidencias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context_handoff>
## Ownership handoff: `api/services/pipeline.py`

**Propiedad exclusiva:** 03-03 (NO tocar en 03-02).

**Razón** (Blocker-1 del plan checker): 03-02 y 03-03 corren en paralelo (ambos Wave 2 con `depends_on: [03-01]`). Si los dos editan `api/services/pipeline.py`, hay conflicto de merge. 03-03 es dueño de la migración ORM (Landmine #9 — ~15 touch points en pipeline.py) y del refactor auth/repo. Todos los edits a `pipeline.py` (incluido el swap del import MES legacy → `MesRepository`) pasan a 03-03.

**Qué deja 03-02 listo para 03-03:**
- `api/services/db.py` re-pointed a `MesRepository` (ver Task 3, subtask 3.7). Este es el facade que `pipeline.py` consume; al cambiarlo aquí, `pipeline.py` recoge el nuevo backend sin necesidad de editar imports propios.
- `MesRepository` y `nexo/data/engines.engine_mes` disponibles e importables.
- Meta-tests (`test_no_raw_pyodbc_in_routers`, `test_no_three_part_names`) NO incluyen `api/services/pipeline.py` como ofensor (los routers/servicios están ya verdes via el facade).

**Qué debe hacer 03-03 respecto a pipeline.py:**
- Si `pipeline.py` mantiene `from api.services import db as mes_service` y llama `mes_service.extract_data(...)`, NO hay cambio necesario (03-02 ya ajustó el facade).
- Si `pipeline.py` importa directamente de `OEE.db.connector`, 03-03 swappea esos imports a `api.services.db.*` o directamente a `nexo.data.repositories.mes.MesRepository` según prefiera el executor (en línea con la migración ORM que 03-03 está haciendo).
- La migración ORM de `api/database.py` → `nexo/data/models_app.py` (también 03-03) toca pipeline.py via re-export; juntar ambos cambios en el mismo commit es atómico.
</context_handoff>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-datos/03-CONTEXT.md
@.planning/phases/03-capa-de-datos/03-RESEARCH.md
@.planning/phases/03-capa-de-datos/03-PATTERNS.md
@.planning/phases/03-capa-de-datos/03-VALIDATION.md
@.planning/phases/03-capa-de-datos/03-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que el executor va a consumir o crear. -->

From nexo/data/engines.py (creado en 03-01):
```python
engine_mes: Engine   # SQL Server, DATABASE=dbizaro en DSN, pool DATA-11
engine_app: Engine   # SQL Server ecs_mobility (legacy api/database.py)
```

From api/deps.py (creado en 03-01):
```python
DbApp      # Annotated[Session, Depends(get_db_app)]
DbNexo     # Annotated[Session, Depends(get_db_nexo)]
EngineMes  # Annotated[Engine, Depends(get_engine_mes)]
```

From OEE/db/connector.py (NO se reescribe — D-04):
```python
def extraer_datos(cfg: dict, fecha_inicio: date, fecha_fin: date) -> list[dict]: ...
def detectar_recursos(cfg: dict) -> list[dict]: ...
def calcular_ciclos_reales(cfg: dict, centro_trabajo: int, dias_atras: int = 30) -> tuple[list[dict], str]: ...
def estado_maquina_live(cfg: dict, centro_trabajo: int, umbral_activo_seg: int = 600) -> dict: ...
def _build_connection_string(cfg: dict) -> str: ...
def load_config() -> dict: ...
```

From api/services/db.py (wrapper existente — el import MIGRA, la función `get_config` se REUSA):
```python
def get_config() -> dict:  # Inyecta settings.db_* si faltan en JSON
def discover_resources() -> list[dict]:
def extract_data(fecha_inicio, fecha_fin, recursos=None) -> list[dict]:
def compute_real_cycles(centro_trabajo, dias_atras=30) -> tuple[list[dict], str]:
def live_status(centro_trabajo, umbral_activo_seg=600) -> dict:
```

MesRepository signature target (copiar de RESEARCH §Wrapping OEE/db/connector.py):
```python
class MesRepository:
    def __init__(self, engine: Engine): ...
    def extraer_datos_produccion(self, *, fecha_inicio: date, fecha_fin: date,
                                 recursos: list[str] | None = None) -> list[dict]: ...
    def detectar_recursos(self) -> list[dict]: ...
    def calcular_ciclos_reales(self, centro_trabajo: int, dias_atras: int = 30) -> tuple[list[dict], str]: ...
    def estado_maquina_live(self, centro_trabajo: int, umbral_activo_seg: int = 600) -> dict: ...
    def consulta_readonly(self, sql: str, database: str = "dbizaro") -> dict:
        """SQL ya validada por el router (D-05). Devuelve {columns, rows}."""
    def centro_mando_fmesmic(self, ct_codes: list[int]) -> list[dict]: ...  # NEW via load_sql
```

Current SQL inline target for extraction (pre-refactor references):
- `api/routers/centro_mando.py:38-88` — `_query_fmesmic` con 3-part names y string interpolation
- `api/routers/capacidad.py:49-130+` — 3 queries con pyodbc crudo
- `api/routers/operarios.py:18-22,34-53` — `_connect()` + query listar
- `api/routers/luk4.py:13,45-80+` — importa `engine` de `api.database` (NOTA: `luk4.*` está en APP, NO en MES — ver nota en action de Task 3 sobre qué hacer)
- `api/routers/bbdd.py:17-52,55-149+` — pyodbc para metadata ops (stays inline per D-05) + whitelist anti-DDL (stays)
</interfaces>

<research_notes>
- **Landmine #1 (repo config)**: `MesRepository` NO reimplementa `load_config()`; importa `from api.services.db import get_config as _get_mes_config` y lo reusa (test fixture mockea ese símbolo).
- **Landmine #9 (luk4 ubicación)**: las tablas `luk4.estado`, `luk4.alarmas`, `luk4.tiempos_ciclo` viven en `ecs_mobility.luk4.*` (APP), NO en `dbizaro`. PATTERNS.md nota este edge en la sección luk4. Decisión del planner: `luk4.py` deja de importar `engine` de `api.database` y en lugar de usar `MesRepository` usa `text()` + `engine_app` vía `DbApp` (sesión) o directamente via `EngineMes`-analog para APP. MÁS SIMPLE: mantener las queries inline en `luk4.py` con `text()` + `engine_app` (vía `Depends(get_engine_app)` si se añade; si no, importar `engine_app` de `nexo.data.engines`).
- **D-01 scope DATA-05 (clarificado 2026-04-19)**: CONTEXT.md D-01 aclara que los `.sql` versionados aplican solo a queries con `text()`. Queries ORM puras (`session.query(Model).filter(...)`) NO requieren `.sql` — el modelo declarativo SQLAlchemy ES la representación canónica. Aplicación concreta en Task 3.5 (luk4): si `luk4.py` usa ORM puro sobre `engine_app`, no se crean `.sql`. Si usa `text("SELECT ...")`, extraer a `nexo/data/sql/app/luk4_*.sql` per D-01.
- **D-05 whitelist**: `api/routers/bbdd.py` tiene (en el handler POST del consulta libre) un regex anti-DDL. Test `tests/data/test_bbdd_whitelist.py` debe exercise que SQL con `DROP`, `DELETE`, `UPDATE`, `INSERT`, `TRUNCATE`, `ALTER` es rechazado antes de llegar al repo.
- **PDF regression (success #5)**: se graba UNA VEZ antes de tocar routers (via `gen_pdf_reference.py`). Script de comparación pos-refactor usa hash SHA-256 primero; si difiere (matplotlib puede emitir bytes levemente distintos entre runs por timestamps), fallback a page_count + size ±5%. Si ambos fallan: FAIL (exit 2).
- **Task ordering (Warning fix)**: Task 1 crea los scripts `gen_pdf_reference.py` + `pdf_regression_check.py`. Task 2 es el checkpoint humano que los ejecuta. Orden invertido respecto a la revisión inicial para resolver la imposibilidad lógica (el operador no podía "correr" un script que aún no existía).
</research_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear MesRepository + DTOs MES + scripts PDF (Wave 1 code — scripts PRIMERO)</name>
  <files>
    nexo/data/repositories/__init__.py (new, empty),
    nexo/data/repositories/mes.py (new),
    nexo/data/dto/mes.py (new),
    scripts/gen_pdf_reference.py (new),
    scripts/pdf_regression_check.py (new),
    tests/data/reference/.gitkeep (new)
  </files>
  <read_first>
    - api/services/db.py completo (reusa `get_config`; D-04 Landmine #1)
    - OEE/db/connector.py primeras 100 líneas (firmas de extraer_datos, detectar_recursos, calcular_ciclos_reales, estado_maquina_live — NO reescribir, solo delegar)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md sección "nexo/data/repositories/mes.py" (líneas 420-469) — pattern canónico
    - .planning/phases/03-capa-de-datos/03-RESEARCH.md §Wrapping OEE/db/connector.py (el MesRepository completo)
    - api/routers/centro_mando.py:38-88 (_query_fmesmic — origen de centro_mando_fmesmic method)
    - api/routers/bbdd.py (ver POST /consulta handler y regex whitelist — queda en router per D-05; `consulta_readonly` recibe sql validada)
    - scripts/init_nexo_schema.py (analog para scripts standalone: argparse + docker compose exec context)
  </read_first>
  <behavior>
    - Test 1: `MesRepository` tiene 6 métodos públicos: `extraer_datos_produccion`, `detectar_recursos`, `calcular_ciclos_reales`, `estado_maquina_live`, `consulta_readonly`, `centro_mando_fmesmic`.
    - Test 2: `extraer_datos_produccion` delega a `OEE.db.connector.extraer_datos` (mock la función y verifica `assert_called_once`).
    - Test 3: `detectar_recursos` delega a `OEE.db.connector.detectar_recursos`.
    - Test 4: `calcular_ciclos_reales` delega a `OEE.db.connector.calcular_ciclos_reales(cfg, centro_trabajo, dias_atras)`.
    - Test 5: `estado_maquina_live` delega a `OEE.db.connector.estado_maquina_live`.
    - Test 6: `consulta_readonly` ejecuta `text(sql)` contra `engine_mes.connect()`, devuelve `{"columns": list[str], "rows": list[list]}`.
    - Test 7: `centro_mando_fmesmic([47, 48])` carga `mes/centro_mando_fmesmic.sql` (mock `load_sql`), construye `text(...).bindparams(bindparam("codes", expanding=True))`, ejecuta con `{"codes": ["47", "48"]}`, devuelve `list[dict]`.
    - Test 8 (DTOs): `ProduccionRow`, `EstadoMaquinaRow`, `CicloRealRow`, `CapacidadRow`, `OperarioRow` son frozen Pydantic BaseModel con `from_attributes=True`.
    - Test 9 (scripts existen): `scripts/gen_pdf_reference.py` y `scripts/pdf_regression_check.py` son ejecutables Python válidos (import sin errores, `argparse` presente).
  </behavior>
  <action>
1. Crear `nexo/data/repositories/__init__.py` vacío.

2. Crear `nexo/data/repositories/mes.py`:

   ```python
   """MesRepository — acceso a SQL Server dbizaro (DATA-02).

   4 métodos son wrappers delgados sobre OEE/db/connector.py (D-04: NO reescribir
   la lógica, solo envolver). El 5º `consulta_readonly` es el entry point para
   `/bbdd` — recibe SQL **ya validada por el whitelist del router** (D-05).
   El 6º `centro_mando_fmesmic` es la query nueva con 2-part names + bindparam
   expanding (DATA-09, reemplaza el string-interpolation del router viejo).
   """
   from __future__ import annotations

   from datetime import date
   from typing import Any

   from sqlalchemy import bindparam, text
   from sqlalchemy.engine import Engine

   from nexo.data.sql.loader import load_sql

   # Reusamos get_config existente (Landmine #1) — no reimplementar.
   from api.services.db import get_config as _get_mes_config

   from OEE.db.connector import (
       calcular_ciclos_reales as _legacy_calcular_ciclos,
       detectar_recursos as _legacy_detectar_recursos,
       estado_maquina_live as _legacy_estado_maquina,
       extraer_datos as _legacy_extraer_datos,
   )


   class MesRepository:
       """Repositorio MES (dbizaro read-only por convencion).

       Engine inyectado (no Session — MES no tiene transaccion de write).
       Los 4 metodos legacy-wrapping usan cfg via `api.services.db.get_config`;
       consulta_readonly y centro_mando_fmesmic usan `self._engine` directamente.
       """

       def __init__(self, engine: Engine):
           self._engine = engine

       # ── Wrappers delgados (D-04) ────────────────────────────────────────
       def extraer_datos_produccion(
           self,
           *,
           fecha_inicio: date,
           fecha_fin: date,
           recursos: list[str] | None = None,
       ) -> list[dict]:
           cfg = _get_mes_config()
           if recursos:
               cfg = {
                   **cfg,
                   "recursos": [
                       r for r in cfg.get("recursos", [])
                       if r.get("nombre") in recursos
                   ],
               }
           return _legacy_extraer_datos(cfg, fecha_inicio, fecha_fin)

       def detectar_recursos(self) -> list[dict]:
           cfg = _get_mes_config()
           return _legacy_detectar_recursos(cfg)

       def calcular_ciclos_reales(
           self,
           centro_trabajo: int,
           dias_atras: int = 30,
       ) -> tuple[list[dict], str]:
           cfg = _get_mes_config()
           return _legacy_calcular_ciclos(cfg, centro_trabajo, dias_atras)

       def estado_maquina_live(
           self,
           centro_trabajo: int,
           umbral_activo_seg: int = 600,
       ) -> dict:
           cfg = _get_mes_config()
           return _legacy_estado_maquina(cfg, centro_trabajo, umbral_activo_seg)

       # ── Consulta libre validada por el router (D-05) ────────────────────
       def consulta_readonly(
           self,
           sql: str,
           database: str = "dbizaro",  # parametro informativo; engine ya apunta alli
       ) -> dict[str, Any]:
           """Ejecuta SQL ya validada por `api/routers/bbdd.py` whitelist.

           Devuelve `{"columns": [...], "rows": [[...], ...]}` shape que
           consume la UI del explorer.
           """
           with self._engine.connect() as conn:
               result = conn.execute(text(sql))
               cols = list(result.keys())
               rows = [list(r) for r in result.fetchall()]
           return {"columns": cols, "rows": rows}

       # ── Query nueva con load_sql + bindparam expanding (DATA-09) ────────
       def centro_mando_fmesmic(self, ct_codes: list[int]) -> list[dict]:
           """Estado actual fmesmic por CT. Reemplaza _query_fmesmic del router viejo.

           Cambios respecto al original:
           - FROM admuser.fmesmic (2-part, no 3-part) — engine_mes ya apunta a dbizaro.
           - IN :codes via bindparam(expanding=True), no string interpolation.
           """
           if not ct_codes:
               return []
           stmt = text(load_sql("mes/centro_mando_fmesmic")).bindparams(
               bindparam("codes", expanding=True)
           )
           with self._engine.connect() as conn:
               result = conn.execute(stmt, {"codes": [str(c) for c in ct_codes]})
               return [dict(r._mapping) for r in result.fetchall()]
   ```

3. Crear `nexo/data/dto/mes.py`:

   ```python
   """DTOs frozen (Pydantic v2) que cruzan el borde router↔repo para MES (DATA-08)."""
   from __future__ import annotations

   from datetime import date, datetime
   from typing import Optional

   from pydantic import BaseModel, ConfigDict, field_validator


   class ProduccionRow(BaseModel):
       """Fila de produccion de extraer_datos_produccion.

       Campos extraidos del output actual de OEE.db.connector.extraer_datos.
       """
       model_config = ConfigDict(frozen=True, from_attributes=True)

       recurso: str
       seccion: str
       fecha: date
       h_ini: Optional[str] = None
       h_fin: Optional[str] = None
       tiempo: float = 0.0
       proceso: Optional[str] = None
       incidencia: str = ""
       cantidad: float = 0.0
       malas: float = 0.0
       recuperadas: float = 0.0
       referencia: str = ""

       @field_validator("fecha", mode="before")
       @classmethod
       def _coerce_fecha(cls, v):
           if hasattr(v, "date") and not isinstance(v, date):
               return v.date()
           return v


   class EstadoMaquinaRow(BaseModel):
       """Output de estado_maquina_live (shape usado por centro_mando y pages)."""
       model_config = ConfigDict(frozen=True, from_attributes=True)

       centro_trabajo: int
       activa: bool
       ultimo_evento_ts: Optional[datetime] = None
       piezas_hoy: int = 0


   class CicloRealRow(BaseModel):
       """Output de calcular_ciclos_reales (tuple[list[dict], str]) por referencia."""
       model_config = ConfigDict(frozen=True, from_attributes=True)

       referencia: str
       ciclo_seg: float
       n_muestras: int = 0


   class CapacidadRow(BaseModel):
       """Shape consumida por /capacidad endpoint."""
       model_config = ConfigDict(frozen=True, from_attributes=True)

       referencia: str
       ct: str
       ct_nombre: str
       piezas_reales: float = 0.0
       tiempo_trabajado_min: float = 0.0
       ciclo_teorico_seg: Optional[float] = None
       fecha_min: Optional[str] = None
       fecha_max: Optional[str] = None


   class OperarioRow(BaseModel):
       """Shape de /operarios listar."""
       model_config = ConfigDict(frozen=True, from_attributes=True)

       codigo: int
       nombre: str
       activo: bool
       n_registros_mes: int = 0
       ultimo_registro: Optional[str] = None
   ```

4. Crear `scripts/gen_pdf_reference.py`:

   ```python
   """Graba PDF de referencia pre-refactor para el gate de regresion (success #5).

   Uso:
       docker compose exec -T web python scripts/gen_pdf_reference.py --fecha=2026-03-15

   Output:
       tests/data/reference/pipeline_<fecha>.pdf
       tests/data/reference/pipeline_<fecha>.pdf.sha256
   """
   from __future__ import annotations

   import argparse
   import hashlib
   from datetime import date
   from pathlib import Path


   REF_DIR = Path(__file__).resolve().parents[1] / "tests" / "data" / "reference"


   def _sha256(path: Path) -> str:
       h = hashlib.sha256()
       h.update(path.read_bytes())
       return h.hexdigest()


   def main() -> int:
       ap = argparse.ArgumentParser()
       ap.add_argument("--fecha", required=True, help="YYYY-MM-DD fecha estable")
       args = ap.parse_args()

       fecha = date.fromisoformat(args.fecha)
       REF_DIR.mkdir(parents=True, exist_ok=True)

       # Invocar el mismo path que /pipeline/run. El import se hace aqui para
       # no ejecutar al cargar el modulo.
       from api.services.pipeline import run_pipeline

       out_dir = REF_DIR / f"run_{fecha.isoformat()}"
       out_dir.mkdir(exist_ok=True)

       # run_pipeline firma actual: ajustar si cambia.
       result = run_pipeline(
           fecha_inicio=fecha,
           fecha_fin=fecha,
           recursos=None,
           source="db",
           output_dir=out_dir,
       )

       # Encontrar el PDF principal generado (el más grande o el primero por convención).
       pdfs = sorted(out_dir.glob("*.pdf"))
       if not pdfs:
           print(f"ERROR: pipeline no genero PDFs en {out_dir}")
           return 1

       main_pdf = pdfs[0]
       target = REF_DIR / f"pipeline_{fecha.isoformat()}.pdf"
       target.write_bytes(main_pdf.read_bytes())

       digest = _sha256(target)
       sha_file = REF_DIR / f"pipeline_{fecha.isoformat()}.pdf.sha256"
       sha_file.write_text(f"{digest}  {target.name}\n", encoding="utf-8")

       print(f"OK — referencia grabada:")
       print(f"  path:   {target}")
       print(f"  sha256: {digest}")
       print(f"  size:   {target.stat().st_size} bytes")
       print(f"  pages:  ver con 'pdfinfo' o similar")
       return 0


   if __name__ == "__main__":
       raise SystemExit(main())
   ```

5. Crear `scripts/pdf_regression_check.py`:

   ```python
   """Compara PDF post-refactor contra baseline (success criterion #5).

   Uso:
       docker compose exec -T web python scripts/pdf_regression_check.py --fecha=2026-03-15

   Exit codes:
       0 = OK (hash identico O page_count identico + size en ±5%)
       1 = WARN (hash difiere, size en ±5%, page_count identico)
       2 = REGRESSION (size fuera de ±5% o page_count distinto)
   """
   from __future__ import annotations

   import argparse
   import hashlib
   import sys
   from datetime import date
   from pathlib import Path


   REF_DIR = Path(__file__).resolve().parents[1] / "tests" / "data" / "reference"
   SIZE_TOLERANCE = 0.05  # ±5%


   def _sha256(path: Path) -> str:
       return hashlib.sha256(path.read_bytes()).hexdigest()


   def _pdf_pages(path: Path) -> int:
       """Conteo rustico de paginas — cuenta ocurrencias de '/Type /Page'."""
       data = path.read_bytes()
       return data.count(b"/Type /Page") - data.count(b"/Type /Pages")


   def main() -> int:
       ap = argparse.ArgumentParser()
       ap.add_argument("--fecha", required=True)
       args = ap.parse_args()

       fecha = date.fromisoformat(args.fecha)
       ref_pdf = REF_DIR / f"pipeline_{fecha.isoformat()}.pdf"
       ref_sha = REF_DIR / f"pipeline_{fecha.isoformat()}.pdf.sha256"

       if not ref_pdf.exists() or not ref_sha.exists():
           print(f"ERROR: baseline ausente. Corre gen_pdf_reference.py --fecha={args.fecha} primero.")
           return 2

       # Re-generar con MesRepository actual (via pipeline).
       from api.services.pipeline import run_pipeline

       tmp_out = REF_DIR / f"_postref_{fecha.isoformat()}"
       tmp_out.mkdir(exist_ok=True)
       run_pipeline(
           fecha_inicio=fecha, fecha_fin=fecha, recursos=None,
           source="db", output_dir=tmp_out,
       )
       new_pdfs = sorted(tmp_out.glob("*.pdf"))
       if not new_pdfs:
           print("ERROR: pipeline post-refactor no generó PDFs.")
           return 2
       new_pdf = new_pdfs[0]

       new_hash = _sha256(new_pdf)
       ref_hash_line = ref_sha.read_text(encoding="utf-8").strip().split()[0]

       if new_hash == ref_hash_line:
           print(f"OK — hash idéntico ({new_hash})")
           return 0

       ref_size = ref_pdf.stat().st_size
       new_size = new_pdf.stat().st_size
       diff_pct = abs(new_size - ref_size) / ref_size if ref_size else 1.0

       ref_pages = _pdf_pages(ref_pdf)
       new_pages = _pdf_pages(new_pdf)

       print(f"hash difiere:")
       print(f"  ref:  {ref_hash_line[:16]}... size={ref_size} pages={ref_pages}")
       print(f"  new:  {new_hash[:16]}... size={new_size} pages={new_pages}")
       print(f"  diff: size {diff_pct*100:.1f}%, pages delta={new_pages - ref_pages}")

       if ref_pages != new_pages:
           print("REGRESSION: page count mismatch")
           return 2
       if diff_pct > SIZE_TOLERANCE:
           print(f"REGRESSION: size diff > {SIZE_TOLERANCE*100:.0f}%")
           return 2
       print(f"WARN: bytes difieren (matplotlib timestamps?) pero size ±{SIZE_TOLERANCE*100:.0f}% y pages OK")
       return 1


   if __name__ == "__main__":
       sys.exit(main())
   ```

6. Crear `tests/data/reference/.gitkeep` vacío (marcador de directorio).

7. Actualizar `.gitignore` (opcional, si el PDF es grande): añadir `tests/data/reference/*.pdf` para trackear solo el `.sha256`. Decisión del executor según el tamaño real del PDF tras grabar.
  </action>
  <verify>
    <automated>docker compose exec -T web python -c "from nexo.data.repositories.mes import MesRepository; assert hasattr(MesRepository, 'extraer_datos_produccion'); assert hasattr(MesRepository, 'consulta_readonly'); assert hasattr(MesRepository, 'centro_mando_fmesmic'); print('MesRepository OK')"</automated>
    <automated>docker compose exec -T web python -c "from nexo.data.dto.mes import ProduccionRow, EstadoMaquinaRow, CicloRealRow, CapacidadRow, OperarioRow; import pydantic; assert ProduccionRow.model_config['frozen'] is True; print('DTOs MES OK')"</automated>
    <automated>test -f scripts/gen_pdf_reference.py && test -f scripts/pdf_regression_check.py && test -f tests/data/reference/.gitkeep && echo "scripts OK"</automated>
    <automated>docker compose exec -T web python -c "import ast; [ast.parse(open(f).read()) for f in ('scripts/gen_pdf_reference.py', 'scripts/pdf_regression_check.py')]; print('scripts parse OK')"</automated>
  </verify>
  <done>
    - `MesRepository` con 6 métodos públicos (4 wrappers + consulta_readonly + centro_mando_fmesmic).
    - Los 4 wrappers delegan verbatim a `OEE.db.connector.*` via `_get_mes_config` (D-04, Landmine #1).
    - 5 DTOs MES frozen Pydantic v2.
    - 2 scripts PDF standalone con argparse **existen como archivos en disco** (prerequisito lógico de Task 2).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (gate 0): Grabar PDF de referencia ANTES del refactor (requiere SQL Server LAN)</name>
  <what-built>
    Task 1 ya creó los scripts `scripts/gen_pdf_reference.py` y `scripts/pdf_regression_check.py` en disco. Este checkpoint es la EJECUCIÓN manual de `gen_pdf_reference.py` contra SQL Server real, cosa que requiere credenciales operador y acceso LAN a `dbizaro` — no puede hacerlo Claude desde un sandbox.
  </what-built>
  <how-to-verify>
    1. El operador debe estar en una máquina con acceso LAN al servidor SQL Server de dbizaro (típicamente el servidor Ubuntu LAN o un laptop con VPN).
    2. `.env` debe tener `NEXO_MES_*` configuradas (del trabajo Phase 1 / CLAUDE.md naming).
    3. Operador corre:
       ```
       docker compose up -d web db
       docker compose exec -T web python scripts/gen_pdf_reference.py --fecha=2026-03-15
       ```
       (fecha parametrizable: elige una fecha histórica con datos estables, al menos 30 días atrás para garantizar que IZARO ya no está actualizando los datos de ese día).
    4. El script debe:
       - Ejecutar `api.services.pipeline.run_pipeline(fecha_inicio=fecha, fecha_fin=fecha, recursos=None, source="db")` (o equivalente — reusar el mismo path que el endpoint `/pipeline/run`).
       - Guardar el PDF principal en `tests/data/reference/pipeline_2026-03-15.pdf`.
       - Calcular SHA-256 y guardarlo en `tests/data/reference/pipeline_2026-03-15.pdf.sha256`.
       - Imprimir el hash y size bytes en stdout.
    5. Operador commitea SOLO el `.sha256` (el PDF se puede gitignorear según peso — añadir a `.gitignore` si >5MB).
  </how-to-verify>
  <resume-signal>Operador escribe "pdf-reference-grabado fecha=YYYY-MM-DD hash=XXXXXXXX" o "no puedo grabar (razón, plazo)". Si no puede: ver Fallback REFORZADO abajo.</resume-signal>

  **Fallback REFORZADO (si el operador no tiene acceso a SQL Server real en este momento):**

  Saltar la EJECUCIÓN AHORA, pero el plan **NO SE CIERRA** hasta que el baseline esté grabado en preprod. Condiciones:

  1. **Deadline fijado: 7 días post-merge de 03-02 en preprod.** Si pasan 7 días sin baseline grabado, 03-02 se re-abre y el merge a main se bloquea.
  2. **Gate POST-merge reforzado**: 03-02 **NO puede marcarse DONE** hasta que el operador ejecute `scripts/gen_pdf_reference.py` + `scripts/pdf_regression_check.py` en preprod (con acceso LAN a MES) Y valide el PDF visualmente contra una muestra pre-Mark-III. La validación visual mitiga el caso de matplotlib emitiendo bytes distintos por timestamps (hash=WARN pero contenido correcto).
  3. **Registro en STATE.md**: al saltar el checkpoint, añadir entrada explícita a `.planning/STATE.md` bajo la sección de "Deferred verifications":
     ```
     - [ ] 03-02 PDF regression baseline — deadline YYYY-MM-DD (operador: <nombre>).
       Ejecutar en preprod: gen_pdf_reference.py + pdf_regression_check.py --fecha=YYYY-MM-DD + inspección visual.
     ```
  4. **SUMMARY.md anotación**: `03-02-SUMMARY.md` debe listar en "Riesgos abiertos" la fecha de ejecución pendiente y la persona responsable.
  5. **Meta-tests siguen siendo bloqueantes AHORA** (no se salta nada del resto del plan): `test_no_three_part_names`, `test_no_raw_pyodbc_in_routers`, `test_bbdd_whitelist`, `test_mes_repository`, smoke HTTP manual con cookie. Solo el PDF baseline queda diferido.

  **Condición estricta:** si el executor salta este checkpoint sin registrar el TODO en STATE.md con deadline y sin añadir la nota en SUMMARY.md, el plan se considera incompleto y el checker lo rechaza.
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear 11 archivos .sql + refactor 5 routers MES en orden D-06 (Wave 2)</name>
  <files>
    nexo/data/sql/mes/extraer_datos_produccion.sql (new, placeholder con comentario T3),
    nexo/data/sql/mes/detectar_recursos.sql (new, placeholder),
    nexo/data/sql/mes/calcular_ciclos_reales.sql (new, placeholder),
    nexo/data/sql/mes/estado_maquina_live.sql (new, placeholder),
    nexo/data/sql/mes/centro_mando_fmesmic.sql (new, 2-part names + :codes),
    nexo/data/sql/mes/capacidad_piezas_linea.sql (new, extraido de capacidad.py:60-87),
    nexo/data/sql/mes/capacidad_ciclos_p10_180d.sql (new, extraido de capacidad.py:119+),
    nexo/data/sql/mes/operarios_listar.sql (new, extraido de operarios.py:34-53),
    nexo/data/sql/mes/operarios_ficha.sql (new, extraido de operarios.py:72+),
    nexo/data/sql/mes/bbdd_list_databases.sql (new, extraido de bbdd.py:62-69),
    nexo/data/sql/mes/bbdd_list_tables.sql (new, extraido de bbdd.py:89-97),
    nexo/data/sql/mes/bbdd_list_columns.sql (new, extraido),
    api/routers/centro_mando.py (refactor — inject EngineMes, delete _query_fmesmic, use MesRepository),
    api/routers/capacidad.py (refactor — remove pyodbc, use EngineMes + load_sql or MesRepository),
    api/routers/operarios.py (refactor — remove _connect, use EngineMes + load_sql),
    api/routers/luk4.py (refactor — import engine from nexo.data.engines.engine_app, NO pyodbc; luk4.* está en APP, not MES),
    api/routers/bbdd.py (refactor — POST /consulta usa MesRepository.consulta_readonly; metadata ops stay inline usando engine_mes; whitelist stays),
    api/services/db.py (modified — repoint legacy imports a MesRepository donde tenga sentido; minimum diff)
  </files>
  <read_first>
    - api/routers/centro_mando.py completo (38-88 es el target; líneas 116-167 reemplazo de firma)
    - api/routers/capacidad.py completo (18-130+)
    - api/routers/operarios.py completo (18-55 + 72+)
    - api/routers/luk4.py completo (11-14 imports, 45-80+ queries — OJO: `luk4.*` en APP, no MES)
    - api/routers/bbdd.py completo (17-52 conn strings, 55-149+ metadata ops, POST /consulta handler con whitelist)
    - api/services/db.py completo (es el facade que `api/services/pipeline.py` usa; minimum diff — 03-02 lo ajusta, pipeline.py NO se toca aquí; ver `<context_handoff>`)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones de cada router (líneas 472-628) — tiene el before/after verbatim
  </read_first>
  <behavior>
    - Test 1: `grep -c "import pyodbc" api/routers/{bbdd,capacidad,operarios,centro_mando,luk4}.py` == 0.
    - Test 2: `grep -c "dbizaro\\." api/` == 0 (excluye `OEE/db/connector.py` por D-04; el grep se hace sobre `api/` no global).
    - Test 3: `nexo/data/sql/mes/extraer_datos_produccion.sql` contiene la cadena `NOTA T3` como header.
    - Test 4: `nexo/data/sql/mes/centro_mando_fmesmic.sql` contiene `FROM admuser.fmesmic` y NO contiene `dbizaro.`.
    - Test 5: `api/routers/centro_mando.py:summary` firma nueva usa `DbApp` + `EngineMes` + `MesRepository`.
    - Test 6: HTTP smoke con cookie válida:
      - `GET /api/dashboard/summary` → 200
      - `GET /api/capacidad?fecha_inicio=2026-03-15&fecha_fin=2026-03-15` → 200 (o 502 si SQL Server no alcanzable, aceptable)
      - `GET /api/operarios` → 200
      - `GET /api/luk4/status` → 200 (o 502 si SQL Server APP no alcanzable)
      - `GET /api/bbdd/databases` → 200
    - Test 7: `api/services/pipeline.py` NO ha sido modificado por este plan (diff contra `HEAD~` en el commit de Task 3 no debe tocar pipeline.py).
  </behavior>
  <action>
**Orden D-06: centro_mando → capacidad → operarios → luk4 → bbdd. Un commit por router. Smoke HTTP 200 antes de pasar al siguiente.**

### 3.1 — Crear los archivos .sql placeholders/extracciones

1. `nexo/data/sql/mes/extraer_datos_produccion.sql`:
   ```sql
   -- NOTA T3: el filtro de turno 3 cruza medianoche (T3 empieza a las 22:00
   -- del dia N y termina a las 06:00 del dia N+1). La logica real esta en
   -- OEE/db/connector.py:extraer_datos — este archivo es un placeholder
   -- para DATA-05 (un .sql por metodo repo) mientras D-04 mantiene la query
   -- canonica en el connector.
   --
   -- NO ejecutar este archivo directamente; MesRepository.extraer_datos_produccion
   -- delega en OEE.db.connector.extraer_datos (wrapper delgado, D-04).
   SELECT 'placeholder — ver OEE/db/connector.extraer_datos' AS note;
   ```

2. `detectar_recursos.sql`, `calcular_ciclos_reales.sql`, `estado_maquina_live.sql`: idéntico patrón placeholder con nota "ver OEE/db/connector.py:<funcion>". Esto satisface DATA-05 ("un archivo por método") sin reescribir el connector (D-04).

3. `nexo/data/sql/mes/centro_mando_fmesmic.sql` — extracción REAL con 2-part names (DATA-09):
   ```sql
   -- NOTA: engine_mes tiene DATABASE=dbizaro en connection string (plan 03-01),
   -- por eso usamos 2-part names (admuser.fmesmic) en lugar de 3-part.
   -- IN :codes es bindparam expanding (SQLAlchemy 2.0).
   SELECT
       CAST(RTRIM(mi020) AS INT)    AS ct,
       COUNT(*)                     AS piezas_hoy,
       MAX(CAST(mi100 AS TIME))     AS ultimo_evento,
       (SELECT TOP 1 RTRIM(m2.mi060)
        FROM admuser.fmesmic m2
        WHERE RTRIM(m2.mi020) = RTRIM(m.mi020)
          AND CONVERT(DATE, m2.mi090) = CONVERT(DATE, GETDATE())
        ORDER BY m2.mi050 DESC)     AS referencia
   FROM admuser.fmesmic m
   WHERE CONVERT(DATE, mi090) = CONVERT(DATE, GETDATE())
     AND RTRIM(mi020) IN :codes
   GROUP BY RTRIM(mi020)
   ```

4. `capacidad_piezas_linea.sql` — extraer verbatim `api/routers/capacidad.py:60-87` reemplazando `?` por `:fecha_inicio`, `:fecha_fin`:
   ```sql
   -- Produccion real solo en CTs cuyo nombre empieza por 'Linea'
   -- (lineas de montaje final). Evita doble contabilidad.
   SELECT
       RTRIM(COALESCE(lof.lo030, ''))          AS referencia,
       RTRIM(CAST(dtc.dt150 AS NVARCHAR(50)))  AS ct,
       RTRIM(rec.re020)                         AS ct_nombre,
       SUM(CASE WHEN dtc.dt110 = '0'
                THEN COALESCE(CAST(dtc.dt130 AS FLOAT), 0)
                ELSE 0 END)                    AS piezas,
       SUM(CASE WHEN dtc.dt110 IN ('0','1')
                THEN COALESCE(CAST(dtc.dt090 AS FLOAT), 0)
                ELSE 0 END)                    AS tiempo_min,
       MIN(dtc.dt060)                           AS fecha_min,
       MAX(dtc.dt060)                           AS fecha_max
   FROM admuser.fmesdtc dtc
   INNER JOIN admuser.fmesrec rec
       ON CAST(rec.re010 AS INT) = CAST(dtc.dt150 AS INT)
   LEFT JOIN admuser.fprolof lof
       ON  RTRIM(lof.lo010) = RTRIM(dtc.dt020)
       AND lof.lo020 = dtc.dt030
   WHERE CONVERT(DATE, dtc.dt060) BETWEEN :fecha_inicio AND :fecha_fin
     AND RTRIM(rec.re020) LIKE 'Linea%'
   GROUP BY RTRIM(COALESCE(lof.lo030, '')),
            RTRIM(CAST(dtc.dt150 AS NVARCHAR(50))),
            RTRIM(rec.re020)
   HAVING SUM(CASE WHEN dtc.dt110 = '0'
                   THEN COALESCE(CAST(dtc.dt130 AS FLOAT), 0)
                   ELSE 0 END) > 0
   ```

5. `capacidad_ciclos_p10_180d.sql` — extraer `api/routers/capacidad.py:119+` (query del P10 ciclos teóricos). La dinámica de `OR`-clause se reemplaza por `:pairs` con `bindparam("pairs", expanding=True)` si el executor encuentra forma; si no, PATTERNS.md sugiere mantenerlo como repo method construyendo `text()` con composite bindparams. Decisión del executor: lo más simple es usar un VALUES inline + JOIN, o dos bindparams (`refs`, `cts`) expanding por separado y un `WHERE RTRIM(...) IN :refs AND RTRIM(...) IN :cts` — valida con el operador; si se complica, mantener la query con dos consultas: una pre-extrae los pairs a filtrar y una aplica filtro `IN` simple sobre la clave concatenada.

6. `operarios_listar.sql` — extraer `api/routers/operarios.py:34-53` verbatim (sin parametros; es la query de listado).

7. `operarios_ficha.sql` — si el endpoint `/operarios/{codigo}` tiene SQL inline, extraer; si no, omitir.

8. `bbdd_list_databases.sql`, `bbdd_list_tables.sql`, `bbdd_list_columns.sql` — extraer queries de `api/routers/bbdd.py` (metadata ops). Parametros via `:database`, `:schema` etc.

### 3.2 — Refactor `api/routers/centro_mando.py`

1. Reemplazar import block (línea 12):
   ```python
   # ANTES:
   from api.database import Recurso, engine, get_db

   # DESPUES:
   from sqlalchemy.orm import Session
   from fastapi import Depends
   from api.deps import DbApp, EngineMes, get_db_app
   from api.database import Recurso  # modelo ORM sigue en api/database.py durante 03-02
   from nexo.data.repositories.mes import MesRepository
   ```

2. **Eliminar** la función `_query_fmesmic` (líneas 38-88). Su lógica pasa a `MesRepository.centro_mando_fmesmic`.

3. Ajustar `_get_cached_data` (líneas 91-113) para recibir `mes_repo: MesRepository` en lugar de llamar a `_query_fmesmic`:
   ```python
   def _get_cached_data(ct_codes: list[int], mes_repo: MesRepository) -> tuple[dict, str | None, float]:
       # ... mismo logic, pero en la llamada al TTL-miss:
       try:
           rows = mes_repo.centro_mando_fmesmic(ct_codes)
           # rows es list[dict] con claves ct/piezas_hoy/ultimo_evento/referencia.
           # Convertir al shape dict interno:
           data = {}
           from datetime import date, datetime
           for r in rows:
               ct = int(r["ct"])
               ultimo = r.get("ultimo_evento")
               ahora = datetime.now().time()
               if ultimo:
                   ultimo_dt = datetime.combine(date.today(), ultimo)
                   ahora_dt = datetime.combine(date.today(), ahora)
                   diff_min = (ahora_dt - ultimo_dt).total_seconds() / 60
                   activa = diff_min < 30
               else:
                   activa = False
               data[ct] = {
                   "piezas_hoy": int(r.get("piezas_hoy", 0)),
                   "ultimo_evento": str(ultimo)[:5] if ultimo else None,
                   "referencia": r.get("referencia") or "",
                   "activa_reciente": activa,
               }
           _cache["data"] = data
           # ... resto igual
   ```

4. Reemplazar handler `summary` (línea 116):
   ```python
   @router.get("/summary")
   def summary(db: DbApp, engine_mes: EngineMes):
       hoy = _fecha_produccion()
       mes_repo = MesRepository(engine=engine_mes)

       all_recursos = (
           db.query(Recurso)
           .filter_by(activo=True)
           .order_by(Recurso.seccion, Recurso.nombre)
           .all()
       )
       ct_codes = [r.centro_trabajo for r in all_recursos]
       mic_data, izaro_error, cache_ts = _get_cached_data(ct_codes, mes_repo)
       # resto del handler igual
   ```

5. Smoke: `curl -b cookies.txt http://localhost:8000/api/dashboard/summary` debe devolver 200 con mismo shape de respuesta que pre-refactor. Commit: `refactor(03-02): kill 3-part names in centro_mando via MesRepository (DATA-09)`.

### 3.3 — Refactor `api/routers/capacidad.py`

1. Eliminar imports de `pyodbc` + `_build_connection_string` + `get_config`.
2. Inject `EngineMes` en el handler:
   ```python
   from api.deps import EngineMes
   from nexo.data.sql.loader import load_sql
   from sqlalchemy import text
   ```
3. Reemplazar el bloque `pyodbc.connect(...)` + queries por `with engine_mes.connect() as conn:` + `text(load_sql("mes/capacidad_piezas_linea"))`. Params pasan como dict named.
4. La query P10 (con `OR` dinámico) — decisión del executor: o (a) extraer a `.sql` con bindparams expanding separados, o (b) construir el `text()` en el handler con `load_sql` + append dinámico si lo anterior es inviable. Preferir (a). Si la migración es arriesgada y consume >20% context, hacer commit intermedio "refactor partial" y dejar una TODO con nota para 03-03 tail.
5. Smoke: `GET /api/capacidad?fecha_inicio=2026-03-15&fecha_fin=2026-03-15` → 200 con JSON igual shape. Commit: `refactor(03-02): capacidad uses MesRepository + engine_mes (no pyodbc)`.

### 3.4 — Refactor `api/routers/operarios.py`

1. Eliminar `_connect()` (líneas 18-22) y el import local de `pyodbc`.
2. Inject `EngineMes`:
   ```python
   from api.deps import EngineMes
   from sqlalchemy import text
   from nexo.data.sql.loader import load_sql
   ```
3. Reemplazar `conn = _connect()` + `c.execute("""...""")` por:
   ```python
   @router.get("")
   def listar_operarios(engine_mes: EngineMes, activos: bool = True):
       with engine_mes.connect() as conn:
           rows = conn.execute(text(load_sql("mes/operarios_listar"))).fetchall()
       # ... resto igual con row._mapping o tuple unpack
   ```
4. Repetir patrón para `/operarios/{codigo}` (ficha) si tiene SQL inline.
5. Smoke: `GET /api/operarios` → 200. Commit: `refactor(03-02): operarios via engine_mes + load_sql (no pyodbc)`.

### 3.5 — Refactor `api/routers/luk4.py` (NOTA: luk4.* está en APP, NO en MES — aplicar D-01 clarificación)

Investigar: `luk4.estado`, `luk4.alarmas`, `luk4.tiempos_ciclo` son tablas del schema `luk4` dentro de `ecs_mobility` (el catálogo APP). Por tanto:

1. Reemplazar `from api.database import engine` por `from nexo.data.engines import engine_app`. NO usar `engine_mes` aquí.
2. **Aplicar clarificación D-01 (CONTEXT.md 2026-04-19) sobre scope DATA-05**:
   - **Inspeccionar primero `api/routers/luk4.py`** para determinar la forma de las queries:
     - Si las queries usan **ORM puro** (`session.query(LukEstado).filter(...)`, `select(LukEstado)`): **NO se crean `.sql`**. Per CONTEXT.md D-01 scope clarification: "ORM puro no requiere `.sql` versionado. El modelo declarativo SQLAlchemy ES la representación canónica."
     - Si las queries usan **`text("SELECT ...")`** con strings SQL hardcoded: **EXTRAER a `nexo/data/sql/app/luk4_<name>.sql`** per D-01 (queries `text()` SÍ requieren `.sql` versionado).
     - Si la mezcla es ambos: **extraer solo las `text()`**, dejar las ORM en el router.
   - **Documentar la decisión en el commit message**. Opción A: "luk4 usa ORM puro sobre engine_app — DATA-05 cumplido sin `.sql` per CONTEXT.md D-01 scope clarification (ORM puro = representación canónica)." Opción B: "luk4 usa text() queries — extraídas a nexo/data/sql/app/luk4_*.sql per D-01."
3. NO crear `MesRepository` method para luk4 — no es MES.
4. El router sigue sin `pyodbc` (ya usa `engine.connect()` que es SQLAlchemy), solo cambia el nombre del engine.
5. Smoke: `GET /api/luk4/status` → 200 o 502 (si la BD no responde). Commit (ajustar el texto según A o B del punto 2):
   - Opción A: `refactor(03-02): luk4 uses engine_app from nexo.data.engines (luk4.* vive en APP; ORM puro = DATA-05 OK per D-01)`
   - Opción B: `refactor(03-02): luk4 uses engine_app + extract text() queries to nexo/data/sql/app/luk4_*.sql (DATA-05 per D-01)`

### 3.6 — Refactor `api/routers/bbdd.py` (más complejo, último del grupo)

1. **Preservar íntegramente el whitelist anti-DDL** (D-05) — regex que rechaza `INSERT|UPDATE|DELETE|DROP|TRUNCATE|ALTER|CREATE|GRANT|REVOKE`. Buscar en el archivo actual su ubicación (probablemente en el handler `POST /consulta`).
2. Las metadata ops (`list_databases`, `list_tables`, `list_columns`, `preview`) quedan inline, pero eliminan `pyodbc.connect()` + `_get_conn_string()` + `_connect()`. En su lugar:
   ```python
   from api.deps import EngineMes
   from sqlalchemy import text
   from nexo.data.sql.loader import load_sql

   @router.get("/databases")
   def list_databases(engine_mes: EngineMes):
       with engine_mes.connect() as conn:
           rows = conn.execute(text(load_sql("mes/bbdd_list_databases"))).fetchall()
       # ... reconstruir el dict shape esperado
   ```
3. El handler `POST /consulta` (SQL libre):
   - Mantener regex whitelist EXACTAMENTE como está.
   - Tras validar, llamar `MesRepository(engine=engine_mes).consulta_readonly(sql, database)`.
   - Devolver el shape `{columns, rows}` que la UI espera.
4. La noción de "cambiar de catalog" (el explorer permite seleccionar BD distinta de dbizaro) — si el código actual usa `pyodbc.connect(DATABASE=<other>)` para otras DBs, tenemos un problema: `engine_mes` está fijado a `dbizaro`. Solución: para `list_databases` y las ops que listan todas las BDs del servidor, usar `engine_mes.connect()` y ejecutar `SELECT name FROM sys.databases` (funciona cross-catalog en SQL Server si la cuenta tiene permiso sobre master). Para queries user-SQL contra otra BD distinta de `dbizaro`, prefixar con `USE {database};` o construir un engine ad-hoc — **decisión pragmática**: restringir el explorer a `dbizaro` en Mark-III (Mark-IV puede generalizar). Documentar en commit: "bbdd explorer limitado a dbizaro en Mark-III; engines multi-catalog en Mark-IV".
5. Smoke: `GET /api/bbdd/databases` → 200; `POST /api/bbdd/consulta` con SQL válido → 200; con SQL que contiene `DROP` → 400. Commit: `refactor(03-02): bbdd via MesRepository.consulta_readonly + whitelist preserved (D-05)`.

### 3.7 — Modificar `api/services/db.py` (minimum diff)

- El facade sigue existiendo (pipeline lo usa). Cambiar las funciones internas que delegaban a `OEE.db.connector.*` para que deleguen a `MesRepository` cuando sea equivalente. Si es el mismo resultado, mantener imports de `OEE.db.connector` solo para `datos_a_csvs`, `test_conexion`, `explorar_columnas_fmesdtc` (que `MesRepository` NO envuelve).
- Añadir una instancia helper:
  ```python
  from nexo.data.engines import engine_mes
  from nexo.data.repositories.mes import MesRepository

  def _repo() -> MesRepository:
      return MesRepository(engine=engine_mes)
  ```
- `discover_resources`, `extract_data`, `compute_real_cycles`, `live_status` → delegar a `_repo().detectar_recursos()` etc.
- **Este es el punto donde 03-02 deja a pipeline.py "re-pointeado" a MesRepository sin tocar pipeline.py**: `pipeline.py` consume `api.services.db.*` (el facade), así que al ajustar el facade aquí, pipeline.py automáticamente usa el nuevo backend sin requerir un edit propio. El swap de imports internos de pipeline.py (si alguno importa directo de `OEE.db.connector`) es propiedad de 03-03 — ver `<context_handoff>`.

### 3.8 — `api/services/pipeline.py` (NO TOCAR — ownership handoff a 03-03)

**Bloqueado por `<context_handoff>`:** este plan NO edita `pipeline.py`. Cualquier cambio necesario (incluido repoint de imports legacy) se hace en 03-03 junto con la migración ORM que también toca ese archivo.

Verificación negativa (test 7 del behavior): en el commit que cierra Task 3, `git diff HEAD~..HEAD -- api/services/pipeline.py` debe estar vacío. Si aparece diff aquí, es un error y debe revertirse.
  </action>
  <verify>
    <automated>docker compose exec -T web bash -c 'grep -c "import pyodbc" api/routers/bbdd.py api/routers/capacidad.py api/routers/operarios.py api/routers/centro_mando.py api/routers/luk4.py | grep -v ":0" | wc -l' # debe ser 0</automated>
    <automated>docker compose exec -T web bash -c 'grep -rn "dbizaro\." api/ | grep -v ".pyc" | wc -l' # debe ser 0</automated>
    <automated>test -f nexo/data/sql/mes/centro_mando_fmesmic.sql && grep -q "admuser.fmesmic" nexo/data/sql/mes/centro_mando_fmesmic.sql && ! grep -q "dbizaro\." nexo/data/sql/mes/centro_mando_fmesmic.sql && echo "centro_mando.sql OK"</automated>
    <automated>grep -q "NOTA T3" nexo/data/sql/mes/extraer_datos_produccion.sql && echo "T3 comment preserved"</automated>
    <automated>docker compose exec -T web python -c "from fastapi.testclient import TestClient; from api.main import app; print('app import OK')"</automated>
    <automated>git diff HEAD~1..HEAD -- api/services/pipeline.py | wc -l  # debe ser 0 (pipeline.py untouched, propiedad de 03-03)</automated>
  </verify>
  <done>
    - 11 archivos `.sql` existen bajo `nexo/data/sql/mes/`.
    - 5 routers refactorizados, `pyodbc` eliminado.
    - `dbizaro.` no aparece en `api/` (excepto `OEE/db/connector.py` que queda tal cual).
    - Comentario T3 preservado en `extraer_datos_produccion.sql`.
    - Cada router commitea con smoke HTTP 200 previo.
    - `luk4.py` refactor documentado con decisión A (ORM puro, D-01 cumplido sin `.sql`) o B (text() → `.sql`); citar D-01 en commit message.
    - **`api/services/pipeline.py` NO modificado (handoff a 03-03 respetado).**
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Crear tests MES + meta-tests (Wave 3)</name>
  <files>
    tests/data/test_mes_repository.py (new),
    tests/data/test_mes_engine_context.py (new),
    tests/data/test_no_three_part_names.py (new),
    tests/data/test_bbdd_whitelist.py (new)
  </files>
  <read_first>
    - nexo/data/repositories/mes.py (creado en task 1)
    - .planning/phases/03-capa-de-datos/03-PATTERNS.md secciones "tests/data/test_mes_repository.py" (líneas 873-905), "tests/data/test_mes_engine_context.py" (líneas 909-930), "tests/data/test_no_raw_pyodbc_in_routers.py + tests/data/test_no_three_part_names.py" (líneas 934-952)
    - api/routers/bbdd.py (el regex whitelist anti-DDL — contract test lo cubre)
    - tests/auth/test_audit_append_only.py:32-54 (patrón de skipif integration-mssql)
  </read_first>
  <behavior>
    - Test 1 (`test_extraer_datos_delega`): patch `nexo.data.repositories.mes._legacy_extraer_datos`, crear repo con MagicMock engine, llamar `extraer_datos_produccion`, assert `assert_called_once` y retorno correcto.
    - Test 2: ídem para `detectar_recursos`, `calcular_ciclos_reales`, `estado_maquina_live`.
    - Test 3 (`test_consulta_readonly_shape`): engine mock con `connect().__enter__` devolviendo cursor que yieldea `keys()=["c1"]` y `fetchall()=[(42,)]`; assert shape `{"columns": ["c1"], "rows": [[42]]}`.
    - Test 4 (`test_centro_mando_fmesmic_uses_bindparam_expanding`): patch `load_sql` con SQL mock + engine mock; verificar que `text(...).bindparams(bindparam("codes", expanding=True))` se construye (inspeccionar el stmt via `compile()` o asserting sobre la llamada a `conn.execute`).
    - Test 5 (`test_centro_mando_fmesmic_empty`): lista vacía → retorna `[]` sin llamar al engine.
    - Test 6 (`test_mes_engine_default_db_is_dbizaro`, integration): `engine_mes.connect().execute(text("SELECT DB_NAME()")).scalar() == "dbizaro"`. Skipif no hay SQL Server.
    - Test 7 (`test_no_three_part_names`, meta grep): `api/` recursivo, excluyendo `__pycache__`, no contiene la cadena `dbizaro.` (insensible a caso). Excluye también archivos `.pyc`. OEE/ está fuera del scope del grep.
    - Test 8 (`test_no_raw_pyodbc_in_routers`, meta grep): `api/routers/*.py` no contienen `import pyodbc`. OK que `bbdd.py` contenga `pyodbc` dentro de un comentario si se dejó (mejor eliminarlo del todo).
    - Test 9 (`test_bbdd_whitelist_rejects_ddl`): la función/método que valida SQL en `api/routers/bbdd.py` rechaza `DROP TABLE x`, `DELETE FROM x`, `UPDATE x SET`, `INSERT INTO`, `TRUNCATE x`, `ALTER TABLE x`, `GRANT`, `REVOKE`, `CREATE TABLE`. Acepta `SELECT * FROM x WHERE y = 1`.
  </behavior>
  <action>
1. Crear `tests/data/test_mes_repository.py`:

   ```python
   """DATA-02: MesRepository contrato + delegación."""
   from __future__ import annotations

   from datetime import date
   from unittest.mock import MagicMock, patch

   import pytest

   from nexo.data.repositories.mes import MesRepository


   @patch("nexo.data.repositories.mes._legacy_extraer_datos")
   @patch("nexo.data.repositories.mes._get_mes_config")
   def test_extraer_datos_delega(mock_cfg, mock_extraer):
       mock_cfg.return_value = {"recursos": [{"nombre": "luk1"}]}
       mock_extraer.return_value = [{"recurso": "luk1", "fecha": "2026-04-01"}]

       repo = MesRepository(engine=MagicMock())
       rows = repo.extraer_datos_produccion(
           fecha_inicio=date(2026, 4, 1), fecha_fin=date(2026, 4, 1), recursos=["luk1"]
       )
       assert len(rows) == 1
       mock_extraer.assert_called_once()


   @patch("nexo.data.repositories.mes._legacy_detectar_recursos")
   @patch("nexo.data.repositories.mes._get_mes_config")
   def test_detectar_recursos_delega(mock_cfg, mock_detectar):
       mock_cfg.return_value = {}
       mock_detectar.return_value = [{"codigo": 47}]
       repo = MesRepository(engine=MagicMock())
       assert repo.detectar_recursos() == [{"codigo": 47}]
       mock_detectar.assert_called_once()


   @patch("nexo.data.repositories.mes._legacy_calcular_ciclos")
   @patch("nexo.data.repositories.mes._get_mes_config")
   def test_calcular_ciclos_delega(mock_cfg, mock_ciclos):
       mock_cfg.return_value = {}
       mock_ciclos.return_value = ([{"ref": "X", "ciclo_seg": 10.5}], "fmesdtc")
       repo = MesRepository(engine=MagicMock())
       rows, fuente = repo.calcular_ciclos_reales(centro_trabajo=47, dias_atras=30)
       assert rows[0]["ciclo_seg"] == 10.5
       mock_ciclos.assert_called_once_with({}, 47, 30)


   @patch("nexo.data.repositories.mes._legacy_estado_maquina")
   @patch("nexo.data.repositories.mes._get_mes_config")
   def test_estado_maquina_delega(mock_cfg, mock_estado):
       mock_cfg.return_value = {}
       mock_estado.return_value = {"activa": True, "piezas_hoy": 5}
       repo = MesRepository(engine=MagicMock())
       assert repo.estado_maquina_live(47, 600) == {"activa": True, "piezas_hoy": 5}


   def test_consulta_readonly_shape():
       engine = MagicMock()
       cursor = engine.connect.return_value.__enter__.return_value
       cursor.execute.return_value.keys.return_value = ["c1", "c2"]
       cursor.execute.return_value.fetchall.return_value = [(1, "a"), (2, "b")]

       repo = MesRepository(engine=engine)
       out = repo.consulta_readonly("SELECT 1", "dbizaro")
       assert out["columns"] == ["c1", "c2"]
       assert out["rows"] == [[1, "a"], [2, "b"]]


   def test_centro_mando_fmesmic_empty_returns_empty():
       repo = MesRepository(engine=MagicMock())
       assert repo.centro_mando_fmesmic([]) == []


   @patch("nexo.data.repositories.mes.load_sql")
   def test_centro_mando_fmesmic_uses_bindparam(mock_load_sql):
       mock_load_sql.return_value = "SELECT * FROM admuser.fmesmic WHERE ct IN :codes"
       engine = MagicMock()
       result_proxy = engine.connect.return_value.__enter__.return_value.execute.return_value
       # row con _mapping es el shape SQLAlchemy 2.0
       fake_row = MagicMock()
       fake_row._mapping = {"ct": 47, "piezas_hoy": 10}
       result_proxy.fetchall.return_value = [fake_row]

       repo = MesRepository(engine=engine)
       rows = repo.centro_mando_fmesmic([47, 48])
       assert rows == [{"ct": 47, "piezas_hoy": 10}]
       mock_load_sql.assert_called_once_with("mes/centro_mando_fmesmic")
   ```

2. Crear `tests/data/test_mes_engine_context.py`:

   ```python
   """DATA-09: engine_mes apunta a dbizaro (smoke contra SQL Server real)."""
   from __future__ import annotations

   import pytest
   from sqlalchemy import text

   from api.config import settings
   from nexo.data.engines import engine_mes


   def _mssql_reachable() -> bool:
       try:
           with engine_mes.connect() as c:
               c.execute(text("SELECT 1"))
           return True
       except Exception:
           return False


   pytestmark = [
       pytest.mark.integration,
       pytest.mark.skipif(
           not _mssql_reachable(),
           reason="SQL Server MES no alcanzable — skipeable en CI",
       ),
   ]


   def test_engine_mes_default_db_is_dbizaro():
       with engine_mes.connect() as conn:
           db = conn.execute(text("SELECT DB_NAME()")).scalar()
       assert db == settings.mes_db
       assert db == "dbizaro", (
           f"engine_mes default catalog esperado 'dbizaro', got {db!r}"
       )


   def test_two_part_name_query_works():
       """Valida que `admuser.fmesmic` resuelve sin prefix de catalog."""
       with engine_mes.connect() as conn:
           n = conn.execute(text("SELECT COUNT(*) FROM admuser.fmesmic")).scalar()
       assert isinstance(n, int)
   ```

3. Crear `tests/data/test_no_three_part_names.py`:

   ```python
   """DATA-09: no hay `dbizaro.<algo>` en api/ (OEE/db/connector.py esta exento por D-04)."""
   from __future__ import annotations

   import pathlib
   import re

   ROOT = pathlib.Path(__file__).resolve().parents[2]
   API_DIR = ROOT / "api"
   NEXO_DIR = ROOT / "nexo"


   def _collect_py_files(base: pathlib.Path) -> list[pathlib.Path]:
       return [p for p in base.rglob("*.py") if "__pycache__" not in str(p)]


   def test_no_three_part_names_in_api_dir():
       """Ni un solo `dbizaro.<something>` en api/ (excepto comentarios)."""
       pattern = re.compile(r"dbizaro\.\w+", re.IGNORECASE)
       offenders: list[tuple[str, int, str]] = []
       for py in _collect_py_files(API_DIR):
           for i, line in enumerate(py.read_text(encoding="utf-8").splitlines(), 1):
               # Ignorar comentarios
               code = line.split("#")[0]
               if pattern.search(code):
                   offenders.append((str(py.relative_to(ROOT)), i, line.strip()))
       assert not offenders, (
           f"3-part names en api/: {offenders}"
       )


   def test_no_three_part_names_in_sql_files():
       """Ni en los .sql versionados de nexo/data/sql/mes/."""
       pattern = re.compile(r"dbizaro\.\w+", re.IGNORECASE)
       offenders: list[tuple[str, int, str]] = []
       for sql in (NEXO_DIR / "data" / "sql" / "mes").glob("*.sql"):
           for i, line in enumerate(sql.read_text(encoding="utf-8").splitlines(), 1):
               # Ignorar lineas de comentario `--`
               code = line.split("--")[0]
               if pattern.search(code):
                   offenders.append((str(sql.relative_to(ROOT)), i, line.strip()))
       assert not offenders, (
           f"3-part names en .sql: {offenders}"
       )


   def test_no_pyodbc_import_in_routers():
       """DATA-07: ningun router importa pyodbc directo."""
       routers = (API_DIR / "routers").glob("*.py")
       offenders = []
       for py in routers:
           code = py.read_text(encoding="utf-8")
           for i, line in enumerate(code.splitlines(), 1):
               stripped = line.strip()
               if stripped.startswith("#"):
                   continue
               if "import pyodbc" in stripped:
                   offenders.append((py.name, i))
       assert not offenders, f"Routers con `import pyodbc`: {offenders}"
   ```

4. Crear `tests/data/test_bbdd_whitelist.py`:

   ```python
   """D-05: contract test del whitelist anti-DDL en api/routers/bbdd.py.

   Mitiga T-03-02-01 (SQL injection via consulta_readonly si el whitelist falla).
   """
   from __future__ import annotations

   import pytest

   # El nombre de la funcion de validacion puede variar segun como esté
   # actualmente en bbdd.py. Ajustar import al símbolo real tras el
   # refactor. Si se llama `_validate_sql` o `_is_readonly_sql`, lo
   # importamos aqui. Si el regex esta inline, extraerlo a funcion
   # module-level durante el refactor de la task 3.6 para poder testear.
   from api.routers.bbdd import _validate_sql  # executor expone esta funcion en 3.6


   FORBIDDEN_STATEMENTS = [
       "DROP TABLE foo",
       "DELETE FROM foo",
       "UPDATE foo SET a=1",
       "INSERT INTO foo VALUES (1)",
       "TRUNCATE TABLE foo",
       "ALTER TABLE foo ADD b INT",
       "GRANT SELECT ON foo TO bar",
       "REVOKE SELECT ON foo FROM bar",
       "CREATE TABLE foo (a INT)",
       "  delete  from foo  ",  # case insensitive + whitespace
       "select 1; DROP TABLE foo;",  # multi-statement injection
   ]

   ALLOWED_STATEMENTS = [
       "SELECT 1",
       "SELECT * FROM admuser.fmesmic WHERE mi020 = '47'",
       "select count(*) from admuser.fmesdtc",
   ]


   @pytest.mark.parametrize("stmt", FORBIDDEN_STATEMENTS)
   def test_whitelist_rejects_ddl_dml(stmt):
       with pytest.raises(Exception):
           _validate_sql(stmt)


   @pytest.mark.parametrize("stmt", ALLOWED_STATEMENTS)
   def test_whitelist_allows_select(stmt):
       # No raise = OK
       _validate_sql(stmt)
   ```

   **Nota para el executor**: en la task 3.6 del refactor de `bbdd.py`, extraer el regex a una función module-level `_validate_sql(sql: str) -> None` (raise si rechaza, None si OK) para poder testearla aquí. Si ya está así en el código actual, genial.
  </action>
  <verify>
    <automated>docker compose exec -T web pytest tests/data/test_mes_repository.py -q -x</automated>
    <automated>docker compose exec -T web pytest tests/data/test_no_three_part_names.py -q -x</automated>
    <automated>docker compose exec -T web pytest tests/data/test_bbdd_whitelist.py -q -x</automated>
    <automated>docker compose exec -T web pytest tests/data/test_mes_engine_context.py -q  # integration, skipea si no hay SQL Server</automated>
  </verify>
  <done>
    - 4 test files creados y pasando (mocks + meta-tests).
    - `test_no_three_part_names` green: `dbizaro.` no aparece en `api/` ni en `.sql`.
    - `test_no_pyodbc_import_in_routers` green.
    - `test_bbdd_whitelist` cubre los 9 DDLs/DMLs prohibidos + 3 SELECT permitidos.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (gate final): PDF regression check + smoke HTTP manual</name>
  <what-built>
    - MesRepository + 11 .sql + 5 routers refactorizados
    - 4 tests (mocks + meta-tests + integration)
    - `scripts/pdf_regression_check.py` listo para correr
  </what-built>
  <how-to-verify>
    1. El operador arranca compose: `make dev`.
    2. Corre los tests unit/meta: `docker compose exec -T web pytest tests/data/ tests/auth/ -q`.
       - Esperado: tests/auth/ → 11 passed / 1 skipped. tests/data/ verde (integration skipea sin SQL Server).
    3. Smoke HTTP (login primero para obtener cookie):
       ```bash
       # Obtener cookie (asumiendo propietario ya existe — creado en 02-04)
       curl -c /tmp/cookies.txt -X POST http://localhost:8000/login \
            -d "email=<propietario>&password=<password>"
       # Testear 5 routers
       for path in /api/dashboard/summary /api/capacidad?fecha_inicio=2026-03-15\&fecha_fin=2026-03-15 /api/operarios /api/luk4/status /api/bbdd/databases; do
         code=$(curl -s -o /dev/null -w "%{http_code}" -b /tmp/cookies.txt "http://localhost:8000$path")
         echo "$path → $code"
       done
       ```
       - Esperado: todos 200. Si algún endpoint devuelve 502 con mensaje "Error conectando a IZARO" es aceptable si el operador no tiene conectividad LAN al MES en ese momento; documentar en el signal de resume.
    4. **PDF regression gate** (solo si el operador grabó el baseline en Task 2):
       ```bash
       docker compose exec -T web python scripts/pdf_regression_check.py --fecha=2026-03-15
       ```
       - Exit 0 = APROBADO.
       - Exit 1 = WARN (hash difiere pero size ±5% + same pages) → aceptable si el operador inspecciona visualmente el PDF y confirma que es idéntico excepto por timestamps de matplotlib.
       - Exit 2 = REGRESSION → BLOCK, revertir refactor y diagnosticar.
    5. **Si el baseline se saltó en Task 2 (fallback)**: verificar que STATE.md tiene la entrada TODO con deadline y que SUMMARY.md lista el gate como "pendiente en preprod, plazo 7 días". 03-02 NO se marca DONE hasta completarse ese TODO.
  </how-to-verify>
  <resume-signal>Operador escribe "PDF regression OK" (y si fue WARN, "WARN aceptado, inspeccionado visualmente") + "smoke HTTP 5 routers 200 OK" o describe los fallos. Si el baseline quedó diferido: "baseline deferido, TODO en STATE.md con deadline YYYY-MM-DD".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User HTTP → `/bbdd/consulta` | SQL libre proporcionado por usuario autenticado (propietario) cruza a ejecución contra `dbizaro` read-only |
| User HTTP → `/capacidad`, `/operarios`, `/dashboard/summary` | parámetros de fecha + auth cookie cruzan a queries parametrizadas |
| MES connector legacy → MesRepository | delegación de lógica legacy preservando semántica (Success #5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T (SQL injection) | `api/routers/bbdd.py:/consulta` → `MesRepository.consulta_readonly` | mitigate | Whitelist regex anti-DDL se preserva en router (D-05); SQL ya validada cuando llega al repo. `tests/data/test_bbdd_whitelist.py` cubre 9 DDL/DML variantes + multi-statement. Preferir extraer el regex a `_validate_sql()` module-level para testabilidad explícita. |
| T-03-02-02 | I (information disclosure via 3-part leak) | Queries con `dbizaro.admuser.*` si alguien olvida migrar | mitigate | `tests/data/test_no_three_part_names.py` es gate bloqueante. `engine_mes` con `DATABASE=dbizaro` en DSN hace que queries con 2-part names funcionen y queries con 3-part names a otra BD fallen (no lift sideways). |
| T-03-02-03 | T (MES semantic regression) | `MesRepository` wrappers introducen bug en extraer_datos/estado_maquina_live | mitigate | D-04: wrapping delgado, NO reescritura. `scripts/pdf_regression_check.py` es gate final; `tests/test_oee_calc.py` + `tests/test_oee_helpers.py` (30 tests) siguen verdes como safety net del pipeline. |
| T-03-02-04 | D (SQL Server DoS) | Pool `engine_mes` size=3 + max_overflow=2 limita concurrencia | accept | 5 conexiones max × rate-limit slowapi → tráfico controlado. Si el operador nota colas en preprod, ajustar `pool_size` en `nexo/data/engines.py`. |
| T-03-02-05 | I (credentials in logs) | DSN MES con password en connection string | accept | SQLAlchemy enmascara password en repr; `settings.debug=True` solo en dev muestra SQL, no credenciales. Verificado en 03-01 task 2. |
| T-03-02-06 | E (bbdd explorer cross-catalog elevation) | Si el whitelist falla, user podría pivotar a otras BBDDs del servidor | mitigate | `engine_mes` fijado a `dbizaro` (no soporta `USE <other_db>` silencioso). Cross-catalog queries requieren 3-part names que el test prohíbe. El explorer de bbdd queda limitado a `dbizaro` en Mark-III (documentado). |

**Block on HIGH severity:** T-03-02-01 es HIGH; mitigación (whitelist + test) es bloqueante para el merge.
</threat_model>

<verification>
Hard gate (ejecutar en orden; cualquier fallo bloquea):

```bash
# 1. Código importable
docker compose exec -T web python -c "from nexo.data.repositories.mes import MesRepository; from nexo.data.dto.mes import ProduccionRow, EstadoMaquinaRow, CicloRealRow, CapacidadRow, OperarioRow; print('imports OK')"

# 2. Los .sql existen
for f in extraer_datos_produccion detectar_recursos calcular_ciclos_reales estado_maquina_live centro_mando_fmesmic capacidad_piezas_linea capacidad_ciclos_p10_180d operarios_listar bbdd_list_databases bbdd_list_tables bbdd_list_columns; do
  test -f "nexo/data/sql/mes/$f.sql" || { echo "MISSING $f.sql"; exit 1; }
done
echo "11 .sql files present"

# 3. Meta-tests: sin pyodbc, sin 3-part names
docker compose exec -T web pytest tests/data/test_no_three_part_names.py -q -x

# 4. Whitelist contract
docker compose exec -T web pytest tests/data/test_bbdd_whitelist.py -q -x

# 5. MES repo tests
docker compose exec -T web pytest tests/data/test_mes_repository.py -q -x

# 6. Integration MES (skipeable en CI)
docker compose exec -T web pytest tests/data/test_mes_engine_context.py -q

# 7. Regression IDENT-06 intacta
docker compose exec -T web pytest tests/auth/ -q
# Esperado: 11 passed / 1 skipped

# 8. pipeline.py NO tocado (handoff a 03-03)
git log --oneline --diff-filter=M -- api/services/pipeline.py | head -1
# Esperado: SIN output nuevo de este plan (si hay diff en pipeline.py, REVERTIR)

# 9. Smoke HTTP (operador, fuera del agent) — ver task 5

# 10. PDF regression gate (operador)
docker compose exec -T web python scripts/pdf_regression_check.py --fecha=2026-03-15
# exit 0 o 1 con justificación aceptable

# 11. T3 comment preserved
grep -q "NOTA T3" nexo/data/sql/mes/extraer_datos_produccion.sql

# 12. 2-part names in centro_mando.sql
grep -q "FROM admuser.fmesmic" nexo/data/sql/mes/centro_mando_fmesmic.sql
! grep -q "dbizaro\." nexo/data/sql/mes/centro_mando_fmesmic.sql
```
</verification>

<success_criteria>
- `MesRepository` con 6 métodos; los 4 wrappers delegan a `OEE.db.connector` (D-04).
- 5 DTOs MES frozen Pydantic v2 en `nexo/data/dto/mes.py`.
- 11 archivos `.sql` bajo `nexo/data/sql/mes/`; comentario T3 presente en `extraer_datos_produccion.sql`.
- `api/routers/{centro_mando,capacidad,operarios,luk4,bbdd}.py` sin `import pyodbc` directo.
- `api/` sin `dbizaro.<algo>` (DATA-09 cerrado).
- Whitelist anti-DDL preservada en `api/routers/bbdd.py` y probada con 9 DDL variantes.
- `scripts/gen_pdf_reference.py` + `scripts/pdf_regression_check.py` existen y funcionales **ANTES del checkpoint humano** (Task 1 crea los archivos, Task 2 los ejecuta).
- Smoke HTTP: 5 routers responden 200 con auth cookie.
- `pytest tests/data/test_mes_*.py tests/data/test_no_three_part_names.py tests/data/test_bbdd_whitelist.py -q` exit 0.
- `pytest tests/auth/ -q` sigue 11 passed / 1 skipped.
- **luk4 refactor documentado con cita explícita a CONTEXT.md D-01** (scope DATA-05: ORM puro no requiere `.sql` — solo `text()` queries): commit message indica opción A (ORM, no `.sql`) u opción B (`text()` → `.sql`).
- **`api/services/pipeline.py` NO modificado por este plan** (propiedad de 03-03 per `<context_handoff>` — verificado con `git diff` test en verification gate 8).
- **PDF baseline ejecutado o deferido con deadline de 7 días en STATE.md**: si se salta en Task 2, `03-02-SUMMARY.md` lista el gate pendiente y STATE.md tiene TODO con deadline. 03-02 NO se marca DONE hasta completarse el baseline en preprod.
- **PDF regression gate**: exit 0 o 1 con inspección manual aprobatoria (si ejecutado) o TODO registrado (si deferido).
- Autonomous: false (requiere operador para PDF baseline + smoke HTTP manual).
</success_criteria>

<output>
After completion, create `.planning/phases/03-capa-de-datos/03-02-SUMMARY.md` con:

- Files created (22) y files modified (6 — pipeline.py EXCLUIDO, handoff a 03-03).
- Commits atómicos recomendados (uno por router en orden D-06; Task 1 ANTES del checkpoint):
  1. `feat(03-02): create MesRepository + 5 MES DTOs + PDF regression scripts (DATA-02, DATA-08)`
  2. `feat(03-02): add sql files for MES queries (DATA-05)`
  3. `refactor(03-02): kill 3-part names in centro_mando via MesRepository (DATA-09)`
  4. `refactor(03-02): capacidad uses engine_mes + load_sql (no pyodbc)`
  5. `refactor(03-02): operarios via engine_mes + load_sql`
  6. `refactor(03-02): luk4 uses engine_app from nexo.data.engines (luk4.* lives in APP; ORM puro = DATA-05 OK per CONTEXT.md D-01)` [opción A]
     — o — `refactor(03-02): luk4 uses engine_app + extract text() queries to nexo/data/sql/app/luk4_*.sql per D-01` [opción B]
  7. `refactor(03-02): bbdd via MesRepository.consulta_readonly + whitelist preserved (D-05)`
  8. `refactor(03-02): repoint api/services/db.py imports to MesRepository (pipeline.py untouched; handoff to 03-03)`
  9. `test(03-02): MES repository + engine context + meta-tests + whitelist contract`
- PDF regression result: `hash=<X>` pre-refactor vs `hash=<Y>` post-refactor; decisión del operador (exit 0 o 1 aceptado) **o bien** "baseline deferido a preprod, TODO en STATE.md con deadline YYYY-MM-DD".
- Riesgos cerrados para 03-03: `MesRepository` disponible para `pipeline.py` via el facade `api/services/db.py`. 03-03 es dueño del edit final de pipeline.py (si hace falta).
- Gap abierto: explorer `/bbdd` limitado a `dbizaro` en Mark-III (documentado).
- Gap abierto (si aplica): PDF baseline en preprod pendiente con deadline registrado.
</output>
</content>
</invoke>