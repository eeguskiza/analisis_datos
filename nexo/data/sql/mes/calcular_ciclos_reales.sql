-- Placeholder DATA-05: la query canonica vive en OEE/db/connector.calcular_ciclos_reales
-- per decision D-04 (NO reescribir el connector legacy, solo envolver).
-- MesRepository.calcular_ciclos_reales delega literalmente en esa funcion.
--
-- NO ejecutar este archivo directamente.
SELECT 'placeholder — ver OEE/db/connector.calcular_ciclos_reales' AS note;
