-- Placeholder DATA-05: la query canonica vive en OEE/db/connector.estado_maquina_live
-- per decision D-04 (NO reescribir el connector legacy, solo envolver).
-- MesRepository.estado_maquina_live delega literalmente en esa funcion.
--
-- NO ejecutar este archivo directamente.
SELECT 'placeholder — ver OEE/db/connector.estado_maquina_live' AS note;
