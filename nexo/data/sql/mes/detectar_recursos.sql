-- Placeholder DATA-05: la query canonica vive en OEE/db/connector.detectar_recursos
-- per decision D-04 (NO reescribir el connector legacy, solo envolver).
-- MesRepository.detectar_recursos delega literalmente en esa funcion.
--
-- NO ejecutar este archivo directamente.
SELECT 'placeholder — ver OEE/db/connector.detectar_recursos' AS note;
