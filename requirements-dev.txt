# Herramientas de desarrollo. No se instalan en el contenedor runtime,
# solo en el entorno local del dev y en el CI de GitHub Actions.
#
# Instalacion:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Lint + formato
# Bump 0.9.8 -> 0.15.11 en Phase 7 / DEVEX-01 (ruff-format sustituye a black
# drop-in; config consolidada en pyproject.toml).
ruff==0.15.11

# Type checking (Phase 7 / DEVEX-01)
# mypy ligero, sin strict; overrides para libs legacy en pyproject.toml.
mypy==1.13.0

# Pre-commit (Phase 7 / DEVEX-01)
# Hooks definidos en .pre-commit-config.yaml; scope ^(api|nexo)/.
# Activar con `pre-commit install` una vez por clone.
pre-commit==4.0.1

# Testing
pytest==8.3.4
pytest-cov==6.0.0
# httpx lo necesita fastapi.testclient (Starlette 1.x usa httpx como backend
# real en TestClient; no viene como dep transitiva de fastapi).
httpx==0.28.1
# Phase 4 / Plan 04-01 — necesario para tests asyncio.to_thread (QUERY-08 /
# pipeline lock) y para el listener LISTEN/NOTIFY (QUERY-02 / D-19 que
# aterriza en Plan 04-04).
pytest-asyncio==0.24.0

# Escaneo de secretos. Nota: gitleaks es binario Go, no se instala por pip;
# se ejecuta desde el workflow de GitHub Actions via gitleaks/gitleaks-action.
# Para correrlo en local: `brew install gitleaks` o
# `curl -sSfL ... | sh` desde https://github.com/gitleaks/gitleaks/releases.
